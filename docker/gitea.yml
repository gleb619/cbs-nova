services:
  gitea:
    image: gitea/gitea:1.22
    ports:
      - "3001:3000"
      - "2222:22"
    volumes:
      - gitea-data:/data
    environment:
      USER_UID: 1000
      USER_GID: 1000

volumes:
  gitea-data:
