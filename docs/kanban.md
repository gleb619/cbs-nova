# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T168 | Done    | Preview/Explain runtime event-listener & report architecture             | backend — Superseded: all acceptance criteria landed via the stage pipeline (`DslExecutionPipeline`/`DslPipeStage`) + `ExecutionListener` SPI + typed `PreviewReport`/`ExplainReport`. `ExternalCallTracker` deleted; `DevDslRuntime` is now a thin delegator to `PreviewDslPipe`/`RunDslPipe`/`ExplainDslPipe`. `TransactionRouting` is a first-class `Context` strategy; diagram export (mermaid/plantuml/bpmn) shipped in `ExplainDiagramRenderer`. One small follow-up noted in plan: null-safe `DevDslRuntime.explain()`. See plan file for the full mapping table.                                     |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T326 | Done | Execution timeline viz — `dsl_run_transactions` flat list, no temporal view | frontend — code-confirmed gap: codegraph `ExecutionTimeline`/`RunTimeline`/`transactionTimeline` queries return zero FE components; T312 surfaced the per-run `dsl_run_transactions` data via BFF+FE table, but the executions detail page renders transactions as a flat collapsible list sorted by `executedAt` — for a multi-step process with retries, saga compensations, or parallel branches, users must scroll and mentally reconstruct temporal order from timestamp strings. Task: `ExecutionTimeline.vue` component (decide + document single-lane chronological track vs swim-lane per transaction name — single-lane default for low-process-count typical case, swim-lane if lane count > threshold), horizontal time axis with zoom (fit-to-view + fixed scales), bars per transaction with status→color mapping (consult `dataviz` skill for accessible palette), duration proportional to width, hover-tooltip with name+status+duration+error, click-bar opens the existing `PayloadTab` for that transaction, empty state when zero transactions, loading + error states per existing conventions, `data-testid` per T257–T261 conventions (`execution-timeline`, `execution-timeline-bar-<idx>`); vitest (renders mock transactions in correct chronological order, status→color map covers all `TransactionExecution` statuses, duration→width math holds, empty state, hover tooltip); confirm no regression to the existing transactions table (timeline lives alongside, not replacing it). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T326-execution-timeline.md`                |
| T340 | Done | JDBC `Recording*` refactor — extract common proxy boilerplate | backend (refactor) — Rescoped: original premise stale. The `Statement` family was already consolidated into an inheritance chain (`RecordingStatement` ← `RecordingPreparedStatement` ← `RecordingCallableStatement`), no method implemented twice, shared record helpers already in the base; there is no exception-wrapping / null-guard boilerplate and no `RecordingException` in the code, and the typed decorator is deliberately reflection-free — so the ≥30% LOC target was withdrawn. Remaining real duplication (10 `prepareStatement`/`prepareCall` overloads in `RecordingConnection` each inlining the same `x == null ? null : new RecordingX(...)` ternary) collapsed into two private `wrapPrepared`/`wrapCallable` helpers mirroring the existing `wrapStatement`. Behavior unchanged; `:starter:test cbs.nova.starter.capture.*` → 114 tests pass. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T340-recording-jdbc-refactor.md`        |
| T364 | Done | `ApiKeyAuthFilter` — opt-in gating via `dsl.auth.enabled` (`@ConditionalOnProperty`) | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/ApiKeyAuthFilterConfiguration.java:12` reads `//TODO: add \`@ConditionalOnProperty\`, by default filter must be turned off`. Current state: `apiKeyAuthFilter` bean is `@ConditionalOnMissingBean` only — it is ALWAYS created and the `FilterRegistrationBean` ALWAYS registers it on `/api/*` at `HIGHEST_PRECEDENCE + 1`, even when no API key is configured. The filter itself short-circuits to pass-through when `configuredApiKey` is null/blank (`ApiKeyAuthFilter.java:35-38`), so behavior is safe today, but: (1) every `/api/*` request pays a servlet-filter hop + null-check for a feature that is off; (2) semantics are implicit — auth is "on" only by side effect of a property being non-blank, invisible in actuator `/beans` planning and startup logs; (3) `DslProperties.Auth` (`DslProperties.java:40-44`) has only `apiKey` — no explicit enable flag, unlike sibling `Worker` section which already has `enabled` (`DslProperties.java:27`), so the config surface is inconsistent. Task: (1) add `private boolean enabled;` to `DslProperties.Auth` (default `false`, matching the TODO's "by default filter must be turned off"; `@Data` generates the getter); (2) annotate BOTH beans in `ApiKeyAuthFilterConfiguration` with `@ConditionalOnProperty(prefix = "dsl.auth", name = "enabled", havingValue = "true")` — the registration bean too, else it fails on a missing filter bean; alternatively single `@ConditionalOnProperty` on the `@Configuration` class (cleaner — one annotation, both beans gated); (3) MIGRATION NOTE in plan + release notes: existing deployments that set `dsl.auth.api-key` and rely on auth MUST add `dsl.auth.enabled=true` or they silently lose auth — evaluate safer default during implementation (`enabled` defaulting to true when `apiKey` is present is expressible via a custom `Condition` or `AnyNestedCondition`; the loop defers this choice to the executor + human review, but the TODO's stated intent is default-off); (4) drop the stale TODO comment. Tests: (a) context test — `dsl.auth.enabled=false` (unset) + no key → no `ApiKeyAuthFilter` bean, no `FilterRegistrationBean`; (b) `enabled=true` + key set → both beans present; (c) `enabled=true` + blank key → filter bean present, requests pass through (existing pass-through test kept green); (d) existing auth integration tests updated to set `enabled=true` in their test properties. Update `docs/architecture-backend.md` security section if it documents the api-key flow. Tier `backend`. Low risk if default chosen consciously; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T364-apikey-filter-optin.md`         |
| T365 | Done | Codegen provenance — `DslGenerated.dslBuildInfo/dslGitInfo` + platform version in `GeneratorMetadata` | backend — code-confirmed TODOs (one logical group): `backend/dsl-platform/dsl-api/src/main/java/cbs/nova/dsl/annotation/DslGenerated.java:20` reads `// TODO: add here two new fields: \`dslBuildInfo\`, \`dslGitInfo\`, so we can know with what version of cbs codegen class was created`; `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/generator/GeneratorMetadata.java:22` reads `// TODO: we also need to add version of dsl platform itself, for later debug`. Current state: `GeneratorMetadata.annotation()` emits `@DslGenerated(generator, timestamp, javaVersion, user)` — generator class name + JVM facts only. Gap: generated classes carry NO trace of (a) which dsl-platform version compiled them, (b) which git commit of the DSL source produced them. When generated workflow code misbehaves in a deployed worker, there is no in-class fingerprint to diff against the repo — support must reconstruct provenance from timestamps and guesswork. Task: (1) add `dslBuildInfo()` and `dslGitInfo()` String members to `@DslGenerated` (SOURCE retention — zero runtime footprint); (2) resolve platform version in `GeneratorMetadata` — prefer `Implementation-Version` from the codegen jar manifest (`getClass().getPackage().getImplementationVersion()`), fallback "unknown" when manifest stripped (gradle `jar` task must enable `implVersion` — verify `dsl-codegen/build.gradle`; note `generateGitProperties` task already exists in the build per repo memory, reuse its output if accessible); (3) git info — read commit sha + dirty flag via JGit (already a dep? verify) or `git` subprocess with graceful "unknown" fallback for non-repo CI contexts — NO hard failure when git absent; (4) thread both values into the `@DslGenerated` text block AND extend the `@Generated(comments=...)` line with `platform=%s` so the javax `@Generated` view also carries it; (5) existing generated-source golden/assertion tests updated for the two new members. Tests: (a) annotation emission includes `dslBuildInfo`/`dslGitInfo` keys with escaped values; (b) fallbacks — version "unknown" when manifest missing, git "unknown" when `.git` absent (test with workdir override); (c) escaping — values containing quotes/newlines produce valid Java source (compile the output in a test). Tier `backend`. Low risk: additive annotation members (SOURCE retention, defaults keep back-compat if any other generator constructs the annotation — grep first); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T365-codegen-provenance.md`        |
| T366 | Done | Config hardcode cleanup — bulkhead acquire timeout + workbench workspace root → `DslProperties`/app.yml | backend — code-confirmed TODOs (one group, same concern: hardcoded operational config in starter): (1) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/service/DslFileBulkhead.java:15` reads `// TODO: redo to use a some \`DslProperties\` property instead of hardcode` — `ACQUIRE_TIMEOUT_SECONDS = 5L` is a static constant; note the semaphore PERMITS are already property-driven (`DslConfiguration.java:152-155` reads `properties.getFiles().getReadBulkheadPermits()`/`getWriteBulkheadPermits()`), so only the timeout is stuck: a saturated bulkhead always throws `IllegalStateException("file read bulkhead saturated")` after exactly 5s regardless of deployment profile — slow disks or hot imports cannot tune it without recompile; (2) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/DslConfiguration.java:137` reads `// TODO: move hardcoded path to app.yml` — the workbench virtual-workspace root `sourceRoot.resolve(".workbench").resolve("drafts-fs")` is hardcoded inline in the `dslWorkspaceResolver` bean; operators cannot relocate scratch state (e.g. to a dedicated volume or tmpfs) via config. Task: (1) add `acquireTimeoutSeconds` (default 5) to `DslProperties.Files` next to the existing permit props; pass into `DslFileBulkhead` (constructor param — the class is already `@RequiredArgsConstructor`-style bean-constructed via `DslConfiguration`, keep the static constant only as the default-value source or drop it); (2) add `workbenchWorkspaceRoot` (or `workspaceSubdir`, default `.workbench/drafts-fs`) to `DslProperties` — resolve relative to `sourceDir` when not absolute, absolute wins; `DefaultDslWorkspaceResolver` construction reads it; (3) remove both stale TODOs and the stray `log.info("[DEBUG] Current path is: {}")` debug line at `DslConfiguration.java:134` (leftover debug logging, not a TODO but same lines — noise in every startup log); (4) document both keys in application.yml defaults block if one exists in starter resources. Tests: (a) bulkhead timeout honored — inject timeout 1s, saturate permits, assert `IllegalStateException` after ~1s not 5s; (b) workspace root — default value resolves to `<sourceDir>/.workbench/drafts-fs`; absolute override wins; (c) existing `DslWorkspaceResolver`/bulkhead tests stay green (constructor signature changes ripple — update fixtures). Tier `backend`. Low risk: pure config surface addition, behavior identical at defaults; rollback = revert one commit. Natural companion to T363 (same `DslProperties` extension — executor may batch). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T366-config-hardcode-cleanup.md` |
| T367 | Done | `DslExecutionEventStage` — publish `DslRunStartedEvent`/`DslRunCompletedEvent` from the run pipe | backend — code-confirmed TODO + dead code: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/listener/DslExecutionEventBus.java:9` reads `//TODO: we need to add a dslpipe stage impl, that uses a \`DslExecutionListener\`/\`DslExecutionEventBus\` thing`. Evidence the feature is half-built: `DslExecutionEvent` (sealed, `core/event/DslExecutionEvent.java`) permits THREE event types, but `grep -rn '\.publish(' starter/src/main` shows exactly ONE publisher — `RunIdKeyedExternalCallRecorder.java:138` publishes `DslExternalCallEvent` only. `DslRunStartedEvent` and `DslRunCompletedEvent` records exist with ZERO publishers = dead types; the bus itself IS a bean (`TemporalConfiguration.java:168`) and accepts listeners, but listener contract is unobservable for run lifecycle — any registered `DslExecutionListener` today hears only external calls, never run start/end. Result: promised observability hook (audit logging, run metrics, future UI push) cannot be built on the bus. Task: (1) new `core/stage/DslExecutionEventStage` following existing stage conventions (see `MetricsStage`, `ExecutionTraceStage` — same constructor/DslPipeStage shape); (2) on execute: publish `DslRunStartedEvent(runId, name, mode)` before delegating to `next.execute(...)` and `DslRunCompletedEvent(runId, name, mode, result)` after (runId resolution — reuse whatever `RunIdKeyedExternalCallRecorder`/`ExecutionTraceStage` use for run identity; do NOT invent a second runId source); (3) register in the run pipe ordering — after/before metrics stage; position decision left to executor, document chosen order + rationale in plan; (4) event bus stays failure-isolated — existing `publish()` already try/catches per listener, keep that (note: `catch (Exception ignored)` silently swallows — consider debug-level log while touching, optional); (5) drop the stale TODO. Tests: (a) registered listener receives Started then Completed (in order) for a successful run; (b) Completed carries the actual `Result` incl. failure result when downstream throws — verify exception propagation NOT swallowed by the stage; (c) misbehaving listener (throws) does not break the run (existing bus semantics); (d) no events for explain/preview pipes unless trivially free — scope = run pipe only, state this in plan. Tier `backend`. Low risk: additive stage, removable by pipe config; existing stages untouched. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T367-execution-event-stage.md` |
| T368 | Done | Idea-plugin sync leak — bound/TTL `DISCOVERED` cache in `DslProjectResolverExtension` | backend (dsl-plugins) — code-confirmed TODO: `backend/dsl-plugins/dsl-idea-plugin/src/main/java/cbs/nova/dsl/idea/sync/DslProjectResolverExtension.java:18` reads `// TODO: redo to a Caffeine with some properties config for ttl`. Current state: `private static final Map<ExternalSystemTaskId, Set<Path>> DISCOVERED = new ConcurrentHashMap<>()` — static, unbounded, lives for the IDE JVM lifetime. Entries are removed ONLY by `drainDiscovered(taskId)` (`DslProjectResolverExtension.java:48`), whose sole production caller is `DslGradleSyncNotificationListener.java:52` — the sync-SUCCESS notification path. Leak vector: a Gradle sync that fails, is cancelled, or dies before the notification listener runs leaves its `Set<Path>` (plus the `ExternalSystemTaskId` key and its held project references) in the static map forever; each leaked entry pins project paths and task-id objects in a long-lived IDE process — heavy IntelliJ sessions re-sync often, so abandoned-sync residue accumulates. Task: bound the cache. Preferred shape per TODO: Caffeine `Cache` with `expireAfterWrite` (TTL default e.g. 10m — a sync that never completes within TTL is dead by definition) + optional `maximumSize` belt-and-braces; `drainDiscovered` becomes `cache.asMap().remove(taskId)` returning null → `Set.of()` (semantics preserved — existing `DslProjectResolverExtensionDrainTest` must stay green modulo cache plumbing). TTL source: plugin settings via `DslProjectStateService`/`PersistentStateComponent` if a state service exists (verify — `DslProjectStateService.getInstance(project)` is referenced at `DslProjectResolverExtension.java:26`, so a state service IS present), else constant + follow-up; do NOT block on a full settings UI — constant-with-clear-name acceptable, note in plan. DEPENDENCY DECISION for executor: adding `com.github.ben-manes.caffeine:caffeine` to the IntelliJ plugin bundles ~900KB into the plugin distribution (gradle-intellij-plugin packs `implementation` deps) — evaluate the lighter zero-dep alternative (scheduled `ConcurrentHashMap` sweep via `ApplicationManager.getApplication().executeOnPooledTimer` or entry timestamps + lazy prune on next `populateModuleExtraModels` call) and pick; if Caffeine chosen, justify size cost in plan. Tests: (a) TTL expiry — injected `Ticker` (Caffeine path) or fake clock (sweep path), stale taskId entries vanish from `drainDiscovered`; (b) existing drain semantics test stays green; (c) failed-sync simulation — no notification listener call → entry still bounded by TTL/size, assert `DISCOVERED` (via accessor) does not grow unbounded. Drop stale TODO. Tier `backend`. Risk low: single class + tests; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T368-idea-plugin-discovered-cache.md` |
| T369 | Done | Lombok ctor sweep — collapse hand-rolled/delegating constructors in 4 starter classes | backend — code-confirmed TODOs (one concern: hand-rolled constructors where Lombok idiom applies), all in `dsl-starter/starter`: `JdbcDslRunRepository.java:43,50` (`// TODO: remove constructor, use lombok's one` ×2 — delegating overloads: `DataSource`→`NamedParameterJdbcTemplate` wrap + `qualifiedTableName(properties)` precompute); `DispatchStage.java:57,66` (same TODO ×2); `TemporalDslProcessService.java:88,100,112,154` (`// TODO: use only lomboks constructor` ×3 + `// TODO: use constructor` ×1 — incl. a `@Deprecated(forRemoval = true)` overload that delegates with `Long.MAX_VALUE` timeout and `Optional.empty()` webhook); `DslPipeContext.java:29,41` (`// TODO: remove constructor` ×2, same file also carries a separate record-redesign TODO — OUT of scope here, ctors only). Project idiom is `@RequiredArgsConstructor` (see `ApiKeyAuthFilter`, `DslFileBulkhead`), but these four classes hand-roll delegating ctor chains that bypass it — every new field forces editing multiple ctors, and the deprecated `TemporalDslProcessService` overload keeps a 11-arg signature alive that callers should not use. NOTE for scoping: these are NOT pure boilerplate — the overloads do real work (derivation, defaults), so the sweep is collapse-to-canonical, not delete: (1) keep ONE canonical `@RequiredArgsConstructor`-generated ctor per class over the true `final` field set; (2) replace derivation overloads with `static` factory methods named for intent (e.g. `JdbcDslRunRepository.fromDataSource(dataSource, ...)`, `TemporalDslProcessService.withDefaults(...)`-style) so derivation logic survives with a name; (3) DELETE the `@Deprecated(forRemoval)` `TemporalDslProcessService` overload outright — it is declared forRemoval, callers must move; compiler finds them; (4) update all call sites — 46 direct `new X(...)` references repo-wide excluding the four classes themselves, the bulk in tests (test fixtures get the factory names); (5) drop the seven ctor TODOs. Acceptance = existing `:starter:test` suite green with zero test deletions (fixture rewrites allowed); no behavior change — delegation order and derived values (e.g. `qualifiedTableName`) identical; where a test relied on a deleted overload, migrate to canonical/factory, do not loosen assertions. Tier `backend`. Mechanical but compiler-driven; risk low; rollback = revert one commit. Natural batch companion: T363/T366 (same module). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T369-lombok-ctor-sweep.md`        |
| T370 | Done | Explain-path failure handling — guard `DevDslRuntime.explain()` null value + failure branch in `DslRuntimeService` | backend — code-confirmed TODO + asymmetry bug: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/DevDslRuntime.java:34` reads `// TODO: add check for null, add default value case` inside `explain()`, which does bare `return result.value();` on the explain pipe's `Result<ExplainReport>`. The run path in the SAME codebase shows the correct pattern: `DslRuntimeService.java:85` (`RuntimeOutcome.error(toErrorResponse(name, runId, result.cause()))`) branches on failure — but the explain path `DslRuntimeService.java:91-93` calls `dslRuntime.explain(...)` then immediately dereferences `report.errors()` with NO failure/null handling. Failure mode: explain pipe returns failure `Result` (e.g. definition not found, explain-stage exception) → `Result.value()` on a Failure throws (or yields null) → NPE/unwrapped exception at line 93 → raw 500 with no runId, no structured `ErrorResponse`, no MDC-correlated diagnostics — worse than the run path's structured error for the same class of failure. Task: (1) `DevDslRuntime.explain()`: handle failure Result — either throw the cause (pipe contract) or return an empty/errored `ExplainReport` carrying the failure as a `PreviewErrorDetail` — pick the option consistent with how `ExplainReportStage` builds reports for soft errors (executor: read that stage first, match its idiom); null-value success Result gets the same default-report treatment (TODO's "default value case"); (2) `DslRuntimeService.explain()`: add the missing failure branch mirroring line 85 — structured `RuntimeOutcome.error(...)` with cause mapping, runId, and the existing `toErrorResponse` chain (DslException / PreviewTimeoutException / fallback already exist there — reuse); (3) tests: (a) explain on unknown definition → structured error response (assert error code + runId present, NOT raw 500); (b) explain-stage throws → same structured path, MDC runId logged; (c) success path unchanged — golden explain report test stays green; (d) null-value success Result (if reachable) → default report, no NPE; (4) drop stale TODO. Tier `backend`. Low risk: error-path addition, happy path untouched; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T370-explain-failure-handling.md` |
| T371 | Backlog | `make test` — one-command backend + frontend test runner | dx (Makefile) — code-confirmed gap: `grep -E '^[a-z-]+:' Makefile` lists help/up/down/backend/publish/frontend/dev/logs/clean/doctor/seed/seed-history — NO test target. Tests exist and are the project's core quality gate (2,600+ BE, 1,300+ FE per recent memory) but running both tiers means remembering two unrelated commands: `backend/dsl-platform/gradlew -p backend/dsl-starter :starter:test` (wrapper + module path + the Java-25/Gradle-8.13 caveat from CLAUDE.md) and `cd frontend && pnpm test` (which itself filters to `@cbs/admin-ui-plugin`). CLAUDE.md's end-to-end check documents publish/dev/seed but is silent on tests; the executor loop (docs/loop.md) and any CI-less contributor must re-derive this every time. Task: add `make test` target to the root Makefile: (1) runs BE tests — `backend/dsl-platform/gradlew -p backend/dsl-starter :starter:test` (NOT root `./gradlew` — fails under Java 25 per CLAUDE.md caveat; also `-x generateGitProperties` NOT needed on main worktree, only linked worktrees — keep plain); (2) runs FE tests — `cd frontend && pnpm test`; (3) per-check failure isolation: each tier's exit code captured, summary line per tier (`[ok] backend: N tests` / `[fail] frontend`), target exits nonzero if ANY tier fails but runs BOTH tiers regardless (no && short-circuit — a BE failure must not hide FE status); (4) optional `make test-backend` / `make test-frontend` granular targets reused by the aggregate; (5) `help` target auto-lists new entries (verify help is pattern-based, else add lines); (6) one-line mention in CLAUDE.md quick-check section. Style: match existing target idioms (`##` comment docs, `.PHONY`, printf banners — see `doctor`/`seed`). Tests/verification: run `make test` against the real tree — both tiers green from clean checkout state; also verify failure propagation (`make test-frontend` with an intentionally failing vitest spec in a scratch branch, revert). Tier `dx`. Zero production-code risk; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T371-make-test.md`               |
| T372 | Backlog | Remove deprecated `DslDefinitionLoader.load(Path, GlobalManager)` — SPI-only loading | backend — code-confirmed TODO + `@Deprecated(forRemoval = true)`: `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/DslDefinitionLoader.java:11` carries `//TODO: remove method, app must work only with spi` on the `load(@NonNull Path sourceDir, @NonNull GlobalManager gm)` overload. Prod code already routes through the SPI variants: `DslConfiguration.java:166` uses `loader.load(GlobalManager.globalManager())`, `DslReloadHandler.java:218` uses `loader.load(classLoader, target)` (reload compiles to a classloader then loads via SPI — Path variant unused there). Remaining surface: the interface declaration, its impl `DefinitionLoader.java:21`, and one test fake override `DslReloadResourceTest.java:583` (delegating stub). Task: (1) delete the overload from `DslDefinitionLoader`; (2) delete the `DefinitionLoader` impl of it — first read the impl body: if it contains load-from-sourceDir logic NOT reachable via any other path, confirm no reflection/ServiceLoader consumer references it (`grep -rn 'load(' backend --include='*.java'` + codegraph callers) before deleting; (3) drop the test-fake override in `DslReloadResourceTest` (stub deletion, not assertion loosening); (4) remove the TODO comment; (5) verify: full `:starter:test` suite green (reload resource + diagnostics tests cover the surviving SPI paths) and `dsl-platform` builds — starter consumes the platform via Maven Local, so run `backend/dsl-platform/gradlew -p backend/dsl-platform publishToMavenLocal -x test` then `:starter:test` per the standard two-module verify. Tier `backend`. Compile-driven, risk low; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T372-remove-path-load-variant.md` |
| T373 | Backlog | Codegen DI — inject `SourcePackageResolver` via `CompileConfig` (drop inline `new` in `SourceCompiler`/`ModelRegistryGenerator`) | backend — code-confirmed TODOs (one logical group, same concern): `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/SourceCompiler.java:78` and `.../generator/ModelRegistryGenerator.java:72` both read `// TODO: redo to bean from \`CompileConfig\`` and both do `var packageResolver = new SourcePackageResolver(new DslPackageNameResolver(codegenNaming));` inline. `CompileConfig` is the module's singleton factory and ALREADY wires sibling deps this exact way: `processCodeGenerator()`/`transactionCodeGenerator()`/`generatedClassProviderGenerator()` pass `new DslPackageNameResolver(codegenNaming())` via constructor. Only `SourceCompiler` and `ModelRegistryGenerator` bypass it. Task: (1) add `public @NonNull SourcePackageResolver sourcePackageResolver()` bean to `CompileConfig` — `singleton(() -> new SourcePackageResolver(new DslPackageNameResolver(codegenNaming())))`; (2) extend `SourceCompiler` ctor (currently 5 deps: definitionProviderGenerator, codeWriter, codegenNaming, dslPreprocessor, modelPreprocessor) with the resolver as 6th param; extend `ModelRegistryGenerator` ctor (currently codeWriter, codegenNaming, modelTypeExtractor) as 4th; (3) update the two `CompileConfig` factory methods to pass `sourcePackageResolver()`; (4) delete both inline constructions + TODOs; drop now-unused `DslPackageNameResolver`/`SourcePackageResolver` imports where applicable; (5) fix any direct `new SourceCompiler(...)`/`new ModelRegistryGenerator(...)` call sites (tests, other factories — grep to enumerate); (6) verify: `backend/dsl-platform/gradlew -p backend/dsl-platform :dsl-codegen:test` green + platform `publishToMavenLocal -x test` if starter consumes changed ABI, then `:starter:test` per standard two-module verify. Tier `backend`. Pure constructor wiring, compiler-driven, rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T373-codegen-package-resolver-di.md` |
| T374 | Backlog | Request-param extraction to typed records via converter mappers (`DslExecutionsHandler` + `DslIntrospectionHandler`) | backend — code-confirmed TODOs (one logical group, identical concern): `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/controller/DslExecutionsHandler.java:56` and `.../controller/DslIntrospectionHandler.java:17` both read `// TODO: Add mapstruct mapper, that map request.param to a record`. Both handlers hand-roll `request.param("x")` chains into loose locals — `DslExecutionsHandler.java:280-283` (processName/status/mode/correlationId for the list filter) and `DslIntrospectionHandler.java:36,56-58` (format/name/type/description for catalog query). MapStruct is ALREADY an established idiom here, not a new pattern: `libs.versions.toml` pins 1.6.3, `starter/build.gradle:48-49` wires `implementation libs.mapstruct` + `annotationProcessor libs.mapstruct.processor`, and `cbs.nova.starter.converter` holds 8 existing mappers (`DslRuntimeMapper` is the reference — `@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.IGNORE)` with per-input-shape `fromXxx` default methods). Task: (1) define query records (e.g. `ExecutionListQuery(processName, status, mode, correlationId)`, `IntrospectionCatalogQuery(format, name, type, description)`) — placement per existing model/query-record idiom in the module; (2) add mapper default methods in `converter` package that accept `ServerRequest` (or the raw param map) and apply the existing defaults/blank-filter semantics EXACTLY (`.filter(s -> !s.isBlank()).orElse(null)`, `orElse("mermaid")` etc.) — read both handlers first and preserve every default verbatim, incl. `DslExecutionsHandler.java:328` raw-param helper semantics if in scope; (3) rewrite the two handlers to call the mappers; drop both TODOs; (4) tests: existing `DslIntrospectionResourceTest` + executions-handler tests stay green; add mapper-level unit tests pinning blank-param→null, missing→default behavior. Tier `backend`. Bounded to 2 handlers + converter pkg; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T374-request-param-records.md` |
| T375 | Backlog | `DslDescriptor` builder — add lombok `@Builder` to the 14-arg record, sweep all 16 positional construction sites | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/stage/PreviewCacheKeyBuilder.java:26` reads `// TODO: find usage of new DslDescriptor and replace to a lombok's builder`. `DslDescriptor` (`backend/dsl-platform/dsl-api/src/main/java/cbs/nova/dsl/DslDescriptor.java`) is a record with FOURTEEN components — name, type, description, inputType, outputType, hasCompensation, hasSideEffects, previewBehavior, parameters, taskQueue, version, startToCloseTimeout, heartbeatTimeout — and there are 16 `new DslDescriptor(...)` sites across 14 files (3 prod: `ProcessDslObject.java:46`, `FunctionDslObject.java:35`, `TransactionDslObject.java:45` in the `dsl` module + the TODO site itself; rest tests incl. `ExplainReportTest`, `ProcessBuilderTest`, `ExplainDiagramRendererTest`, `ExplainReportStageTest`, `DslIntrospectionMapperTest` ×2). Consecutive `@Nullable` params (description/inputType/outputType; taskQueue/version; the two Duration timeouts) make positional args swap-prone — compiler cannot catch a `null` landing in the wrong slot, and one prod site (`PreviewCacheKeyBuilder` helper-describe path) already stacks 4+ adjacent nulls. Task: (1) add `@Builder` to the `DslDescriptor` record (lombok already used in `dsl-api` — see `history/DslRun.java`; match its annotation style, keep `@NonNull`/`@Nullable` untouched); (2) sweep ALL 16 sites to named-arg builder calls — `.name(...).type(...)...` — preserving every value verbatim; this is also an audit: if any site's positional args look suspicious (e.g. null in a `@NonNull` slot), flag it in the PR/commit message instead of silently "fixing"; (3) drop the TODO; (4) verify: `:dsl-api` + `:dsl` module tests, republish platform (`publishToMavenLocal -x test`), then `:starter:test` — ExplainReport/Introspection suites exercise the descriptor paths. Tier `backend`. Mechanical, compiler-driven, rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T375-dsl-descriptor-builder.md` |
| T376 | Backlog | Test fake: implement lazy `registerHelper(name, Supplier)` + cover `HelperManager` supplier delegation | backend (test) — code-confirmed TODO: `backend/dsl-platform/dsl/src/test/java/cbs/nova/dsl/HelperManagerTest.java:190` — the inline fake registry's `registerHelper(@NonNull String name, @NonNull Supplier<Executable<?, ?>> helperSupplier)` override is an EMPTY no-op body carrying `// TODO: implement lazy helper registrations for tests`. This masks a real prod path: `HelperManager.java:46` delegates the supplier variant to `DefaultHelperRegistry` (lazy — supplier must NOT be invoked until first execution), and that supplier path is what generated SPI code exercises (`HelperSpiProcessor` → `HelperResolver.registerHelpers` → `GlobalManager.registerHelper(name, supplier)` at `GlobalManager.java:68-74`). With the fake no-opping it, `HelperManager`'s supplier-delegation line has zero test coverage at its own level (registry-level semantics live separately in `DefaultHelperRegistryTest`). Task: (1) implement the fake's lazy registration — store `Supplier` per name, resolve on first lookup/execution, honoring whatever semantics `DefaultHelperRegistry` documents (READ `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/registry/DefaultHelperRegistry.java:28+` first and mirror them: memoized single invocation vs per-call, null-name rejection, overwrite behavior); (2) add `HelperManagerTest` cases: (a) supplier NOT invoked at registration (lazy), (b) supplier invoked exactly once on first execution (memoized — assert invocation count via counting supplier), (c) eagerly-registered helper and lazily-registered helper coexist, (d) supplier returning null / throwing → behavior mirrors `DefaultHelperRegistry` (don't invent semantics); (3) drop the TODO; (4) keep the fake's existing eager `registerHelper(name, Executable)` behavior untouched — other tests depend on it. Tier `backend`. Test-only change, zero prod risk; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T376-helper-manager-lazy-test.md` |
| T377 | Backlog | Stale scan-TODO cleanup — drop already-satisfied entity/repo scan TODOs with verification | backend (hygiene) — code-confirmed STALE TODOs (one group, same concern: Spring scanning scope, both already satisfied): (1) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/entity/DslRunEntity.java:10` reads `//TODO: we need limit entity scan, only to one package cbs.nova.starter.entity` — but Spring Data JDBC has NO entity scanning concept (that is JPA's `@EntityScan`); SD JDBC discovers `@Table` types through repository domain types and `NamingStrategy` (`DslRunNamingStrategy` bean exists in `DslRunRepositoryConfiguration`). A repo-wide grep for `@EntityScan` in `starter/src/main` returns ZERO — there is nothing to limit; (2) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/persistence/DslRunJdbcRepository.java:16` reads `// TODO: we need to limit package scan for repositories, only to cbs.nova.starter.persistence` — but `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/DslRunRepositoryConfiguration.java:39` ALREADY declares `@EnableJdbcRepositories(basePackages = "cbs.nova.starter.persistence")`. Risk of keeping them: a future executor "resolves" the TODOs by adding JPA-only annotations (`@EntityScan`) or redundant scan limits — introducing confusion and dead config. Task: (1) re-verify both claims at execution time (`grep -rn '@EntityScan' backend/dsl-starter/starter/src/main` → zero; read `DslRunRepositoryConfiguration.java:39` → scoped); (2) replace each TODO with a one-line comment recording WHY nothing is needed (e.g. `// scanning already scoped: @EnableJdbcRepositories(basePackages=...) in DslRunRepositoryConfiguration; SD JDBC has no entity scan` — match file comment style) — or delete outright if surrounding code self-documents, executor's judgment per file style; (3) if verification DISCONFIRMS either claim (config changed since planning), stop and update this row instead of forcing the cleanup; (4) `:starter:test` green (context-load tests exercise the scan config). Tier `backend`. Zero behavior change; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T377-stale-scan-todo-cleanup.md` |
| T378 | Backlog | `docs/dsl/helpers.md` catch-up — document the T350–T358 helper wave (9 shipped, 0 documented) | backend (docs) — code-confirmed doc drift: `docs/dsl/helpers.md` (484 lines, 33 sections) was last extended by T348/T349 (`git log -- docs/dsl/helpers.md` → 76719fc3 dateMath, 213fb77e metric, fa0b3889 backoff) but the ENTIRE T350–T358 helper wave shipped with zero doc updates — per-name grep in helpers.md: `JsonPatch` 0, `ListOps` 0, `MathHelper` 0, `HttpAuth` 0, `QueryString` 0, `Compression` 0, `Random` 0, `Semver` 0, `Jwt` 0. All nine classes exist in `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/helper/`: `JsonPatchHelper`, `ListOpsHelper`, `MathHelper`, `HttpAuthHelper`, `QueryStringHelper`, `CompressionHelper`, `RandomHelper`, `SemverHelper`, `JwtHelper` — merged Done on kanban T350–T358. The workbench catalog (T311) serves the UI, but `helpers.md` is the human reference for DSL authors, and it silently stops at the T349 wave. Task: (1) for EACH of the nine helpers read its class + its test file (methods, op discriminators, in/out shapes, error cases) — tests are the source of truth for behavior; (2) add one section per helper in helpers.md following the existing section idiom (grouped tables of methods, one runnable DSL snippet each — match the DateMath/Metric sections' structure added by T348/T349); (3) snippets must be REAL: cross-check each against the helper's test so no fictional method names ship in docs; where a natural combo exists (HttpAuth→HttpCall, QueryString→UrlEncode, Jwt parse→verify chain), show it in one snippet; (4) add the nine to any summary/TOC list at the top of helpers.md if one exists; (5) acceptance: `grep -c` for each of the nine names in helpers.md ≥1, and every documented method name exists in the corresponding helper class (`grep` spot-check). Tier `backend` (docs). Docs-only, zero code risk; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T378-helpers-doc-catchup.md` |
| T379 | Backlog | DSL example recipes catch-up — `examples.md` + `dsl-examples` module for the T333–T358 helper wave | backend (examples) — code-confirmed example drift: `docs/dsl/examples.md` (326 lines) is the recipe guide tied to the `backend/dsl-examples` module, but its per-helper recipe sections stop at the T332 wave (`## Content hashing with sha256` is the last, git 30ff221e) — every helper shipped T333–T358 (yaml, csv, dateMath, metric, backoff, jsonPatch, listOps, math, httpAuth, queryString, compression, random, semver, jwt) has ZERO recipes. Distinct from T378 (helpers.md = API reference tables; this = task-oriented recipes + real example DSL files). Task: pick the SIX highest-demo-value recipes and ship each as (a) a new compact-source example file under `backend/dsl-examples/src/` following the module's `define()` + fluent-API idiom, and (b) a `## ...` recipe section in examples.md pointing at that file — suggested set (executor may swap for equal value): (1) `jwt` parse+verify then `httpAuth` bearer → `httpCall` — authenticated API call end-to-end; (2) `jsonPatch` apply to a stored document + `jsonExtract` read-back — state-update round trip; (3) `listOps` pluck/groupBy → `math` mean/max — aggregation pipeline over JSON records; (4) `semver` compare gate + `dateMath` add — release-window check; (5) `parseCsv`/`parseYaml` → transform → `formatCsv` export; (6) `queryString` build + `urlEncode` — paginated URL construction. Constraints: every example must COMPILE and run through the module's existing verification (`:dsl-examples:compileDsl` + the integration-test harness the module already uses — read `docs/dsl/examples.md` "Building/Running" sections first and follow them exactly); recipe text references the real file path and real helper method names (cross-check against helper classes, no fictional API — same rule as T378); one recipe per concern, no mega-example. Acceptance: 6 new example files compile via `compileDsl`; 6 new recipe sections in examples.md; each recipe's method names exist in the corresponding helper class. Tier `backend`. Test-covered by the module's existing integration harness; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T379-example-recipes-catchup.md` |
| T380 | Backlog | Doc-drift guard — test asserting every registered `@Helper` appears in `docs/dsl/helpers.md` | backend (test) — code-confirmed recurrence pattern: `docs/dsl/helpers.md` drifted behind the shipped surface for NINE helpers (T350–T358 wave — see T378 for the catch-up), and `docs/dsl/examples.md` drifted for FOURTEEN (T333–T358, see T379). Nothing enforces doc↔code consistency, so every future helper ship can silently repeat it. Task: add a guard test in `dsl-starter/starter` that (1) enumerates registered helper names at runtime — boot the existing test context (reuse the pattern of an existing helper-registry-aware test; read how `GlobalManager`/`HelperRegistry` expose `allNames()` — `HelperManagerTest` fake and `DefaultHelperRegistry` show the API) and collect every helper name the registry actually serves; (2) resolves `docs/dsl/helpers.md` from the test working dir (module dir → repo root is `../../../..`; guard with `Assumptions.assumeTrue(Files.exists(...))` so the test self-skips when the module runs without the repo layout, e.g. from a published artifact context); (3) asserts each registered name has a case-insensitive substring match in helpers.md; on failure, prints the missing names with a one-line fix hint ("add a section to docs/dsl/helpers.md or map an explicit doc alias"); (4) maintain an explicit ALLOWED-UNDOCUMENTED set (empty initially, post-T378) for names intentionally undocumented (test fixtures like `UnreliableApiHelper`, `ConditionalFailingHelper`, `FileLatchHelper` if they are registered — CHECK which test-support helpers register and pre-seed the set with justification comments). Tier `backend` (test-only). Depends on T378 landing first or the guard fails immediately — do NOT run before T378 merges. Zero prod risk; rollback = revert one commit. See plan. |  Medium  | loop  | -      | T378       | `./docs/plans/T380-doc-drift-guard.md` |
| T381 | Backlog | OpenAPI 3.1 export as build artifact — `make openapi` writes `docs/openapi.json` from live `/v3/api-docs` | backend (dx, Epic 1) — roadmap-endorsed gap (`docs/roadmap.md` Epic 1 "Schema source of truth": "Publish a complete OpenAPI 3.1 document from the Spring Boot side (springdoc is already present). Treat it as a build artifact consumed by the frontend."): springdoc deps are wired (`starter-launcher/build.gradle:16` `implementation`, `starter/build.gradle:56` `compileOnly`) but NO openapi artifact exists anywhere in the repo (`find -name 'openapi*.json'` → zero) and kanban has no OpenAPI task. The DSL HTTP surface lives only as hand-written Nitro proxy routes + T335 contract tests that catch drift after the fact. Task: (1) verify at runtime what `GET /v3/api-docs` actually returns with the full DSL surface — CRITICAL: most endpoints are functional routers (`RouterFunction` handlers: `DslIntrospectionHandler`, `DslExecutionsHandler`, drafts/reload/schedule/webhook handlers), which springdoc includes only when the webmvc router discovery picks them up — boot the app (standard `SERVER_PORT=8090` bootRun from CLAUDE.md), fetch `/v3/api-docs`, and diff the listed paths against the known surface (definitions/executions/runs/preview/explain/drafts/reload/schedules/webhooks); if RouterFunction paths are missing, add the minimal springdoc config to register them and add `@Operation`/`@ApiResponse` summaries ONLY where a path would otherwise be undocumented (name+summary, not full schema work — that is Epic 1's generated-client workstream, out of scope); (2) add `make openapi` target: boots backend headless (reuse the `make backend`/doctor pattern), curls `/v3/api-docs` to `docs/openapi.json`, validates it parsed as JSON with a `paths` object containing ≥ the known DSL route count, shuts down, exits nonzero on validation failure (this makes it CI-able later); (3) commit the first generated `docs/openapi.json`; (4) CLAUDE.md quick-check gains one line. Acceptance: `make openapi` regenerates a diff-stable (normalize key ordering if needed) `docs/openapi.json`; document which paths are covered vs known-missing (if RouterFunction coverage proves impossible without heavy config, STOP and record findings on this row — that is a replanning signal for Epic 1, not a failure). Tier `backend`. Additive dx, no prod code paths touched beyond optional annotations; rollback = revert one commit. See plan. |  High  | loop  | -      | -          | `./docs/plans/T381-openapi-export-artifact.md` |
| T382 | Backlog | Pagination envelope unification — one `{items, total, offset, limit}` shape for every list endpoint | backend (+FE contract) — roadmap-endorsed gap (`docs/roadmap.md` Epic 1 "Pagination convention": "One `{items, total, offset, limit}` envelope for every list endpoint (executions, drafts, definitions, schedules)"). Code-confirmed inconsistency: executions returns `ExecutionListResponse(items, total)` (`DslExecutionsHandler.java:93`, model has NO offset/limit echo — client cannot confirm the clamped values it actually got), while drafts and schedules return BARE ARRAYS (`DslDraftHandler` builds a `summaries` list, schedules return `List<ScheduleSummary>`-style shapes, `DslFileModels`/`ScheduleModels` contain no list-envelope record) — a bare array cannot express `total`, so the frontend cannot page drafts/schedules at all today. Task: (1) add a shared generic envelope `PageResponse<T>(List<T> items, long total, int offset, int limit)` in `cbs.nova.starter.model` (Jackson-3 generics note: check how existing generic records serialize — mirror `ExecutionListResponse`); (2) executions: return the shared envelope echoing the CLAMPED limit/offset (`clampLimit`/`clampOffset` results) — shape-compatible superset of today's `{items,total}`; (3) drafts list + schedules list + definitions list: accept `limit`/`offset` query params (sane defaults, clamp like executions) and wrap in the envelope — for these the response changes from array to object, so (4) update the T335 BFF↔backend contract tests and the FE consumers (find them via the BFF proxy routes for drafts/schedules/definitions → the composables/stores parsing those responses; adapt destructuring to `.items` and use `total` for hasMore/pagination UI where trivially available — do NOT build new pagination UI, only keep current behavior working); (5) handler tests + contract tests green. Tier `backend` (with FE contract follow-through included). BREAKING for drafts/schedules/definitions consumers — repo-internal only (BFF passes JSON through verbatim), acceptable in one commit. Value: Epic-1 convention + unlocks real FE paging later. Rollback = revert one commit. See plan. |  High  | loop  | -      | -          | `./docs/plans/T382-pagination-envelope.md` |
| T383 | Backlog | Audit trail phase 1 — append-only `dsl_audit` table + writes on control-plane mutations + `GET /api/v1/dsl/audit` | backend — roadmap-endorsed gap (`docs/roadmap.md` Epic 2 "Audit logging": "Append-only `dsl_audit` table: actor (triggered_by), action, target, correlation id, timestamp, outcome. Surface it as `GET /api/audit` and an Executions-page tab."). Current state: zero audit surface — the ONLY actor record is `dsl_runs.triggered_by` (V1 migration, run-path only); control-plane mutations (publish, reload, draft write/bulk-write, schedule create/update/delete, run cancel) leave NO trace of who/what/when. Migrations live at `backend/dsl-starter/starter/src/main/resources/db/migration/{postgres,h2}/V1__init.sql` — both dialects, single version. Task (phase 1, backend only — FE tab is a later task): (1) `V2__dsl_audit.sql` for BOTH postgres and h2: `dsl_audit (id BIGSERIAL PK, occurred_at TIMESTAMPTZ NOT NULL, actor VARCHAR(320) NOT NULL, action VARCHAR(64) NOT NULL, target VARCHAR(512) NOT NULL, correlation_id VARCHAR(200), outcome VARCHAR(16) NOT NULL, details_json TEXT)` + index on `(occurred_at DESC)` and `(action)`; no UPDATE/DELETE anywhere — append-only by convention, enforced in repo (only insert + query methods); (2) `DslAuditRepository` (JDBC, mirror `JdbcDslRunRepository` idioms — NamedParameterJdbcTemplate, lombok ctor) + `DslAuditService` with one `record(actor, action, target, correlationId, outcome, details)` method that never throws into the caller's flow (audit failure = warn log + swallow, per append-only observability semantics — pin with a test); (3) actor resolution: reuse the existing auth context (X-Api-Key principal / OIDC subject if present, else `anonymous`) — read how `ApiKeyAuthFilter`/OIDC expose the principal and follow that idiom; correlation id from the existing `X-Correlation-Id`/MDC plumbing; (4) wire writes into the control-plane handlers: publish, reload, draft write + bulk-write, schedule create/update/delete, run cancel — one `record(...)` call per mutation with the outcome (SUCCESS/FAILURE — failure path too, this is what audits are for); (5) `GET /api/v1/dsl/audit` in a new `DslAuditHandler` following the RouterFunction handler idiom: limit/offset + total, envelope per T382's `PageResponse` if landed else `{items,total}` matching executions today (adapt at execution time); (6) BFF proxy route `frontend/admin-ui-plugin/server/api/v1/dsl/audit.get.ts` (explicit file, per CLAUDE.md caveat); (7) tests: repo round-trip (h2), service swallow-on-failure, one handler test per audited mutation asserting a row lands with right action/outcome, audit endpoint pagination. Tier `backend`. Additive table + endpoint; no existing behavior change; rollback = revert one commit (drop V2 note: flyway repair on revert — document in PR). See plan. |  High  | loop  | -      | -          | `./docs/plans/T383-audit-trail-phase1.md` |
| T384 | Backlog | Log correlation completion — expose `rid`/`correlationId` MDC in log patterns, global correlation filter, BFF log fields, runbook query recipe | backend (+BFF, Epic 4) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 "Log correlation": "Ensure rid / correlation_id is on every log line in backend and BFF; document the query recipe"). Code-confirmed holes: (1) MDC is POPULATED but INVISIBLE — `RequestIdFilter.java:27` (`MDC.put(REQUEST_ID_MDC_KEY, requestId)`) and 5 more MDC sites (`TemporalDslProcessService:498` `runId`, `DslRuntimeService:136`, `LoggingExecutionListener:91`, `DryRunLoggingContextPropagator:41`) write MDC keys, but `starter/src/main/resources/application.yml` has ZERO `logging.pattern` configuration and Spring Boot's default console pattern prints only `traceId/spanId` — the MDC values never reach the log output; (2) `X-Correlation-Id` is validated (`service/CorrelationId.java`) and stored on `dsl_runs.correlation_id` but NOT put into MDC for the request scope — only ad-hoc on the run path (and reusing the requestId key); (3) BFF `writeLog` (`frontend/admin-ui-plugin/server/utils/httpClient.ts`) logs `requestId` in structured data but never the correlation id it already extracts; (4) runbook (`docs/runbook.md`, T336) has no log-correlation query recipe. Task: (1) add `logging.pattern.level: "%5p [rid=%X{rid} cid=%X{cid}]"`-style config (read `RequestIdFilter` for the actual MDC key name first and align naming; keep keys short and grep-friendly; null-MDC must not render noisy empty brackets for uncorrelated lines — use Boot's pattern defaults or a wrapper that omits empty) covering BOTH the handler threads and, if separate, the Temporal worker logging config; (2) extend `RequestIdFilter` (or add a sibling `CorrelationIdMdcFilter`) to put a VALIDATED `X-Correlation-Id` (reuse `CorrelationId` validation) into MDC as its OWN key for every request, clear in finally; generate nothing — absence means absent, don't fake ids; (3) BFF `writeLog`: include `correlationId` alongside `requestId` in the structured data (it already reads the header); (4) runbook: new "Correlating a request across logs" section — recipe: what a client sends (`X-Request-Id`/`X-Correlation-Id`), what appears in BE logs (`rid=... cid=...`), BFF logs (requestId/correlationId JSON fields), `dsl_runs.correlation_id` lookup, and one grep+jq example over `make logs` output; (5) tests: filter test asserting MDC set/cleared incl. invalid correlation id rejection path; one log-capture test (ListAppender) asserting a request-scoped log line contains both keys. Tier `backend`. Config+filter+docs, no behavior change to business logic; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T384-log-correlation.md` |
| T385 | Backlog | `DslProperties` → record — convert mutable `@Data` config class to constructor-bound record (unify with `DryRunProperties` idiom) | backend (refactoring) — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/properties/DslProperties.java:9` reads `//TODO: redo to a record(e.g. check git history)`. The class is `@Data @AllArgsConstructor @NoArgsConstructor @ConfigurationProperties(prefix = "cbs.dsl")` with 9 mutable fields (sourceDir, taskQueue, worker, reload, auth, drafts, files, git, fileBuffer) + 8 nested `@Data` classes carrying inline defaults (`taskQueue = "dsl-task-queue"`, `files.flushIntervalSeconds = 5`, `drafts.historyLimit = 20`, git/filebuffer defaults). In-repo precedent for the target idiom exists in the SAME package: `DryRunProperties` is already a record using `@DefaultValue` annotations + compact constructors for null-defaults + `@Valid` cascading — DslProperties just never got the same treatment. Value: constructor-bound immutable config (no accidental runtime mutation), one canonical props idiom in `config/properties`, closes the TODO. Companion cleanup in same concern: `DryRunProperties.java:11` reads `// TODO: redo to a class with lombok's annotations` — this TODO points the OPPOSITE direction (record→lombok class) and contradicts the target idiom; resolve it by DELETING the comment with a one-line justification (record + @DefaultValue is the chosen idiom, see DslProperties) rather than converting back — do NOT actually convert DryRunProperties to a class. Task: (1) git-history check the TODO mentions (`git log --follow -p -- '**/DslProperties.java' | head -200`) — if the class was previously a record and was reverted for a concrete reason, STOP and record findings on this row instead of forcing the conversion; (2) convert DslProperties + all 8 nested classes to records: inline field defaults become `@DefaultValue("...")` on record components (strings verbatim, numbers as strings — mirror DryRunProperties.Log `@DefaultValue("1000")`), nested-object defaults via compact constructor null-checks (mirror DryRunProperties's `context = context == null ? new Context("threadlocal") : context`) or `@DefaultValue` on nested records — Boot supports nested record defaults both ways, pick one style and stay consistent; keep javadoc on components verbatim; (3) accessor names unchanged (`getSourceDir()` etc.) so the ~6 production readers (`DslConfiguration`, services) compile untouched — verify with a grep of `properties.get`/`props.get` call sites before editing; (4) test-fixture migration: ~10 test files construct `new DslProperties()` then chain setters (`properties.setSourceDir(...)`, `props.getFiles().setFlushIntervalSeconds(0)`, `props.getFileBuffer().setMaxEntries(3)`, `props.getDrafts().setHistoryLimit(2)` — see DslFileHandlerTest, DslDefinitionBundleResourceTest, DslDefinitionStatusResolverTest, DslFileServiceTest, DslReloadDiagnosticsTest, DslDraftResourceTest, DslFileBufferTest, DslVersioningIntegrationTest) — add a small test-side factory (e.g. static `DslPropertiesFixture.with(...)`-style helper or a `@Builder` on the record — lombok `@Builder` works on records and keeps call sites named-arg) and sweep the fixtures; zero test deletions, assertions unchanged; (5) integration-test builder `.setTaskQueue(TASK_QUEUE)` at DslRunCancellationIntegrationTest.java:112 — check whether that is DslProperties or a different builder before touching; (6) drop both stale TODOs (DslProperties:9, DryRunProperties:11). Acceptance: `:starter:test` + `:starter:integrationTest` (if wired) green, zero test deletions; bound config from `application.yml` still resolves defaults identically — pin with at least one binding assertion that omitted keys yield the documented defaults (extend existing properties-binding test if one exists, else one new context test using `ApplicationContextRunner` with `cbs.dsl.*` unset). Sequencing note: T366 adds `acquireTimeoutSeconds` to `DslProperties.Files` and T384's filters read nothing here — land either order, but if both are pending the second one rebases trivially (record component add). Tier `backend`. Mechanical, compiler-driven; risk low-medium (binding semantics are the only subtlety — @DefaultValue coverage must be exhaustive or Boot binds nulls where mutable class had defaults); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T385-dsl-properties-record.md` |
| T386 | Backlog | Codegen whole-object transfer — generated process/transaction impls resolve typed `DslObject` via generated provider instead of string registry lookup | backend (codegen) — code-confirmed TODO group, same concern in both generators: `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/generator/ProcessCodeGenerator.java:106` reads `// TODO: add new method that allow to transfer whole object(e.g. without usage of registry, e.g. use dsl directly). Like var process = new GeneratedDslDefinitionProvider().byName("${processName}").ifPresentOrElse(...)` with a matching marker at `:126` (`//TODO: place here new code var process = ...`), and `TransactionCodeGenerator.java:104` reads `// TODO: add new method that allow to transfer whole object, like in ProcessCodeGenerator`. Today the generated impl resolves at runtime purely by STRINGS: `GlobalManager.globalManager().runProcess("${processName}", VERSION, ctx)` + `compensateProcess("${processName}", compCtx, error)` — a typo or registry mismatch surfaces only at invocation time, and the typed definition object is never in hand. Infrastructure for the fix ALREADY exists: `GeneratedClassProviderGenerator` already emits `new LoanDsl().byName("LoanDisbursement").orElseThrow()` (see `GeneratedClassProviderGenerator.java:178` and its tests), and `DslDefinitionProvider.byName(String)` (`dsl-api/DslDefinitionProvider.java:13`) returns `Optional<DslObject>`. What is missing: (1) `GlobalManager` (`backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/GlobalManager.java`) has NO `DslObject`-accepting overloads — only string-based `runProcess(name, version, ctx)` (`:119`), `runTransaction` (`:126`), `compensateProcess` (`:297`), `runProcessWithCompensation` (`:309`); (2) the process/transaction impl templates do not resolve the definition object at all. Task: (1) add `DslObject`/`ProcessDslObject`-accepting overloads to `GlobalManager` mirroring the string variants (`runProcess(ProcessDslObject, ctx)`, `compensateProcess(ProcessDslObject, compCtx, error)`, `runProcessWithCompensation(...)` object form; same for transaction) — implementation delegates to the SAME internal paths the string lookups use after registry resolution (refactor string variant = resolve + delegate to object variant, no duplicated logic; preserve failure semantics: what happens today when name misses the registry must happen identically); (2) update `ProcessCodeGenerator.generateImpl` template: resolve once at execute-time via the generated provider idiom — `var process = new ${generatedProviderName}().byName("${processName}").orElseThrow(...)` — exact provider class name/lookup source comes from how `GeneratedClassProviderGenerator` wires it; pick resolution timing deliberately (per-execute vs static init — registry hot-reload semantics differ; read how reload works before choosing, document choice in plan) and pass the object to the new overloads; (3) same for `TransactionCodeGenerator.generateImpl`; (4) drop the three TODOs (`:106`, `:126`, `TransactionCodeGenerator:104`); (5) tests: regenerate/existing golden-file tests for both generators updated to assert the new body shape (typed resolution + object-delegating calls, no raw string `runProcess` call in generated impl), plus `GlobalManager` unit tests for the object overloads incl. name/version consistency (object's own name/version win over none — object IS the source of truth); (6) verify per standard platform flow: `:dsl-codegen:test` then `publishToMavenLocal -x test` then `:starter:test`. Tier `backend`. Sequencing note: T365 (codegen provenance, In Progress) edits these same templates via `GeneratorMetadata.annotation(...)` lines — land after T365 merges or rebase trivially. Additive API + template change; medium risk (generated-code behavior), contained by golden tests; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T386-codegen-whole-object-transfer.md` |
| T387 | Backlog | Codegen default base package externalization — `cbs.nova.dsl.generated` hardcoded in `CodegenNaming` → `DslCompilerOptions` setting | backend (codegen) — code-confirmed TODO: `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/model/CodegenNaming.java:8` reads `// TODO: move to compiler/gradle settings` above `private static final String BASE_PACKAGE = "cbs.nova.dsl.generated"`. The constant is only the DEFAULT — callers already override per-call via nullable `targetPackage` params (`registryPackage`, `versionedPackage`, `versionedBasePackage` all take `@Nullable String targetPackage`) — but the default itself is compiled in: any embedding project that wants generated code under its own namespace by default must pass `targetPackage` at EVERY call site or fork the constant. Plumbing for the fix already exists end-to-end: `DslCompiler.main` builds `DslCompilerOptions.fromProperties(args[0])` (`DslCompiler.java:60`), threads `options.targetPackage()` through `compileInternal` (`:74,113,116,123,126,133`), and `SourceCompiler` carries its own `CompileOptions` record (`SourceCompiler.java:390`); `CodegenNaming` itself is a `CompileConfig` singleton (`CompileConfig.java:40` `singleton(CodegenNaming::new)`), so the default never sees options today. Task: (1) add `basePackage` (nullable, no default baked at parse layer) to wherever options live — `DslCompilerOptions` properties parsing (+ the `SourceCompiler.CompileOptions` pass-through if the value must reach naming inside source compilation); validate: non-blank, valid java package pattern (reuse whatever validation `targetPackage` gets, if none, add the same light check to BOTH — do not validate one and not the other); (2) make `CodegenNaming` constructor-injected: `new CodegenNaming(String defaultBasePackage)` replacing the static constant; keep `"cbs.nova.dsl.generated"` as the fallback default ONLY at the single composition point; all three public methods keep their nullable `targetPackage` override semantics (explicit targetPackage still wins over configured default — preserve precedence: explicit arg > configured option > built-in constant; pin precedence with tests); (3) wire through `CompileConfig` — note `CompileConfig` has no options access today; EITHER add a `basePackage()` accessor reading the same singleton-supplied value the compiler entry sets, or construct `CodegenNaming` at the `DslCompiler.compile` composition point and pass it down — follow how T373 plans to wire `SourcePackageResolver` (land order note below), do NOT invent a second config mechanism; (4) drop the TODO; (5) tests: options parsing round-trip (property present/absent/blank), precedence triple (arg > option > constant) for all three naming methods, package-validation rejection; update golden tests if generated package strings change under a non-default option (default path output must be BYTE-IDENTICAL — golden tests prove no accidental default change); (6) gradle-plugin surface IF one exists (`grep -rn 'basePackage\|targetPackage' backend/dsl-plugins` — if a gradle plugin forwards options, expose the matching property there too; if none, note it in the plan and stop). Acceptance: `:dsl-codegen:test` green, default-path golden output unchanged, non-default option demonstrably relocates the generated package (one new test). Tier `backend`. Additive option + ctor refactor, compiler-driven, low risk; rollback = revert one commit. Sequencing: touches `CompileConfig`/`CodegenNaming` same area as T373 (SourcePackageResolver DI) and T386 (generator templates) — any order works, last one rebases; if executing together with T373 prefer doing both wirings in one pass. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T387-codegen-base-package-option.md` |
| T388 | Backlog | Cross-process flush guard — `DslFileService` `ReentrantLock` is JVM-local; docker multi-instance needs DB-level flush coordination | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/service/DslFileService.java:37` reads `// TODO: since app will be started in docker, we need some other way of lock, to prevent multi flush`. The scheduled flush (`flushExecutor.scheduleWithFixedDelay(this::flushPending, ...)` at `:51`, also triggered from `:69,117,134`) is guarded ONLY by `private final ReentrantLock flushLock` (`:38`) — JVM-internal. Two app replicas sharing one Postgres = both schedulers fire `flushPending()` on the same staged buffer state per instance; double-flush races on the repository write path (buffer drain semantics assume one drainer). Today single-instance hides this, but the TODO names the docker deployment intent explicitly. RECOMMENDED approach (in-house, no new dependency, respects "no new patterns without discussion"): Postgres session advisory lock — wrap the flush critical section so the draining instance holds `pg_advisory_xact_lock(<fixed key>)` (or `pg_try_advisory_xact_lock` + skip-if-not-acquired: another replica is flushing, ours skips this tick — skipping is CORRECT for a scheduled flush, unlike blocking) inside the existing transaction; fixed bigint key derived once (e.g. constant + hash of `dsl.source-dir` so distinct deployments sharing a DB but not a source dir do not serialize each other accidentally — check whether that case is real; if one DB = one deployment, a plain constant is fine, document the choice); h2 (tests/dev, single process) = no-op/direct execution — dialect detection via existing datasource/dialect plumbing if present, else `DatabaseMetaData.getDatabaseProductName()`. Alternative needing team discussion, NOT to be done unilaterally: ShedLock (`@SchedulerLock` + JdbcTemplateLockProvider) — standard but adds a dependency + lock table migration; note it in the PR as rejected-for-now. Task: (1) read `flushPending()` (`:140`) + `DslFileBuffer` drain contract to find the exact critical section (lock must cover buffer.drain → repository write, and `FlushResult` accounting must reflect skipped-vs-flushed: add a SKIPPED outcome or return existing empty result — executor reads FlushResult shape and picks, do not silently report 0 flushed as success if it misleads callers); (2) extract the cross-process guard behind a small interface (e.g. `FlushCoordinator.tryAcquire()`/`release()` or a `runExclusive(Supplier<FlushResult>)`) with two impls: postgres advisory, no-op — bean selection in `DslConfiguration` next to bulkhead wiring; keep the existing `ReentrantLock` as the in-JVM second layer (scheduled + manual flush both funnel through the single-thread executor already — verify whether ReentrantLock is even still needed, simplify if the executor already serializes; if kept, document why); (3) drop the TODO; (4) tests: unit tests with no-op coordinator (behavior identical to today — existing DslFileServiceTest suite stays green unchanged); one postgres-dialect coordinator test if a Testcontainers postgres harness exists (grep integrationTest for existing postgres containers — if none, h2-based test asserting no-op path + a plain-JDBC test against an embedded postgres is OUT of scope, note as follow-up); (5) document the multi-instance flush contract in `docs/runbook.md` deployment section if one exists (one paragraph). Tier `backend`. Low-medium risk: flush path is background and idempotent-ish (verify — if double-flush is actually harmless today because buffer.drain atomically claims entries, STOP and record that finding on this row instead: the TODO may be stale); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T388-flush-cross-process-lock.md` |
| T389 | Backlog | `DslRunNamingStrategy` dedup — delete deprecated `qualifiedTableName` in `DslRunRepositoryConfiguration`, route table naming through the strategy bean | backend (refactoring) — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/DslRunRepositoryConfiguration.java:87` reads `// TODO: use \`dslRunNamingStrategy\` instead` above a `@Deprecated(forRemoval = true) private String qualifiedTableName(DslRunPersistenceProperties)` helper used ONCE at `:75` (`new JdbcDslRunRepository(jdbcTemplate, jdbcRepository, mapper, encryptor, qualifiedTableName(properties))`). The replacement already exists and is a BEAN in the same class: `dslRunNamingStrategy()` (`:58`) constructs `DslRunNamingStrategy` whose private `qualifiedTableName()` carries BYTE-IDENTICAL logic (default `dsl_runs`, schema-qualified when non-blank — compare `DslRunNamingStrategy.java:28-33` vs the config helper `:89-95`). Two copies of the table-naming rule = drift risk: a future schema/table override fix applied to one silently misses the other (they serve the same physical table through SDJDBC mapping vs raw-JDBC respectively). Task: (1) expose the qualified name from `DslRunNamingStrategy` — preferred: make the method public `qualifiedTableName()` (rename-free, keeps SDJDBC `getTableName(DslRunEntity.class)` delegating to it so ONE code path serves both SDJDBC entity mapping and raw JDBC repo); (2) change the `dslRunRepository(...)` bean method to take `DslRunNamingStrategy` as a parameter (it is a `@ConditionalOnMissingBean` bean in the SAME config class — same-context injection works; pass `dslRunNamingStrategy(properties)` directly if parameter injection ordering is awkward, pick whichever matches the class's existing bean-composition style); (3) DELETE the deprecated helper + its TODO; (4) check `JdbcTransactionExecutionRepository` for a parallel copy of the same naming rule (its bean at `:80-86` passes NO qualified name — verify where its table name comes from; if a duplicate rule exists there too, extend the strategy or note it as a follow-up row, do NOT scope-creep this task); (5) tests: existing `DslRunRepositoryConfiguration` context tests + `JdbcDslRunRepository` tests stay green (no behavior change — same string produced); add ONE test if no coverage pins schema-qualified naming through the strategy (default name + custom `tableName`/`schema` override — likely already covered by naming-strategy tests, check first and only add what is missing). Acceptance: zero behavior change, deprecated helper gone, single source of truth for the qualified table name, `:starter:test` green. Tier `backend`. Trivial-to-small effort, compiler-driven, near-zero risk; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T389-naming-strategy-dedup.md` |
| T390 | Backlog | Readiness/liveness probe split — `/actuator/health/liveness` + `/actuator/health/readiness` with Temporal reachability in readiness ONLY | backend (Epic 4 — SLOs & health) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 workstream "SLOs & health": "Define SLOs (run success %, p95 run latency, preview latency). Expand `/actuator/health` detail; readiness vs liveness split"); kanban has ZERO readiness/liveness coverage (grep confirmed). Code-confirmed current state: health IS exposed and rich — `management.endpoints.web.exposure.include: health,info,metrics` + `show-details: always` + `show-components: always` in `starter-launcher/src/main/resources/application.yml:122-130` (and starter main/test/integrationTest ymls), backed by `DslHealthIndicator` (`starter/src/main/java/cbs/nova/starter/config/DslHealthIndicator.java`) which folds DSL registry counts (processes/transactions/helpers) AND `TemporalHealthProbe` reachability into the ONE composite health — but there is NO `management.endpoint.health.probes.enabled`, NO `group.liveness`/`group.readiness` config, no probe URLs. Operational consequence: a Temporal outage (external system, pod often NOT at fault) marks the whole `/actuator/health` DOWN; any orchestrator wiring liveness to that endpoint (app/compose/orchestration.yml healthcheck or future k8s) restarts healthy pods in a loop instead of pulling them from rotation. Task: (1) enable probes + groups in ALL FOUR application.yml variants (starter main, launcher, test, integrationTest — keep them consistent): `management.endpoint.health.probes.enabled: true` + `management.endpoint.health.group.liveness.include: livenessState` + `group.readiness.include: readinessState,db,<dsl/temporal indicators>` (exact include-list: verify component names at runtime via `show-components` output — `db` is the Boot datasource indicator name; the DSL indicator registers under its bean name); (2) restructure for correct fault-domain placement — split `DslHealthIndicator` into `DslReadinessIndicator` (temporal reachability via `TemporalHealthProbe`, DSL registry loaded/counts) and keep liveness trivial (in-process state only: registry initialized, no external calls — liveness must NEVER depend on Temporal/DB reachability); the existing composite `/actuator/health` should keep today's overall shape (do not silently change plain `/health` semantics — deprecate-or-keep decision documented in plan; existing `DslHealthIndicatorTest` files at both `starter/src/test/.../config/DslHealthIndicatorTest.java` and `starter/src/test/.../DslHealthIndicatorTest.java` (two — check both, likely dup test files worth consolidating while touching) must stay green or migrate deliberately); (3) compose wiring: check `app/compose/orchestration.yml` + `docker-compose.yml` healthchecks — if they curl `/actuator/health`, switch to the readiness URL (a restart-looping Temporaless container in compose is the same bug); (4) SLOs themselves (success %, p95 latency definitions) are OUT of scope — separate row later; (5) docs: runbook section "Health endpoints" — what each URL means, what responds DOWN, what an operator should do. Tests: (a) liveness UP with Temporal probe UNREACHABLE (mock probe returning down) — THE regression test for this whole task; (b) readiness DOWN under same mock; (c) groups present in all four yml variants (a config-parse test if one exists, else yml assertions); (d) plain `/actuator/health` unchanged-or-documented. Acceptance: both probe URLs live, fault domains correct, tests pin the Temporal-down ≠ liveness-down contract. Tier `backend`. Additive config + indicator split; low risk if plain health shape preserved; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T390-readiness-liveness-split.md` |
| T391 | Backlog | DSL `@Helper` — `MaskHelper` (redact sensitive values: keep-edges, fixed-width, custom char) | backend (@Helper addition) — gap in the shipped helper surface (32 registered names enumerated from `@Helper(name=...)` across starter — no `mask`/`redact` anywhere; zero hits for mask/redact in starter+dsl main sources, so no util to reuse either). Value: DSL authors handling credentials/IBANs/cards/emails/phone numbers currently have NO way to safely log or emit a value — Epic 4 log correlation (T384) and Epic 2 audit trail (T383, `details_json`) both increase how much potentially-sensitive data flows into logs/audit rows; a `mask` helper gives authors a one-call redaction primitive at the point of use. Distinct from `formatMessage` (MessageFormat interpolation — different concern; no overlap). API sketch (match existing helper conventions — read `Base64Helper`/`HexHelper` for the registration + method shape, jqwik property tests are the established pattern — see `BackoffHelper`/`HexHelper` tests): `mask(value)` → keep first 0/last 4 by default for length ≥ 8 else full-width mask; `mask(value, keepFirst, keepLast)` → visible edges, middle replaced; `maskFixed(value)` → always render exactly N mask chars regardless of input length (CRITICAL: default shape must NOT leak length — fixed-width is the safe default for short values, document which default the plain overload picks and WHY); `mask(value, keepFirst, keepLast, maskChar)`. Rules: null/empty → return as-is or empty per existing helper null conventions (check siblings, stay consistent); non-string inputs → String.valueOf or dedicated overload, follow sibling convention; never throw on unicode — count chars by code points (surrogate pairs), mask char `'*'` default; keepFirst+keepLast ≥ length → return value unmasked? NO — that defeats masking; clamp so at least one char is always masked or return fully masked, pick + test the rule explicitly. Tests: unit cases (card/iban/email/short/empty/null/unicode emoji), jqwik properties (mask output never contains any substring of the hidden region; fixed-width output length independent of input length; idempotence NOT expected — document), golden examples for docs. Docs: section in `docs/dsl/helpers.md` + 2–3 recipes in `docs/dsl/examples.md` (log-safe card in notification, audit detail redaction) — note T378/T380 doc-drift guard context: docs updates are expected to land WITH the helper. Acceptance: helper registered + all overloads tested incl. the clamp rule and length-leak properties, docs + examples updated, `:starter:test` green. Tier `backend`. Additive helper, zero existing-code changes, low risk; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T391-mask-helper.md` |
| T392 | Backlog | `ExtendedSelectQueryExecutor.Builder` condition combinators — `and`/`or` (+`andIf`/`orIf`) with explicit semantics; clarify multi-`where` behavior | backend (refactoring) — code-confirmed TODOs (one logical group, same concern: condition composition in the query builder): `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/persistence/ExtendedSelectQueryExecutor.java:55` reads `//TODO: add 'or' and 'and' methods, for proper configuration of conditions`; sibling `:23` reads `//TODO: add here a some lamdba, to make api fluent one, like stream api` (the empty-args `select()` method — vague, resolve by implementing or deleting, see below). Real usage already NEEDS this: `JdbcDslRunRepository` chains MULTIPLE `.where()` calls — `:152-153` (`where(finishedAt < cutoff).where(status != ...)`) and `:244-245` (`where(startedAt >= windowStart).where(startedAt < windowEnd)`) — and what N chained criteria MEAN is implicit in however squigglesql's `Query` joins repeated `addCriteria` (AND? comma? last-wins? verify empirically — `ExtendedSelectQuery.java:57 addCriteria`); `whereIf` (`:65`) has the same ambiguity when combined with `where`. A reader cannot tell `a AND b` from `a, b` (invalid SQL) from the call site. Task: (1) FIRST pin current semantics empirically: build a two-criteria query via existing API, capture `toString()`, record the SQL — this is the compatibility contract; (2) add explicit combinators on `Builder`: `and(Criteria)` and `or(Criteria)` combining the ACCUMULATED criteria with squigglesql's `Criteria.and(left, right)`/`Criteria.or(...)` (verify exact combinator names on the squigglesql Criteria class in the dependency jar — adjust to actual API), plus `andIf(boolean, Supplier<Criteria>)`/`orIf(...)` mirroring `whereIf`; semantics: `where(a).and(b)` ≡ one combined `a AND b` criteria object — NOT a second addCriteria (that is the entire point: explicit composition, not implicit joining); `or` binds to the accumulated criteria (left-assoc chain: `where(a).and(b).or(c)` = `(a AND b) OR c` — document precedence explicitly in javadoc, left-assoc is the simple truth, do not pretend it is precedence-correct math); (3) multiple `where()` calls after this task: EITHER deprecate chaining (javadoc: call once, compose with and/or) or keep documented behavior from step (1) — pick, document, add a test pinning it; (4) the `:23` empty `select()` + lambda TODO: implement the intended entry point as `select(Selectable...)`-style overload or DELETE the dead stub if `Builder.select` already covers it (grep call sites of `dslQueries.select(` — it appears unused today; dead code + vague TODO = deletion is a valid resolution, note the decision); (5) migrate `JdbcDslRunRepository` multi-where sites (`:152-153`, `:244-245`, check `:99-105` searchQuery whereIf chain) to explicit `and` composition — generated SQL must be IDENTICAL to step (1)'s pinned output (character-for-character, that is the regression test); (6) drop both TODOs. Tests: combinator unit tests (and/or/andIf/orIf, left-assoc precedence, empty-criteria edge: and() with nothing accumulated = seed like where), SQL-pin tests before/after migration, existing `JdbcDslRunRepository` suite green. Acceptance: semantics explicit + tested, call sites migrated with byte-identical SQL, TODOs gone. Tier `backend`. Low risk if SQL-pin tests hold; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T392-query-builder-combinators.md` |
| T393 | Backlog | `DslRunTableColumns` → record — derived-columns class to record + static factory, sweep field reads to accessors | backend (refactoring) — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/persistence/DslRunTableColumns.java:5` reads `//TODO: redo to a record` (line moved from :8 after recent edits). The class is a mutable-fields holder: 13 package-private `TableColumn` fields derived in the ONE constructor from a single `String name` param (`table = new Table(name); id = table.get("id"); ... correlationId = table.get("correlation_id")`), plus `refer()`. All derivation, no invariants beyond construction. Record-blocking detail: a record's canonical ctor takes the COMPONENTS, but here 14 fields derive from ONE param — so the conversion shape is: `record DslRunTableColumns(Table table, TableColumn id, TableColumn runId, TableColumn processName, TableColumn status, TableColumn inputJson, TableColumn outputJson, TableColumn errorMessage, TableColumn contextJson, TableColumn startedAt, TableColumn finishedAt, TableColumn executionMode, TableColumn triggeredBy, TableColumn correlationId)` with (1) static factory `static DslRunTableColumns of(String name)` carrying the current derivation verbatim, (2) `refer()` kept as an instance method delegating to `table.refer()`. Components stay package-private → accessors package-private, fine for the two consuming classes (same package). Sweep: 4 construction sites in `JdbcDslRunRepository` (`:71,144,178,238` — `new DslRunTableColumns(tableName)` → `DslRunTableColumns.of(tableName)`) and ALL direct field reads `t.finishedAt`-style → accessor form `t.finishedAt()` in `JdbcDslRunRepository` + `DslRunQueryCriteria` (`matchesProcessName`/`matchesStatusIgnoreCase`/`matchesModeIgnoreCase`/`matchesCorrelationId`/`fullSelection`/`minuteBucket` — enumerate with grep `t\.[a-z]` over both files, expect ~25+ reads). Also: dead `import lombok.Value` at `DslRunTableColumns.java:6` (class is NOT `@Value`-annotated — leftover) — delete with the conversion. Tests: no unit test touches this class directly (grep confirms only the two main-source consumers); existing `JdbcDslRunRepository` suite + `DslRunQueryCriteria`-exercising tests ARE the coverage — generated SQL must be unchanged (same `Table`/`TableColumn` objects constructed identically; if any repo test pins SQL text it must stay byte-identical). Acceptance: record + factory landed, zero `new DslRunTableColumns(` constructor calls remain, TODO + dead import gone, `:starter:test` green. Tier `backend`. Pure mechanical refactor, compiler-driven, zero-risk (same objects, same package); rollback = revert one commit. Natural batch companion: T392 (same files — `JdbcDslRunRepository` call sites) and T385/T369 record/lombok wave. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T393-dsl-run-table-columns-record.md` |
| T394 | Backlog | Definition history diff — `GET drafts/{name}/history/{ts}` content + `GET .../diff` vs current published; workbench preview + confirm-before-restore | backend (+BFF/FE, Epic 5) — roadmap-endorsed gap (`docs/roadmap.md` Epic 5 "Versioned definitions": "view, diff (reuse `ExplainDiffView` / `ASTDiffNode`), roll back"). Code-confirmed state: view (list) ✅ (`DslDraftHandler.history:90` → `DefinitionHistoryEntry(timestamp, millis, size, modified)`), roll back ✅ (`restore:101` writes entry straight to PUBLISHED after snapshotting) — but the restore is a BLIND one-click overwrite: `readEntry` (`DslDefinitionHistoryService:60`) returns full `DraftRequest` content yet is exposed ONLY to the restore path; there is NO endpoint to fetch a single entry's content, NO diff against current published, NO FE preview before restore (BFF has only `history/index.get.ts` + `history/[timestamp]/restore.post.ts`; FE composables `useWorkbenchDraft`/`useDslApi` restore without showing what changes). An operator restoring the wrong timestamp silently rewrites the published definition — the safety step the roadmap's "diff" item exists for. Task: (1) backend `GET /api/v1/dsl/drafts/{name}/history/{timestamp}` — expose `readEntry` content as `DraftRequest` JSON (404 via existing `ErrorResponse` idiom when entry missing, mirror `restore`'s NOT_FOUND branch); (2) backend `GET /api/v1/dsl/drafts/{name}/history/{timestamp}/diff` — response `{name, timestamp, before, after, hunks[]}` where after = entry source, before = current `.workbench/published` source (read via the same `safePublishedFile` path the service already has — add a `readPublished(dir, name)` sibling to `readEntry`, same warn-and-empty semantics); hunks = line-level unified diff — check for an existing diff util in backend first (grep `diff`/`Diff` in starter+dsl main; the AST-diff machinery referenced by roadmap lives FE-side, `ExplainDiffView.vue`/`ASTDiffNode.vue` in `frontend/components/src/components/runner/`); if none, implement a minimal LCS line-diff helper (bounded: cap hunk count like compile diagnostics cap — pick + document a sane cap, e.g. 200 hunks, and note truncation flag in response); diff the DSL SOURCE strings (the yaml/json text), not parsed ASTs — AST diff is a follow-up, keep phase 1 textual; (3) BFF proxy routes: `history/[timestamp]/index.get.ts` + `history/[timestamp]/diff.get.ts` (explicit files per CLAUDE.md caveat); (4) FE: workbench history panel — click entry → fetch content + diff, render side-by-side or unified diff (reuse runner diff component VISUALS if trivially adaptable, else a simple `<pre>` two-pane; do NOT force ASTDiffNode onto textual diff if shapes mismatch — judgment call, document), and gate `restore` behind showing the diff + explicit confirm (destructive action guard); (5) contract tests: BFF routes in existing route test suites; handler tests: entry content 200/404, diff response shape incl. empty-history + no-published edge (before = null/empty with a flag, do not 500), hunk cap flag. Acceptance: restore is never one-click-blind anymore — content + diff visible before confirm; tier `backend` with FE follow-through included (T382 precedent). Medium effort; low-medium risk (all additive GETs + FE guard; restore endpoint untouched). Rollback = revert one commit. See plan. |  High  | loop  | -      | -          | `./docs/plans/T394-definition-history-diff.md` |
| T395 | Backlog | Machine-readable compile diagnostics — `code` field on `CompileDiagnostic`, javac codes wired, `SemanticValidator` stringly errors → structured `(code, message)` registry | backend (Epic 4 — structured diagnostics) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 workstream "Structured diagnostics": "Persist compile diagnostics (not just inline, not just 20). ... Machine-readable diagnostic codes."); persistence part is a separate later row, THIS task is the codes. Code-confirmed current state: `CompileDiagnostic` (`starter/src/main/java/cbs/nova/starter/model/CompileDiagnostic.java`) = `record(file, line, column, message, severity)` — NO code field; consumers serialize it in `VcsModels` draft responses and `ErrorResponse`. Two diagnostic producers: (1) javac pipeline — `DslReloadHandler.toCompileDiagnostic` (`:290-302`) maps `Diagnostic<? extends JavaFileObject>` but DROPS `d.getCode()` — javac already provides stable machine codes (`compiler.err.expected`, `compiler.err.cannot.find.symbol`, ...) for free; (2) semantic pipeline — `SemanticValidator` (`dsl-codegen/.../SemanticValidator.java`) returns `List<String>` of FREE-TEXT messages ("Process has blank name", "Duplicate name: X", "Process 'p' references unknown helper: h") — no structure, no stability; any message rewording silently breaks any client that matches text, and FE/CI cannot branch on error kind. Task: (1) add `String code` component to `CompileDiagnostic` (nullable for transition, `@JsonInclude(NON_NULL)` style already used by enclosing models — keep JSON backward-compatible: old consumers ignore it); (2) javac path: populate from `d.getCode()` verbatim (document that javac codes are JDK-versioned — stable enough for branching, note caveat in javadoc); (3) semantic path: change `SemanticValidator` from `List<String>` to a structured record (e.g. `ValidationIssue(String code, String message)`) with a STABLE code registry — enumerate every `errors.add(...)` branch in the validator (blank name ×3 kinds, duplicate name, unknown helper, + read the rest of the file — there may be more branches) and assign `SCREAMING_SNAKE` codes (`BLANK_NAME`, `DUPLICATE_NAME`, `UNKNOWN_HELPER` — decide on prefix convention `DSL_` vs bare, document once in the registry javadoc); keep human messages EXACTLY as today (message drift is what codes exist to prevent — changing both in one step defeats the point); (4) thread `ValidationIssue` through `DslCompiler.validate` consumer (`:104`) and wherever semantic errors become `CompileDiagnostic`s in the starter (grep how the string list maps into handler responses — likely `DslDraftHandler:265` region); map to `CompileDiagnostic(code=..., message=..., severity="error", file=<definition name>)`; (5) FE: no change required (additive field) but if the workbench renders diagnostics, surface the code as a small badge ONLY if trivially free — else leave FE untouched, note follow-up; (6) docs: code registry table in `docs/dsl/authoring.md` (or wherever compile diagnostics are documented — grep docs for existing diagnostic docs first); (7) tests: validator test asserting each branch emits the right code + unchanged message; javac mapping test (existing compilation-failure tests extended to assert code present); JSON shape test (code serialized, absent-when-null). Acceptance: every semantic error carries a stable code, javac diagnostics carry javac codes, zero message-text change, JSON backward-compatible. Tier `backend`. Low-medium risk (validator return-type ripple is the widest change — compiler-driven); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T395-diagnostic-codes.md` |
