# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                       |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:----------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options. | High | loop | - | - | `./docs/plans/T165-preview-execution-sandboxing.md` |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file. | High | loop | T173 | - | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T184 | Done | AesFieldEncryptor tests | backend — `AesFieldEncryptor` (85 lines, 0 tests) is the AES-256/GCM field encryptor backing run persistence (T176) — protects sensitive persisted fields at rest. Pin security-critical behavior: ctor null/blank-key IAE, null passthrough, round-trip (normal/empty/unicode/long), IV nonce freshness (same plaintext → different ciphertext), GCM tamper detection (flip byte → IllegalStateException), truncation, wrong-key auth failure. Pure unit, no Spring. Silent break here = security regression, must be caught. | High | loop | - | - | `./docs/plans/T184-aes-field-encryptor-tests.md` |
| T185 | Done | SimpleExpressionEvaluator tests | backend — `SimpleExpressionEvaluator` (277 lines, 0 tests) is the sandboxed expression engine behind `Context#eval` (`SimpleContext`) — powers DSL `{name}` interpolation + `${a + b}` arithmetic. Largest untested pure-logic class in :dsl, hot path of every ctx.eval. Pin: interpolation modes, variable resolution (present/absent/null→""), arithmetic precedence/parens/unary, BigDecimal division (DECIMAL64) + stripTrailingZeros render, string-concat branch, and the error/sandbox boundary (unterminated string, unexpected char = the no-reflection guard, missing paren, trailing tokens, bad conversion). Parameterized where apt. | High | loop | - | - | `./docs/plans/T185-simple-expression-evaluator-tests.md` |
| T186 | Done | Surface STALE run status in frontend | frontend — T170 added backend `DslRunStatus.STALE` (healthcheck-driven, non-terminal), but FE `ExecutionStatus` union = Pending/Running/Completed/Failed/Compensated — no Stale. A STALE run from /api/v1/executions has no StatusBadge entry (`Record<ExecutionStatus,string>` miss) → invisible/mis-rendered. Add Stale to union + StatusBadge (distinct amber/yellow, not Compensated-orange) + ExecutionFilters option + handle in index.vue/RecentExecutions aggregates. Verify exact serialized literal vs backend enum. Completes T170 end-to-end. | High | loop | - | - | `./docs/plans/T186-surface-stale-run-status-frontend.md` |
| T187 | Done | ExecutionTraceCollector/TreeCollector/DefaultExecutionListener — pipe-stage refactor | backend — 3 classes (`ExecutionTraceCollector.java:10`, `ExecutionTreeCollector.java:26`, `runner/DefaultExecutionListener.java:11`) carry identical "memory leak — change to new pipe stage" TODOs. Each keeps per-run state in a long-lived runId-keyed map (`ConcurrentHashMap`); entries orphaned if `stop()` skipped (exception path). Refactor: scope per-run trace/tree state to the pipe (per-run stage instances on pipe payload/context, mirroring `ExecutionTreeStage` per-run pattern) so state dies with the run. Eliminate runId-keyed singleton maps. See plan file. | High | loop | - | - | `./docs/plans/T187-execution-trace-collector-pipe-stage.md` |
| T188 | Done | JDBC capture handlers — remove reflection | backend — 3 proxy InvocationHandlers under `starter/.../capture/` carry "reflection forbidden, add typed handler" TODOs: `DataSourceProxyBeanPostProcessor.java:21`, `ConnectionInvocationHandler.java:31`, `PreparedStatementInvocationHandler.java:25`. Current dispatch routes every call through `Method.invoke`. Goal: zero reflective `Method.invoke` on JDBC capture hot path; typed per-method dispatch (new `DataSourceInvocationHandler` + typed Connection/Statement handlers). JDBC capture test surface must stay green. See plan file. | High | loop | - | - | `./docs/plans/T188-jdbc-capture-remove-reflection.md` |
| T189 | Done | SourceCompiler classpath — optional + Gradle/source | backend — `dsl-codegen/.../SourceCompiler.java:50` TODO: make classpath var optional, accept from gradle plugin, fallback system property. Currently hardwired/required. Goal: Gradle plugin passes resolved classpath to SourceCompiler at compile time; system-property fallback for non-Gradle runtime (dev reload). Affects SourceCompiler + callers + `dsl-gradle-plugin` (DslCompilerPlugin/Task/Extension) + starter autoconfig. See plan. | High | loop | - | - | `./docs/plans/T189-source-compiler-classpath-source.md` |
| T190 | Done | Config binding — typed @ConfigurationProperties records | backend — 7 sites share "replace with configuration properties record instead" TODO (scattered `@Value` instead of typed records): `PreviewCacheAutoConfiguration.java:13`, `DslWorkerConfiguration.java:22`, `DslAutoConfiguration.java:36`, `DryRunLoggingAutoConfiguration.java:53`, `DslReloadResource.java:51`, `TemporalTestConfiguration.java:15`. Goal: one typed `@ConfigurationProperties` record per config area, eliminate scattered `@Value`, keys unchanged. See plan. | High | loop | - | - | `./docs/plans/T190-config-properties-records.md` |
| T191 | Done | CompensationRegistry — interface + remove synchronized | backend — `dsl/.../CompensationRegistry.java:14` TODO: add interface, remove `synchronized`. Currently concrete class used directly (no SPI), `synchronized` blocks for thread-safety. Goal: extract `CompensationRegistry` interface + default impl; replace `synchronized` with lock-free/finer-grained concurrency (ConcurrentHashMap/RW lock). Confirm per-run scoping (T187) before finalizing. See plan. | High | loop | - | - | `./docs/plans/T191-compensation-registry-interface.md` |
| T192 | Done | JsonSchemaGenerator — library + bean | backend — `dsl/.../JsonSchemaGenerator.java` two TODOs: line 94 "use library instead of handwritten schemas", line 96 "instead of static access, create a bean". Currently hand-builds schemas as strings + static access. Goal: library-backed schema gen (victools/everit/networknt TBD), instance bean behind interface, static removed. Must align with T171 JSON runtime. See plan. | High | loop | - | - | `./docs/plans/T192-json-schema-generator-library-bean.md` |
| T193 | Done | DSL package cleanup + dead-code removal | backend — 7 cleanup TODOs across `dsl`/`dsl-api`: `SemanticValidator.java:18` (move→dsl-codegen), `RetryPolicy.java:5` (move→dsl/model), `PropertyResolver.java:8` (remove if dead), `MapInputConverter.java:20` (bean in DslConfig), `ProcessMain.java:10` (remove if dead), `SimpleContext.java:15` (lombok builder), `DefinitionLoader.java:67` (experimental—decide). "unused" claims verified before deletion. See plan. | High | loop | - | - | `./docs/plans/T193-dsl-package-cleanup.md` |
| T194 | Done | DSL JSON-native extraction (JsonExtractHelper) | backend — `starter/.../helpers/JsonExtractHelper.java:14` TODO: better json integration, DSL must be json-native. Currently ad-hoc Map/List hand-extraction. Goal: first-class JSON value access (JsonElement/path) over T171 runtime; helper removed or facade-only. Ties to T171/T185/T188. See plan. | High | loop | - | - | `./docs/plans/T194-json-native-dsl-helper.md` |
| T195 | Backlog | DslExceptionHandler — SPI override | backend — `starter/.../controllers/DslExceptionHandler.java:41` TODO: instead of static, create interface + impl so end-user can override. Currently @ControllerAdvice w/ static response shape. Goal: injectable mapper interface + default impl, @ConditionalOnMissingBean lets host app override. Error shape stays back-compat. See plan. | High | loop | - | - | `./docs/plans/T195-dsl-exception-handler-spi.md` |
| T196 | Backlog | DSL Gradle plugin — optional runtime/starter module config | backend — `dsl-gradle-plugin/.../DslCompilerPlugin.java:46` TODO: starter module is optional, could be another; need config at `DslCompileExtension`/`DslCompileTask`. Plugin hardcodes starter as runtime module. Goal: configurable runtime-module property on extension+task (default=current). Plugin side of T189 classpath hand-off. See plan. | High | loop | - | - | `./docs/plans/T196-dsl-gradle-plugin-optional-starter.md` |
| T197 | Backlog | ScopedValueDryRunLoggingContext — pipe stage | backend — `dsl/.../logging/ScopedValueDryRunLoggingContext.java:15` TODO: remove, change to a new pipe stage. DryRunLoggingContext impl backed by Java ScopedValue; TODO wants dry-run log accumulation carried by the pipe (per-run stage/context) instead, consistent with T187. Ties to T173. See plan. | High | loop | - | - | `./docs/plans/T197-scoped-value-dryrun-context-pipe-stage.md` |
| T198 | Backlog | Rename mock→fake + two-way fake config | backend — QA sessions need predefined responses for helpers/functions/transactions (fake external services). Prior agent named the mechanism "mock" and built only ONE path (execution-time, via `cbs.nova.preview.mocks` metadata key + REST `DslRequest.mocks()`). Goal: (1) rename mock→fake across recorder/stage/interceptors (`MockResolver`→`FakeResolver`, `MapBasedMockResolver`→`MapBasedFakeResolver`, collapse DUPLICATE nested class in `RunScopedExternalCallRecorder:162`, `findMock`→`findFake`, `startMocking`→`startFaking`, `stopMocking`→`stopFaking`, metadata flags `mockApplied`/`mockConfigured`→`fake*`); (2) add Way A — startup `app.yml`-driven `@ConfigurationProperties(prefix="cbs.nova.fakes")` record consumed by a NEW pipe stage at `DevDslRuntime`; (3) add Way B — typed `Context.fakes()`/`withFakes()` field (same `FakeConfig` shape as Way A) replacing the metadata-map hack. Both paths feed one shared resolver (Way A base, Way B per-key override). Coverage gaps flagged: `HttpCallHelper` uses raw `HttpClient` (not intercepted), DB/HTTP interceptors `findMock`-and-discard (observe-only), `RunDslPipe` has no recording stage. See plan. | High | loop | - | - | `./docs/plans/T198-two-way-fake-config-mock-rename.md` |
| T199 | Backlog | Frontend STALE auto-refresh | frontend — T186 added `STALE` to `StatusBadge`/`ExecutionFilters` but execution list/detail views have no auto-refresh. A STALE run from `/api/v1/executions` stays amber until the user manually reloads. Goal: when an execution is STALE, the UI polls the backend on a short interval (e.g. 5s) and re-renders once the status transitions to a terminal state (or back to Running). Apply to `ExecutionList` aggregates + `ExecutionDetail` page. Configurable poll interval, pause on tab blur, vitest spec locking the polling + transition behavior. Tier frontend. See plan. | High | loop | - | - | `./docs/plans/T199-frontend-stale-auto-refresh.md` |
