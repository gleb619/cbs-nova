# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                                     | Description                                                                                                                                                                                                                                                                                                                                          | Priority | Owner | Blocks | Blocked By | Plan File                                                             |
|:-----|:--------|:------------------------------------------------------------------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:----------------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                                              | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.          |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`                   |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture                              | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                          |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md`          |
| T222 | Blocked | DSL Workbench save/publish backend design                                                 | frontend/backend — `useDslWorkbench.ts` `saveConstruct()`/`publishConstruct()` are stubs (no backend endpoint); the DSL is compile-time-authored from Java source today, so "saving" an edit needs a product/architecture decision on persistence model and publish semantics before any plan or code is written. See plan file.                     |  Medium  | loop  | -      | -          | `./docs/plans/T222-workbench-save-publish-backend-design.md`          |
| T223 | Done    | Add dedicated tests for `RecordingStatement`/`RecordingCallableStatement`                   | backend — T206 replaced JDBC capture proxies with typed decorators; `RecordingConnection`/`RecordingDataSource`/`RecordingPreparedStatement` each got a dedicated test class but `RecordingStatement` (382 lines) and `RecordingCallableStatement` (672 lines, largest new class) did not — only covered indirectly. Test-only change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T223-recording-statement-callable-statement-tests.md` |
| T224 | Done    | Add dedicated tests for `CompilingDslDefinitionLoader`/`ServiceLoaderDslDefinitionLoader` | backend — T207 split `DefinitionLoader` into an interface with two implementations; only the composite `DefinitionLoaderTest` exists today, the two implementations are only covered indirectly. Test-only change. See plan.                                                                                                                       |  Medium  | loop  | -      | -          | `./docs/plans/T224-definition-loader-impl-tests.md`                   |
| T225 | Done | Compose stack validation test                                                              | backend — `app/compose/*` was refactored to a shared postgres service with per-domain DB init scripts (`01-create-dbs.sh`) but nothing validates the stack: no test runs `docker compose config`, checks referenced env vars, or syntax-lints the initdb scripts. A regression in an include file or a renamed DB would only surface at `docker compose up`. Test-only change. See plan.                                     |  Medium  | loop  | -      | -          | `./docs/plans/T225-compose-stack-validation-test.md`  |
| T226 | Done | Document app/ deployment topology in architecture docs                                                | backend (docs) — the new `app/` surface (host server with SPI-loaded DSLs, `app/dsl` publish flow, `@cbs/operator-portal` UI, shared-postgres compose stack) landed after the T220 doc refresh; `grep` finds zero mentions of `app/server`, `app/dsl`, `app/ui`, or the operator portal in `architecture-backend.md`/`architecture-ui.md`. Docs-only change. See plan.                              |   High   | loop  | -      | -          | `./docs/plans/T226-app-topology-arch-docs.md`                 |
| T227 | Done | Unit tests for remaining untested dsl-idea-plugin classes                                                     | backend — plugin has tests for file type, compile action, overrider, sync contributor, resolver drain; but `DslGradleSyncNotificationListener` (80 lines, sync-notification logic), `DslCheckStartupActivity` (45), `DslProjectStateService` (28), `CbsDslSyntaxHighlighterFactory` (52) have none. Test-only change. See plan.                        |  Medium  | loop  | -      | -          | `./docs/plans/T227-idea-plugin-remaining-tests.md`           |
| T228 | In Progress | Remove deprecated starter listeners package                                                          | backend — all six concrete listeners (`HttpCallListener`, `DatabaseCallListener`, `MqCallListener`, `FileSystemCallListener`, `MicroserviceCallListener`, `ExternalApiCallListener`) plus `ExternalCallListener` are `@Deprecated(forRemoval = true)` with zero references outside the package, yet remain `@Component` beans Spring instantiates at startup. Dead code + orphan beans. See plan.                              |  Medium  | loop  | -      | -          | `./docs/plans/T228-remove-deprecated-listeners.md`            |
| T229 | Backlog | ApplicationContextRunner tests for untested starter auto-configs                                      | backend — `PreviewCacheAutoConfiguration`, `PreviewMetricsAutoConfiguration`, `DslHealthIndicator`, `DryRunLoggingAutoConfiguration` are only exercised via full-context integration tests; their conditional wiring (enabled/disabled properties, fallback beans) is untested. House pattern exists (`FeignCallAutoConfigurationTest`). Test-only change. See plan.       |  Medium  | loop  | -      | -          | `./docs/plans/T229-autoconfig-conditional-tests.md`          |
| T230 | Backlog | Convert `DslExecutionsResource` to WebMvc.fn RouterFunctions                                            | backend — Replace the `@RestController` class with a functional `DslExecutionsHandler` + `DslExecutionsRouterConfiguration` using `ServerRequest`/`ServerResponse`. Keep path `/api/executions`, query param semantics, and OpenAPI metadata. Update unit tests to exercise the `RouterFunction`. See plan.                                     |  Medium  | loop  | -      | -          | `./docs/plans/T230-convert-dsl-executions-resource-to-routerfunctions.md` |
| T231 | Backlog | Convert `DslIntrospectionResource` to WebMvc.fn RouterFunctions                                         | backend — Replace the `@RestController` DSL introspection class with a functional handler + router config. Preserve all seven GET endpoints, `GlobalManager` queries, internal DTO records, and OpenAPI schema generation for `/api/dsl/*`. Update unit tests. See plan.                                                                       |  Medium  | loop  | -      | -          | `./docs/plans/T231-convert-dsl-introspection-resource-to-routerfunctions.md` |
| T232 | Backlog | Convert `DslRuntimeResource` to WebMvc.fn RouterFunctions                                               | backend — Replace the `@RestController` runtime class with a functional handler + router config. Preserve `/api/dsl/preview/{name}`, `/api/dsl/run/{name}`, `/api/dsl/explain/{name}` plus `DslRequest` record, status mapping, and OpenAPI schema enrichment (`PreviewReport`, `ExplainReport`). Update tests. See plan.                         |  Medium  | loop  | -      | -          | `./docs/plans/T232-convert-dsl-runtime-resource-to-routerfunctions.md` |
| T233 | Backlog | Align `DslReloadResource` with the new RouterFunction conventions                                         | backend — The reload endpoint is already functional (`ServerRequest`/`ServerResponse`) but uses a different naming/package style than the other three resources. Rename to `DslReloadHandler`, enrich `@RouterOperation` metadata, and optionally consolidate the `/api/dsl/*` routers behind a single `DslRouterConfiguration` facade. See plan.      |  Low     | loop  | -      | T230-T232  | `./docs/plans/T233-align-dsl-reload-resource-with-routerfunction-conventions.md` |
| T234 | Backlog | Unit tests for `useLocalStorageState` composable | frontend — New composable `frontend/components/src/composables/useLocalStorageState.ts` (localStorage-backed `ref` with custom read/write + deep watch) ships with no spec while all four sibling composables have one; it backs sidebar toggle state, `ConstructExplorer` layout, and workbench draft persistence. Test-only change. See plan. | Medium | loop | - | - | `./docs/plans/T234-use-local-storage-state-tests.md` |
| T235 | Backlog | Component tests for DSL workbench panels | frontend — `frontend/components/src/components/dsl/` holds 10 workbench components with zero specs while the sibling `components/` directory has 22; first batch: `ProblemsPanel` (errors list), `DraftRestoreBanner` (draft restore UX), `ReferencePicker`, `StructureTab` (steps list). Test-only change. See plan. | Medium | loop | - | - | `./docs/plans/T235-dsl-panel-component-tests.md` |
| T236 | Backlog | Fix stale Makefile backend/publish targets | backend (DX) — `make backend` runs `cd backend && ./gradlew :starter:bootRun`, broken twice over: the root wrapper is Gradle 8.13 which fails under the required Java 25, and the starter now lives in `backend/dsl-starter` (module `:starter-launcher`) after the restructure. No `make publish` target exists for the documented `publishToMavenLocal` step. Update targets to use `backend/dsl-platform/gradlew`, correct module paths, and default `SERVER_PORT=8090` to match the BFF. | Medium | loop | - | - | `./docs/plans/T236-makefile-targets-refresh.md` |
| T237 | Backlog | BFF proxy routes for missing `/api/dsl/*` endpoints | frontend (BFF) — backend exposes `/api/dsl/helpers`, `/helpers/search`, `/processes`, `/processes/{name}`, `/transactions`, `/transactions/{name}` but the Nitro BFF proxies none of them (no catch-all by design). Add six route files + route tests + a drift-guard coverage test so new backend DSL paths can't silently go unproxied. See plan. | Medium | loop | - | - | `./docs/plans/T237-bff-proxy-missing-dsl-routes.md` |
| T238 | Backlog | README for `@cbs/admin-ui-plugin` Nuxt module | frontend (docs) — the module's stated purpose is mounting the admin UI into any host Nuxt app, but it ships with no README: integrators must reverse-engineer `module.ts` (`ModuleOptions`: `routePrefix`, `backendBaseUrl`, `backendApiKey`, env fallbacks), the Nitro BFF proxy surface, and the port-mismatch dev workflow. Docs-only change; `app/ui` is the reference host. See plan. | Medium | loop | - | - | `./docs/plans/T238-admin-ui-plugin-readme.md` |
