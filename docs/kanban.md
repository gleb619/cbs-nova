# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture             | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                                                                                                                                  |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T310 | Done | Page-level specs — dashboard + executions list uncovered | frontend (tests) — inventory-confirmed gap: page specs exist for runner, dsl-workbench, and executions detail, but NOT for `pages/index.vue` (dashboard — T278 stat tiles/recent-runs/loading/error, most-visited page) or `pages/executions/index.vue` (list — most task-churned page: T254 filters, T269 auto-refresh while RUNNING, T281 cancel+confirm, T284 pagination). Recent behavior changes verified only via composable specs — template wiring (filter binding, cancel visibility per status, polling start/stop, empty/error states) unpinned. Task: mount specs for both pages following runner/[id] conventions (mocked APIs, testid selectors) — dashboard: tiles+counts, recent-runs links, loading/error/empty; list: rows, filter→params binding, cancel only for RUNNING, fake-timer polling start/stop, error retry, pagination offset; fixes only for proven bugs. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T310-page-specs-dashboard-list.md`          |
| T311 | Done | Helper catalog UI — endpoint shipped, no FE consumer | frontend — grep-confirmed gap: backend `DslIntrospectionHandler.helpers()` (`:62-63`, dedicated service+mapper) and BFF route `dsl/helpers/index.get.ts` are fully shipped, yet `useDslApi.ts` contains zero helper functions and nothing in the UI consumes the catalog. The 12 registered helpers (`HttpCallHelper`, `JsonExtractHelper`, `SortRecordsHelper`, `FilterRecordsHelper`, `ArithmeticHelper`, `FormatMessageHelper`, `CurrentTimestampHelper`, …) are invisible to DSL authors writing `ctx.runHelper("<name>", …)` — discovering names, input/output types, or side-effect flags (matters for preview faking) requires reading Java source. Task: `listHelpers()` in `useDslApi` (+spec), `HelperCatalog.vue` component (name/types/side-effect badge/description columns aligned to actual payload, substring search, empty/error/loading states), workbench sidebar entry or tab, testids per convention, render+search vitest specs, verify BFF passthrough of full payload. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T311-helper-catalog-ui.md`                 |
| T312 | Done | `dsl_run_transactions` write-only — per-run transaction executions never surfaced | backend — code-confirmed gap: write path alive (platform `DefaultExecutionListener` holds the `TransactionExecutionRepository` and records every `TransactionExecution` during runs; starter wires JDBC impl `DslRunRepositoryConfiguration:102-106` against V3's `dsl_run_transactions`), but read path dead — `findByRunId` (`JdbcTransactionExecutionRepository:55`) has zero business callers, no endpoint, no BFF route, no FE consumer. The table is write-only persistence holding the *structured* per-transaction record (name, input_json, executed_at) — the relational sibling of T296's flat trace strings, which heuristic parsing cannot substitute for. Executions detail for a multi-transaction process shows nothing about which transactions ran. Task: `GET /api/executions/{id}/transactions` (404 unknown run, documented order, keeps detail DTO lean), router + OpenAPI entry, in-memory parity so non-JDBC modes work, BFF proxy route + routeCoverage/routes spec entries, FE transactions section on detail (name/time/collapsible input, PayloadTab pattern, testids), tests per layer. See plan. |  High   | loop  | -      | -          | `./docs/plans/T312-run-transactions-surfacing.md`      |
| T313 | Done | Architecture docs miss the entire ops/security layer | docs (backend) — grep-confirmed gap: `architecture-backend.md` (151 lines) mentions NONE of OIDC guard (T275), rate limiting (T287), run cancellation (T274), input validation (T291), payload caps (T279), retention purger (T276), health indicator + Temporal probe (T295), preview timeout (T298), OTel (T277), Micrometer (T264/T282), BFF pass-through (T280), Keycloak realm (T288) — only an incidental "idempotent" in a table. CLAUDE.md routes every backend task to these docs first; the documented architecture describes a pre-ops-era system, forcing every task to rediscover structure from source. Task: new "Security" section (opt-in model, filter order, Keycloak pointer), new "Observability & operations" section (metrics per path, tracing, health incl. Temporal probe + fail-status, retention, caps, preview timeout), drift-fix existing construct/module tables against current code, architecture-ui.md BFF security/proxy notes, cross-link (not copy) to T301 config reference when it lands, every claim cites the class/property it describes. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T313-architecture-docs-ops-layer.md`     |
| T314 | Done | Bad query params 500 — `?limit=abc` throws NumberFormatException | backend — code-confirmed gap: `DslExecutionsHandler` parses `limit` (`:61`), `offset` (`:62`), `topProcesses` (`:90`) via raw `Integer::parseInt` — non-numeric values throw `NumberFormatException`, which `DslExceptionHandler` does not map, so `GET /api/executions?limit=abc` returns HTTP 500 with a server-error body. Typo'd or hostile query strings masquerade as server faults: noise in error metrics/logs, wrong signal to consumers. Same family as T291's input validation, one layer up (query string vs body). Task: tolerant parse helper or `@ExceptionHandler(NumberFormatException)` mapping → 400 `ErrorResponse` naming the param + bad value (existing body shape), clamp semantics unchanged, sweep other handlers for the pattern, tests per param (non-numeric → 400 w/ code + param name; numeric → current clamped behavior). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T314-param-hardening.md`                   |
| T315 | Done | `finished_at` index for retention purge — cleanup seq-scans the table it shrinks | backend (perf) — code-confirmed gap: V4 indexes cover `(status, started_at)`, `(process_name, started_at)`, `(execution_mode)` — nothing on `finished_at`; the purger's predicate is terminal-status + `finished_at < cutoff` (T276; T305's rework added no index — V5 is `triggered_by`). Every purge cycle seq-scans `dsl_runs`, so cleanup gets more expensive exactly as the table grows — worst when retention is most needed. RUNNING rows have NULL `finished_at`, so a partial index (`WHERE finished_at IS NOT NULL`) serves the predicate with minimal write overhead. Task: migration `Vn__dsl_runs_finished_at_index.sql` (next free number, coordinate with T308's column if it lands first) with partial `CREATE INDEX IF NOT EXISTS`, Testcontainers migration test proving the purge DELETE's plan uses the index on a seeded table (or documented actual-plan justification), no regression on list/stats hot paths, record plan before/after in plan notes. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T315-finished-at-purge-index.md`            |
| T316 | Done | Stuck RUNNING runs never resolve — crash-orphaned rows immortal in `dsl_runs` | backend — code-confirmed gap: run status is written only by in-process lifecycle callbacks in `TemporalDslProcessService`; zero `describeWorkflow`/`getHistory`/reconcile paths exist in starter (codegraph-confirmed no matches). T306 graceful shutdown covers clean stops only — crash, `kill -9`, OOM, or worker-host loss orphans `RUNNING` rows forever: `JdbcDslRunRepository` CAS (`:164`,`:182`) never sees the terminal write, stats (`ExecutionStatsResponse`) count ghosts as active, executions list shows RUNNING eternally, and the retention purger's predicate (terminal + `finished_at < cutoff`) can NEVER remove them — NULL `finished_at` rows are immortal, unbounded growth exactly when things went wrong (compounds T305/T315). Task: scheduled reconciliation executor (sibling of `cbsNovaDslRunRetentionExecutor`) scanning RUNNING rows past a configurable grace period, Temporal `WorkflowExecution.describe`/history lookup mapping completed/failed/timeout/cancelled/not-found → terminal status + best-effort `finished_at`, batch idempotent CAS update via existing repo pattern, config knobs for interval/grace, per-close-state unit tests, runtime.md lifecycle note (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T316-stuck-running-run-reconciliation.md` |
| T317 | Done | Definition publish history + restore — publish overwrites the only copy | backend — code-confirmed gap: `DslDraftHandler.publish` (`:73`) does `writePayload(published/<safeName>.json)` — flat in-place overwrite of `.workbench/published/<name>.json`, no prior copy anywhere (codegraph-confirmed: zero version-history/rollback logic; only JDBC `Connection.rollback` exists). Registry reload is atomic (T255) but the file itself is destroyed — last-good definition recoverable only out-of-band (git / editor undo). Edit a draft after publishing, publish a broken variant → last-good gone with no trace. Task: snapshot previous published file on every publish to per-definition history (`.workbench/history/<name>/<ts>.json`), bounded retention (keep-N prune, configurable), `GET` history list + `POST` restore-by-timestamp reusing publish semantics (failed compile must NOT clobber current published, mirror T297 failed-reload rule), `safeFileName`-consistent traversal safety, BFF proxy routes, FE workbench history panel optional stretch, unit tests (snapshot-on-publish, prune, restore round-trip, broken-restore leaves current intact), runtime.md publish-section note (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T317-definition-publish-history.md` |
| T318 | Done | Temporal Schedules — no way to run a definition on a schedule | backend — code-confirmed gap: zero Temporal `Schedule`/`ScheduleClient` usage in starter (codegraph query returns only the two unrelated `@Scheduled` executors `cbsNovaDslProcessHealthcheckExecutor`/`cbsNovaDslRunRetentionExecutor`); `TemporalDslProcessLauncher` is one-shot `startWorkflow` only, so every run requires a manual trigger (API call / workbench button) — an orchestration engine with no recurring-run story. Temporal's native Schedule API gives dedup/overlap/catchup machinery for free. Task: `POST/GET/DELETE /api/v1/dsl/schedules` backed by `ScheduleClient` (definition name + cron/interval + optional static input), decide+document overlap policy (default Skip), catchup policy, timezone (default UTC), schedule-ID naming; runs launched by schedule attributed `triggeredBy=schedule` via T304's `RunIdentityResolver`; validation (published definition, cron spec → 400, conflict → 409); BFF proxy routes + minimal FE workbench schedules panel (list/create/delete); tests service (mocked ScheduleClient) + handler + BFF; runtime.md schedules section (APPEND, never rewrite). See plan. |  High  | loop  | -      | -          | `./docs/plans/T318-temporal-schedules.md` |
| T319 | Done | Definition export/import bundle — no env-to-env migration path | backend — code-confirmed gap: zero export/import bundle symbols anywhere (codegraph `export` query returns only OTel span-exporter imports); `DslDraftHandler` publishes one definition at a time via `writePayload(published/<name>.json)` with no bulk read-out or write-in, so published definitions are stranded on the local `.workbench/` dir — fresh compose environment comes up empty and must be rebuilt definition-by-definition by hand, dev→staging→prod migration impossible. Task: `GET /api/v1/dsl/definitions/export` JSON bundle (engine/format version, exported-at, per-definition entries; drafts optional via `?include=drafts`), `POST .../import` with per-entry compile validation + per-entry result (published / failed+diagnostic, no silent partials) and `?dryRun=true` report-only mode, clear 400s for bad format/unsupported version, flow through the existing publish path so T317 history snapshots apply (no direct file writes), BFF proxy routes, handler/service/BFF tests, runtime.md section (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T319-definition-export-import-bundle.md` |
| T320 | Done | Run time-series stats — dashboard blind to run-rate/failure trend | backend — code-confirmed gap: `DslRunStatsRepository` exposes exactly one method `stats(windowStart, topProcessesLimit)` (trailing-window counters + top-process counts); no bucketed/time-series aggregation anywhere (no `date_trunc`/bucket/timeseries code — grep-confirmed), `ExecutionStatsResponse` (`:18`) carries nothing chartable over time, and the FE dashboard renders counters only — run-rate spikes and failure bursts are invisible between window snapshots. Task: `GET /api/v1/dsl/executions/stats/timeseries?windowHours&bucketMinutes` (validated + clamped params, T314 lesson) → per-bucket per-status counts via `date_trunc` on `started_at` (V4 `(status, started_at)` index serves it), zero-filled empty buckets for a uniform FE axis, in-memory repo parity, BFF proxy route + composable, FE dashboard trend chart (stacked/multi-series by status, `dataviz` skill when charting), tests repo bucketing (boundaries, empty buckets, clamp) + handler + BFF + FE render smoke. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T320-run-timeseries-stats.md` |
| T321 | Done | Executions CSV export — no bulk extraction path for run history | backend — code-confirmed gap: zero csv/CSV symbols anywhere in backend or frontend (codegraph + grep confirmed); executions list endpoint returns JSON page DTOs only and the FE list has no export affordance — run data leaves the system only row-by-row via the detail endpoint, so reporting/auditing outside the UI means hand-rolled DB queries. Task: `GET /api/v1/dsl/executions/export.csv` accepting the list filters (status, processName, correlationId post-T308, time bounds; validated/clamped T314-style), RFC 4180 quoting/escaping (input/output JSON contains commas/quotes/newlines), configurable row cap with `X-Export-Truncated: true` when hit, optional char-budget truncation of input/output JSON (decide + document default), proper `text/csv` + `Content-Disposition` headers, columns runId/processName/status/mode/triggeredBy/correlationId/startedAt/finishedAt/duration/error, BFF passthrough route (content-type preserved, not JSON-wrapped), FE export button honoring active filters, tests: quoting/escaping (comma/quote/newline/unicode), filters, cap+header, BFF, FE render. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T321-executions-csv-export.md` |
| T322 | Done | Run-completion webhooks — consumers can only poll for outcomes | backend — code-confirmed gap: zero webhook/notification/callback-delivery code anywhere (codegraph `Webhook` query: no results; no webhook task in kanban history); `TemporalDslProcessService`'s terminal-state write is the natural hook point but nothing observes it externally, so downstream systems and alerting must poll the executions list — latency + load for everyone. Task: opt-in config-driven subscriptions (definition name/pattern, URL, optional HMAC secret, optional status filter; empty default = zero behavior change), async delivery off the run path (enqueue at terminal write, executor delivers), HMAC-SHA256 `X-Cbs-Signature` + timestamp anti-replay headers, bounded retries with backoff, HTTPS-only by default (dev plain-HTTP flag), fixed payload schema `{event, runId, definition, status, startedAt, finishedAt, error?}` with NO input/output by default (secrets), delivery log for debugging, tests (signature, retry on 5xx/timeout, filter exclusion, slow-callback doesn't block run path, config-off = zero side effects, WireMock/MockWebServer), runtime.md webhooks section (APPEND, never rewrite). See plan. |  High  | loop  | -      | -          | `./docs/plans/T322-run-completion-webhooks.md` |
| T323 | Done | Workbench new-definition + template gallery — authoring has no bootstrap | frontend — code-confirmed gap: `dsl-workbench.vue` drafts panel lists existing drafts only (`dsl-draft-picker`), no create/template/new affordance on the page, while the backend already supports saving a new-named draft (`drafts/[name]/save.post.ts`) and `docs/dsl/examples.md` documents Java examples not workbench JSON starters — first-run operator authors from blank editor or hand-copied existing definition. Task: verify-first (confirm no new-draft path hides in composables; else templates-only scope), "New definition" action with validated name input (safe filename chars, draft collision → inline error, opens editor pre-filled + dirty so T292 guard applies), 3–4 curated JSON skeletons (plain process, transaction+saga, http pipeline, retry policy) as FE-static assets preferred (zero BE surface), template bodies spec-validated to compile cleanly, vitest (gallery render, selection populates editor, name validation, collision), examples.md pointer (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T323-workbench-template-gallery.md` |
| T324 | Done | DSL `@Helper` — `UuidV7Helper` for idempotency keys & sortable IDs | backend — code-confirmed gap: zero UUID/ULID helper registered (codegraph `Uuid` query returns no helper symbols; the `@Helper` registry covers HTTP, JSON extract, sort/filter records, arithmetic, format message, current timestamp — none for ID generation). DSL authors needing idempotency keys for HTTP retries (`HttpCallHelper` already exists but no built-in key helper), external writes, or correlation IDs must inline a workaround (string concat, `runtime.now()`) — no native support, and T311 helper catalog will surface whatever is registered. Task: `@Helper("uuidV7")` returning an RFC 9562 v7 string (timestamp-prefixed, lexicographically sortable, 128-bit, 122 random bits), follow `CurrentTimestampHelper` registration shape (singleton bean, helper catalog auto-registration, optional namespace argument for partitioned IDs to keep determinism per source), unit test (format regex `^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`, monotonicity across calls within same ms via embedded counter, optional namespace determinism), `docs/dsl/examples.md` entry showing idempotency-key usage with `HttpCallHelper` (APPEND, never rewrite). No FE work needed — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T324-uuid-v7-helper.md`                  |
| T325 | Done | Workbench dirty-state UI — no save indicator, no `Ctrl+S` shortcut | frontend — code-confirmed gap: codegraph `useDirty`/`useAutosave`/`markDirty` query returns zero FE composables (only an unrelated `isDirty` text in `AppFooter.vue`); T292 added the `beforeunload` dirty guard but shipped no visible save status, so the workbench header is silent about whether the current draft is saved, dirty, saving, failed, or last-saved-at-time — every edit is invisible and an accidental tab close loses unsaved work without warning beyond the browser's native prompt. A `Ctrl+S`/`Cmd+S` shortcut (expected in every code editor) is also missing. Task: `useDraftDirty` composable wrapping the editor's existing dirty flag (no T292 logic duplication), `useDraftSave` composable consolidating the existing `drafts/[name]/save.post.ts` call (returns reactive status: `idle`/`dirty`/`saving`/`saved`/`error` + `lastSavedAt`), header status pill rendering the four states (Unsaved / Saving… / Saved HH:MM / Save failed — Retry), `Ctrl+S`/`Cmd+S` shortcut via `@vueuse/core` `useEventListener` (skip when focus is in Monaco and Monaco's own binding wins), `data-testid` per T257–T261 conventions (`draft-save-status`, `draft-save-retry`), vitest (status state transitions: dirty→saving→saved on success, dirty→saving→error on failure; shortcut triggers save only when dirty; no shortcut when input is read-only or in confirm modal), confirm no regression to T292's `beforeunload` guard. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T325-workbench-dirty-ui.md`               |
| T326 | Blocked | Execution timeline viz — `dsl_run_transactions` flat list, no temporal view | frontend — code-confirmed gap: codegraph `ExecutionTimeline`/`RunTimeline`/`transactionTimeline` queries return zero FE components; T312 surfaced the per-run `dsl_run_transactions` data via BFF+FE table, but the executions detail page renders transactions as a flat collapsible list sorted by `executedAt` — for a multi-step process with retries, saga compensations, or parallel branches, users must scroll and mentally reconstruct temporal order from timestamp strings. Task: `ExecutionTimeline.vue` component (decide + document single-lane chronological track vs swim-lane per transaction name — single-lane default for low-process-count typical case, swim-lane if lane count > threshold), horizontal time axis with zoom (fit-to-view + fixed scales), bars per transaction with status→color mapping (consult `dataviz` skill for accessible palette), duration proportional to width, hover-tooltip with name+status+duration+error, click-bar opens the existing `PayloadTab` for that transaction, empty state when zero transactions, loading + error states per existing conventions, `data-testid` per T257–T261 conventions (`execution-timeline`, `execution-timeline-bar-<idx>`); vitest (renders mock transactions in correct chronological order, status→color map covers all `TransactionExecution` statuses, duration→width math holds, empty state, hover tooltip); confirm no regression to the existing transactions table (timeline lives alongside, not replacing it). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T326-execution-timeline.md`                |
| T327 | Done | DSL `@Helper` — `Base64Helper` (encode/decode, standard + URL-safe) | backend — code-confirmed gap: codegraph `Base64` `--kind class` query returns zero `@Helper`-annotated classes (only stdlib imports in `AesFieldEncryptor` and `cbs.nova.starter.persistence::java.util.Base64`); the registered helper set covers HTTP/JSON/sort/filter/arithmetic/format/timestamp/uuid-v7 (T324) — none for base64. DSL authors handling JWT header encode, OAuth state, signed-URL parameters, or any binary blob metadata must currently inline a string-concat workaround — error-prone, off-catalog. Task: `@Helper("base64")` with `mode ∈ {"encode","decode"}` and optional `urlSafe: Boolean` flag (default false → standard RFC 4648 §4; true → URL-safe §5 with `+`→`-`, `/`→`_`, padding retained unless explicitly disabled) — decide+justify single helper vs split (`base64Encode`+`base64Decode`) in plan; UTF-8 explicit encoding for string inputs (no platform default), throw clear error on invalid input for decode (no silent return of empty string), platform registration per `CurrentTimestampHelper`/`UuidV7Helper` shape, helper unit tests (round-trip ASCII, round-trip non-ASCII UTF-8 bytes, URL-safe `+`→`-`/`/`→`_` mapping, padding edge cases incl. missing-pad decode, invalid-input error path), `docs/dsl/examples.md` entry showing a JWT-header encode example (APPEND, never rewrite). No FE work needed — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T327-base64-helper.md`                     |
| T328 | Backlog | DSL `@Helper` — `FormatDateHelper` / `ParseDateHelper` (ISO + pattern + tz) | backend — code-confirmed gap: codegraph `FormatDate`/`ParseDate` queries return zero `@Helper`-annotated classes (only an unrelated FE `formatDate` call in `AppFooter.vue`); helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64 (T324/T327) — none for date/time formatting or parsing. DSL authors needing to format timestamps for outbound HTTP (`If-Modified-Since`, log lines), parse partner-API date strings (`RFC 1123`, `yyyy-MM-dd'T'HH:mm:ss.SSSZ`), or convert across timezones must currently inline `runtime.now()` + string concat workarounds — every DSL with external API or DB integration re-invents this. Task: split into two helpers (decide+justify in plan: split beats single `mode` flag — keeps signatures honest, errors stay per-helper); `@Helper("formatDate")` takes `(input: ISO-8601 string or epoch-millis long, pattern: String, zone: optional String default UTC)` returning formatted string via `java.time.format.DateTimeFormatter`, pattern preset aliases (`ISO_INSTANT`, `ISO_LOCAL_DATE`, `RFC_1123_DATE_TIME`, plus custom); `@Helper("parseDate")` is the inverse `(input: String, pattern: String, zone: optional String default UTC)` returning ISO-8601 string (or epoch-millis — decide) plus `ZonedDateTime` companion if needed; explicit UTC default and a `zone` lookup (IANA `ZoneId.ofOf`, fail-fast on invalid); empty/invalid input → typed helper error (no silent empty return — base64/T327 lesson); registration per `CurrentTimestampHelper` shape, unit tests (round-trip ISO+pattern across DST + zone change, invalid pattern, invalid zone, RFC 1123 preset, epoch-millis input mode), `docs/dsl/examples.md` entry showing format+parse round-trip and HTTP `If-Modified-Since` example (APPEND, never rewrite). No FE work — T311 catalog surfaces both. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T328-format-parse-date-helper.md`          |
| T329 | Backlog | DSL `@Helper` — `RegexHelper` (match / extract / replace) | backend — code-confirmed gap: codegraph `Regex` `--kind class` query returns zero `@Helper`-annotated classes (only `Pattern` matches unrelated to DSL helpers); helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date (T324/T327/T328) — none for regex. DSL authors parsing log lines, validating IDs/tokens/headers, extracting sub-strings from partner API responses, or doing bulk string transformations must currently inline workarounds (string slicing via `JsonExtractHelper` on JSON-encoded strings, or external pre-processing) — every DSL doing text inspection re-invents this. Task: `@Helper("regex")` with `op ∈ {"match","extract","replace","split"}` (decide+justify single helper vs split — leaning single with `op` discriminator since args overlap heavily; matches T327 base64 split decision only if signatures truly diverge); `match(pattern, input)` returns boolean; `extract(pattern, input, group?)` returns captured group (default group 0 = whole match) or empty string if no match — decide return type for DSL; `replace(pattern, input, replacement)` returns string with all matches replaced (literal vs backref — decide, default literal for safety per `String.replace` vs `replaceAll`); `split(pattern, input)` returns list of strings; pattern compiled once + cached (bounded LRU keyed by pattern string, fail-fast on bad pattern with typed helper error); empty/null input → typed helper error (T327/T328 lesson — no silent empty); registration per `CurrentTimestampHelper` shape, helper unit tests (valid match, no-match, multiple groups, named group, replace all vs first, split edge cases, invalid pattern error, empty input error, bounded LRU eviction), `docs/dsl/examples.md` entry showing log-line parse + ID-validation examples (APPEND, never rewrite). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T329-regex-helper.md`                     |
| T330 | Backlog | DSL `@Helper` — `HmacSha256Helper` (HMAC-SHA256 sign / verify) | backend — code-confirmed gap: codegraph `HmacSha256`/`Hmac`/`Webhook` queries all return zero `@Helper`-annotated classes (only `WebhookSignatureVerifier` style references exist in unrelated `JsonSchemaValidator` docstring context); helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex (T324/T327/T328/T329) — none for HMAC signing. The T322 run-completion webhook plan explicitly requires HMAC-SHA256 `X-Cbs-Signature` header generation on outbound webhooks AND verification on inbound partner-API integrations — without this helper, T322 cannot ship DSL-side webhook signing, and DSL authors integrating with signed-URL partner APIs (AWS-style, Stripe-style, GitHub-style) cannot sign canonical requests. Task: `@Helper("hmacSha256")` with `op ∈ {"sign","verify"}` (decide+justify single vs split — leaning split into `hmacSha256Sign` + `hmacSha256Verify` to keep signatures honest and errors per-op, matches T327/T328 split preference); `sign(message, secret, encoding?)` returns HMAC-SHA256 hex digest (default) or base64 (when `encoding="base64"`); `verify(message, secret, signature, encoding?)` returns boolean (constant-time compare via `MessageDigest.isEqual` to prevent timing attacks — explicit security note); secret required non-empty (fail-fast typed helper error, no silent empty secret); UTF-8 explicit encoding (no platform default); invalid `encoding` arg → typed helper error listing allowed values; registration per `CurrentTimestampHelper` shape, helper unit tests (sign round-trip ASCII, sign+verify round-trip with both hex+base64 encodings, invalid encoding error, empty secret error, empty message edge case, signature mismatch returns false, constant-time compare documented via test code path), `docs/dsl/examples.md` entry showing AWS-style canonical-request signing and T322 webhook signing (APPEND, never rewrite). No FE work — T311 catalog surfaces both. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T330-hmac-sha256-helper.md`               |
| T331 | Backlog | DSL `@Helper` — `UrlEncodeHelper` / `UrlDecodeHelper` (percent + form) | backend — code-confirmed gap: codegraph `UrlEncode`/`URLEncoder` queries return zero `@Helper`-annotated classes (the `java.net.URLEncoder` stdlib is referenced from `HttpCallHelper` for `application/x-www-form-urlencoded` request bodies, but is not exposed as a DSL-facing helper); helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256 (T324/T327/T328/T329/T330) — none for percent-encoding. DSL authors building OAuth authorization queries (`redirect_uri`, `state`, PKCE `code_verifier`), constructing canonical URLs for partner APIs, or building `application/x-www-form-urlencoded` payloads for `HttpCallHelper` currently inline the JDK `URLEncoder.encode` call — error-prone (default charset ambiguity, double-encoding bugs, no decode counterpart exposed). Task: split into two helpers (`urlEncode` + `urlDecode`) — distinct signatures (encode takes `String`, decode takes `String`), symmetric shape matches T327/T328/T330 split preference; `urlEncode(input, charset?, form?)` returns percent-encoded string via `URLEncoder.encode` (charset explicit default `UTF-8`, `form=true` switches to `application/x-www-form-urlencoded` style which encodes space as `+` vs `%20` — decide+justify which mode is default; document the difference); `urlDecode(input, charset?, form?)` is the inverse via `URLDecoder.decode` with same charset/form semantics; empty input → typed helper error (T327/T328/T330 lesson — no silent empty); invalid percent sequence in decode → typed helper error (`IllegalArgumentException` mapped at helper boundary); UTF-8 explicit (no platform default — matches T330); registration per `CurrentTimestampHelper` shape, helper unit tests (round-trip ASCII, round-trip non-ASCII UTF-8 bytes, space encoding `+` vs `%20` per form flag, decode invalid percent sequence error, empty input error, charset argument behavior), `docs/dsl/examples.md` entry showing OAuth `redirect_uri` building with `code_verifier` and `state` percent-encoding (APPEND, never rewrite). No FE work — T311 catalog surfaces both. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T331-url-encode-decode-helper.md`          |
| T332 | Backlog | DSL `@Helper` — `Sha256Helper` (content hash, hex + base64) | backend — code-confirmed gap: codegraph `Sha256` query returns only the internal `IdempotencyKeys.deriveRunId` static method (uses `sha256Hex` for run-id derivation) — no DSL-facing `@Helper` exposes SHA-256 to DSL authors. The helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256/url-encode+decode (T324/T327/T328/T329/T330/T331) — none for plain (non-keyed) content hashing. DSL authors needing content fingerprinting for ETag/`If-None-Match` headers, dedup-hash inputs, or content-addressable cache keys must currently inline `MessageDigest.getInstance("SHA-256")` — JDK boilerplate, error-prone (no instance reuse, no encoding choice, no string-returning convenience). Task: `@Helper("sha256")` with `encoding ∈ {"hex","base64","base64url"}` (default `"hex"` — matches T330 HMAC default); `sha256(input, encoding?)` returns digest string for the UTF-8 bytes of `input` (UTF-8 explicit, no platform default — T330/T331 lesson); `MessageDigest` instance created per call (acceptable — DSL helper calls are not hot-path) OR cached via `ThreadLocal` (decide+justify); empty input returns hash of empty string (document explicitly — no error; hashing empty is a legitimate use case unlike encoding), invalid `encoding` arg → typed helper error listing allowed values; registration per `CurrentTimestampHelper` shape, helper unit tests (known-vector test against NIST FIPS 180-4 example `abc` → `ba7816bf...`), round-trip ASCII/non-ASCII, all three encodings (hex lowercase, base64 std, base64url), empty input, invalid encoding error, `docs/dsl/examples.md` entry showing ETag/`If-None-Match` workflow for cache-friendly HTTP polling (APPEND, never rewrite). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T332-sha256-helper.md`                    |
| T333 | Backlog | `docs/adr/` — Architecture Decision Records | docs (cross-tier) — code-confirmed gap: `ls docs/adr` returns no such directory; `ls docs/ | grep -i adr` empty; sibling directories `cookbook` and `runbook` also absent — no formal record of WHY key architectural decisions were made. `architecture-backend.md` and `architecture-ui.md` describe current shape but never capture the rejected alternatives, the trade-offs weighed, or the date the call was made — every new contributor (or future-self) re-derives context from code, and refactor proposals repeatedly surface "we already tried X, see PR #N" folklore that has no anchor in the repo. T313's ops-layer doc update mentioned drift between docs and code; without an ADR log the drift compounds. Task: create `docs/adr/` directory + `docs/adr/README.md` index (Michael Nygard ADR format: Title, Status, Context, Decision, Consequences, plus `NNNN-short-slug.md` filename convention), seed 3 foundational ADRs (1. Temporal as orchestration engine vs Spring State Machine / Camunda / custom — references the local single-process MVP evolution; 2. DSL `@Helper` SPI vs direct method dispatch — references the `HelperSpiProcessor` annotation-processor path; 3. BFF via Nitro in admin-ui-plugin vs separate service — references the Nuxt mount design), cross-link from `architecture-backend.md` and `architecture-ui.md` (one-sentence pointer to `docs/adr/` near the top, not a copy), document ADR authoring convention in README (when to write one: significant scope or interface change; lightweight: a few sentences; supersede vs replace pattern). No code change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T333-adr-log.md`                         |
| T334 | Backlog | DSL helpers cookbook (`docs/dsl/helpers.md`) | docs (cross-tier) — code-confirmed gap: `grep -i "helper\|uuid\|base64" docs/dsl/examples.md` returns zero matches; the existing `examples.md` is 102 lines covering the *Java* DSL example module (`backend/dsl-examples/src/*.java`) and the `compileDsl` Gradle task, NOT DSL-callable helper patterns. Despite 7 helpers shipping/queued (T324 UuidV7, T327 Base64, T328 formatDate/parseDate, T329 regex, T330 HmacSha256, T331 UrlEncode/Decode, T332 Sha256), zero reference docs show a DSL author how to combine them into real-world patterns — every plan stub promises to "APPEND one entry to `docs/dsl/examples.md`" but that file is the wrong surface (helper call patterns ≠ Java source files). DSL authors discovering the catalog must read helper source to learn which to pick for which use case. Task: create `docs/dsl/helpers.md` distinct from `examples.md`, organized by use case (Identity, Encoding, Hashing, Text processing, Date/Time, HTTP integration, Observability), each section: short problem statement + minimal DSL call snippet + non-trivial example (e.g. HMAC-SHA256 for AWS-style canonical-request signing needs `formatDate` + `sha256` + `hmacSha256Sign` chained), cross-link to `architecture-backend.md` helper SPI section, mention T311 helper catalog as the machine-readable equivalent, mention T333 ADRs for SPI rationale. Pure docs; no code; no tests. The 7 individual T324-T332 plan stubs should reference this file as the destination for their per-helper "examples" entries (defer per-helper entries to a follow-up or write a few key ones here directly). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T334-helpers-cookbook.md`                |
| T335 | Backlog | BFF↔backend contract tests — pin JSON shape drift | frontend (tests) — code-confirmed gap: `find frontend/admin-ui-plugin/server` shows route-level specs (`executions.spec.ts`, `objects.spec.ts`, `drafts.spec.ts`, `routes.spec.ts`, `routeCoverage.spec.ts`) and `httpClient.spec.ts` covering the BFF's HTTP client, but ZERO spec that pins the *shape* of BFF responses against the *shape* of backend responses — BFF can silently drop/rename a field (a trivial `pick`/`omit` bug in `proxy.ts`, a missing `defineEventHandler` transform) and FE breaks in prod because nothing compares BFF output to canonical backend output. CLAUDE.md already warns "No generic `/api/v1/dsl/*` catch-all. BFF routes are explicit Nitro files… Add a matching proxy route when exposing a new backend DSL path" — but the contract itself is unenforced. Task: new `frontend/admin-ui-plugin/server/api/v1/__tests__/contract.spec.ts` that for each covered BFF route (start with the top-N most-trafficked: `/api/v1/dsl/definitions`, `/api/v1/dsl/executions`, `/api/v1/dsl/executions/stats`, `/api/v1/dsl/helpers`, `/api/v1/dsl/drafts`): (1) hits backend via Testcontainers (or recorded fixtures if Testcontainers overhead proves too slow for the contract suite — decide+justify), (2) hits BFF route via Nitro's `defineEventHandler` test harness, (3) snapshots *only the shape* (keys + types, not values — values change run-to-run) of BFF response against backend response, (4) on diff lists the added/removed/typed-changed fields. Decide whether to compare full subtree or whitelist known-bounded subtrees. Document any *intentional* deltas (e.g. BFF strips `internal-*` fields, BFF reorders lists) in a `contract-deltas.md` and skip those subtrees. Tests run in `pnpm test` alongside existing specs. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T335-bff-backend-contract-tests.md`     |
| T336 | Backlog | `docs/runbook.md` — operator incident playbook | docs (ops) — code-confirmed gap: `ls docs/runbook.md` returns no such file, yet the word "runbook" appears 4× in other docs that point to nothing: T195 dsl-exception-handler-spi plan ("operational runbooks that expect every failure in Sentry should be updated when a host overrides the mapper"), `docs/frontend/execution-details.md` ("Suggestions or runbook links when configured"), `docs/frontend/execution-details.md` ("users can share links, bookmark them, and embed them in alerts or runbooks"), `docs/frontend/runner.md` ("Use Explain to generate diagrams for runbooks"). Every existing incident response lives in operator memory (or PR comment folklore), and T313's ops-layer doc update + T287 rate-limiting + T295 health probe + T298 preview timeout + T316 stuck-run reconciliation + T315 retention purger all add operational surface with zero on-call playbook. Task: create `docs/runbook.md` organized by incident class, each with: symptom (what does operator see — alert, dashboard, log), triage (which logs/metrics to check first, commands to run), root-cause hypotheses ranked by likelihood, mitigation (immediate workaround to stop bleeding), permanent fix (link to kanban/plan task if known), and verification (how to confirm recovery). Initial incident classes: (1) Temporal disconnect (workflow timeouts, retries piling up), (2) OIDC/Keycloak outage (login loop, 401 spike, JWT verify failures — T275/T287/T288 surface), (3) retention purger stuck or aggressive (T276/T315 — dsl_runs growth, slow DELETE), (4) stuck RUNNING reconciliation lag (T316 — terminal-status drift, stats inflation), (5) BFF proxy 5xx storm (specific BFF route returning 502 — check backend health, Nitro logs, BFF timeout config), (6) helper catalog empty (T311 — new helper not appearing after `bootRun` restart), (7) FE auto-refresh storm (T269 — list page polling loop, browser memory pressure), (8) preview sandbox/escape (T165 — currently blocked, but operator still needs guidance if requested). Cross-link to architecture-backend.md (T313) for security/ops layer, to relevant kanban tasks per incident, and to Sentry/Otel dashboards (T277). Pure docs; no code; no tests. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T336-operator-runbook.md`               |
| T337 | Backlog | `make doctor` CLI — operator smoke-check tool | dx (Makefile + bash) — code-confirmed gap: `Makefile` has `up` (with per-service curl health checks) but zero `doctor` / `health` / `smoke` target — operators answering "is the system healthy?" after a deploy or a `make up` must hand-pick which curl/jq commands to run, and T336's runbook triage steps will need to enumerate the same checks repeatedly. `scripts/` has only `dev.sh` + `merge-t291.sh`, no shared diagnostic helper. Task: add `make doctor` target that runs a fixed battery of smoke checks and reports pass/fail per check (non-zero exit on any failure, partial-success tolerated for optional services like Temporal). Checks: (1) `docker compose ps` (services running?), (2) Postgres reachable via `pg_isready` or TCP probe to 5432, (3) backend `/actuator/health` (200 + status=UP), (4) Temporal namespace `default` reachable via `temporal operator namespace describe` (or HTTP probe to Temporal UI), (5) BFF reachable (curl `http://localhost:3000/`), (6) helper catalog non-empty (`curl /api/v1/dsl/helpers | jq 'length > 0'`), (7) at least one DSL definition published (`curl /api/v1/dsl/definitions | jq 'length >= 0'` — empty is OK for fresh install), (8) BFF→backend round-trip works (`curl BFF /api/v1/dsl/definitions` returns same shape as direct backend curl — T335 cross-link). Reuse Makefile conventions: `printf` status with `[ok]/[fail]/[skip]` markers, exit codes, retry+timeout per check (like `up` target's `curl --retry 60 --retry-delay 2 --retry-connrefused`). Pure bash + Makefile, no new dependencies. `make help` includes the new target. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T337-make-doctor.md`                    |
| T338 | Backlog | Property-based tests (jqwik) for catalog helpers | backend (tests) — code-confirmed gap: `grep -r "jqwik" backend/dsl-platform/` returns zero matches; 9 catalog helpers shipped/queued (T324 UuidV7, T327 Base64, T328 formatDate/parseDate, T329 regex, T330 HmacSha256, T331 UrlEncode/Decode, T332 Sha256) each got hand-rolled unit tests with a few hand-picked edge cases (e.g. base64 tests cover ASCII round-trip, non-ASCII bytes, URL-safe, padding — but miss boundary inputs like 1MB payloads, strings with embedded NULs, random invalid encodings). Human-authored tests systematically miss edge cases that a property-based fuzzer would surface: malformed base64 sequences, regex catastrophic-backtracking patterns, date format strings with `%n` tokens, UTF-16 surrogate pairs. PBT catches what hand tests miss. Task: add jqwik test dep to `backend/dsl-starter` (Maven group `net.jqwik`, latest stable 1.x — same major across all helpers for consistency), pilot with `Base64Helper` round-trip property: for all `String s`, `decode(encode(s, standard), standard) == s` and `decode(encode(s, urlSafe), urlSafe) == s`; run 1000+ iterations per property; add shrinking so failure inputs are minimized. Document the pattern in plan: how to write properties per helper (e.g. for regex: pattern+input → `replace(extract(match(p,i),p,i)) == i`; for sha256: digest length invariants, hex-only char set, deterministic). Decide in plan: (a) add PBT to ALL existing helpers at once (broader blast radius, harder to review) vs (b) pilot with 1-2 helpers, expand in follow-up tasks (T339 jqwik-base64, T340 jqwik-regex, etc.) — recommend (b) for low risk. Each new helper added in T324-T332 (or future) gets a PBT test alongside its hand-rolled tests; subsequent `glm-loop` fires can add PBT coverage as a separate task per helper. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T338-jqwik-pbt-helpers.md`              |
| T339 | Backlog | DSL `@Helper` — `ValidateJsonHelper` (schema + payload) | backend — code-confirmed gap: codegraph `validateJson` query returns zero `@Helper`-annotated class; the BE ships a full `JsonSchemaValidator` (`cbs.nova.starter.validation::JsonSchemaValidator` covering object/array/string/number/boolean/null/any types, `properties`, `required`, `items`) and a `JsonSchemaGenerator` (`cbs.nova.dsl.config::DslConfig::jsonSchemaGenerator` returns `Replaceable<JsonSchemaGenerator>`) — both are consumed internally but NOT exposed to DSL authors. The helper catalog covers identity/encoding/hashing/text/date/format-message (T324/T327/T328/T329/T330/T331/T332) but has zero *assertion*-style helpers — every helper is a transformation. DSL authors integrating with partner APIs have no built-in way to pre-validate a payload before sending it via `HttpCallHelper`, or to assert a partner response matches an expected shape before storing/transforming. The current workaround is to inline schema generation in a separate BE service or rely on the partner's error responses. Task: `@Helper("validateJson")` taking `(payload: String, schema: String)` returning a list of validation errors (empty list = valid — matches typical validator convention; DSL-typed result). Wire to the existing `JsonSchemaValidator` (no new validator logic) — pass payload + schema as JSON strings, deserialize internally, run validation, return the violation list (field path + message + expected/actual) or empty list. Empty payload → typed helper error (T327/T328/T330/T331 lesson — no silent empty). Empty schema → typed helper error (fail-fast). Malformed schema → typed helper error (`JsonSchemaValidator` throws on parse failure, map at helper boundary). Decide in plan: return as DSL list of error objects vs single concatenated error string — list is more programmatic; concat is simpler. Registration per `CurrentTimestampHelper` shape, helper unit tests (valid payload + matching schema → empty errors; valid payload + strict schema with `required: ["x"]` and missing x → 1 error; nested object/array error paths; malformed schema error; empty payload error; empty schema error; non-string payload coercion — pass JSON, not String, decide). `docs/dsl/helpers.md` (T334) entry showing pre-HTTP-call validation pattern. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T339-validate-json-helper.md`           |
| T340 | Backlog | JDBC `Recording*` refactor — extract common proxy boilerplate | backend (refactor) — codegraph-flagged gap: `wc -l` on `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/capture/` shows `RecordingCallableStatement.java` 672 lines, `RecordingConnection.java` 445, `RecordingStatement.java` 382, `RecordingPreparedStatement.java` likely similar size — four JDBC proxy classes implementing the recording/capture SPI for test support, totaling ~1900+ lines. The `Recording*` pattern (delegate to underlying JDBC object, record call, return) is highly repetitive: every method on `Connection`/`Statement`/`PreparedStatement`/`CallableStatement` has the same shape — `try { record(...); return delegate.method(...); } catch (SQLException e) { throw new RecordingException(...); }`. Same exception wrapping, same logging call, same null-delegate guard. Without a shared base class or aspect, fixing a bug in error handling requires editing 4 files. The recent memory note flags "BaseContainers refactor for Temporal+SQLite" — extending that pattern to Recording* keeps the test infrastructure consistent. Task: (1) profile the 4 files to identify the exact shared boilerplate (delegate forwarding, exception wrapping, call recording, null guard), (2) extract either (a) a `RecordingJdbcSupport` static-helper / `RecordingProxySupport` abstract base class, OR (b) a small interceptor aspect — decide+justify in plan; (a) is simpler and pure-JDK, (b) requires an AOP dep. (3) Refactor the 4 Recording* classes to use the new base; preserve all existing behavior. (4) Existing test infra (`BaseContainers` etc.) must continue to pass — no test changes expected, but re-run integration tests to confirm. (5) Measure LOC delta: target ≥30% reduction across the 4 files. No public API change, no BE feature change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T340-recording-jdbc-refactor.md`        |
| T341 | Backlog | DSL `@Helper` — `XmlExtractHelper` (XPath over string payload) | backend — helper catalog gap: codegraph shows `JsonExtractHelper` (`cbs.nova.starter.helper::JsonExtractHelper`, 73 lines — `extractJson(payload, jsonPath)` returning single match or list of matches) but ZERO XML counterpart. `grep -rE 'jdom\|jaxen\|vtd-xml\|dom4j\|xmlUnit\|xalan' backend/dsl-platform/*/build.gradle*` returns zero matches → no XML dep yet. DSL authors integrating with enterprise partners (SOAP gateways, EDI-on-XML, government/utility APIs) have no built-in way to pull a value out of an XML envelope — current workaround is to write a custom BE Java activity, high friction. T339 (`ValidateJsonHelper`) is the in-progress assertion helper; T341 is the *extract* helper — `extractXml(payload, xpath)` returning matched text or list, sibling to `extractJson`. Task: `@Helper("extractXml")` taking `(payload: String, xpath: String)` returning single match string or `List<String>` (matches `JsonExtractHelper` cardinality convention). Pick XPath engine in plan: (a) `jaxen` (XPath 1.0, ~200KB, no DOM roundtrip, mature — RECOMMENDED), (b) `vtd-xml` (faster, less familiar API), (c) JDK `XPathFactory` (no dep, slower + DOM roundtrip + XPath 1.0). Empty payload → typed helper error. Empty xpath → typed helper error. Malformed XML → typed helper error mapped from SAX parse exception at boundary. No-match → empty list (NOT null — T327/T328 lesson). XXE/external-entity expansion MUST be disabled in parser (security baseline). Helper unit tests: simple envelope single-node extract; attribute-axis (`@id`); multi-node (`/book/*/title`); namespace-qualified (`//ns:body` with namespace map); malformed XML error; empty payload error; empty xpath error; no-match returns empty list; XXE attempt (entity in payload) raises error, does NOT resolve external URL. `docs/dsl/helpers.md` (T334) entry — pre/post-XML-call extraction pattern. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T341-xml-extract-helper.md`             |
| T342 | Backlog | DSL `@Helper` — `ParseCsvHelper` + `FormatCsvHelper` (CSV parse/format pair) | backend — helper catalog gap, code-confirmed: codegraph `query Csv --kind class` returns ONLY `cbs.nova.starter.service::ExecutionCsvWriter` (BE-internal writer for `T321` exec CSV export) and its test. NO `@Helper`-annotated CSV class. The DSL helper catalogue has 14 transforms (identity T324, encoding T327/T331, hashing T330/T332, text T328/T329, format-message, etc.) but zero columnar data helpers. `FilterRecordsHelper` + `SortRecordsHelper` operate on pre-structured lists; DSL authors receiving CSV from a partner (bank statements, EDI feeds, product catalogs — still a massive enterprise surface) have no path except: (a) call `HttpCallHelper` to get the bytes, (b) write a custom BE Java activity to parse, (c) recurse into the DSL. Mirror T328's format/parse date pair as TWO sibling helpers: `@Helper("parseCsv")` returning `List<List<String>>` (rows × cols, header-optional via flag) and `@Helper("formatCsv")` returning `String` (rows → CSV with configurable delimiter, quote, escape, optional header re-write). Pick CSV library in plan: (a) `org.apache.commons:commons-csv` (mature, ~250KB, RFC 4180 + Excel + MySQL + Tab dialects — RECOMMENDED), (b) `com.fasterxml.jackson.dataformat:jackson-dataformat-csv` (already have jackson core; tight integration), (c) plain-JDK `String.split` (zero dep, but no quote/escape handling — risky on real-world CSV). Empty payload → typed helper error. Empty input for `formatCsv` → typed helper error. Mismatched column counts in `formatCsv` → typed helper error (T340 lesson — silent normalization is worse than fail-fast). CRLF handling: respect `\r\n` vs `\n` from input, emit consistently. Helper unit tests: simple `a,b,c\n1,2,3` round-trip → identical output; quoted fields with embedded comma; escaped quotes (`""`); header row toggle; mismatched col count error; empty payload error on parse; empty rows error on format; CRLF normalized; Tab dialect variant (defer if scope grows). `docs/dsl/helpers.md` (T334) entry — DSL CSV-in / CSV-out pattern + bank-statement example. No FE work — T311 catalog surfaces both. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T342-csv-helpers.md`                    |
