# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                              |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:-----------------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options. | High | loop | - | - | `./docs/plans/T165-preview-execution-sandboxing.md`                    |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file. | High | loop | T173 | - | `./docs/plans/T168-preview-explain-listener-architecture.md`           |
| T202 | Done | Remove dead WhatIfConfigPanel (per-request mocks no longer exist) | frontend — `WhatIfConfigPanel` built a `type:target:operation` → JSON payload map sent as `DslRequest.mocks()`. T198 removed per-request mocking; `DslRequest` is now `record(Object body, Map<String,Object> metadata)` with no `mocks` field, so anything configured in the panel was silently dropped. Goal: remove the component from `runner.vue` (and any other mount), drop `mocks` from request-building composables, strip related vitest specs, add brief informational note pointing authors at `cbs.nova.fakes.config.entries` startup YAML. Keep diff small — cleanup of our own T198 backend change, not new feature. Tier frontend. See plan. | High | loop | - | - | `./docs/plans/T202-remove-dead-whatif-mock-panel.md`                  |
| T203 | Done | Refresh architecture docs to reflect T184–T201 shipped work | docs — `docs/architecture-backend.md` and `docs/architecture-ui.md` (plus `architecture.md`) still describe system as of T183 refresh (T146–T182); 18 tasks shipped since then and several statements are now factually wrong. Specific corrections: rewrite "What-if mock injection" section for T198 fakes/helper-boundary/stage-driven startup YAML; rewrite dry-run logging section for T197 `DryRunLogBuffer`/`DryRunLogBufferRegistry`/`DryRunLogStage` (was `ScopedValue`); check `JsonSchemaGenerator`/`CompensationRegistry`/`MapInputConverter` references; append T184–T201 rows to roadmap table; add Executions-page section (T186/T199/T200) to UI doc; add Workbench-draft section (T201). Doc-accuracy only, no code. Tier backend (docs only). See plan. | High | loop | - | - | `./docs/plans/T203-refresh-architecture-docs-t184-t201.md`            |
| T204 | Done | Enrich execution detail endpoint with input/output/error | backend — `GET /api/executions/{id}` returns `ExecutionDto` with only `id`/`entity`/`entityType`/`mode`/`status`/`startedAt`/`completedAt`/`duration`, but FE `ExecutionDetail` type declares `input`/`output`/`errors` and `DslRun` already persists raw `input`/`output`/`error` JSON/text strings. Goal: add detail-only optional fields to `ExecutionDto` — `input: Object` (parsed JSON or raw string), `output: Object` (same), `errors: List<ErrorEntry>` (single element from `DslRun.error()` when non-null, empty otherwise); leave list endpoint lean; never fabricate `trace`/`logs`/`mermaidDiagram` (RUN-mode capture is a separate, larger task). Parse failures must not 500. Extend `DslExecutionsResourceTest`. No `DslRun`/repo changes. Tier backend. See plan. | High | loop | - | - | `./docs/plans/T204-execution-detail-enrichment.md`                   |
| T205 | Done | Move MapInputConverter to starter and remove reflection | backend — Move `MapInputConverter` from `dsl` to `starter`, convert known input types via cached Avaje Jsonb adapters, and fall back to the injected Jackson `ObjectMapper` for unknown types. Goal: zero `java.lang.reflect` in the converter and no record/array reflection on the hot path. See plan. | High | loop | - | T141 | `./docs/plans/T205-mapinputconverter-move-to-starter-avaje-jackson.md` |
| T206 | Done | Replace JDBC capture dynamic proxies with typed decorators | backend — Delete the `InvocationHandler`/proxy machinery in `starter/capture` and replace it with hand-written `RecordingDataSource`, `RecordingConnection`, `RecordingStatement`, `RecordingPreparedStatement`, and `RecordingCallableStatement` decorators. Every JDBC method is a typed delegate; only `execute*` methods record. See plan. | High | loop | - | T188 | `./docs/plans/T206-jdbc-capture-typed-wrappers-no-proxy.md`            |
| T207 | In Progress | Split DefinitionLoader into interface with reflective and non-reflective impls | backend — Introduce `DslDefinitionLoader` interface, move the SPI-only path into `ServiceLoaderDslDefinitionLoader`, and isolate runtime compilation/classloading into `CompilingDslDefinitionLoader`. Wire the appropriate implementation into `DslReloadResource` and Spring config. See plan. | High | loop | - | - | `./docs/plans/T207-definition-loader-interface-impls.md`               |
| T208 | Backlog | Replace Victools JSON schema generator with Jackson 3 generator | backend — Remove the custom `Field` reflection from `VictoolsJsonSchemaGenerator` by building schemas with Jackson 3's `JsonFormatVisitorWrapper`. Delete Victools implementation and dependencies once the new generator matches current schema output. See plan. | High | loop | - | T192 | `./docs/plans/T208-jackson3-json-schema-generator.md`                  |
| T209 | Backlog | Remove `synchronized` from `DryRunLogBuffer` via queue injection | backend — Replace `Collections.synchronizedList` in `DryRunLogBuffer` with an injected `Deque<DryRunLogEvent>` created by `DryRunLogStage` as a `LinkedBlockingDeque`. Scope: `DryRunLogBuffer.java`, `DryRunLogStage.java`, `DryRunLogbackAppenderTest.java`. Acceptance: no `synchronized`, bounded drop-oldest preserved, tests pass. See plan. | High | loop | - | - | `./docs/plans/T209-dry-run-log-buffer-queue-injection.md`              |
| T210 | Backlog | Add persistent `TransactionExecutionRepository` with JDBC and Flyway | backend — Add `TransactionExecutionRepository` in `dsl-api`, an in-memory fallback in `dsl`, and a JDBC-backed implementation in `starter` mirroring `DslRunRepository`. Includes Spring Data JDBC entity, repository, MapStruct mapper, `JdbcTransactionExecutionRepository`, `DslConfig` replaceable, and Flyway `V3__create_dsl_run_transactions.sql`. Scope: files listed in plan. Acceptance: persist/retrieve transaction executions by `runId`, JDBC bean wired when `DataSource` exists, tests pass. See plan. | High | loop | T211 | - | `./docs/plans/T210-transaction-execution-repository-jdbc-flyway.md`     |
| T211 | Backlog | Integrate `DefaultExecutionListener` with `TransactionExecutionRepository` | backend — Rewrite `DefaultExecutionListener` to store successful transaction history in `TransactionExecutionRepository` keyed by `runId` instead of an in-memory list. Update `DefaultProcessRunner` and `GlobalManager.runProcessWithCompensation` to pass `runId` and repository. Scope: `DefaultExecutionListener.java`, `DefaultProcessRunner.java`, `GlobalManager.java`. Acceptance: no in-memory list, no `synchronized`, compensation order preserved, `GlobalManagerTest` passes. See plan. | High | loop | - | T210 | `./docs/plans/T211-default-execution-listener-repository-integration.md` |
| T212 | Backlog | Remove `synchronized` from `ExecutionTreeCollector` | backend — Move `ExecutionTreeCollector` mutable state into constructors and remove all `synchronized (stack)` blocks. Scope: `ExecutionTreeCollector.java`, `ExecutionTreeCollectorTest.java`, `ExecutionTreeCollectorGuardTest.java`. Acceptance: no inline mutable init, no synchronization, existing tree/depth/cycle/external-call tests pass. See plan. | High | loop | - | - | `./docs/plans/T212-execution-tree-collector-lock-free.md`              |
| T213 | Backlog | Move `TemporalDslService` worker setup to startup registration | backend — Move `TemporalDslService` lazy `WorkerFactory` creation and worker registration into a startup `ApplicationRunner` in `TemporalConfiguration`. Simplify `TemporalDslService` to hold only `WorkflowClient` and injected `WorkerFactory`. Scope: `TemporalDslService.java`, `TemporalConfiguration.java`, `TemporalDslServiceTest.java`. Acceptance: no double-checked locking, no `registeredQueues`, tests updated. See plan. | High | loop | - | - | `./docs/plans/T213-temporal-dsl-service-workerfactory-startup.md`       |
| T214 | Backlog | Remove `synchronized` from `InMemoryDslRunRepository` | backend — Replace `synchronized` methods over `LinkedHashMap` with `ConcurrentHashMap` plus a bounded eviction queue. Scope: `InMemoryDslRunRepository.java`, `InMemoryDslRunRepositoryTest.java`. Acceptance: no `synchronized`, capacity (100) and insertion-order eviction preserved, updates do not change eviction order, tests pass. See plan. | High | loop | - | - | `./docs/plans/T214-in-memory-dsl-run-repository-synchronized-removal.md` |
| T215 | Backlog | Remove double-checked locking from `GlobalManager` | backend — Replace `volatile GlobalManager INSTANCE` + `synchronized (GlobalManager.class)` with a lock-free `AtomicReference` idiom while preserving `resetForTests()`. Scope: `GlobalManager.java`, `GlobalManagerTest.java`. Acceptance: no double-checked locking, thread-safe lazy init, tests pass. See plan. | High | loop | - | - | `./docs/plans/T215-global-manager-double-checked-locking-removal.md`    |
| T216 | Backlog | Remove `synchronized` from `ExecutionTraceCollector` | backend — Replace `Collections.synchronizedList` and `synchronized (entries)` blocks with `ConcurrentLinkedQueue` and `AtomicBoolean`. Scope: `ExecutionTraceCollector.java`, `ExecutionTraceCollectorTest.java`. Acceptance: no `synchronized`, cross-thread add test passes, snapshot immutability and per-run isolation preserved. See plan. | High | loop | - | - | `./docs/plans/T216-execution-trace-collector-synchronized-removal.md`  |
| T217 | Backlog | Add offset pagination to executions list endpoint | backend — `GET /api/executions` accepts only `limit` (default 50, max 500); response carries `total` but rows beyond the first page are unreachable — no `offset`. Goal: add optional `offset` request param (default 0, clamped ≥0), apply as `skip(offset)` before `limit` in `DslExecutionsResource.list`, keep `total` unpaginated, update OpenAPI annotations. Extend `DslExecutionsResourceTest` with offset+limit combination cases. Additive, backwards-compatible — existing calls without `offset` unchanged. FE paging controls are a separate follow-up, not in scope. Tier backend. See plan. | High | loop | - | - | `./docs/plans/T217-executions-list-offset-pagination.md`          |
| T218 | Backlog | Executions page pagination controls in admin UI | frontend — `useExecutionsApi` sends no `offset`/`limit`; Executions page fetches once, no way to page (follow-up to T217's backend `offset` param). Goal: pass-through `offset`/`limit` query params in BFF route `server/api/v1/executions/index.get.ts`, extend `useExecutionsApi.list()` with optional paging args, add prev/next paging controls to the Executions page using `total` + page size for page count, reset to first page on filter change. Extend `useExecutionsApi.spec.ts` and BFF `routes.spec.ts` with offset pass-through cases. Depends on T217 endpoint. Tier frontend. See plan. | High | loop | - | T217 | `./docs/plans/T218-executions-page-pagination-controls.md`          |
