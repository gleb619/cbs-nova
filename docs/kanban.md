# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture             | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                                                                                                                                  |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T326 | Blocked | Execution timeline viz — `dsl_run_transactions` flat list, no temporal view | frontend — code-confirmed gap: codegraph `ExecutionTimeline`/`RunTimeline`/`transactionTimeline` queries return zero FE components; T312 surfaced the per-run `dsl_run_transactions` data via BFF+FE table, but the executions detail page renders transactions as a flat collapsible list sorted by `executedAt` — for a multi-step process with retries, saga compensations, or parallel branches, users must scroll and mentally reconstruct temporal order from timestamp strings. Task: `ExecutionTimeline.vue` component (decide + document single-lane chronological track vs swim-lane per transaction name — single-lane default for low-process-count typical case, swim-lane if lane count > threshold), horizontal time axis with zoom (fit-to-view + fixed scales), bars per transaction with status→color mapping (consult `dataviz` skill for accessible palette), duration proportional to width, hover-tooltip with name+status+duration+error, click-bar opens the existing `PayloadTab` for that transaction, empty state when zero transactions, loading + error states per existing conventions, `data-testid` per T257–T261 conventions (`execution-timeline`, `execution-timeline-bar-<idx>`); vitest (renders mock transactions in correct chronological order, status→color map covers all `TransactionExecution` statuses, duration→width math holds, empty state, hover tooltip); confirm no regression to the existing transactions table (timeline lives alongside, not replacing it). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T326-execution-timeline.md`                |
| T340 | Blocked | JDBC `Recording*` refactor — extract common proxy boilerplate | backend (refactor) — codegraph-flagged gap: `wc -l` on `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/capture/` shows `RecordingCallableStatement.java` 672 lines, `RecordingConnection.java` 445, `RecordingStatement.java` 382, `RecordingPreparedStatement.java` likely similar size — four JDBC proxy classes implementing the recording/capture SPI for test support, totaling ~1900+ lines. The `Recording*` pattern (delegate to underlying JDBC object, record call, return) is highly repetitive: every method on `Connection`/`Statement`/`PreparedStatement`/`CallableStatement` has the same shape — `try { record(...); return delegate.method(...); } catch (SQLException e) { throw new RecordingException(...); }`. Same exception wrapping, same logging call, same null-delegate guard. Without a shared base class or aspect, fixing a bug in error handling requires editing 4 files. The recent memory note flags "BaseContainers refactor for Temporal+SQLite" — extending that pattern to Recording* keeps the test infrastructure consistent. Task: (1) profile the 4 files to identify the exact shared boilerplate (delegate forwarding, exception wrapping, call recording, null guard), (2) extract either (a) a `RecordingJdbcSupport` static-helper / `RecordingProxySupport` abstract base class, OR (b) a small interceptor aspect — decide+justify in plan; (a) is simpler and pure-JDK, (b) requires an AOP dep. (3) Refactor the 4 Recording* classes to use the new base; preserve all existing behavior. (4) Existing test infra (`BaseContainers` etc.) must continue to pass — no test changes expected, but re-run integration tests to confirm. (5) Measure LOC delta: target ≥30% reduction across the 4 files. No public API change, no BE feature change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T340-recording-jdbc-refactor.md`        |
| T349 | Done | DSL `@Helper` — `DateMathHelper` (date arithmetic: add/diff/before/after/startOf) | backend — code-confirmed gap: `codegraph query DateMath --kind class` returns zero `@Helper`-annotated classes. `FormatDateHelper` + `ParseDateHelper` (T328) cover format/parse but NOT arithmetic — DSL authors computing "next business day", "30 days from now", "is request timestamp before cutoff", or "truncate timestamp to start-of-day for cache key" must currently chain `formatDate(now, "yyyy-MM-dd")` → `LocalDate.parse` (impossible from DSL) → `plusDays(30)` (impossible) → `formatDate(result, "yyyy-MM-dd")` (round-trip). Every DSL with date math re-invents this string-soup. `FormatDateHelper` already imports `java.time.{LocalDate, LocalDateTime, ZonedDateTime}` — `DateMathHelper` slots into the same time-package footprint. Task: `@Helper("dateMath")` single class w/ pure-math methods (no I/O, no Temporal integration — pure value transforms); (1) `add(date: String, amount: long, unit: String = "days", zone: String = "UTC")` → ISO-8601 string — units ∈ `{millis, seconds, minutes, hours, days, weeks, months, years}` (months/years use `Period`/`plus` per unit, not millisecond-multiply — DST-safe); (2) `diff(start: String, end: String, unit: String = "millis", zone: String = "UTC")` → `long` — returns the duration between two timestamps in the requested unit; (3) `before(a: String, b: String, zone: String = "UTC")` → `boolean`; (4) `after(a: String, b: String, zone: String = "UTC")` → `boolean`; (5) `startOf(date: String, unit: String = "day", zone: String = "UTC")` → ISO-8601 string — truncate to start of day/hour/minute/month/year for cache-key derivation. `zone` arg uses IANA `ZoneId.of(...)` w/ fail-fast on invalid (T328 pattern). Date parsing: accept ISO-8601 input only (LocalDate, LocalDateTime, OffsetDateTime, ZonedDateTime — auto-detect from format). Decide+justify in plan: output format for `add`/`startOf` (leaning `Instant.toString` for millis-precision, `LocalDateTime.toString` for date-precision — depends on input). Validations (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348 lesson — typed errors): empty `date` arg → error; unparseable `date` → error w/ format-hint; invalid `unit` → error listing allowed values; invalid `zone` → error; `add` w/ negative `amount` is fine (subtracts); `diff` w/ `start > end` is fine (returns negative). Registration per `CurrentTimestampHelper` shape; helper unit tests (add days across DST boundary in `America/New_York` zone, add 1 month on Jan 31 → Feb 28/29 leap-year handling, add 1 year on Feb 29 → Feb 28 (leap-year normalization, document), diff across DST, diff w/ start > end returns negative, before/after true/false, startOf day/hour/minute/month/year, all unit invalid errors, all zone invalid errors, empty date error, unparseable date error w/ format hint). `docs/dsl/helpers.md` (T334) entry showing "next-business-day" pattern + "is-order-stale" pattern paired w/ `CurrentTimestamp` + T328. Cross-link T328 FormatDate/ParseDate. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T349-date-math-helper.md````             |
| T350 | Done | DSL `@Helper` — `JsonPatchHelper` (RFC 7396 JSON merge patch apply + diff) | backend — code-confirmed gap: `codegraph query JsonPatch --kind class` returns zero `@Helper`-annotated classes; `query MergePatch --limit 5` also zero. `JsonExtractHelper` (shipped) covers READ (`JsonPath` queries), but WRITE / mutate surface is zero — DSL authors needing to apply a state-update to a stored JSON document, compute the minimal diff between two JSON states (audit-log generation, optimistic-concurrency ETag generation, partner-API delta-sync, web-form PATCH operations) must currently inline Jackson `ObjectMapper` calls via custom BE Java activities — error-prone, off-catalog, no first-class DSL surface for the well-defined RFC 7396 spec. Jackson `JsonMergePatch` class (`com.fasterxml.jackson.databind.JsonMergePatch`, since Jackson 2.10) already implements RFC 7396 exactly — DSL helper is a thin wrapper w/ string-input/string-output for DSL ergonomics, no new dep. Task: `@Helper("jsonPatch")` single class w/ two methods: (1) `apply(source: String, patch: String) → String` — apply RFC 7396 merge patch: keys in `patch` that exist in `source` are replaced; keys w/ value `null` in `patch` are removed from `source`; recursive for nested objects. Uses Jackson `JsonMergePatch.fromJsonNode(patchNode).apply(sourceNode)`. (2) `diff(source: String, target: String) → String` — generate minimal RFC 7396 patch transforming `source` → `target` (Jackson `JsonMergePatch.fromDiff(sourceNode, targetNode)`). Validations (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348/T349 lesson — typed errors): empty `source` / `patch` / `target` → `IllegalArgumentException` (RFC 7396 requires object root, empty JSON is not a valid object); malformed JSON → `IllegalArgumentException` w/ Jackson parse-error hint; non-object root → `IllegalArgumentException` (RFC 7396 §1 — spec implies object root). `apply` w/ empty patch → returns `source` unchanged (no-op documented, NOT an error — sensible default). `diff` w/ identical source/target → returns `{}` (RFC 7396 §4 example, NOT an error). Decide+justify in plan: output JSON formatting (leaning compact `JsonGenerator` w/o pretty-print). Registration per `CurrentTimestampHelper` shape; helper unit tests (`apply` simple key-replace, `apply` nested-object-replace, `apply` null-value removes key, `apply` empty patch returns source unchanged, `apply` adds new key, `apply` recursive nested w/ array leaf preserved, `diff` identical → `{}`, `diff` simple add → `{key: value}`, `diff` modify → `{key: newValue}`, `diff` delete → `{key: null}`, `diff` nested objects, `apply` empty source error, `apply` malformed JSON error, `apply` non-object root error, `apply` malformed patch error). `docs/dsl/helpers.md` (T334) entry showing: (a) optimistic-concurrency ETag pattern — `jsonPatch.diff(prevState, newState)` → store as patch, replay later; (b) partner-API delta-sync — fetch current → `jsonPatch.diff(server, local)` → upload minimal patch. Cross-link T327 Base64 (for ETag transport), T330 HmacSha256 (for patch signature). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T350-json-patch-helper.md````            |
| T351 | Done | DSL `@Helper` — `ListOpsHelper` (pluck/flatten/distinct/countBy/sumBy/groupBy/minBy/maxBy) | backend — code-confirmed gap: `codegraph query ListOps --kind class` returns zero; `Pluck` zero; `GroupBy` zero. Existing helpers cover sort (`SortRecordsHelper`) and filter (`FilterRecordsHelper`) but NOT other record/list operations — DSL authors iterating over JSON API responses / DB query results / partner payloads currently inline awkward workarounds: (a) for "extract all `email` fields from a list of users" they must nest `JsonExtractHelper` in a loop; (b) for "group orders by status" they must filter-then-loop per status value; (c) for "sum revenue across records" they must chain `ArithmeticHelper` (basic math only — no aggregation); (d) for "dedupe records by id" they must run `JsonPatch` round-trip per element. Every DSL w/ list-records re-invents these ops. Task: `@Helper("listOps")` single class w/ typed-input methods following `Base64Helper` shape (`encode`/`decode` precedent): (1) `pluck(records: List, field: String) → List` — extract single field across all records (returns flat list of field values, skips records where field missing → typed error per T327-T350 lesson — fail-fast not silent skip); (2) `flatten(nested: List) → List` — flatten one level (recursive depth parameter — decide+justify in plan, default `1`, set `-1` for full-depth); (3) `distinct(list: List) → List` — dedupe preserving insertion order via `LinkedHashSet` (preserves deterministic ordering — temporal DSL semantic); (4) `groupBy(records: List, field: String) → Map<Object, List>` — group records by field value, preserves first-seen group order; (5) `countBy(records: List, field: String) → Map<Object, Long>` — frequency count per field value; (6) `sumBy(records: List, field: String) → Double` — sum of numeric field (Decimal not BigDecimal — DSL-friendly, document precision trade-off for monetary use; punt BigDecimal to T352-or-similar if monetary precision becomes a real requirement); (7) `minBy(records: List, field: String) → Map` — record w/ smallest field value; (8) `maxBy(records: List, field: String) → Map` — record w/ largest field value; ties broken by first occurrence. `records` shape: `List<Map<String, Object>>` matching `FilterRecordsHelper` / `SortRecordsHelper` precedent. Validations (T327-T350 lesson — typed errors): null `records` → error; `field` empty/blank → error; missing `field` on any record (pluck/countBy/groupBy/sumBy/minBy/maxBy) → error w/ index; non-numeric field on sumBy/minBy/maxBy → error w/ value-preview; empty list on minBy/maxBy/sumBy → error (no zero-default — these are aggregations, distinct from existing helpers' semantics; document). `flatten` w/ element not being a List → error w/ index. Registration per `CurrentTimestampHelper` shape; helper unit tests (`pluck` simple field extraction, `pluck` missing field → error w/ index, `flatten` 1-level, `flatten` recursive `-1`, `flatten` non-list element → error, `distinct` preserves order, `distinct` w/ mixed types (String/Integer/Map deduped separately by equals), `groupBy` 3 groups, `groupBy` preserves first-seen order, `countBy` frequency check, `sumBy` numeric field sum, `sumBy` non-numeric → error, `minBy` picks smallest, `maxBy` picks largest, `minBy` ties broken by first-seen, empty-list errors). `docs/dsl/helpers.md` (T334) entry showing "summarize orders by status" pattern: `listOps.countBy(orders, "status")` → log via `metric.counter(...)` (T348); "extract all emails" pattern: `listOps.pluck(users, "email")` → `httpCall(...)` for marketing blast. Cross-link FilterRecordsHelper/SortRecordsHelper. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T351-list-ops-helper.md````              |
| T352 | Done | DSL `@Helper` — `MathHelper` (descriptive statistics + scalar math) | backend — code-confirmed gap: `codegraph query Math --kind class` returns zero `@Helper`-annotated classes; `ArithmeticHelper` (shipped) covers ONLY element-wise `add`/`subtract`/`multiply`/`divide` returning `BigDecimal` — no AGGREGATIONS (`sum`/`mean`/`median`/`stddev`/`min`/`max`/`percentile`), no SCALAR transforms (`clamp`/`round`/`abs`/`floor`/`ceil`). DSL authors computing "average response time across these requests", "p99 latency", "clamp a value to a business range", "round to 2 decimal places for currency display", "minimum bid from list of offers" must currently either (a) write a custom BE Java activity, (b) chain ArithmeticHelper calls in DSL arithmetic expressions (which work for sum of 2-3 known values but not for "sum over a list" — list traversal impossible from DSL arithmetic), or (c) compose list-of-numbers via `ListOpsHelper::sumBy` (T351) then arithmetic. `sumBy` (T351) covers `sum` of one field; T352 covers the rest. Task: `@Helper("math")` single class w/ typed-input methods (`Base64Helper` shape): AGGREGATIONS over `List<Number>` — (1) `sum(numbers: List<Number>) → Double` (Double — DSL-friendly, matches T351 `sumBy` precision); (2) `min(numbers: List<Number>) → Double`; (3) `max(numbers: List<Number>) → Double`; (4) `mean(numbers: List<Number>) → Double`; (5) `median(numbers: List<Number>) → Double` — for even-length lists, returns average of two middle values; (6) `percentile(numbers: List<Number>, p: double) → Double` — `p ∈ [0, 100]`, uses nearest-rank method (`index = ceil(p/100 * N)`, 1-based) w/ interpolation between adjacent values for non-integer rank (numpy-linear method); (7) `stddev(numbers: List<Number>) → Double` — sample standard deviation (Bessel-corrected, divide by `N-1`, NOT population `N`). SCALAR transforms — (8) `clamp(value: Number, min: Number, max: Number) → Double` (returns value bounded to `[min, max]`); (9) `round(value: Number, scale: int = 0) → Double` (BigDecimal under the hood — `HALF_UP` rounding, scale `-1` for integer truncation, `0` for 2-decimal-place rounding); (10) `abs(value: Number) → Double`; (11) `floor(value: Number) → Long`; (12) `ceil(value: Number) → Long`. Decide+justify in plan: result types per method (Double vs Long — Double for fractional stats, Long for floor/ceil integer-only). Validations (T327-T351 lesson — typed errors): null/empty list → `IllegalArgumentException`; `p < 0 || p > 100` on percentile → error; `min > max` on clamp → error; `scale < -1 || scale > 15` on round → error (JDK BigDecimal scale limits); non-`Number` element in list → error w/ index. `stddev` w/ single-element list → `IllegalArgumentException` (sample stddev undefined for `N=1`); document the Bessel correction decision. Registration per `CurrentTimestampHelper` shape; helper unit tests (`sum` simple, `sum` empty → error, `mean` rounding precision for `[1,2,3,4,5]` → `3.0`, `median` odd-length returns middle, `median` even-length returns avg of middles, `percentile(50)` ≡ `median` for odd-length, `percentile(99)` for `n=100` returns 99th value, `percentile` p out of range error, `stddev` known vector `[2,4,4,4,5,5,7,9]` → `2.138` (sample stddev), `stddev` single-element → error, `clamp` value in range returns value, `clamp` value below min returns min, `clamp` value above max returns max, `clamp` min > max error, `round(3.14159, 2)` → `3.14`, `round(3.5, 0)` → `4.0` (HALF_UP), `round(2.5, 0)` → `3.0` (HALF_UP — note: banker's rounding NOT used), `abs` positive/negative, `floor(3.7)` → `3`, `ceil(3.2)` → `4`). `docs/dsl/helpers.md` (T334) entry showing: (a) "p99 latency" — `math.percentile(timings, 99)` after `listOps.pluck(httpCallLogs, "durationMs")`; (b) "currency display" — `math.round(price, 2)` after `arithmetic.multiply(price, taxRate)`; (c) "bid clamping" — `math.clamp(bid, minAcceptable, maxAcceptable)`. Cross-link T351 ListOpsHelper (pluck → math), T347 BackoffHelper (latency budgets). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T352-math-helper.md````                   |
| T353 | Done | DSL `@Helper` — `HttpAuthHelper` (build Authorization/header maps: Bearer/Basic/ApiKey) | backend — code-confirmed gap: `codegraph query HttpAuth --kind class` returns zero; `query Authorization` zero; `query Bearer` returns only `SecurityConfiguration` and `GuardEnabledTestApp` (unrelated — the BE-side Keycloak/OIDC filter, NOT DSL-callable). `HttpCallHelper` accepts `Map<String, String> headers` (`HttpCallIn.java:39` field), but DSL authors building authenticated HTTP calls to partner APIs (OAuth2-protected endpoints, Basic-auth internal services, custom ApiKey partner APIs like Stripe / AWS-style) currently inline string-concat workarounds: `"Bearer " + token`, `"Basic " + base64(user:pass)`, `"ApiKey " + key` — error-prone (whitespace, casing, base64 padding for Basic per RFC 7617), off-catalog. `Base64Helper` (T327) exists but isn't wired into a Basic-auth convenience method. Task: `@Helper("httpAuth")` single class w/ typed-input methods returning `Map<String, String>` (direct plug-in to `HttpCallHelper.headers`): (1) `bearer(token: String) → Map<String, String>` — returns `{"Authorization": "Bearer <token>"}` (single space after scheme per RFC 6750 §2.1); (2) `basic(username: String, password: String) → Map<String, String>` — returns `{"Authorization": "Basic <base64>"}` where `<base64> = base64(username:password)` UTF-8-encoded w/ `:` separator per RFC 7617 (uses `Base64Helper.encode(...)` internally — T327 dependency; if `urlSafe=true` not specified use RFC 4648 §4 standard base64 w/ padding, NOT URL-safe, since HTTP auth header is RFC-7617-standard); (3) `apiKey(key: String, header: String = "X-Api-Key", prefix: String = "") → Map<String, String>` — returns `{<header>: "<prefix> <key>"}` (or `<key>` if prefix blank); supports `Stripe`-style `{"Authorization": "Bearer sk_live_..."}` (caller passes `header="Authorization", prefix="Bearer"`) and `AWS`-style `{"X-Api-Key": "<key>"}` (caller uses defaults); (4) optional `custom(header: String, value: String) → Map<String, String>` — escape hatch for arbitrary headers (decide+justify in plan — leaning include since it's trivial and DSL authors may need it for non-standard auth like custom `X-Auth-Token`). Validations (T327-T352 lesson — typed errors): null/empty `token` / `key` → error; empty `username` → error (Basic auth w/o user is meaningless); empty `password` allowed (some Basic auth endpoints accept empty password); empty/blank `header` arg → error (no auth header is meaningless); null `value` → error. Helper unit tests (`bearer` simple token → map, `bearer` empty token error, `bearer` token w/ leading/trailing whitespace preserved as-is, `basic` known vector `Aladdin:open sesame` → `QWxhZGRpbjpvcGVuIHNlc2FtZQ==` → map, `basic` empty username error, `basic` empty password allowed, `apiKey` default X-Api-Key header, `apiKey` custom Authorization header + Bearer prefix, `apiKey` empty prefix → key only, `apiKey` empty key error, `apiKey` empty header error, `custom` arbitrary value, `custom` empty header error, `custom` empty value error). `docs/dsl/helpers.md` (T334) entry showing: (a) "OAuth2 partner call" — fetch token → `httpAuth.bearer(token)` → `httpCall(headers=..., url=...)`; (b) "Basic-auth internal service" — `httpAuth.basic(svcUser, svcPass)` → `httpCall(headers=...)`; (c) "Stripe-style" — `httpAuth.apiKey(stripeKey, "Authorization", "Bearer")` → `httpCall(...)`. Cross-link T327 Base64 (used internally by `basic`), T330 HmacSha256 (for request-signing variants), HttpCallHelper (header input). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T353-http-auth-helper.md````              |
| T354 | Done | DSL `@Helper` — `QueryStringHelper` (build/parse URL query strings) | backend — code-confirmed gap: `codegraph query QueryString --kind class` returns zero `@Helper`-annotated classes. `UrlEncodeHelper` + `UrlDecodeHelper` (T331, shipped) cover per-component percent-encoding but DSL authors building paginated REST-API URLs (Google Maps-style `?lat=37.42&lng=-122.08&q=...`, OAuth authorization `?client_id=...&redirect_uri=...&state=...&scope=...`, partner-API filters `?since=2026-01-01&limit=100&status=active`) currently inline string-concat: `"?key=" + urlEncode(key) + "&q=" + urlEncode(query)` — forgetting the leading `?` separator, double-encoding issues (encode-then-re-encode), missing handling of null values, no symmetric parse for response pagination links (HTTP `Link` header, `next` URL `?cursor=...` field). Every DSL calling a paginated API reinvents this. Task: `@Helper("queryString")` single class w/ two methods: (1) `build(params: Map<String, String>) → String` — returns `"k1=v1&k2=v2"` w/ proper URL encoding (each key + each value percent-encoded via T331 `UrlEncodeHelper`; `=` separator between key + value; `&` separator between pairs; keys w/ `null` value → typed error — T327-T353 lesson); empty `params` map → returns `""` (NOT error — sensible default; document). (2) `parse(queryString: String) → Map<String, String>` — inverse direction: split on `&` then `=`, percent-decode each component via T331 `UrlDecodeHelper`; handles both `?key=val` (leading `?` allowed) and `key=val` (no leading `?`); empty `queryString` → returns empty map (NOT error); malformed input (no `=` in pair) → skip the malformed pair w/ logged warning (decide+justify in plan — leaning skip w/ warning over hard error since real-world partner URLs sometimes contain garbage; document). Decide+justify in plan: parameter ordering for `build` output (leaning `LinkedHashMap` insertion-order to preserve caller intent — DSL authors rely on this for human-readable URLs and signature canonicalization; document). Implement manually using T331 helpers (no new deps); URL-decode via `URLDecoder.decode(s, "UTF-8")` per RFC 3986 §2.5. Validations (T327-T353 lesson — typed errors): null `params` map → `IllegalArgumentException`; null key in map → error (URL requires string key); null value in map → error; empty `params` → returns `""` (no-op); empty `queryString` → returns `{}` (no-op). Helper unit tests (`build` simple `{"a":"1","b":"2"}` → `"a=1&b=2"` order-preserved, `build` w/ special chars → percent-encoded (e.g. `{"q":"hello world"}` → `"q=hello+world"` for form-encoding via T331; document form vs path encoding), `build` empty map → `""`, `build` null key → error, `build` null value → error, `parse` simple → map, `parse` w/ leading `?` strips it, `parse` w/ multiple params in correct order, `parse` w/ percent-encoded values decodes correctly (`"q=hello%20world"` → `{"q":"hello world"}`), `parse` empty → `{}`, `parse` malformed pair skipped w/ warning, `build`/`parse` round-trip preserves content for ASCII keys + values). `docs/dsl/helpers.md` (T334) entry showing: (a) "Google Maps URL" — `queryString.build({"q": query, "key": apiKey})` → `httpCall(url = "https://maps.googleapis.com/...?" + qs)`; (b) "OAuth authorization URL" — `queryString.build({"client_id": id, "redirect_uri": url, "state": state, "scope": scopes})` → redirect; (c) "Parse Link header cursor" — `queryString.parse(extractAfter(nextUrl, "?"))` → pass to next call. Cross-link T331 UrlEncodeHelper/UrlDecodeHelper (used internally), HttpCallHelper (URL building). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T354-query-string-helper.md````         |
| T355 | Done | DSL `@Helper` — `CompressionHelper` (gzip/deflate strings to base64) | backend — code-confirmed gap: `codegraph query Compression --kind class` returns zero; `query Gzip` zero; `query Deflate` zero. `query GZIPOutputStream` zero (no JDK stdlib usage in helpers). Helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256/hex/backoff/metric/date-math/json-patch/list-ops/math/http-auth/query-string (T324/T327-T354) — none for compression. DSL authors sending/receiving compressed payloads to partner APIs (HTTP `Content-Encoding: gzip` for large request bodies, partner APIs returning gzip'd JSON for cost reduction, AWS S3 multipart upload w/ gzip-compressed metadata, Kubernetes YAML manifests transmitted over WebSocket) currently inline `ByteArrayOutputStream` + `GZIPOutputStream` + `Base64.encode` boilerplate — error-prone (resource leaks w/o try-with-resources, charset ambiguity, header-included vs raw DEFLATE confusion). JDK `java.util.zip.{GZIPInputStream, GZIPOutputStream, Deflater, Inflater}` already on classpath — pure wrapping, zero new deps. Task: `@Helper("compression")` single class w/ four methods: (1) `gzip(input: String) → String` — UTF-8 encode input bytes, gzip-compress (RFC 1952 w/ 10-byte header + 8-byte trailer, CRC-32 checksum), base64-encode result → return String (callers pipe into `httpCall` body as text); (2) `gunzip(input: String) → String` — inverse: base64-decode (via JDK `java.util.Base64`), gzip-decompress (validate CRC-32), UTF-8 decode → return String; (3) `deflate(input: String) → String` — raw DEFLATE (RFC 1951, NO gzip wrapper headers — used for HTTP `Content-Encoding: deflate` per RFC 7230 §4.2.2 — note: HTTP spec actually says zlib format w/ header but real-world partners use raw deflate; pick raw DEFLATE w/ documented warning), base64-encode → return String; (4) `inflate(input: String) → String` — inverse: base64-decode, raw DEFLATE decompress, UTF-8 decode → return String. Decide+justify in plan: gzip vs deflate default (leaning `gzip` as default for HTTP body compression — common standard). Compression level for `gzip`/`deflate`: default JDK `Deflater.DEFAULT_COMPRESSION` (= level 6, balanced speed/ratio); expose `level: int = -1` arg allowing values `[-1, 0..9]` where `-1` = default, `0` = `NO_COMPRESSION` (stored), `1` = `BEST_SPEED`, `9` = `BEST_COMPRESSION` (decide+justify in plan — leaning include level arg for tuning). Validations (T327-T354 lesson — typed errors): null/empty `input` → `IllegalArgumentException` (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348/T349/T350/T351/T352/T353/T354 lesson — no silent empty); malformed base64 in decode methods → error w/ `IllegalArgumentException`; malformed gzip/deflate stream in decompress methods → error w/ `IllegalArgumentException` (don't leak `ZipException` from JDK — map at helper boundary); `level < -1 || level > 9` → error. UTF-8 explicit (T327/T330/T331/T346 lesson). Use JDK `java.util.Base64` internally (NOT T327 Base64Helper — T327 is DSL-facing; internal JDK Base64 is faster and avoids bean lookup). Helper unit tests (`gzip` ASCII string round-trip → original, `gzip` UTF-8 multi-byte string round-trip → original, `gzip` empty string error, `gzip` large string (10MB) round-trip, `gzip` known vector `""` → base64 of empty gzip wrapper, `gunzip` non-gzip base64 → error, `gunzip` corrupted stream → error, `deflate` ASCII round-trip → original, `inflate` non-deflate base64 → error, `level = 9` produces smaller output than `level = 1` for the same input, `level = -1` accepted as default, `level = 10` error, `level = -2` error). `docs/dsl/helpers.md` (T334) entry showing: (a) "compress HTTP request body" — `compression.gzip(largePayload)` → `httpCall(headers={"Content-Encoding": "gzip", "Content-Type": "application/json"}, body=gzipped)`; (b) "decompress HTTP response body" — `compression.gunzip(responseBody)` → `jsonExtract(decompressed, "$.data")`; (c) "S3 metadata gzip" — `compression.gzip(yamlManifest)` → upload as base64 S3 metadata. Cross-link T327 Base64 (used internally), HttpCallHelper (body input), T334 cookbook. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T355-compression-helper.md````          |
| T356 | Done | DSL `@Helper` — `RandomHelper` (random int/long/double/string/choice) | backend — code-confirmed gap: `codegraph query Random --kind class` returns zero `@Helper`-annotated classes (only `UuidV7Helper` — identity, not general RNG). `ThreadLocalRandom` IS used internally by `UnreliableApiHelper` (line 15) and `UuidV7Helper` (line 17), but there's NO DSL-callable RNG surface. DSL authors needing random data for test fixtures, A/B decision routing (10% sample), retry-jitter seed, sampling, nonce generation (when `UuidV7` is overkill), password-token generation (random alphanumeric strings for partner-API onboarding keys), or `redact/sanitize` operations (replace real data w/ random data for log forwarding) currently inline `new Random().nextInt(...)` / `Math.random()` / `UUID.randomUUID()` boilerplate — every DSL re-implements basic RNG. Task: `@Helper("random")` single class w/ typed-input methods (`Base64Helper` shape): (1) `int(min: int, max: int) → int` — uniform int in `[min, max]` INCLUSIVE (use `ThreadLocalRandom.current().nextInt(min, max + 1)` — JDK's `nextInt(min, max)` is exclusive of `max`, must add 1 for inclusive); (2) `long(min: long, max: long) → long` — uniform long in `[min, max]` inclusive; (3) `double(min: double = 0.0, max: double = 1.0) → double` — uniform double in `[min, max)` (per `ThreadLocalRandom.nextDouble` convention); (4) `string(length: int, charset: String = "alphanumeric") → String` — random string of `length` chars; `charset ∈ {"alphanumeric", "alpha", "numeric", "hex", "base64url"}` — preset char pools (decide+justify in plan, leaning preset list over custom char-set arg — DSL authors rarely need custom char pools + this avoids insecure / non-deterministic charset errors); (5) `choice(list: List) → Object` — uniformly pick one element at random (returns the element, NOT its index). Use `ThreadLocalRandom` throughout (NOT `new SecureRandom` — cryptographic security is NOT the goal; for that DSL authors compose `UuidV7` + `hmacSha256` per T330). `UuidV7Helper` (T324) covers secure identity; `RandomHelper` covers non-cryptographic random. Validations (T327-T355 lesson — typed errors): null `list` on choice → error; empty `list` on choice → error; `min > max` on int/long/double → error; `length < 0` on string → error; `length > 100000` on string → error (cap to prevent DoS — large random strings are memory bombs); unknown `charset` value → error listing allowed values. Helper unit tests (`int(1, 6)` over 1000 iterations returns value in `[1, 6]` inclusive and distribution is roughly uniform, `int(5, 5)` always returns 5 (degenerate case), `int(1, 2)` returns 1 or 2 across iterations, `int` w/ min > max → error, `long` similar tests for `[Long.MIN_VALUE, Long.MAX_VALUE]` bounds, `double(0.0, 1.0)` returns in `[0.0, 1.0)`, `double(2.5, 7.5)` returns in `[2.5, 7.5)`, `string(10, "alphanumeric")` returns 10-char string from `[a-zA-Z0-9]`, `string(0, "alpha")` returns `""`, `string(10, "hex")` returns 10 chars from `[0-9a-f]`, `string(-1)` error, `string(100001)` error, `string(10, "unknown")` error, `choice([1,2,3])` returns one of `1,2,3` across iterations, `choice([])` error, `choice(null)` error, determinism check — `int(1,100)` over 10000 iterations hits each value at least once for small ranges). `docs/dsl/helpers.md` (T334) entry showing: (a) "A/B test routing" — `random.int(1, 10) <= 1` → 10% bucket → variant A; else variant B; (b) "Onboarding API key" — `random.string(32, "base64url")` → partner registration; (c) "Sample list" — `random.choice(partnerEndpoints)`. Cross-link T324 UuidV7 (cryptographic identity — different surface), T330 HMAC (for actual cryptographic randomness). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T356-random-helper.md````               | backend — code-confirmed gap: `codegraph query Compression --kind class` returns zero; `query Gzip` zero; `query Deflate` zero. `query GZIPOutputStream` zero (no JDK stdlib usage in helpers). Helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256/hex/backoff/metric/date-math/json-patch/list-ops/math/http-auth/query-string (T324/T327-T354) — none for compression. DSL authors sending/receiving compressed payloads to partner APIs (HTTP `Content-Encoding: gzip` for large request bodies, partner APIs returning gzip'd JSON for cost reduction, AWS S3 multipart upload w/ gzip-compressed metadata, Kubernetes YAML manifests transmitted over WebSocket) currently inline `ByteArrayOutputStream` + `GZIPOutputStream` + `Base64.encode` boilerplate — error-prone (resource leaks w/o try-with-resources, charset ambiguity, header-included vs raw DEFLATE confusion). JDK `java.util.zip.{GZIPInputStream, GZIPOutputStream, Deflater, Inflater}` already on classpath — pure wrapping, zero new deps. Task: `@Helper("compression")` single class w/ four methods: (1) `gzip(input: String) → String` — UTF-8 encode input bytes, gzip-compress (RFC 1952 w/ 10-byte header + 8-byte trailer, CRC-32 checksum), base64-encode result → return String (callers pipe into `httpCall` body as text); (2) `gunzip(input: String) → String` — inverse: base64-decode (via JDK `java.util.Base64`), gzip-decompress (validate CRC-32), UTF-8 decode → return String; (3) `deflate(input: String) → String` — raw DEFLATE (RFC 1951, NO gzip wrapper headers — used for HTTP `Content-Encoding: deflate` per RFC 7230 §4.2.2 — note: HTTP spec actually says zlib format w/ header but real-world partners use raw deflate; pick raw DEFLATE w/ documented warning), base64-encode → return String; (4) `inflate(input: String) → String` — inverse: base64-decode, raw DEFLATE decompress, UTF-8 decode → return String. Decide+justify in plan: gzip vs deflate default (leaning `gzip` as default for HTTP body compression — common standard). Compression level for `gzip`/`deflate`: default JDK `Deflater.DEFAULT_COMPRESSION` (= level 6, balanced speed/ratio); expose `level: int = -1` arg allowing values `[-1, 0..9]` where `-1` = default, `0` = `NO_COMPRESSION` (stored), `1` = `BEST_SPEED`, `9` = `BEST_COMPRESSION` (decide+justify in plan — leaning include level arg for tuning). Validations (T327-T354 lesson — typed errors): null/empty `input` → `IllegalArgumentException` (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348/T349/T350/T351/T352/T353/T354 lesson — no silent empty); malformed base64 in decode methods → error w/ `IllegalArgumentException`; malformed gzip/deflate stream in decompress methods → error w/ `IllegalArgumentException` (don't leak `ZipException` from JDK — map at helper boundary); `level < -1 || level > 9` → error. UTF-8 explicit (T327/T330/T331/T346 lesson). Use JDK `java.util.Base64` internally (NOT T327 Base64Helper — T327 is DSL-facing; internal JDK Base64 is faster and avoids bean lookup). Helper unit tests (`gzip` ASCII string round-trip → original, `gzip` UTF-8 multi-byte string round-trip → original, `gzip` empty string error, `gzip` large string (10MB) round-trip, `gzip` known vector `""` → base64 of empty gzip wrapper, `gunzip` non-gzip base64 → error, `gunzip` corrupted stream → error, `deflate` ASCII round-trip → original, `inflate` non-deflate base64 → error, `level = 9` produces smaller output than `level = 1` for the same input, `level = -1` accepted as default, `level = 10` error, `level = -2` error). `docs/dsl/helpers.md` (T334) entry showing: (a) "compress HTTP request body" — `compression.gzip(largePayload)` → `httpCall(headers={"Content-Encoding": "gzip", "Content-Type": "application/json"}, body=gzipped)"; (b) "decompress HTTP response body" — `compression.gunzip(responseBody)` → `jsonExtract(decompressed, "$.data")`; (c) "S3 metadata gzip" — `compression.gzip(yamlManifest)` → upload as base64 S3 metadata. Cross-link T327 Base64 (used internally), HttpCallHelper (body input), T334 cookbook. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T355-compression-helper.md````          | backend — code-confirmed gap: `codegraph query QueryString --kind class` returns zero `@Helper`-annotated classes. `UrlEncodeHelper` + `UrlDecodeHelper` (T331, shipped) cover per-component percent-encoding but DSL authors building paginated REST-API URLs (Google Maps-style `?lat=37.42&lng=-122.08&q=...`, OAuth authorization `?client_id=...&redirect_uri=...&state=...&scope=...`, partner-API filters `?since=2026-01-01&limit=100&status=active`) currently inline string-concat: `"?key=" + urlEncode(key) + "&q=" + urlEncode(query)` — forgetting the leading `?` separator, double-encoding issues (encode-then-re-encode), missing handling of null values, no symmetric parse for response pagination links (HTTP `Link` header, `next` URL `?cursor=...` field). Every DSL calling a paginated API reinvents this. Task: `@Helper("queryString")` single class w/ two methods: (1) `build(params: Map<String, String>) → String` — returns `"k1=v1&k2=v2"` w/ proper URL encoding (each key + each value percent-encoded via T331 `UrlEncodeHelper`; `=` separator between key + value; `&` separator between pairs; keys w/ `null` value → typed error — T327-T353 lesson); empty `params` map → returns `""` (NOT error — sensible default; document). (2) `parse(queryString: String) → Map<String, String>` — inverse direction: split on `&` then `=`, percent-decode each component via T331 `UrlDecodeHelper`; handles both `?key=val` (leading `?` allowed) and `key=val` (no leading `?`); empty `queryString` → returns empty map (NOT error); malformed input (no `=` in pair) → skip the malformed pair w/ logged warning (decide+justify in plan — leaning skip w/ warning over hard error since real-world partner URLs sometimes contain garbage; document). Decide+justify in plan: parameter ordering for `build` output (leaning `LinkedHashMap` insertion-order to preserve caller intent — DSL authors rely on this for human-readable URLs and signature canonicalization; document). Implement manually using T331 helpers (no new deps); URL-decode via `URLDecoder.decode(s, "UTF-8")` per RFC 3986 §2.5. Validations (T327-T353 lesson — typed errors): null `params` map → `IllegalArgumentException`; null key in map → error (URL requires string key); null value in map → error; empty `params` → returns `""` (no-op); empty `queryString` → returns `{}` (no-op). Helper unit tests (`build` simple `{"a":"1","b":"2"}` → `"a=1&b=2"` order-preserved, `build` w/ special chars → percent-encoded (e.g. `{"q":"hello world"}` → `"q=hello+world"` for form-encoding via T331; document form vs path encoding), `build` empty map → `""`, `build` null key → error, `build` null value → error, `parse` simple → map, `parse` w/ leading `?` strips it, `parse` w/ multiple params in correct order, `parse` w/ percent-encoded values decodes correctly (`"q=hello%20world"` → `{"q":"hello world"}`), `parse` empty → `{}`, `parse` malformed pair skipped w/ warning, `build`/`parse` round-trip preserves content for ASCII keys + values). `docs/dsl/helpers.md` (T334) entry showing: (a) "Google Maps URL" — `queryString.build({"q": query, "key": apiKey})` → `httpCall(url = "https://maps.googleapis.com/...?" + qs)`; (b) "OAuth authorization URL" — `queryString.build({"client_id": id, "redirect_uri": url, "state": state, "scope": scopes})` → redirect; (c) "Parse Link header cursor" — `queryString.parse(extractAfter(nextUrl, "?"))` → pass to next call. Cross-link T331 UrlEncodeHelper/UrlDecodeHelper (used internally), HttpCallHelper (URL building). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T354-query-string-helper.md````         | backend — code-confirmed gap: `codegraph query HttpAuth --kind class` returns zero; `query Authorization` zero; `query Bearer` returns only `SecurityConfiguration` and `GuardEnabledTestApp` (unrelated — the BE-side Keycloak/OIDC filter, NOT DSL-callable). `HttpCallHelper` accepts `Map<String, String> headers` (`HttpCallIn.java:39` field), but DSL authors building authenticated HTTP calls to partner APIs (OAuth2-protected endpoints, Basic-auth internal services, custom ApiKey partner APIs like Stripe / AWS-style) currently inline string-concat workarounds: `"Bearer " + token`, `"Basic " + base64(user:pass)`, `"ApiKey " + key` — error-prone (whitespace, casing, base64 padding for Basic per RFC 7617), off-catalog. `Base64Helper` (T327) exists but isn't wired into a Basic-auth convenience method. Task: `@Helper("httpAuth")` single class w/ typed-input methods returning `Map<String, String>` (direct plug-in to `HttpCallHelper.headers`): (1) `bearer(token: String) → Map<String, String>` — returns `{"Authorization": "Bearer <token>"}` (single space after scheme per RFC 6750 §2.1); (2) `basic(username: String, password: String) → Map<String, String>` — returns `{"Authorization": "Basic <base64>"}` where `<base64> = base64(username:password)` UTF-8-encoded w/ `:` separator per RFC 7617 (uses `Base64Helper.encode(...)` internally — T327 dependency; if `urlSafe=true` not specified use RFC 4648 §4 standard base64 w/ padding, NOT URL-safe, since HTTP auth header is RFC-7617-standard); (3) `apiKey(key: String, header: String = "X-Api-Key", prefix: String = "") → Map<String, String>` — returns `{<header>: "<prefix> <key>"}` (or `<key>` if prefix blank); supports `Stripe`-style `{"Authorization": "Bearer sk_live_..."}` (caller passes `header="Authorization", prefix="Bearer"`) and `AWS`-style `{"X-Api-Key": "<key>"}` (caller uses defaults); (4) optional `custom(header: String, value: String) → Map<String, String>` — escape hatch for arbitrary headers (decide+justify in plan — leaning include since it's trivial and DSL authors may need it for non-standard auth like custom `X-Auth-Token`). Validations (T327-T352 lesson — typed errors): null/empty `token` / `key` → error; empty `username` → error (Basic auth w/o user is meaningless); empty `password` allowed (some Basic auth endpoints accept empty password); empty/blank `header` arg → error (no auth header is meaningless); null `value` → error. Helper unit tests (`bearer` simple token → map, `bearer` empty token error, `bearer` token w/ leading/trailing whitespace preserved as-is, `basic` known vector `Aladdin:open sesame` → `QWxhZGRpbjpvcGVuIHNlc2FtZQ==` → map, `basic` empty username error, `basic` empty password allowed, `apiKey` default X-Api-Key header, `apiKey` custom Authorization header + Bearer prefix, `apiKey` empty prefix → key only, `apiKey` empty key error, `apiKey` empty header error, `custom` arbitrary value, `custom` empty header error, `custom` empty value error). `docs/dsl/helpers.md` (T334) entry showing: (a) "OAuth2 partner call" — fetch token → `httpAuth.bearer(token)` → `httpCall(headers=..., url=...)`; (b) "Basic-auth internal service" — `httpAuth.basic(svcUser, svcPass)` → `httpCall(headers=...)`; (c) "Stripe-style" — `httpAuth.apiKey(stripeKey, "Authorization", "Bearer")` → `httpCall(...)`. Cross-link T327 Base64 (used internally by `basic`), T330 HmacSha256 (for request-signing variants), HttpCallHelper (header input). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T353-http-auth-helper.md````              | backend — code-confirmed gap: `codegraph query Math --kind class` returns zero `@Helper`-annotated classes; `ArithmeticHelper` (shipped) covers ONLY element-wise `add`/`subtract`/`multiply`/`divide` returning `BigDecimal` — no AGGREGATIONS (`sum`/`mean`/`median`/`stddev`/`min`/`max`/`percentile`), no SCALAR transforms (`clamp`/`round`/`abs`/`floor`/`ceil`). DSL authors computing "average response time across these requests", "p99 latency", "clamp a value to a business range", "round to 2 decimal places for currency display", "minimum bid from list of offers" must currently either (a) write a custom BE Java activity, (b) chain ArithmeticHelper calls in DSL arithmetic expressions (which work for sum of 2-3 known values but not for "sum over a list" — list traversal impossible from DSL arithmetic), or (c) compose list-of-numbers via `ListOpsHelper::sumBy` (T351) then arithmetic. `sumBy` (T351) covers `sum` of one field; T352 covers the rest. Task: `@Helper("math")` single class w/ typed-input methods (`Base64Helper` shape): AGGREGATIONS over `List<Number>` — (1) `sum(numbers: List<Number>) → Double` (Double — DSL-friendly, matches T351 `sumBy` precision); (2) `min(numbers: List<Number>) → Double`; (3) `max(numbers: List<Number>) → Double`; (4) `mean(numbers: List<Number>) → Double`; (5) `median(numbers: List<Number>) → Double` — for even-length lists, returns average of two middle values; (6) `percentile(numbers: List<Number>, p: double) → Double` — `p ∈ [0, 100]`, uses nearest-rank method (`index = ceil(p/100 * N)`, 1-based) w/ interpolation between adjacent values for non-integer rank (numpy-linear method); (7) `stddev(numbers: List<Number>) → Double` — sample standard deviation (Bessel-corrected, divide by `N-1`, NOT population `N`). SCALAR transforms — (8) `clamp(value: Number, min: Number, max: Number) → Double` (returns value bounded to `[min, max]`); (9) `round(value: Number, scale: int = 0) → Double` (BigDecimal under the hood — `HALF_UP` rounding, scale `-1` for integer truncation, `0` for 2-decimal-place rounding); (10) `abs(value: Number) → Double`; (11) `floor(value: Number) → Long`; (12) `ceil(value: Number) → Long`. Decide+justify in plan: result types per method (Double vs Long — Double for fractional stats, Long for floor/ceil integer-only). Validations (T327-T351 lesson — typed errors): null/empty list → `IllegalArgumentException`; `p < 0 || p > 100` on percentile → error; `min > max` on clamp → error; `scale < -1 || scale > 15` on round → error (JDK BigDecimal scale limits); non-`Number` element in list → error w/ index. `stddev` w/ single-element list → `IllegalArgumentException` (sample stddev undefined for `N=1`); document the Bessel correction decision. Registration per `CurrentTimestampHelper` shape; helper unit tests (`sum` simple, `sum` empty → error, `mean` rounding precision for `[1,2,3,4,5]` → `3.0`, `median` odd-length returns middle, `median` even-length returns avg of middles, `percentile(50)` ≡ `median` for odd-length, `percentile(99)` for `n=100` returns 99th value, `percentile` p out of range error, `stddev` known vector `[2,4,4,4,5,5,7,9]` → `2.138` (sample stddev), `stddev` single-element → error, `clamp` value in range returns value, `clamp` value below min returns min, `clamp` value above max returns max, `clamp` min > max error, `round(3.14159, 2)` → `3.14`, `round(3.5, 0)` → `4.0` (HALF_UP), `round(2.5, 0)` → `3.0` (HALF_UP — note: banker's rounding NOT used), `abs` positive/negative, `floor(3.7)` → `3`, `ceil(3.2)` → `4`). `docs/dsl/helpers.md` (T334) entry showing: (a) "p99 latency" — `math.percentile(timings, 99)` after `listOps.pluck(httpCallLogs, "durationMs")`; (b) "currency display" — `math.round(price, 2)` after `arithmetic.multiply(price, taxRate)`; (c) "bid clamping" — `math.clamp(bid, minAcceptable, maxAcceptable)`. Cross-link T351 ListOpsHelper (pluck → math), T347 BackoffHelper (latency budgets). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T352-math-helper.md````                   | backend — code-confirmed gap: `codegraph query ListOps --kind class` returns zero; `Pluck` zero; `GroupBy` zero. Existing helpers cover sort (`SortRecordsHelper`) and filter (`FilterRecordsHelper`) but NOT other record/list operations — DSL authors iterating over JSON API responses / DB query results / partner payloads currently inline awkward workarounds: (a) for "extract all `email` fields from a list of users" they must nest `JsonExtractHelper` in a loop; (b) for "group orders by status" they must filter-then-loop per status value; (c) for "sum revenue across records" they must chain `ArithmeticHelper` (basic math only — no aggregation); (d) for "dedupe records by id" they must run `JsonPatch` round-trip per element. Every DSL w/ list-records re-invents these ops. Task: `@Helper("listOps")` single class w/ typed-input methods following `Base64Helper` shape (`encode`/`decode` precedent): (1) `pluck(records: List, field: String) → List` — extract single field across all records (returns flat list of field values, skips records where field missing → typed error per T327-T350 lesson — fail-fast not silent skip); (2) `flatten(nested: List) → List` — flatten one level (recursive depth parameter — decide+justify in plan, default `1`, set `-1` for full-depth); (3) `distinct(list: List) → List` — dedupe preserving insertion order via `LinkedHashSet` (preserves deterministic ordering — temporal DSL semantic); (4) `groupBy(records: List, field: String) → Map<Object, List>` — group records by field value, preserves first-seen group order; (5) `countBy(records: List, field: String) → Map<Object, Long>` — frequency count per field value; (6) `sumBy(records: List, field: String) → Double` — sum of numeric field (Decimal not BigDecimal — DSL-friendly, document precision trade-off for monetary use; punt BigDecimal to T352-or-similar if monetary precision becomes a real requirement); (7) `minBy(records: List, field: String) → Map` — record w/ smallest field value; (8) `maxBy(records: List, field: String) → Map` — record w/ largest field value; ties broken by first occurrence. `records` shape: `List<Map<String, Object>>` matching `FilterRecordsHelper` / `SortRecordsHelper` precedent. Validations (T327-T350 lesson — typed errors): null `records` → error; `field` empty/blank → error; missing `field` on any record (pluck/countBy/groupBy/sumBy/minBy/maxBy) → error w/ index; non-numeric field on sumBy/minBy/maxBy → error w/ value-preview; empty list on minBy/maxBy/sumBy → error (no zero-default — these are aggregations, distinct from existing helpers' semantics; document). `flatten` w/ element not being a List → error w/ index. Registration per `CurrentTimestampHelper` shape; helper unit tests (`pluck` simple field extraction, `pluck` missing field → error w/ index, `flatten` 1-level, `flatten` recursive `-1`, `flatten` non-list element → error, `distinct` preserves order, `distinct` w/ mixed types (String/Integer/Map deduped separately by equals), `groupBy` 3 groups, `groupBy` preserves first-seen order, `countBy` frequency check, `sumBy` numeric field sum, `sumBy` non-numeric → error, `minBy` picks smallest, `maxBy` picks largest, `minBy` ties broken by first-seen, empty-list errors). `docs/dsl/helpers.md` (T334) entry showing "summarize orders by status" pattern: `listOps.countBy(orders, "status")` → log via `metric.counter(...)` (T348); "extract all emails" pattern: `listOps.pluck(users, "email")` → `httpCall(...)` for marketing blast. Cross-link FilterRecordsHelper/SortRecordsHelper. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T351-list-ops-helper.md````              | backend — code-confirmed gap: `codegraph query JsonPatch --kind class` returns zero `@Helper`-annotated classes; `query MergePatch --limit 5` also zero. `JsonExtractHelper` (shipped) covers READ (`JsonPath` queries), but WRITE / mutate surface is zero — DSL authors needing to apply a state-update to a stored JSON document, compute the minimal diff between two JSON states (audit-log generation, optimistic-concurrency ETag generation, partner-API delta-sync, web-form PATCH operations) must currently inline Jackson `ObjectMapper` calls via custom BE Java activities — error-prone, off-catalog, no first-class DSL surface for the well-defined RFC 7396 spec. Jackson `JsonMergePatch` class (`com.fasterxml.jackson.databind.JsonMergePatch`, since Jackson 2.10) already implements RFC 7396 exactly — DSL helper is a thin wrapper w/ string-input/string-output for DSL ergonomics, no new dep. Task: `@Helper("jsonPatch")` single class w/ two methods: (1) `apply(source: String, patch: String) → String` — apply RFC 7396 merge patch: keys in `patch` that exist in `source` are replaced; keys w/ value `null` in `patch` are removed from `source`; recursive for nested objects. Uses Jackson `JsonMergePatch.fromJsonNode(patchNode).apply(sourceNode)`. (2) `diff(source: String, target: String) → String` — generate minimal RFC 7396 patch transforming `source` → `target` (Jackson `JsonMergePatch.fromDiff(sourceNode, targetNode)`). Validations (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348/T349 lesson — typed errors): empty `source` / `patch` / `target` → `IllegalArgumentException` (RFC 7396 requires object root, empty JSON is not a valid object); malformed JSON → `IllegalArgumentException` w/ Jackson parse-error hint; non-object root → `IllegalArgumentException` (RFC 7396 §1 "If the provided merge patch contains members that do not appear within the target, those members are added. If the target does contain the member, the value is replaced. Null values in the merge patch are given special meaning to indicate the removal of existing values in the target." — spec implies object root). `apply` w/ empty patch → returns `source` unchanged (no-op documented, NOT an error — sensible default). `diff` w/ identical source/target → returns `{}` (RFC 7396 §4 example, NOT an error). Decide+justify in plan: output JSON formatting (leaning compact `JsonGenerator` w/o pretty-print — DSL authors rarely need pretty-printed patches; document escape-hatch for pretty via separate `formatJson` helper). Registration per `CurrentTimestampHelper` shape; helper unit tests (`apply` simple key-replace, `apply` nested-object-replace, `apply` null-value removes key, `apply` empty patch returns source unchanged, `apply` adds new key, `apply` recursive nested w/ array leaf preserved, `diff` identical → `{}`, `diff` simple add → `{key: value}`, `diff` modify → `{key: newValue}`, `diff` delete → `{key: null}`, `diff` nested objects, `apply` empty source error, `apply` malformed JSON error, `apply` non-object root error, `apply` malformed patch error). `docs/dsl/helpers.md` (T334) entry showing: (a) optimistic-concurrency ETag pattern — `jsonPatch.diff(prevState, newState)` → store as patch, replay later; (b) partner-API delta-sync — fetch current → `jsonPatch.diff(server, local)` → upload minimal patch. Cross-link T327 Base64 (for ETag transport), T330 HmacSha256 (for patch signature). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T350-json-patch-helper.md````            | backend — code-confirmed gap: `codegraph query DateMath --kind class` returns zero `@Helper`-annotated classes. `FormatDateHelper` + `ParseDateHelper` (T328) cover format/parse but NOT arithmetic — DSL authors computing "next business day", "30 days from now", "is request timestamp before cutoff", or "truncate timestamp to start-of-day for cache key" must currently chain `formatDate(now, "yyyy-MM-dd")` → `LocalDate.parse` (impossible from DSL) → `plusDays(30)` (impossible) → `formatDate(result, "yyyy-MM-dd")` (round-trip). Every DSL with date math re-invents this string-soup. `FormatDateHelper` already imports `java.time.{LocalDate, LocalDateTime, ZonedDateTime}` — `DateMathHelper` slots into the same time-package footprint. Task: `@Helper("dateMath")` single class w/ pure-math methods (no I/O, no Temporal integration — pure value transforms); (1) `add(date: String, amount: long, unit: String = "days", zone: String = "UTC")` → ISO-8601 string — units ∈ `{millis, seconds, minutes, hours, days, weeks, months, years}` (months/years use `Period`/`plus` per unit, not millisecond-multiply — DST-safe); (2) `diff(start: String, end: String, unit: String = "millis", zone: String = "UTC")` → `long` — returns the duration between two timestamps in the requested unit; (3) `before(a: String, b: String, zone: String = "UTC")` → `boolean`; (4) `after(a: String, b: String, zone: String = "UTC")` → `boolean`; (5) `startOf(date: String, unit: String = "day", zone: String = "UTC")` → ISO-8601 string — truncate to start of day/hour/minute/month/year for cache-key derivation. `zone` arg uses IANA `ZoneId.of(...)` w/ fail-fast on invalid (T328 pattern). Date parsing: accept ISO-8601 input only (LocalDate, LocalDateTime, OffsetDateTime, ZonedDateTime — auto-detect from format). Decide+justify in plan: output format for `add`/`startOf` (leaning `Instant.toString` for millis-precision, `LocalDateTime.toString` for date-precision — depends on input). Validations (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347/T348 lesson — typed errors): empty `date` arg → error; unparseable `date` → error w/ format-hint; invalid `unit` → error listing allowed values; invalid `zone` → error; `add` w/ negative `amount` is fine (subtracts); `diff` w/ `start > end` is fine (returns negative). Registration per `CurrentTimestampHelper` shape; helper unit tests (add days across DST boundary in `America/New_York` zone, add 1 month on Jan 31 → Feb 28/29 leap-year handling, add 1 year on Feb 29 → Feb 28 (leap-year normalization, document), diff across DST, diff w/ start > end returns negative, before/after true/false, startOf day/hour/minute/month/year, all unit invalid errors, all zone invalid errors, empty date error, unparseable date error w/ format hint). `docs/dsl/helpers.md` (T334) entry showing "next-business-day" pattern + "is-order-stale" pattern paired w/ `CurrentTimestamp` + T328. Cross-link T328 FormatDate/ParseDate. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T349-date-math-helper.md````             | | backend — code-confirmed gap: `codegraph query Metric --kind class` returns ZERO `@Helper`-annotated classes (only `MetricsStage` internal pipeline stage and `PreviewMetricsCollector` preview-only collector — neither DSL-callable). `MeterRegistry` bean is already wired across `DslRunReconciliationService`, `DslRunRetentionPurger`, `TemporalDslProcessService`, `ExplainDslPipe`, etc., but DSL authors have no surface to emit CUSTOM metrics — every KPI a DSL flow cares about (e.g. "orders processed per minute", "fallback rate", "user-segment A/B outcome count") is invisible to Prometheus/OTel because there's no DSL-callable emitter. `Architecture Backend.md` Observability section explicitly lists run-path / preview-path metrics auto-instrumented by `MetricsStage`, but says nothing about user-defined DSL metrics. Task: `@Helper("metric")` single class, methods: (1) `counter(name: String, tags: Map<String,String> = {}, amount: long = 1)` → `void` — increments counter (Micrometer `Counter.builder(name).tags(tags).register(registry).increment(amount)`); (2) `gauge(name: String, value: double, tags: Map<String,String> = {})` → `void` — records gauge value (use `Gauge.builder(name, holder, doubleSupplier).tags(tags).register(registry)` w/ a `DoubleHolder` weak-reference — Micrometer gauge lifecycle gotcha, document); (3) `timer(name: String, durationMs: long, tags: Map<String,String> = {})` → `void` — records timer (`Timer.builder(name).tags(tags).register(registry).record(Duration.ofMillis(durationMs))`); (4) optional `distributionSummary(name: String, value: double, tags: Map<String,String> = {})` → `void` (decide+justify — leaning include since DSL authors may want histogram of payload sizes, response latencies, etc.). Validations (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346/T347 lesson — typed errors): empty/blank name → error; null tag keys → error; empty tag map OK (default to `{}`); negative `amount` on counter → error; negative `durationMs` on timer → error. Naming convention: enforce `<namespace>.<unit>.<verb>` shape (e.g. `orders.placed.count`, `partnerapi.fallback.rate`) via optional regex constant — decide+justify in plan (leaning soft warn not hard enforce). Registration per `CurrentTimestampHelper` shape; helper unit tests (counter w/ + w/o tags, amount > 1, gauge set+read back, timer records duration, distribution summary records value, empty name error, null tag key error, negative amount error, negative duration error, Micrometer `MeterRegistry` mock verifies `counter.increment` was called). `docs/dsl/helpers.md` (T334) entry showing DSL-author KPI pattern: emit `orders.placed.count` on successful order placement, `partnerapi.timeout.count` on HttpCallHelper timeout, `partnerapi.latency_ms` distribution summary. Cross-link `architecture-backend.md` Observability section + T277 (OTel tracing). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T348-metric-helper.md````                | backend — code-confirmed gap: `codegraph query Backoff --kind class` returns zero `@Helper`-annotated classes; `query Sleep --kind class` returns zero `@Helper` (only an unrelated `SleepingWorkflowImpl` integration-test fixture). Helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256/hex (T324/T327/T328/T329/T330/T346) — none for retry timing. DSL authors implementing custom retry flows (around partner APIs with rate limits, around flaky internal services, around idempotent-but-degraded endpoints) currently inline `Math.pow(2, attempt) * 1000 + ThreadLocalRandom.current().nextLong()` boilerplate — every retry DSL reinvents the math, gets jitter semantics wrong (no jitter → thundering herd; wrong jitter distribution → still thundering herd), and ignores AWS-style "full jitter" / "equal jitter" / "decorrelated jitter" trade-offs documented in Marc Brooker's "Exponential Backoff And Jitter" (2015). Task: `@Helper("backoff")` single class w/ pure-math `delay(...)` method returning `long` ms; signatures (decide+justify in plan, recommended default `delay(attempt, baseMillis, maxMillis, jitter)` → `long`): (1) `delay(attempt: int = 0, baseMillis: long = 1000, maxMillis: long = 60000, jitter: String = "full")` — full-jitter formula `random(0, min(maxMillis, baseMillis * 2^attempt))` (Marc Brooker "full" jitter); `jitter ∈ {"none", "full", "equal", "decorrelated"}` — `none` returns `min(maxMillis, baseMillis * 2^attempt)` (no randomness), `equal` returns `temp = min(maxMillis, baseMillis * 2^attempt); temp/2 + random(0, temp/2)` (50% deterministic + 50% jitter), `decorrelated` requires `previousDelay: long` arg (decide+justify signature, leaning optional 5th arg with sentinel `-1` meaning first attempt) → `min(max, random(base, previous * 3)` (AWS Architecture Blog formula). Validations: `attempt >= 0`, `baseMillis > 0`, `maxMillis > 0`, `baseMillis <= maxMillis`, `jitter ∈ allowed set`, `decorrelated` requires `previousDelay >= baseMillis` when supplied. All invalid → typed helper error (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344/T346 lesson — no silent fallback). Pure math, no I/O, no thread blocking — DSL author applies the result via Temporal `Timer` (workflow) or `Thread.sleep` (preview/dev). Registration per `CurrentTimestampHelper` shape, helper unit tests (`none` deterministic table for attempts 0..5, `full` returns value in `[0, temp)` band over many iterations, `equal` returns value in `[temp/2, temp)` band, `decorrelated` second-attempt returns value in `[base, prev*3)` band, `attempt = -1` error, `baseMillis = 0` error, `baseMillis > maxMillis` error, unknown `jitter` value error, `decorrelated` w/ `previousDelay < baseMillis` error, max-cap holds for large attempt counts to prevent overflow). `docs/dsl/helpers.md` (T334) entry showing retry-loop pattern using `backoff.delay(...)` paired with `HttpCallHelper` (T339) `validateJson` response check, plus a separate Temporal-`Timer`-based pattern for production runs. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T347-backoff-helper.md````                |                        | backend — code-confirmed gap: `codegraph query Hex --kind class` returns zero `@Helper`-annotated classes (HMAC mentions only because `HmacSha256SignHelper` returns hex digest). Helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256 (T324/T327/T328/T329/T330) plus 6 more queued (T331/T332/T339/T341/T342/T344) — none for plain hex encode/decode. DSL authors building crypto-adjacent flows (binary blob metadata, hex-encoded JWT header payloads, content fingerprints in human-readable form, log lines for binary protocols, hex-encoded UUIDs for older systems) currently inline `String.format("%02x", b)` boilerplate or call base64 then re-convert — every helper pair shipping without a hex companion leaves a gap. Task: `@Helper("hex")` matching `Base64Helper` shape (single class with `encode` + `decode` methods; T327 single-class decision stands); `encode(input: String) → String` returns lowercase hex of UTF-8 bytes (UTF-8 explicit — T327/T328/T330/T331/T332/T339/T341/T342/T344 lesson); `decode(input: String) → String` returns UTF-8 string of decoded bytes; lowercase-only output (matches T330 HMAC default, document the trade-off in plan); empty input → typed helper error (T327/T328/T329/T330/T331/T332/T339/T341/T342/T344 lesson — no silent empty); odd-length hex input → typed helper error (cannot decode half-byte); non-hex chars → typed helper error with position info; registration per `CurrentTimestampHelper` shape, helper unit tests (round-trip ASCII, round-trip non-ASCII UTF-8 bytes, known vectors `""`→`""`, `"abc"`→`"616263"`, `" "`→`"00"`, `"ÿ"`→`"ff"`, odd-length error, non-hex char error with position, empty input error, lowercase-only output documented). `docs/dsl/helpers.md` (T334) entry showing hex-encoded content fingerprint workflow paired with `sha256` (T332) and `hmacSha256` (T330). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T346-hex-helper.md````                   | | dx (Makefile + SQL/JSON) — code-confirmed gap: `grep -E 'seed' Makefile` returns zero matches. New developers following CLAUDE.md's `make up` instructions land on an empty dashboard ("0 runs", empty executions list) with no way to see what a healthy system looks like end-to-end. `scripts/` has only `dev.sh` + `merge-t291.sh` — no first-run onboarding helper. Mirrors T337 (`make doctor` checks health) and T336 (runbook incident triage). Task: add `make seed` target that runs AFTER `make up` and: (1) loads one canonical "hello-world" DSL definition via the BFF POST endpoint (`/api/v1/dsl/draft` then publish) — choose a non-trivial example that exercises 2-3 helpers (e.g., `uuidV7` + `arithmetic` + `formatMessage`); (2) triggers one sample run via `POST /api/v1/dsl/runs` so the dashboard immediately shows ≥1 successful execution; (3) optionally inserts 3-5 historical runs across varying statuses (Completed, Failed, Running, Cancelled) for richer UI testing — make optional via `make seed-history`. The seeds should be idempotent — if a definition named `seed-hello-world` already exists, skip silently. Implementation: pure Makefile + bash + `curl` calls; no new deps. Reuse existing `BACKEND_BASE_URL` / `BFF_BASE_URL` env conventions. Cross-link from `CLAUDE.md`'s "Quick end-to-end check" section to add `make seed` after step 5. `make help` lists new target. Tests: a bats-test or smoke-script that runs `make up && make seed && curl /api/v1/dsl/definitions | jq 'length >= 1'` and asserts the seed landed; document in plan, defer the bats test to follow-up task. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T345-make-seed.md````                    |
| T357 | Done | DSL `@Helper` — `SemverHelper` (parse/compare/bump/format) | backend — code-confirmed gap: `codegraph query Semver --kind class` returns zero `@Helper`-annotated classes; `query Version --kind class` returns only `VersionProbeModels` (DSL example fixture) and `DslVersioningIntegrationTest` (unrelated, tests DSL-process versioning not SemVer 2.0 parsing). Helper registry covers HTTP/JSON/sort/filter/arithmetic/format-message/current-timestamp/uuid-v7/base64/format-date+parse-date/regex/hmac-sha256/hex/backoff/metric/date-math/json-patch/list-ops/math/http-auth/query-string/random/compression (T324/T327-T356) — none for SemVer 2.0 spec (semver.org). DSL authors building partner-API version negotiation (Stripe `2024-06-20` API version pin, GitHub `X-GitHub-Api-Version` header, Kubernetes chart `appVersion` checks, npm package compatibility checks, Maven artifact promotion) currently inline regex `^(\d+)\.(\d+)\.(\d+)(?:-([\w.-]+))?(?:\+([\w.-]+))?$` parsing + manual component arithmetic — every DSL with version semantics re-implements the parser, gets precedence rules wrong (SemVer §2: MAJOR.MINOR.PATCH + optional pre-release/build), mishandles pre-release ordering (SemVer §11: pre-release < release, numeric < alpha, shorter wins on tie), and misses build-metadata ignore for precedence (SemVer §10). Task: `@Helper("semver")` single class matching `Base64Helper` / `HexHelper` shape. Methods: (1) `parse(version: String) → Map<String, Object>` — returns `{major: int, minor: int, patch: int, preRelease: String?, build: String?}` (DSL-friendly map so authors can `extractJson`/`pluck` per field; raw object format TBD — decide+justify in plan between `Map<String,Object>` vs flat struct); (2) `compare(a: String, b: String) → int` — standard semver comparison: returns `-1` if a < b, `0` if equal, `1` if a > b; build-metadata IGNORED per SemVer §10; (3) `satisfies(version: String, range: String) → boolean` — npm-style semver range matcher (subset: `^1.2.3`, `~1.2.3`, `>=1.2.3 <2.0.0`, `1.x`, `1.2.x`, exact `1.2.3`); (4) `bump(version: String, type: String) → String` — `type ∈ {"major","minor","patch","preRelease"}` returns next version (`bump("1.2.3","major")` → `"2.0.0"`, `bump("1.2.3","minor")` → `"1.3.0"`, `bump("1.2.3","patch")` → `"1.2.4"`, `bump("1.2.3","preRelease")` → `"1.2.4-0"`; pre-existing pre-release → bump suffix; (5) `format(major: int, minor: int, patch: int, preRelease: String = "", build: String = "") → String` — compose from parts (escape-hatch when DSL already has numeric components). Validations (T327-T356 lesson — typed errors): null/empty `version` → `IllegalArgumentException`; malformed `version` regex fail → typed error w/ position; invalid `type` arg → error listing allowed values; invalid `range` expression → error. Pick impl in plan: (a) zero-dep pure-JDK regex parser (~150 LOC, no precedence-table edge cases, pre-release sort falls back to lex-compare — accept SemVer §11 subset: numeric < alpha, shorter prefix wins, document the limitation vs full SemVer §11), (b) `com.vdurmont:semver4j` (~70KB, mature, full §11 precedence table, supports range syntax natively — RECOMMENDED if zero-dep rule not strict), (c) `org.semver:parser` (legacy, abandoned). Recommend (b) for full spec coverage. Helper unit tests (`parse` `1.2.3` → `{1,2,3,null,null}`, `parse` `1.2.3-rc.1` → `{1,2,3,"rc.1",null}`, `parse` `1.2.3+build.5` → `{1,2,3,null,"build.5"}`, `parse` `1.2.3-rc.1+build.5` → all 4 fields set, `parse` malformed `1.2` → error, `parse` `v1.2.3` w/ leading `v` → decide handling; `compare` SemVer §11 table: `1.0.0-alpha < 1.0.0-alpha.1 < 1.0.0-alpha.beta < 1.0.0-beta < 1.0.0-beta.2 < 1.0.0-beta.11 < 1.0.0-rc.1 < 1.0.0`, build metadata IGNORED (`1.0.0+foo == 1.0.0+bar`), `compare` w/ identical → 0; `satisfies` `^1.2.3` allows `1.2.3`..`<2.0.0`, `~1.2.3` allows `1.2.3`..`<1.3.0`, `>=1.0.0` truthy, `1.x` matches `1.0.0`+`1.99.0`, `1.2.3` exact match, `satisfies` invalid range → error; `bump` major/minor/patch/preRelease tables, `bump` w/ pre-existing pre-release bumps suffix digit (`1.2.3-rc.1` + `patch` → `1.2.4`, `1.2.3-rc.1` + `preRelease` → `1.2.3-rc.2` — decide+justify), `bump` invalid `type` → error; `format` simple, format w/ preRelease + build, format w/ empty preRelease+build → just `1.2.3`). `docs/dsl/helpers.md` (T334) entry showing: (a) "partner-API version pin check" — `if (semver.compare(partnerApiVersion, "2024-06-20") < 0) throw error(...)`; (b) "Kubernetes chart appVersion guard" — `if (!semver.satisfies(k8sVersion, ">=1.25.0 <1.30.0")) skip`; (c) "artifact promotion bump" — `set version = semver.bump(currentVersion, "minor")`. Cross-link T327 Base64 (if decision to embed semver in opaque API token), T330 HmacSha256 (for signed version manifests), T356 Random (for pre-release RC suffix generation). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T357-semver-helper.md`                   |
| T358 | Done | DSL `@Helper` — `JwtHelper` (parse + verify + sign HS256) | backend — code-confirmed gap: `codegraph query Jwt --kind class` returns ONLY `SecurityConfiguration` (BE-side Keycloak/OIDC filter, NOT DSL-callable) and `GuardEnabledTestApp`; `query JsonWebToken --kind class` returns zero. Helper registry covers identity/encoding/hashing/HMAC/format-message (T324/T327/T330/T331/T332), HTTP/JSON/auth-header/cron-extract operations (T339/T341/T342/T350/T353/T354), list/math/random/compression (T351/T352/T355/T356/T357) — none for JWT (RFC 7519) parse or verify. DSL authors integrating with JWT-issuing partner APIs (Auth0/Firebase/Okta token verification, Stripe-Connect account-id extraction from bearer tokens, GitHub App installation token inspection, AWS Cognito user-pools claim extraction) currently inline base64url-decode (T327) + JSON-parse (JsonExtractHelper) + HMAC-verify (T330) boilerplate — every DSL with JWT semantics re-implements the 3-segment split (header.payload.signature), gets the base64url padding rules wrong (RFC 7515 §2 — base64url NO padding, but T327 urlSafe=true preserves padding — DSL author must remember to strip), mis-handles `alg: none` attack (CVE-2015-9235 class — explicit deny non-HMAC algs), and gets claim type coercion wrong (`exp` is NumericDate per RFC 7519 §2 — UNIX seconds, NOT millis). T330 HmacSha256Helper provides `sign`/`verify` primitives but NO JWT structure awareness; T353 HttpAuthHelper builds auth-header maps but does NOT decode the token. Task: `@Helper("jwt")` single class w/ 4 methods: (1) `parse(token: String) → Map<String, Object>` — returns full claims map (`{header: {...}, payload: {...}, signature: "..."}`) without verification (NEVER use for trust decisions — document loudly); (2) `verify(token: String, secret: String, algorithm: String = "HS256") → Map<String, Object>` — verifies signature + returns claims map; throws typed helper error on bad signature / expired / not-yet-valid / `alg: none` (CVE-2015-9235); supported `algorithm ∈ {"HS256", "HS384", "HS512"}` (HMAC family only — RS256/ES256 require asymmetric key handling, defer to follow-up T359-or-similar); (3) `sign(payload: Map<String, Object>, secret: String, algorithm: String = "HS256", ttlSeconds: long = 3600) → String` — builds JWT w/ `iat=now`, `exp=now+ttl`, optional `iss`/`sub`/`aud` from payload, returns compact serialization `header.payload.signature`; (4) `claim(token: String, name: String) → Object` — convenience: `parse(token).payload[name]` w/ typed error on missing claim. Validations (T327-T357 lesson — typed errors): null/empty `token` → error; non-3-segment token (no dots / too many dots) → error; malformed base64url in any segment → error; missing required claim `exp` when `verify` called → error (strict mode); `exp` past → error w/ expired-since delta; `nbf` future → error; `alg: none` → error (security baseline — explicit deny); `verify` w/ unknown `algorithm` → error listing allowed set; `sign` w/ missing `secret` → error; `sign` w/ negative `ttlSeconds` → error (use 0 for non-expiring — document). Implementation: pure-JDK + Jackson; reuse `HmacSha256Helper` (T330) internally for HS256 sign/verify (DRY), use `MessageDigest.getInstance("HmacSHA256")` for HS384/HS512 (`HmacSHA384`, `HmacSHA512` — JDK stdlib, no dep). base64url decode via `java.util.Base64.getUrlDecoder()` (NOT T327 urlSafe=true — JDK is faster, no bean lookup); strip padding manually per RFC 7515 §2 (`s + "=".repeat((4 - s.length() % 4) % 4)`). Helper unit tests (`parse` known-vector `eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxMjMifQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c` → correct split, `parse` malformed 2-segment token → error, `parse` non-base64url chars → error, `verify` known-vector HS256 signed fixture → returns claims + no error, `verify` tampered payload (flip one char) → error, `verify` expired token (`exp` 1 hour past) → error w/ delta, `verify` future `nbf` (5 min ahead) → error, `verify` `alg: none` → error (CVE protection), `verify` w/ wrong secret → error, `verify` w/ HS512-signed token but algorithm arg HS256 → error (alg-confusion attack prevention — verify the alg in header matches the arg), `sign` round-trip — `sign(payload, secret)` then `verify(token, secret)` returns same payload, `sign` custom `ttlSeconds = 60` → `exp` exactly 60s after `iat`, `sign` missing secret error, `sign` negative ttl error, `claim` extract `sub` from known fixture, `claim` missing claim → error). `docs/dsl/helpers.md` (T334) entry showing: (a) "Auth0 token verification" — `jwt.verify(token, jwtSecret)` → `claim(token, "email")` → log/forward; (b) "Stripe Connect account_id extraction" — `jwt.verify(stripeToken, secret)` → `jwt.claim(token, "account_id")` → use in downstream `httpCall`; (c) "internal service-to-service token mint" — `jwt.sign({sub: svcName, scope: "internal.write"}, svcSecret, ttlSeconds=300)` → `httpCall(headers=httpAuth.bearer(token))`. Cross-link T330 HmacSha256 (used internally for HS256), T353 HttpAuthHelper (consumes signed tokens), T327 Base64 (base64url codec), JsonExtractHelper (claim extraction), T348 MetricHelper (token-verify failure counter pattern). Security note in plan + helper Javadoc: `parse` is DECODE-ONLY and MUST NOT be used for trust decisions; only `verify` is safe. No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T358-jwt-helper.md`                      |
| T359 | Backlog | `make loadtest` — operator smoke load-test target | dx (Makefile + bash) — code-confirmed gap: `codegraph query LoadTest --kind class` returns zero; `grep -E 'loadtest|load-test|hey |k6 |wrk ' Makefile scripts/*.sh` returns zero matches. Existing operator DX covers `make doctor` (T337 — health checks) + `make seed` (T345 — first-run data) but the operator's third question — *"can this thing handle real traffic?"* — has no first-class answer. Every deploy currently relies on subjective "looks fine" gut-feel: a fresh `make up && make seed` shows healthy /actuator/health and a few definitions, but zero signal on latency under load, throughput ceiling, error rate under concurrency, or hot-endpoint identification. Operators discovering p99 latency regressions or N+1 query pathologies in production do so via Sentry/OTel AFTER user impact — no pre-deploy smoke that surfaces the regression at PR time. T277 OTel tracing + T348 MetricHelper emit production metrics but offer no offline dry-run. Task: add `make loadtest` target to Makefile. Behavior: (1) accept `DURATION` (default 30s), `CONCURRENCY` (default 10), `RPS` (default 50) env vars for tunable load; (2) hit the top-N `/api/v1/dsl/*` BFF endpoints via parallel curl w/ rate limiting — at minimum: GET `/api/v1/dsl/definitions`, GET `/api/v1/dsl/helpers`, GET `/api/v1/dsl/executions?limit=20`, GET `/api/v1/dsl/drafts`; (3) report per-endpoint latency stats (p50/p95/p99), error rate, total requests, RPS achieved; (4) fail (non-zero exit) if any endpoint p99 exceeds `MAX_P99_MS` (default 2000ms) OR error rate exceeds `MAX_ERROR_RATE` (default 5%) — operators can tighten via env var; (5) reuse existing `BACKEND_BASE_URL` / `BFF_BASE_URL` env conventions; (6) skip gracefully (exit 0 + warn) if `make up` services not running. Pick impl in plan: (a) pure bash + curl + `xargs -P` parallel + `date +%s%N` timing (~80 LOC Makefile target, zero dep — RECOMMENDED for DX consistency w/ `doctor` + `seed`, fits cbs-nova's bash-first tradition), (b) k6 script (`scripts/loadtest.js` + Makefile wraps `k6 run` — full-featured but requires `npm install k6` ~50MB, breaking bash-first DX), (c) hey binary (Go single-binary ~5MB, simple HTTP load — leaner than k6 but adds Go-binary dep, harder to customize per-endpoint). Recommend (a). Endpoints targeted: read-only GETs (no state mutation; the load test must not corrupt dev data); if any non-GET endpoints needed for coverage, gate behind explicit `ALLOW_MUTATIONS=1` env var. Output format: ASCII table — per-endpoint `endpoint | rps | p50 | p95 | p99 | err% | total`, sorted by p99 descending. Tests: bats test `test/loadtest.bats` that runs `make loadtest` against a stub server (`python -m http.server` or built-in test endpoint) and asserts non-zero exit on a slow stub + zero exit on a fast stub — defer test to follow-up task; document the bats stub in plan, defer impl. Cross-link T337 (doctor prerequisites), T345 (seed prerequisite so there's data to load-test against), T277 (OTel tracing — load-test should run w/ OTel on to compare traces), T326 (execution timeline — load-test should exercise executions endpoint), T348 (MetricHelper — operator can compose `loadtest → metric.timer` for cohort comparison). Pure Makefile + bash, no new deps, no new helper, no FE change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T359-make-loadtest.md`                   |
| T360 | Backlog | DSL `@Helper` — `CronHelper` (parse + next-fire-time + description + match) | backend — code-confirmed gap: `codegraph query Cron --kind class` returns zero; `query CronExpression --kind class` returns zero; `query Schedule --kind class` returns only `ScheduleModels`/`ScheduleConflictException`/`DslScheduleRouterConfiguration` (DSL internal schedule persistence, NOT a cron-expression parser/exposer). `grep -E 'cron-utils|CronExpression' backend/dsl-platform/*/build.gradle*` returns zero — no cron parser lib on classpath. Helper registry covers identity/encoding/hashing/HMAC/JWT/format-message (T324/T327/T328/T329/T330/T331/T332/T346/T357/T358), HTTP/JSON/auth-header operations (T339/T341/T342/T350/T353/T354), list/math/random/compression (T351/T352/T355/T356), observability/time/retry (T347/T348/T349), UUID (T324) — none for cron expression parse/validate/next-fire-time computation. DSL authors building cron-driven flows (scheduled report generation, periodic partner-API polling, deferred cleanup jobs, retry-with-cron-backoff patterns, SLA-deadline scheduling) currently inline string-split `split.split(" \s+")` + manual field-validation + arithmetic to find next firing — every DSL with cron semantics re-implements 5-field vs 6-field vs 7-field detection, mishandles "?" wildcard semantics (RFC 5545 §3.3.10: "?" only valid in day-of-month AND day-of-week fields, mutually exclusive), gets "L" (last-day) / "W" (weekday) / "#" (nth-weekday) extensions wrong, and computes "next-fire after X" by walking minute-by-minute (terrible for "0 0 12 * * ?" — 12 hours × 60 = 720 iterations to find next noon). `TemporalDslProcessService` has its own cron integration but that's INTERNAL — DSL authors have no surface to introspect a partner-supplied cron or compute next-fire windows. Task: `@Helper("cron")` single class w/ 4 methods: (1) `parse(expression: String) → Map<String, Object>` — returns parsed fields `{minutes, hours, dayOfMonth, month, dayOfWeek, year?, raw}` + flag `{isValid, errorMessage, fieldCount}`; supports 5-field ("./gradle"-style: "* * * * *"), 6-field (Quartz "sec min hr dom mon dow"), and 7-field (Unix "sec min hr dom mon dow year") cron dialects — auto-detect by field count; rejects malformed expressions w/ typed helper error. (2) `nextFireTime(expression: String, fromEpochMillis: long = 0) → long` — returns epoch-millis of next firing after `fromEpochMillis` (default = now via `runtime.now()`); use `com.cronutils:cron-utils` library (mature, supports all 3 dialects, ~150KB) — RECOMMENDED, OR pure-JDK `java.time.cron.CronExpression` (JDK 9+ but doesn't support 6/7-field Quartz dialects or "?" wildcard — only adequate for 5-field Unix cron); (3) `nextFireTimes(expression: String, count: int = 5, fromEpochMillis: long = 0) → List<Long>` — returns next N firing epoch-millis (calls `nextFireTime` iteratively, stopping early if count exhausted within reasonable bounds); (4) `describe(expression: String) → String` — human-readable "every 5 minutes", "at midnight on Sundays", "on the 1st and 15th of each month" — partner-API debug aid; use `cron-utils` description API; (5) optional `isValid(expression: String) → boolean` — non-throwing variant of parse for conditional DSL flows. Validations (T327-T358 lesson — typed errors): null/empty `expression` → error; malformed (wrong field count for detected dialect) → error w/ detected dialect + expected; `nextFireTime` on unparseable expr → error (don't return -1 sentinel — explicit error per T327/T328 lesson); `nextFireTimes` w/ negative `count` → error; `count > 10000` → error (DoS cap, matches T356 string-length cap pattern); `describe` on unparseable expr → error. Pick impl in plan: (a) `com.cronutils:cron-utils` ~150KB, full RFC 5545 + Quartz + Unix dialect support, mature since 2014 — RECOMMENDED; (b) JDK `java.time.cron.CronExpression` — zero new dep but 5-field-Unix-only, no "?" wildcard, no description; (c) pure-JDK manual parser — ~400 LOC, brittle on edge cases (DST, leap-year, "#" extension). Recommend (a). Helper unit tests (`parse` 5-field `"* * * * *"` → valid, `parse` 6-field Quartz `"0 0 12 * * ?"` → valid, `parse` 7-field Unix `"0 0 12 * * ? 2026"` → valid, `parse` malformed 4-field → error w/ dialect hint, `parse` "?" in wrong field → error, `parse` "L" / "W" / "#" extensions parse correctly, `nextFireTime` "0 0 12 * *" from 2026-01-01T00:00:00Z → 2026-01-01T12:00:00Z epoch-millis, `nextFireTime` "0 0 * * 1" (Mondays) from 2026-01-04T10:00:00Z (Sunday) → 2026-01-05T00:00:00Z (next Monday midnight), `nextFireTime` "* * * * 1" w/ "from" already on Monday 10:30 → Monday 10:31 (next minute), `nextFireTime` "0 0 29 2 *" (Feb 29 only) from 2026-01-01 → 2028-02-29T00:00:00Z (skip non-leap years), `nextFireTime` unparseable → error, `nextFireTimes" "* * * * *" count=5 → 5 timestamps 1 minute apart, `nextFireTimes" count=-1 → error, `nextFireTimes" count=10001 → error, `describe" "0 0 * * *" → "every hour" (or human-friendly equivalent), `describe" "0 9 * * 1-5" → "at 09:00 AM, Monday through Friday", `isValid" valid → true, `isValid" malformed → false w/o throw). `docs/dsl/helpers.md` (T334) entry showing: (a) "next scheduled report run" — `cron.nextFireTime("0 9 * * 1", now)\` → format w/ T328 → log; (b) "is-deferred-job-overdue" — `now > cron.nextFireTime(schedule, prevRunTime + 60000)\`; (c) "describe partner cron for UI" — `cron.describe(partnerSchedule)\` → return in API response. Cross-link T328 FormatDate/ParseDate (epoch-millis conversion), T349 DateMathHelper (date arithmetic for "next fire within 24h"), T347 BackoffHelper (retry timing), T348 MetricHelper (cron-fire counter). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T360-cron-helper.md`                    |
| T361 | Backlog | DSL `@Helper` — `OpenTelemetryHelper` (span emit + baggage + status + context inject/extract) | backend — code-confirmed gap: `codegraph query OpenTelemetry --kind class` returns only `OpenTelemetryContextPropagator` (BE-internal context propagator) + `TracingConfiguration` (Spring config wiring); `query Span --kind class` returns ONLY those two internal classes — zero DSL-callable span-emission surface. `grep -rE 'opentelemetry|otel-' backend/dsl-platform/` returns zero matches in build.gradle files; the OTel API is on classpath transitively via Spring Boot 3.x `micrometer-tracing-bridge-otel` (T277 infra layer), but no direct DSL-facing surface exists. Helper registry covers identity/encoding/hashing/HMAC/JWT/format-message (T324/T327/T328/T329/T330/T331/T332/T346/T357/T358), HTTP/JSON/auth-header operations (T339/T341/T342/T350/T353/T354), list/math/random/compression (T351/T352/T355/T356), observability/time/retry (T347/T348/T349), UUID/scheduling (T324/T360) — T348 covers metrics, but traces are absent. DSL authors building observability-critical flows (debugging partner-API latency outliers, marking business-logic boundaries for trace analysis, propagating W3C trace-context headers across multiple `HttpCallHelper` calls in a single workflow, attaching customer-id / order-id baggage for cross-service correlation) currently inline `GlobalOpenTelemetry.get()` + `Tracer.spanBuilder(...).startSpan()` + `try-with-resources` boilerplate via custom BE Java activities — high friction, off-catalog, no first-class DSL surface. T277 added OTel infra but exposed zero DSL ops. Task: `@Helper("otel")` single class w/ 6 methods: (1) `span(name: String, attributes: Map<String,String> = {}) → String` — starts a new span w/ given name + attributes, returns span-context-id in W3C traceparent form (`<version>-<traceId>-<spanId>-<flags>` per W3C Trace Context §3.2); DOES NOT auto-close — caller wraps subsequent work and passes result to `endSpan`; supports nested spans (uses current OTel context as parent). (2) `endSpan(spanId: String, statusCode: String = "OK", errorMessage: String = "") → void` — closes the span, sets status (statusCode ∈ {"OK","ERROR","UNSET"} per OTel StatusCode enum), attaches optional error event w/ message if ERROR. (3) `addEvent(spanId: String, name: String, attributes: Map<String,String> = {}) → void` — adds event to existing span (OTel `Span.addEvent`, used for tracing checkpoints like "DB query started" / "response received"). (4) `setBaggage(key: String, value: String) → void` — sets W3C baggage entry (OTel `Baggage.current().toBuilder().put(key, value).build().makeCurrent()`, propagates across spans + remote services via standard propagators). (5) `getBaggage(key: String) → String` — reads baggage value (returns empty string if unset — T327/T328 lesson: NEVER return null from DSL surface). (6) `injectContext(headers: Map<String,String>) → Map<String,String>` — injects current trace-context + baggage into HTTP header map (returns input map + `traceparent` + `tracestate` + `baggage` entries), direct plug-in to `HttpCallHelper.headers`. (7) `extractContext(headers: Map<String,String>) → String` — extracts trace-context from inbound headers (returns span-id of the extracted remote span, or empty string if no traceparent header — DSL author passes this to subsequent `span(...)` calls as the parent). Validations (T327-T360 lesson — typed errors): null/empty `spanId` → error; invalid spanId format (must match W3C traceparent) → error; `statusCode` not in allowed set → error; `setBaggage` empty key → error; nested `span` outside of any workflow context (no current OTel context) → error with hint; `endSpan` on already-ended span → error (idempotent close is dangerous — silent bugs). Pick impl in plan: (a) direct OTel API `io.opentelemetry:opentelemetry-api` ~600KB, official API — RECOMMENDED if not transitively present, (b) re-use the existing `OpenTelemetryContextPropagator` class (T277 infra) via Spring bean injection — leaner, consistent w/ internal usage; (c) Micrometer `io.micrometer:micrometer-tracing` + `Observation` API ~300KB — alternative abstraction but adds a new mental model. Recommend (b) if the existing propagator exposes a `Tracer` bean; recommend (a) if not. Helper unit tests (`span` basic span created + returns W3C traceparent, `span` w/ attributes attached, nested `span` child parented to current, `span` outside context → error, `endSpan` happy path sets status OK, `endSpan` w/ ERROR statusCode + errorMessage attaches error event, `endSpan` statusCode not in allowed → error, `endSpan` already-ended span → error, `addEvent` adds event w/ attributes, `setBaggage` then `getBaggage` round-trip, `getBaggage` unset key → empty string (not null), `setBaggage` empty key → error, `injectContext` adds traceparent + baggage headers to input map, `injectContext` preserves existing headers (immutable copy), `extractContext` w/ valid traceparent header returns span-id, `extractContext` w/o traceparent → empty string, `endSpan` invalid spanId format → error). `docs/dsl/helpers.md` (T334) entry showing: (a) "business-logic span boundary" — `spanId = otel.span("processOrder", {customerId: id})` → "process payment" → "update inventory" → `otel.endSpan(spanId)`; (b) "cross-service trace propagation" — `headers = otel.injectContext({})` → `httpCall(url=..., headers=headers)`; (c) "customer-id baggage for log correlation" — `otel.setBaggage("customerId", id)` → all subsequent spans auto-attach baggage → log forwarder includes in every line. Cross-link T348 MetricHelper (paired span + metric pattern), T353 HttpAuthHelper (headers in), HttpCallHelper (headers input), T277 (infra layer), T326 (execution timeline — span emits add visual segments). No FE work — T311 catalog surfaces it. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T361-otel-helper.md`                    |
| T362 | Backlog | DSL workbench autocomplete from `T311` helper catalog (Monaco completion provider) | frontend — code-confirmed gap: `grep -rE 'registerCompletionItemProvider\|provideCompletionItems' frontend --include='*.vue' --include='*.ts' --exclude-dir=node_modules` returns ZERO matches; `grep MonacoEditor.vue` shows Monaco editor mounted (`monaco.editor.create(container, {...})`) in `frontend/components/src/components/dsl/MonacoEditor.vue` with NO completion provider registered. Monaco IS the editor — used in `MonacoEditor.vue`, `CodeTab.vue`, `BodyEditor.spec.ts`; but no provider pulls from the T311 helper catalog. Result: DSL authors typing `@helper.uuidV7.something` get zero in-editor help — they must alt-tab to the helper catalog browser page or read `docs/dsl/helpers.md` (T334) to remember the helper signature, return to the editor, type from memory, repeat for each call. Onboarding cost is HIGH: 22+ helpers × 5-10 methods each = ~150 signatures to memorize before productivity. Recent memory shows workbench focus (Saved Drafts widget, drawer, store refactor); autocomplete is the natural next FE feature and the obvious ROI from T311's catalog backend. Task: add Monaco completion provider bound to the T311 helper catalog. Implementation: (1) new composable `frontend/components/src/composables/useHelperCompletion.ts` — fetches `GET /api/v1/dsl/helpers` ONCE on first use, caches in-memory + invalidates on helper-catalog version change (header `ETag` or `Last-Modified`); (2) new composable `frontend/components/src/composables/useMonacoHelperCompletion.ts` — wraps Monaco's `monaco.languages.registerCompletionItemProvider('java', { provideCompletionItems(model, position) { ... } })`, returns `CompletionItem[]` matching the trigger context; (3) trigger pattern: when the token at `position` matches `@<word>` or `@<word>.`, show completion list filtered to helper names starting w/ `<word>`; when `@<word>.` (after dot), show completion list of helper methods w/ full signature in `detail` + 1-line description in `documentation`; (4) CompletionItem shape: `{ label: 'uuidV7.generate', kind: MonacoCompletionItemKind.Method, insertText: 'uuidV7.generate()', detail: 'UUID v7 (RFC 9562)', documentation: 'Generate a new UUID v7 with millisecond timestamp + random tail. Used for time-ordered IDs.' }`; snippet support for methods w/ args via `insertTextRules: InsertTextRule.InsertAsSnippet`; (5) wire into `MonacoEditor.vue` via the two composables (or extract the editor-injection side-effect into a separate `installMonacoCompletions(monaco, helpers)` helper that the editor calls in `onMounted`); (6) dedupe — only one global registration across multiple editor instances; use module-level `let installed = false` guard. Tests: (a) vitest unit test for `useHelperCompletion` — fetch mocked, cache hit/miss, ETag-driven re-fetch; (b) vitest unit test for the completion provider logic — given a buffer + position, returns expected items for trigger patterns; (c) extend `frontend/components/src/components/dsl/__tests__/MonacoEditor.spec.ts` — assert provider registered post-mount; (d) manual smoke: load `/dsl-workbench`, type `@uuid`, expect popup w/ `uuidV7.generate`. Empty/error states: catalog fetch fails → log warn + register provider returning `[]` (graceful degrade — autocomplete silent rather than blocking editor); catalog empty → same. No BE work needed — T311 catalog already exposes the data via `GET /api/v1/dsl/helpers` (verify shape includes name + description + per-method signature; if not, file a follow-up task to enrich the response). Cross-link T311 (helper catalog backend), T334 (helpers cookbook — autocomplete snippets mirror the cookbook patterns), T325 (workbench dirty-state UI — completion-triggered edits should NOT mark dirty unless value actually changes; verify Monaco's `onDidChangeModelContent` semantics). Tier `frontend`. Pure FE work; no BE schema change; no new dep (Monaco already on `monaco-editor@0.56.0` via node_modules). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T362-workbench-autocomplete.md`         |
| T363 | Backlog | `DslFileBuffer` — bound staged-content buffer via Caffeine (fix unbounded map) | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/service/DslFileBuffer.java:12` reads `// TODO: redo to caffeine, it can leaad to a memory leak now`; the backing store at line 13 is a bare `ConcurrentHashMap<String, String>` with NO size bound and NO eviction — every `stage(relativePath, content)` inserts file content (DSL definitions can be KBs each) and entries leave ONLY via `drain()`. If a client stages files but the flush/commit path never runs (crash between stage and drain, abandoned draft flow, bulkhead rejection mid-sequence), entries accumulate for the lifetime of the Spring context = unbounded heap growth on a long-running server. Same unbounded-map pattern TODO exists in `dsl-idea-plugin/.../sync/DslProjectResolverExtension.java:19` (separate module — out of scope here, follow-up if wanted). Caffeine is ALREADY a dependency of the starter module (`backend/dsl-starter/starter/build.gradle:75` → `implementation libs.caffeine`; versioned in `backend/gradle/libs.versions.toml:119`) — zero new deps. Task: replace the raw `ConcurrentHashMap` with `Caffeine.newBuilder().maximumSize(<bound>).expireAfterWrite(<ttl>).build()`, bounds driven by a new `DslProperties` nested field (e.g. `dsl.file-buffer.max-entries`, `dsl.file-buffer.expire-after-write`) with sane defaults (e.g. 1024 entries / 5m) — this also resolves the adjacent smell that buffer policy is currently hardcoded. Keep the public API (`stage`, `get`, `drain`, `pendingCount`) and its semantics EXACTLY: `drain()` must remain atomic per-entry (Caffeine `asMap()` iteration + `remove(key, value)` two-arg form preserves the current compare-and-remove race behavior); `normalize()` path-traversal guard untouched. Tests: extend the existing `DslFileServiceTest`/buffer tests with (1) eviction — stage `max-entries + N` distinct paths, assert `pendingCount() <= max-entries` and that the OLDEST staged entries were evicted (FIFO/size-based, assert insertion-order eviction semantics documented); (2) TTL expiry — use Caffeine's `Ticker`/`Expiry` injection or a fake ticker, assert expired entries vanish from `get`/`drain` without explicit removal; (3) `drain()` still returns each staged entry exactly once under concurrent stage/drain (existing race test if present, else add one). Tier `backend`. Low risk: single class + properties + tests; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T363-dslfilebuffer-caffeine.md`       |
| T364 | Backlog | `ApiKeyAuthFilter` — opt-in gating via `dsl.auth.enabled` (`@ConditionalOnProperty`) | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/ApiKeyAuthFilterConfiguration.java:12` reads `//TODO: add \`@ConditionalOnProperty\`, by default filter must be turned off`. Current state: `apiKeyAuthFilter` bean is `@ConditionalOnMissingBean` only — it is ALWAYS created and the `FilterRegistrationBean` ALWAYS registers it on `/api/*` at `HIGHEST_PRECEDENCE + 1`, even when no API key is configured. The filter itself short-circuits to pass-through when `configuredApiKey` is null/blank (`ApiKeyAuthFilter.java:35-38`), so behavior is safe today, but: (1) every `/api/*` request pays a servlet-filter hop + null-check for a feature that is off; (2) semantics are implicit — auth is "on" only by side effect of a property being non-blank, invisible in actuator `/beans` planning and startup logs; (3) `DslProperties.Auth` (`DslProperties.java:40-44`) has only `apiKey` — no explicit enable flag, unlike sibling `Worker` section which already has `enabled` (`DslProperties.java:27`), so the config surface is inconsistent. Task: (1) add `private boolean enabled;` to `DslProperties.Auth` (default `false`, matching the TODO's "by default filter must be turned off"; `@Data` generates the getter); (2) annotate BOTH beans in `ApiKeyAuthFilterConfiguration` with `@ConditionalOnProperty(prefix = "dsl.auth", name = "enabled", havingValue = "true")` — the registration bean too, else it fails on a missing filter bean; alternatively single `@ConditionalOnProperty` on the `@Configuration` class (cleaner — one annotation, both beans gated); (3) MIGRATION NOTE in plan + release notes: existing deployments that set `dsl.auth.api-key` and rely on auth MUST add `dsl.auth.enabled=true` or they silently lose auth — evaluate safer default during implementation (`enabled` defaulting to true when `apiKey` is present is expressible via a custom `Condition` or `AnyNestedCondition`; the loop defers this choice to the executor + human review, but the TODO's stated intent is default-off); (4) drop the stale TODO comment. Tests: (a) context test — `dsl.auth.enabled=false` (unset) + no key → no `ApiKeyAuthFilter` bean, no `FilterRegistrationBean`; (b) `enabled=true` + key set → both beans present; (c) `enabled=true` + blank key → filter bean present, requests pass through (existing pass-through test kept green); (d) existing auth integration tests updated to set `enabled=true` in their test properties. Update `docs/architecture-backend.md` security section if it documents the api-key flow. Tier `backend`. Low risk if default chosen consciously; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T364-apikey-filter-optin.md`         |
| T365 | Backlog | Codegen provenance — `DslGenerated.dslBuildInfo/dslGitInfo` + platform version in `GeneratorMetadata` | backend — code-confirmed TODOs (one logical group): `backend/dsl-platform/dsl-api/src/main/java/cbs/nova/dsl/annotation/DslGenerated.java:20` reads `// TODO: add here two new fields: \`dslBuildInfo\`, \`dslGitInfo\`, so we can know with what version of cbs codegen class was created`; `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/generator/GeneratorMetadata.java:22` reads `// TODO: we also need to add version of dsl platform itself, for later debug`. Current state: `GeneratorMetadata.annotation()` emits `@DslGenerated(generator, timestamp, javaVersion, user)` — generator class name + JVM facts only. Gap: generated classes carry NO trace of (a) which dsl-platform version compiled them, (b) which git commit of the DSL source produced them. When generated workflow code misbehaves in a deployed worker, there is no in-class fingerprint to diff against the repo — support must reconstruct provenance from timestamps and guesswork. Task: (1) add `dslBuildInfo()` and `dslGitInfo()` String members to `@DslGenerated` (SOURCE retention — zero runtime footprint); (2) resolve platform version in `GeneratorMetadata` — prefer `Implementation-Version` from the codegen jar manifest (`getClass().getPackage().getImplementationVersion()`), fallback "unknown" when manifest stripped (gradle `jar` task must enable `implVersion` — verify `dsl-codegen/build.gradle`; note `generateGitProperties` task already exists in the build per repo memory, reuse its output if accessible); (3) git info — read commit sha + dirty flag via JGit (already a dep? verify) or `git` subprocess with graceful "unknown" fallback for non-repo CI contexts — NO hard failure when git absent; (4) thread both values into the `@DslGenerated` text block AND extend the `@Generated(comments=...)` line with `platform=%s` so the javax `@Generated` view also carries it; (5) existing generated-source golden/assertion tests updated for the two new members. Tests: (a) annotation emission includes `dslBuildInfo`/`dslGitInfo` keys with escaped values; (b) fallbacks — version "unknown" when manifest missing, git "unknown" when `.git` absent (test with workdir override); (c) escaping — values containing quotes/newlines produce valid Java source (compile the output in a test). Tier `backend`. Low risk: additive annotation members (SOURCE retention, defaults keep back-compat if any other generator constructs the annotation — grep first); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T365-codegen-provenance.md`        |
| T366 | Backlog | Config hardcode cleanup — bulkhead acquire timeout + workbench workspace root → `DslProperties`/app.yml | backend — code-confirmed TODOs (one group, same concern: hardcoded operational config in starter): (1) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/service/DslFileBulkhead.java:15` reads `// TODO: redo to use a some \`DslProperties\` property instead of hardcode` — `ACQUIRE_TIMEOUT_SECONDS = 5L` is a static constant; note the semaphore PERMITS are already property-driven (`DslConfiguration.java:152-155` reads `properties.getFiles().getReadBulkheadPermits()`/`getWriteBulkheadPermits()`), so only the timeout is stuck: a saturated bulkhead always throws `IllegalStateException("file read bulkhead saturated")` after exactly 5s regardless of deployment profile — slow disks or hot imports cannot tune it without recompile; (2) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/DslConfiguration.java:137` reads `// TODO: move hardcoded path to app.yml` — the workbench virtual-workspace root `sourceRoot.resolve(".workbench").resolve("drafts-fs")` is hardcoded inline in the `dslWorkspaceResolver` bean; operators cannot relocate scratch state (e.g. to a dedicated volume or tmpfs) via config. Task: (1) add `acquireTimeoutSeconds` (default 5) to `DslProperties.Files` next to the existing permit props; pass into `DslFileBulkhead` (constructor param — the class is already `@RequiredArgsConstructor`-style bean-constructed via `DslConfiguration`, keep the static constant only as the default-value source or drop it); (2) add `workbenchWorkspaceRoot` (or `workspaceSubdir`, default `.workbench/drafts-fs`) to `DslProperties` — resolve relative to `sourceDir` when not absolute, absolute wins; `DefaultDslWorkspaceResolver` construction reads it; (3) remove both stale TODOs and the stray `log.info("[DEBUG] Current path is: {}")` debug line at `DslConfiguration.java:134` (leftover debug logging, not a TODO but same lines — noise in every startup log); (4) document both keys in application.yml defaults block if one exists in starter resources. Tests: (a) bulkhead timeout honored — inject timeout 1s, saturate permits, assert `IllegalStateException` after ~1s not 5s; (b) workspace root — default value resolves to `<sourceDir>/.workbench/drafts-fs`; absolute override wins; (c) existing `DslWorkspaceResolver`/bulkhead tests stay green (constructor signature changes ripple — update fixtures). Tier `backend`. Low risk: pure config surface addition, behavior identical at defaults; rollback = revert one commit. Natural companion to T363 (same `DslProperties` extension — executor may batch). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T366-config-hardcode-cleanup.md` |
| T367 | Backlog | `DslExecutionEventStage` — publish `DslRunStartedEvent`/`DslRunCompletedEvent` from the run pipe | backend — code-confirmed TODO + dead code: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/listener/DslExecutionEventBus.java:9` reads `//TODO: we need to add a dslpipe stage impl, that uses a \`DslExecutionListener\`/\`DslExecutionEventBus\` thing`. Evidence the feature is half-built: `DslExecutionEvent` (sealed, `core/event/DslExecutionEvent.java`) permits THREE event types, but `grep -rn '\.publish(' starter/src/main` shows exactly ONE publisher — `RunIdKeyedExternalCallRecorder.java:138` publishes `DslExternalCallEvent` only. `DslRunStartedEvent` and `DslRunCompletedEvent` records exist with ZERO publishers = dead types; the bus itself IS a bean (`TemporalConfiguration.java:168`) and accepts listeners, but listener contract is unobservable for run lifecycle — any registered `DslExecutionListener` today hears only external calls, never run start/end. Result: promised observability hook (audit logging, run metrics, future UI push) cannot be built on the bus. Task: (1) new `core/stage/DslExecutionEventStage` following existing stage conventions (see `MetricsStage`, `ExecutionTraceStage` — same constructor/DslPipeStage shape); (2) on execute: publish `DslRunStartedEvent(runId, name, mode)` before delegating to `next.execute(...)` and `DslRunCompletedEvent(runId, name, mode, result)` after (runId resolution — reuse whatever `RunIdKeyedExternalCallRecorder`/`ExecutionTraceStage` use for run identity; do NOT invent a second runId source); (3) register in the run pipe ordering — after/before metrics stage; position decision left to executor, document chosen order + rationale in plan; (4) event bus stays failure-isolated — existing `publish()` already try/catches per listener, keep that (note: `catch (Exception ignored)` silently swallows — consider debug-level log while touching, optional); (5) drop the stale TODO. Tests: (a) registered listener receives Started then Completed (in order) for a successful run; (b) Completed carries the actual `Result` incl. failure result when downstream throws — verify exception propagation NOT swallowed by the stage; (c) misbehaving listener (throws) does not break the run (existing bus semantics); (d) no events for explain/preview pipes unless trivially free — scope = run pipe only, state this in plan. Tier `backend`. Low risk: additive stage, removable by pipe config; existing stages untouched. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T367-execution-event-stage.md` |
| T368 | Backlog | Idea-plugin sync leak — bound/TTL `DISCOVERED` cache in `DslProjectResolverExtension` | backend (dsl-plugins) — code-confirmed TODO: `backend/dsl-plugins/dsl-idea-plugin/src/main/java/cbs/nova/dsl/idea/sync/DslProjectResolverExtension.java:18` reads `// TODO: redo to a Caffeine with some properties config for ttl`. Current state: `private static final Map<ExternalSystemTaskId, Set<Path>> DISCOVERED = new ConcurrentHashMap<>()` — static, unbounded, lives for the IDE JVM lifetime. Entries are removed ONLY by `drainDiscovered(taskId)` (`DslProjectResolverExtension.java:48`), whose sole production caller is `DslGradleSyncNotificationListener.java:52` — the sync-SUCCESS notification path. Leak vector: a Gradle sync that fails, is cancelled, or dies before the notification listener runs leaves its `Set<Path>` (plus the `ExternalSystemTaskId` key and its held project references) in the static map forever; each leaked entry pins project paths and task-id objects in a long-lived IDE process — heavy IntelliJ sessions re-sync often, so abandoned-sync residue accumulates. Task: bound the cache. Preferred shape per TODO: Caffeine `Cache` with `expireAfterWrite` (TTL default e.g. 10m — a sync that never completes within TTL is dead by definition) + optional `maximumSize` belt-and-braces; `drainDiscovered` becomes `cache.asMap().remove(taskId)` returning null → `Set.of()` (semantics preserved — existing `DslProjectResolverExtensionDrainTest` must stay green modulo cache plumbing). TTL source: plugin settings via `DslProjectStateService`/`PersistentStateComponent` if a state service exists (verify — `DslProjectStateService.getInstance(project)` is referenced at `DslProjectResolverExtension.java:26`, so a state service IS present), else constant + follow-up; do NOT block on a full settings UI — constant-with-clear-name acceptable, note in plan. DEPENDENCY DECISION for executor: adding `com.github.ben-manes.caffeine:caffeine` to the IntelliJ plugin bundles ~900KB into the plugin distribution (gradle-intellij-plugin packs `implementation` deps) — evaluate the lighter zero-dep alternative (scheduled `ConcurrentHashMap` sweep via `ApplicationManager.getApplication().executeOnPooledTimer` or entry timestamps + lazy prune on next `populateModuleExtraModels` call) and pick; if Caffeine chosen, justify size cost in plan. Tests: (a) TTL expiry — injected `Ticker` (Caffeine path) or fake clock (sweep path), stale taskId entries vanish from `drainDiscovered`; (b) existing drain semantics test stays green; (c) failed-sync simulation — no notification listener call → entry still bounded by TTL/size, assert `DISCOVERED` (via accessor) does not grow unbounded. Drop stale TODO. Tier `backend`. Risk low: single class + tests; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T368-idea-plugin-discovered-cache.md` |
| T369 | Backlog | Lombok ctor sweep — collapse hand-rolled/delegating constructors in 4 starter classes | backend — code-confirmed TODOs (one concern: hand-rolled constructors where Lombok idiom applies), all in `dsl-starter/starter`: `JdbcDslRunRepository.java:43,50` (`// TODO: remove constructor, use lombok's one` ×2 — delegating overloads: `DataSource`→`NamedParameterJdbcTemplate` wrap + `qualifiedTableName(properties)` precompute); `DispatchStage.java:57,66` (same TODO ×2); `TemporalDslProcessService.java:88,100,112,154` (`// TODO: use only lomboks constructor` ×3 + `// TODO: use constructor` ×1 — incl. a `@Deprecated(forRemoval = true)` overload that delegates with `Long.MAX_VALUE` timeout and `Optional.empty()` webhook); `DslPipeContext.java:29,41` (`// TODO: remove constructor` ×2, same file also carries a separate record-redesign TODO — OUT of scope here, ctors only). Project idiom is `@RequiredArgsConstructor` (see `ApiKeyAuthFilter`, `DslFileBulkhead`), but these four classes hand-roll delegating ctor chains that bypass it — every new field forces editing multiple ctors, and the deprecated `TemporalDslProcessService` overload keeps a 11-arg signature alive that callers should not use. NOTE for scoping: these are NOT pure boilerplate — the overloads do real work (derivation, defaults), so the sweep is collapse-to-canonical, not delete: (1) keep ONE canonical `@RequiredArgsConstructor`-generated ctor per class over the true `final` field set; (2) replace derivation overloads with `static` factory methods named for intent (e.g. `JdbcDslRunRepository.fromDataSource(dataSource, ...)`, `TemporalDslProcessService.withDefaults(...)`-style) so derivation logic survives with a name; (3) DELETE the `@Deprecated(forRemoval)` `TemporalDslProcessService` overload outright — it is declared forRemoval, callers must move; compiler finds them; (4) update all call sites — 46 direct `new X(...)` references repo-wide excluding the four classes themselves, the bulk in tests (test fixtures get the factory names); (5) drop the seven ctor TODOs. Acceptance = existing `:starter:test` suite green with zero test deletions (fixture rewrites allowed); no behavior change — delegation order and derived values (e.g. `qualifiedTableName`) identical; where a test relied on a deleted overload, migrate to canonical/factory, do not loosen assertions. Tier `backend`. Mechanical but compiler-driven; risk low; rollback = revert one commit. Natural batch companion: T363/T366 (same module). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T369-lombok-ctor-sweep.md`        |
| T370 | Backlog | Explain-path failure handling — guard `DevDslRuntime.explain()` null value + failure branch in `DslRuntimeService` | backend — code-confirmed TODO + asymmetry bug: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/DevDslRuntime.java:34` reads `// TODO: add check for null, add default value case` inside `explain()`, which does bare `return result.value();` on the explain pipe's `Result<ExplainReport>`. The run path in the SAME codebase shows the correct pattern: `DslRuntimeService.java:85` (`RuntimeOutcome.error(toErrorResponse(name, runId, result.cause()))`) branches on failure — but the explain path `DslRuntimeService.java:91-93` calls `dslRuntime.explain(...)` then immediately dereferences `report.errors()` with NO failure/null handling. Failure mode: explain pipe returns failure `Result` (e.g. definition not found, explain-stage exception) → `Result.value()` on a Failure throws (or yields null) → NPE/unwrapped exception at line 93 → raw 500 with no runId, no structured `ErrorResponse`, no MDC-correlated diagnostics — worse than the run path's structured error for the same class of failure. Task: (1) `DevDslRuntime.explain()`: handle failure Result — either throw the cause (pipe contract) or return an empty/errored `ExplainReport` carrying the failure as a `PreviewErrorDetail` — pick the option consistent with how `ExplainReportStage` builds reports for soft errors (executor: read that stage first, match its idiom); null-value success Result gets the same default-report treatment (TODO's "default value case"); (2) `DslRuntimeService.explain()`: add the missing failure branch mirroring line 85 — structured `RuntimeOutcome.error(...)` with cause mapping, runId, and the existing `toErrorResponse` chain (DslException / PreviewTimeoutException / fallback already exist there — reuse); (3) tests: (a) explain on unknown definition → structured error response (assert error code + runId present, NOT raw 500); (b) explain-stage throws → same structured path, MDC runId logged; (c) success path unchanged — golden explain report test stays green; (d) null-value success Result (if reachable) → default report, no NPE; (4) drop stale TODO. Tier `backend`. Low risk: error-path addition, happy path untouched; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T370-explain-failure-handling.md` |
| T371 | Backlog | `make test` — one-command backend + frontend test runner | dx (Makefile) — code-confirmed gap: `grep -E '^[a-z-]+:' Makefile` lists help/up/down/backend/publish/frontend/dev/logs/clean/doctor/seed/seed-history — NO test target. Tests exist and are the project's core quality gate (2,600+ BE, 1,300+ FE per recent memory) but running both tiers means remembering two unrelated commands: `backend/dsl-platform/gradlew -p backend/dsl-starter :starter:test` (wrapper + module path + the Java-25/Gradle-8.13 caveat from CLAUDE.md) and `cd frontend && pnpm test` (which itself filters to `@cbs/admin-ui-plugin`). CLAUDE.md's end-to-end check documents publish/dev/seed but is silent on tests; the executor loop (docs/loop.md) and any CI-less contributor must re-derive this every time. Task: add `make test` target to the root Makefile: (1) runs BE tests — `backend/dsl-platform/gradlew -p backend/dsl-starter :starter:test` (NOT root `./gradlew` — fails under Java 25 per CLAUDE.md caveat; also `-x generateGitProperties` NOT needed on main worktree, only linked worktrees — keep plain); (2) runs FE tests — `cd frontend && pnpm test`; (3) per-check failure isolation: each tier's exit code captured, summary line per tier (`[ok] backend: N tests` / `[fail] frontend`), target exits nonzero if ANY tier fails but runs BOTH tiers regardless (no && short-circuit — a BE failure must not hide FE status); (4) optional `make test-backend` / `make test-frontend` granular targets reused by the aggregate; (5) `help` target auto-lists new entries (verify help is pattern-based, else add lines); (6) one-line mention in CLAUDE.md quick-check section. Style: match existing target idioms (`##` comment docs, `.PHONY`, printf banners — see `doctor`/`seed`). Tests/verification: run `make test` against the real tree — both tiers green from clean checkout state; also verify failure propagation (`make test-frontend` with an intentionally failing vitest spec in a scratch branch, revert). Tier `dx`. Zero production-code risk; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T371-make-test.md`               |
| T372 | Backlog | Remove deprecated `DslDefinitionLoader.load(Path, GlobalManager)` — SPI-only loading | backend — code-confirmed TODO + `@Deprecated(forRemoval = true)`: `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/DslDefinitionLoader.java:11` carries `//TODO: remove method, app must work only with spi` on the `load(@NonNull Path sourceDir, @NonNull GlobalManager gm)` overload. Prod code already routes through the SPI variants: `DslConfiguration.java:166` uses `loader.load(GlobalManager.globalManager())`, `DslReloadHandler.java:218` uses `loader.load(classLoader, target)` (reload compiles to a classloader then loads via SPI — Path variant unused there). Remaining surface: the interface declaration, its impl `DefinitionLoader.java:21`, and one test fake override `DslReloadResourceTest.java:583` (delegating stub). Task: (1) delete the overload from `DslDefinitionLoader`; (2) delete the `DefinitionLoader` impl of it — first read the impl body: if it contains load-from-sourceDir logic NOT reachable via any other path, confirm no reflection/ServiceLoader consumer references it (`grep -rn 'load(' backend --include='*.java'` + codegraph callers) before deleting; (3) drop the test-fake override in `DslReloadResourceTest` (stub deletion, not assertion loosening); (4) remove the TODO comment; (5) verify: full `:starter:test` suite green (reload resource + diagnostics tests cover the surviving SPI paths) and `dsl-platform` builds — starter consumes the platform via Maven Local, so run `backend/dsl-platform/gradlew -p backend/dsl-platform publishToMavenLocal -x test` then `:starter:test` per the standard two-module verify. Tier `backend`. Compile-driven, risk low; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T372-remove-path-load-variant.md` |
| T373 | Backlog | Codegen DI — inject `SourcePackageResolver` via `CompileConfig` (drop inline `new` in `SourceCompiler`/`ModelRegistryGenerator`) | backend — code-confirmed TODOs (one logical group, same concern): `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/SourceCompiler.java:78` and `.../generator/ModelRegistryGenerator.java:72` both read `// TODO: redo to bean from \`CompileConfig\`` and both do `var packageResolver = new SourcePackageResolver(new DslPackageNameResolver(codegenNaming));` inline. `CompileConfig` is the module's singleton factory and ALREADY wires sibling deps this exact way: `processCodeGenerator()`/`transactionCodeGenerator()`/`generatedClassProviderGenerator()` pass `new DslPackageNameResolver(codegenNaming())` via constructor. Only `SourceCompiler` and `ModelRegistryGenerator` bypass it. Task: (1) add `public @NonNull SourcePackageResolver sourcePackageResolver()` bean to `CompileConfig` — `singleton(() -> new SourcePackageResolver(new DslPackageNameResolver(codegenNaming())))`; (2) extend `SourceCompiler` ctor (currently 5 deps: definitionProviderGenerator, codeWriter, codegenNaming, dslPreprocessor, modelPreprocessor) with the resolver as 6th param; extend `ModelRegistryGenerator` ctor (currently codeWriter, codegenNaming, modelTypeExtractor) as 4th; (3) update the two `CompileConfig` factory methods to pass `sourcePackageResolver()`; (4) delete both inline constructions + TODOs; drop now-unused `DslPackageNameResolver`/`SourcePackageResolver` imports where applicable; (5) fix any direct `new SourceCompiler(...)`/`new ModelRegistryGenerator(...)` call sites (tests, other factories — grep to enumerate); (6) verify: `backend/dsl-platform/gradlew -p backend/dsl-platform :dsl-codegen:test` green + platform `publishToMavenLocal -x test` if starter consumes changed ABI, then `:starter:test` per standard two-module verify. Tier `backend`. Pure constructor wiring, compiler-driven, rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T373-codegen-package-resolver-di.md` |
| T374 | Backlog | Request-param extraction to typed records via converter mappers (`DslExecutionsHandler` + `DslIntrospectionHandler`) | backend — code-confirmed TODOs (one logical group, identical concern): `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/controller/DslExecutionsHandler.java:56` and `.../controller/DslIntrospectionHandler.java:17` both read `// TODO: Add mapstruct mapper, that map request.param to a record`. Both handlers hand-roll `request.param("x")` chains into loose locals — `DslExecutionsHandler.java:280-283` (processName/status/mode/correlationId for the list filter) and `DslIntrospectionHandler.java:36,56-58` (format/name/type/description for catalog query). MapStruct is ALREADY an established idiom here, not a new pattern: `libs.versions.toml` pins 1.6.3, `starter/build.gradle:48-49` wires `implementation libs.mapstruct` + `annotationProcessor libs.mapstruct.processor`, and `cbs.nova.starter.converter` holds 8 existing mappers (`DslRuntimeMapper` is the reference — `@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.IGNORE)` with per-input-shape `fromXxx` default methods). Task: (1) define query records (e.g. `ExecutionListQuery(processName, status, mode, correlationId)`, `IntrospectionCatalogQuery(format, name, type, description)`) — placement per existing model/query-record idiom in the module; (2) add mapper default methods in `converter` package that accept `ServerRequest` (or the raw param map) and apply the existing defaults/blank-filter semantics EXACTLY (`.filter(s -> !s.isBlank()).orElse(null)`, `orElse("mermaid")` etc.) — read both handlers first and preserve every default verbatim, incl. `DslExecutionsHandler.java:328` raw-param helper semantics if in scope; (3) rewrite the two handlers to call the mappers; drop both TODOs; (4) tests: existing `DslIntrospectionResourceTest` + executions-handler tests stay green; add mapper-level unit tests pinning blank-param→null, missing→default behavior. Tier `backend`. Bounded to 2 handlers + converter pkg; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T374-request-param-records.md` |
| T375 | Backlog | `DslDescriptor` builder — add lombok `@Builder` to the 14-arg record, sweep all 16 positional construction sites | backend — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/stage/PreviewCacheKeyBuilder.java:26` reads `// TODO: find usage of new DslDescriptor and replace to a lombok's builder`. `DslDescriptor` (`backend/dsl-platform/dsl-api/src/main/java/cbs/nova/dsl/DslDescriptor.java`) is a record with FOURTEEN components — name, type, description, inputType, outputType, hasCompensation, hasSideEffects, previewBehavior, parameters, taskQueue, version, startToCloseTimeout, heartbeatTimeout — and there are 16 `new DslDescriptor(...)` sites across 14 files (3 prod: `ProcessDslObject.java:46`, `FunctionDslObject.java:35`, `TransactionDslObject.java:45` in the `dsl` module + the TODO site itself; rest tests incl. `ExplainReportTest`, `ProcessBuilderTest`, `ExplainDiagramRendererTest`, `ExplainReportStageTest`, `DslIntrospectionMapperTest` ×2). Consecutive `@Nullable` params (description/inputType/outputType; taskQueue/version; the two Duration timeouts) make positional args swap-prone — compiler cannot catch a `null` landing in the wrong slot, and one prod site (`PreviewCacheKeyBuilder` helper-describe path) already stacks 4+ adjacent nulls. Task: (1) add `@Builder` to the `DslDescriptor` record (lombok already used in `dsl-api` — see `history/DslRun.java`; match its annotation style, keep `@NonNull`/`@Nullable` untouched); (2) sweep ALL 16 sites to named-arg builder calls — `.name(...).type(...)...` — preserving every value verbatim; this is also an audit: if any site's positional args look suspicious (e.g. null in a `@NonNull` slot), flag it in the PR/commit message instead of silently "fixing"; (3) drop the TODO; (4) verify: `:dsl-api` + `:dsl` module tests, republish platform (`publishToMavenLocal -x test`), then `:starter:test` — ExplainReport/Introspection suites exercise the descriptor paths. Tier `backend`. Mechanical, compiler-driven, rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T375-dsl-descriptor-builder.md` |
| T376 | Backlog | Test fake: implement lazy `registerHelper(name, Supplier)` + cover `HelperManager` supplier delegation | backend (test) — code-confirmed TODO: `backend/dsl-platform/dsl/src/test/java/cbs/nova/dsl/HelperManagerTest.java:190` — the inline fake registry's `registerHelper(@NonNull String name, @NonNull Supplier<Executable<?, ?>> helperSupplier)` override is an EMPTY no-op body carrying `// TODO: implement lazy helper registrations for tests`. This masks a real prod path: `HelperManager.java:46` delegates the supplier variant to `DefaultHelperRegistry` (lazy — supplier must NOT be invoked until first execution), and that supplier path is what generated SPI code exercises (`HelperSpiProcessor` → `HelperResolver.registerHelpers` → `GlobalManager.registerHelper(name, supplier)` at `GlobalManager.java:68-74`). With the fake no-opping it, `HelperManager`'s supplier-delegation line has zero test coverage at its own level (registry-level semantics live separately in `DefaultHelperRegistryTest`). Task: (1) implement the fake's lazy registration — store `Supplier` per name, resolve on first lookup/execution, honoring whatever semantics `DefaultHelperRegistry` documents (READ `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/registry/DefaultHelperRegistry.java:28+` first and mirror them: memoized single invocation vs per-call, null-name rejection, overwrite behavior); (2) add `HelperManagerTest` cases: (a) supplier NOT invoked at registration (lazy), (b) supplier invoked exactly once on first execution (memoized — assert invocation count via counting supplier), (c) eagerly-registered helper and lazily-registered helper coexist, (d) supplier returning null / throwing → behavior mirrors `DefaultHelperRegistry` (don't invent semantics); (3) drop the TODO; (4) keep the fake's existing eager `registerHelper(name, Executable)` behavior untouched — other tests depend on it. Tier `backend`. Test-only change, zero prod risk; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T376-helper-manager-lazy-test.md` |
| T377 | Backlog | Stale scan-TODO cleanup — drop already-satisfied entity/repo scan TODOs with verification | backend (hygiene) — code-confirmed STALE TODOs (one group, same concern: Spring scanning scope, both already satisfied): (1) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/entity/DslRunEntity.java:10` reads `//TODO: we need limit entity scan, only to one package cbs.nova.starter.entity` — but Spring Data JDBC has NO entity scanning concept (that is JPA's `@EntityScan`); SD JDBC discovers `@Table` types through repository domain types and `NamingStrategy` (`DslRunNamingStrategy` bean exists in `DslRunRepositoryConfiguration`). A repo-wide grep for `@EntityScan` in `starter/src/main` returns ZERO — there is nothing to limit; (2) `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/persistence/DslRunJdbcRepository.java:16` reads `// TODO: we need to limit package scan for repositories, only to cbs.nova.starter.persistence` — but `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/config/DslRunRepositoryConfiguration.java:39` ALREADY declares `@EnableJdbcRepositories(basePackages = "cbs.nova.starter.persistence")`. Risk of keeping them: a future executor "resolves" the TODOs by adding JPA-only annotations (`@EntityScan`) or redundant scan limits — introducing confusion and dead config. Task: (1) re-verify both claims at execution time (`grep -rn '@EntityScan' backend/dsl-starter/starter/src/main` → zero; read `DslRunRepositoryConfiguration.java:39` → scoped); (2) replace each TODO with a one-line comment recording WHY nothing is needed (e.g. `// scanning already scoped: @EnableJdbcRepositories(basePackages=...) in DslRunRepositoryConfiguration; SD JDBC has no entity scan` — match file comment style) — or delete outright if surrounding code self-documents, executor's judgment per file style; (3) if verification DISCONFIRMS either claim (config changed since planning), stop and update this row instead of forcing the cleanup; (4) `:starter:test` green (context-load tests exercise the scan config). Tier `backend`. Zero behavior change; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T377-stale-scan-todo-cleanup.md` |
| T378 | Backlog | `docs/dsl/helpers.md` catch-up — document the T350–T358 helper wave (9 shipped, 0 documented) | backend (docs) — code-confirmed doc drift: `docs/dsl/helpers.md` (484 lines, 33 sections) was last extended by T348/T349 (`git log -- docs/dsl/helpers.md` → 76719fc3 dateMath, 213fb77e metric, fa0b3889 backoff) but the ENTIRE T350–T358 helper wave shipped with zero doc updates — per-name grep in helpers.md: `JsonPatch` 0, `ListOps` 0, `MathHelper` 0, `HttpAuth` 0, `QueryString` 0, `Compression` 0, `Random` 0, `Semver` 0, `Jwt` 0. All nine classes exist in `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/helper/`: `JsonPatchHelper`, `ListOpsHelper`, `MathHelper`, `HttpAuthHelper`, `QueryStringHelper`, `CompressionHelper`, `RandomHelper`, `SemverHelper`, `JwtHelper` — merged Done on kanban T350–T358. The workbench catalog (T311) serves the UI, but `helpers.md` is the human reference for DSL authors, and it silently stops at the T349 wave. Task: (1) for EACH of the nine helpers read its class + its test file (methods, op discriminators, in/out shapes, error cases) — tests are the source of truth for behavior; (2) add one section per helper in helpers.md following the existing section idiom (grouped tables of methods, one runnable DSL snippet each — match the DateMath/Metric sections' structure added by T348/T349); (3) snippets must be REAL: cross-check each against the helper's test so no fictional method names ship in docs; where a natural combo exists (HttpAuth→HttpCall, QueryString→UrlEncode, Jwt parse→verify chain), show it in one snippet; (4) add the nine to any summary/TOC list at the top of helpers.md if one exists; (5) acceptance: `grep -c` for each of the nine names in helpers.md ≥1, and every documented method name exists in the corresponding helper class (`grep` spot-check). Tier `backend` (docs). Docs-only, zero code risk; rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T378-helpers-doc-catchup.md` |
