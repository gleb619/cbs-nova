# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture             | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                                                                                                                                  |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T291 | Done | Run/preview accept any input shape — server-side validation exists but unused on execute path | backend — codegraph-confirmed gap: platform ships `JacksonJsonSchemaGenerator` (`dsl-platform/dsl/.../jsonschema/`) and the FE runner validates client-side against it (`InputForm.vue:37-38` checks `schema.properties`), but `DslRuntimeHandler.run/preview/explain` (`:25-45`) deserialize `request.body(DslRequest.class)` and execute blind — no server-side check of the body against the construct's `inputType` schema. Malformed input fails deep inside the Temporal workflow / preview pipeline with a cryptic coercion error AFTER the run row + workflow already started. T279 (in progress) caps size only, not shape. Task: resolve descriptor for the named construct (reuse `PreviewCacheKeyBuilder`-style GlobalManager lookup), generate input schema via existing generator (cache by name+descriptor hash — Caffeine pattern T268), validate body before execution, reject with 422 + field-pointer error details (shape the FE `ProblemsPanel` already renders), opt-out property for backward compat, unit + handler tests. See plan. |   High   | loop  | -      | -          | `./docs/plans/T291-server-side-input-validation.md`              |
| T292 | Done | Workbench loses unsaved edits — dirty flag tracked but never guarded | frontend — grep-confirmed gap: `useWorkbenchDraft.ts:72` exposes `dirty` (body vs lastSavedAt) and the workbench binds it, but NO `beforeunload` listener, no `onBeforeRouteLeave` guard, and no confirm on switching the selected construct (`dsl-workbench.vue:52-61` swaps `draftName` → draft reload, discarding the local body silently). One misclick (sidebar link, construct click, browser refresh/back) destroys uncommitted editor work with zero warning. Task: `onBeforeRouteLeave` + `window.beforeunload` guards keyed on `dirty`, confirm dialog when switching constructs while dirty (reuse T285 modal pattern when it lands), visible dirty indicator in the workbench toolbar (e.g. dot + "unsaved changes"), tests: guard fires on dirty, not on clean; construct-switch confirm accept/deny paths. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T292-workbench-unsaved-guard.md`                     |
| T293 | Done | Run-again from executions detail — runner prefill half-built, no bridge | frontend — grep-confirmed gap: runner page already prefills from query (`runner.vue:65-72` reads `route.query.name` + `.mode`, watches `:79`), but executions detail (`[id].vue`) has no run-again action — after inspecting a failed/completed run's input (PayloadTab), the only path is manual: open runner, re-pick the process, re-type the input. Task: "Run again" button on detail page (visible when process resolvable), stash input JSON in sessionStorage under agreed key, `navigateTo('/runner?name=<process>&mode=<mode>')`, runner consumes stashed input into InputForm (clearing after consume), button + handoff testids, vitest coverage (stash/consume round-trip, stale stash discarded when name mismatches), e2e optional via T273. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T293-run-again-from-detail.md`                     |
| T294 | Done | Drafts list/read endpoints — workbench draft surface is write-only | backend — grep-confirmed gap at every layer: backend drafts routes are only save/publish/delete (`DslDraftRouterConfiguration.java:48-50`, no GET), BFF mirrors (`drafts/[name]/save|publish|delete` only), FE `useDslApi.ts` exposes `saveDraft`/`publishDraft`/`deleteDraft` with no list/read. Drafts land in `.workbench/drafts/<name>.json` and are never read back — after a page refresh the workbench starts blank, saved drafts are invisible and can never be reopened, and T283's delete has no UI discovery path. Task: `GET /api/dsl/drafts` (summaries: name/type/status/version/updatedAt from mtime, no bodies) + `GET /api/dsl/drafts/{name}` (full stored payload, 404 unknown, path-traversal-safe like delete), BFF proxy routes `drafts/index.get.ts` + `drafts/[name].get.ts`, `listDrafts`/`readDraft` in `useDslApi`, workbench draft picker that loads a draft into the editor, tests per layer. See plan. |  High   | loop  | -      | -          | `./docs/plans/T294-drafts-list-read-endpoints.md`            |
| T295 | Done | Actuator health lies about Temporal — indicator counts registry only | backend (ops) — code-confirmed gap: `DslHealthIndicator.java` returns `Health.up()` unconditionally, details are only `GlobalManager` registry counts (processes/transactions/helpers) — no Temporal connectivity signal exists anywhere in starter, though every run request depends on the Temporal server + registered workers. With Temporal down, `/actuator/health` stays UP while `POST /api/dsl/run/{name}` fails for every caller; T271's compose `service_healthy` gate on the spring app consumes this vacuous UP, and any future k8s readiness probe would too. Task: `TemporalHealthProbe` collaborator (lightweight `WorkflowServiceStubs` reachability call with short timeout, worker-registration/task-queue detail where cheap), enrich health with `temporal` section, non-breaking failure semantics by default (`cbs.health.temporal-fail-status: none|down`, default `none` preserves current behavior), conditional wiring so non-Temporal runtime modes unaffected, unit tests (reachable/unreachable/timeout/policy both modes), compose + runtime.md docs for `down` mode. See plan. |  High   | loop  | -      | -          | `./docs/plans/T295-temporal-health-check.md`                 |
| T296 | Done | Run trace persisted but never returned — detail page Trace/Logs tabs dead | backend — code-confirmed gap: `TemporalDslProcessService.java:381` stores `serializeTrace(traceCollector.snapshot())` as `context_json` (`{"trace":[...]}`) on every completed Temporal run, `DslRun` carries `@Nullable contextJson`, yet `ExecutionDto.fromDetail` (`ExecutionDto.java:50-68`) maps only input/output/errors and drops it. FE detail page (`executions/[id].vue:74`) reads `selectedExecution?.trace ?? []` → always empty, so `ExecutionTrace` (renders `v-if steps.length > 0`), `CompensationLane`, `TraceNode`/`CallTreeNode` never fire, and `LogsTab` (`:194`) binds a `logs` field the backend never sends — the trace/logs half of the detail page is decorative for production runs (same dead-UI class as T266 mermaid, different surface; data already in Postgres). Task: parse `context_json` in `fromDetail` (malformed-tolerant, defensive cap), reconcile flat trace strings vs FE Step shape (server-side mapping preferred, document in OpenAPI schema), give LogsTab real content or hide it when no log source, verify BFF passthrough, handler tests + FE detail render test. See plan. |  High   | loop  | -      | -          | `./docs/plans/T296-execution-trace-logs-surfacing.md`      |
| T297 | Done | Preview cache never invalidated on reload/publish — stale previews after republish | backend — code-confirmed gap: `PreviewResultCache.invalidateByDslHash` (`:38`) and `clear()` (`:42`) have ZERO callers in starter main sources — dead code. Cache key (`PreviewCacheKeyBuilder.build`) is `(name, dslDescriptorHash, inputHash)` where the descriptor hash serializes the `DslDescriptor` record: metadata only (name/types/description/params/timeouts), NOT the construct implementation. Edit process logic in the workbench while descriptor metadata stays unchanged (the common case for body/steps edits), publish (registry reload), preview with same input → identical key → old cached preview served as if it reflected new code — the edit→publish→verify loop silently lies. Task: invalidate preview cache on successful reload (`DslReloadHandler.reloadDefinitions`, also reached via `DslDraftHandler.publish`; `clear()` acceptable, per-construct only if reload-diff cheap), failed reload must NOT flush (registry unchanged, T255 atomic swap), conditional wiring for cache-less modes, tests (republish same-hash changed-behavior → fresh preview; failed reload preserves), decide fate of unused `invalidateByDslHash`. See plan. |  High   | loop  | -      | -          | `./docs/plans/T297-preview-cache-invalidation.md`       |
| T298 | Done | Preview execution timeout — unbounded user DSL pins request threads | backend — code-confirmed gap: `PreviewDslPipe` pipeline ends in `DispatchStage` (`core/stage/DispatchStage.java`, 78 lines) with zero timeout/executor/interrupt handling — user DSL runs inline on the Tomcat request thread with no time bound, and `CbsNovaPreviewProperties` has no execution-timeout setting (call-tree depth only). A looping/blocked construct hangs the request thread indefinitely; client timeout doesn't free it; a handful of such previews exhausts the Tomcat pool and takes down the whole admin API. T291 validates input shape — well-shaped input can still loop. NOT a T165 dupe: sandboxing is isolation (Blocked on JDK 25), this is duration bounding only — no sandbox needed. Task: `cbs.nova.preview.execution-timeout` (default 10–30s, 0=disabled for compat), dispatch on bounded named executor with future timeout, prompt 504/`PREVIEW_TIMEOUT` error to caller, document honest cancellation limits (interrupt stops waits, CPU-spin threads may linger — bounded pool + metrics), ensure timed-out previews not cached (verify PreviewCacheStage ordering), cover explain path if it shares dispatch, tests (fast unaffected, slow → timeout within bound, disabled preserves inline). See plan. |  High   | loop  | -      | -          | `./docs/plans/T298-preview-execution-timeout.md`       |
| T299 | Done | Transaction/compensation engine: 9 core classes, 1 test | backend (tests) — inventory-confirmed gap: `dsl-platform/dsl/.../transaction/` holds 9 classes (`TransactionManager`, `TransactionRunner`, `CompensationRegistry`, `TransactionBuilder`, `TransactionDescriptor`, `TransactionDslObject`, `TransactionRegistry`, `TransactionRichContext`, `CompensationRichContext`) against exactly 1 test (`TemporalTransactionFailurePropagationTest`). This package is the compensation-semantics core — execution order, failure-triggered compensation, reverse ordering, compensation-of-compensation failure — and nothing pins any of it. Blast radius of a silent regression: wrong compensating actions in production workflows. T168 (Blocked, awaiting replan) will restructure event/listener flow around exactly this machinery — characterization tests first make that replan verifiable. Task: unit specs for manager/runner (happy path order + output propagation, step failure → downstream skipped + compensation invoked, compensation failure doesn't mask original error, nested transactions), registry build-validate paths (duplicate names, dangling compensation refs, overwrite semantics), rich-context contract assertions (body/prior outputs/failure visibility T168 must preserve); fix + test together only if a bug is proven; report platform test count. See plan. |  High   | loop  | -      | -          | `./docs/plans/T299-transaction-engine-tests.md`        |
| T300 | Done | Platform engine test pass — runner/process/function near-zero coverage | backend (tests) — inventory-confirmed gap (companion to T299, different packages): `dsl-platform/dsl/` main-vs-test counts are `runner/` 6:0, `process/` 7:1, `function/` 4:0 — the process-execution semantics core (runner orchestration, process dispatch, function invocation) with essentially no regression net. T299 covers `transaction/` only; these three packages are equally bare and equally load-bearing, and refactors like the T168 listener replan or future helper-SPI changes land blind without them. Task: characterization specs per package — runner entry points (input wiring, `Result` propagation, error cause preservation), process execution (step sequencing, failure short-circuit, descriptor dispatch not covered by the single existing test), function invocation contract (input mapping, output coercion, exception wrapping); assert current behavior incl. quirks, file suspected-wrong behavior rather than silently changing it, production fixes only for proven bugs (fix + test together), report `:dsl:test` count delta. See plan. |  High   | loop  | -      | -          | `./docs/plans/T300-platform-engine-tests.md`         |
| T301 | Done | Configuration reference — 11+ property prefixes, zero consolidated docs | docs (backend) — inventory-confirmed gap: starter ships 11+ `@ConfigurationProperties` prefixes (`cbs.security.ratelimit`, `cbs.security.oidc`, `cbs.runtime.input-validation`, `cbs.runs` registered by TWO classes — `DslRunRetentionProperties` + `DslRunsProperties`, `cbs.nova.preview`, `cbs.nova.cache`, `cbs.nova.fakes`, `cbs.nova.dry-run`, `cbs.nova.logging`, `cbs.nova.persistence.run`) plus `dsl.drafts.enabled`/`dsl.source-dir`, while `application.yml` is 6 boilerplate lines and runtime.md documents only 3 props in passing — everything else lives in gitignored plan files and source. Incoming T295/T297/T298 add more props. Task: consolidated config reference (`docs/dsl/configuration.md` or runtime.md section — one home, cross-linked): per-prefix table of key/type/default/effect derived from actual properties classes, interaction notes (opt-in security default-off, 0-disables escape hatches), document the `cbs.runs` double-registration hazard explicitly, cross-reference compose env overrides, grep-validate no invented/missing keys, pointers from runtime.md + architecture-backend.md. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T301-config-reference-doc.md`          |
| T302 | Done | Temporal UI deployed but unlinked — execution detail can't reach workflow history | frontend — grep-confirmed gap: compose runs Temporal Web UI at `:8233` (`orchestration.yml:30-33`) and the backend already returns `workflowId` on the detail response (workflowId = runId), yet the FE has zero references to it — `8233`/`temporal-ui` appear nowhere in `frontend/` and the detail page (`executions/[id].vue`) doesn't even render `workflowId`. The richest debugging surface for stuck/failed runs (event history, retries, timers, stack traces — the artifacts behind T274/T281 cancel and T256 stale-race investigations) is disconnected from the admin UI. Task: configurable `temporalUiBaseUrl` + namespace exposed via the established BFF config pattern (blank default = render nothing), detail metadata shows copyable `workflowId` + external `View in Temporal` link (`rel="noopener"`, new tab, verify URL shape against deployed UI version), optional list-row link for RUNNING rows, testids + vitest (rendered when configured / absent when not / URL construction), one-line docs in runtime.md + compose README. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T302-temporal-ui-deeplink.md`             |
| T303 | Done | Compile diagnostics discarded — reload/publish failure tells the user nothing | backend — code-confirmed gap: `DslReloadHandler.compileJavaSources` (`:206-231`) collects full javac diagnostics in a `DiagnosticCollector`, then only `log.error`s them and throws `IllegalStateException("Failed to compile DSL source: <file>")` — diagnostics die in the logs. Reload endpoint failure = 500 `RELOAD_FAILED` with that one line (`:99-102`); publish path is worse — `DslDraftHandler.publish` catches and returns 200 with bare `reloaded=false` + empty `LoadResult` (`:84-89`), so a typo'd DSL publish is indistinguishable from other outcomes and the workbench operator must tail Spring logs. Failure-case mirror of T272 (success drilldown). Task: propagate structured diagnostics `{file,line,column,message,severity}` from `compileJavaSources`, carry them in the reload `ErrorResponse` details, enrich `DraftResponse` publish failure info (keep 200 — draft persisted), verify BFF passthrough, render via existing `ProblemsPanel` in workbench, cap payload (first ~20), handler tests with a known-bad compile fixture + FE render tests. See plan. |  High   | loop  | -      | -          | `./docs/plans/T303-compile-diagnostics-response.md`       |
| T304 | Done | Run attribution — `triggeredBy` dead field, no column, auth context unused | backend — code-confirmed gap: `ExecutionDto` exposes `triggeredBy` but zero setters exist in starter main — always null in list/detail/stats; `dsl_runs` has no `triggered_by` column at all (V1), so attribution is missing at persistence level too. Meanwhile T275's opt-in OIDC guard authenticates principals, `X-Api-Key` is enforced, and internal writers (stale sweep, purger, cancellation) are distinguishable as `system` — none of it lands on the run row. With security enabled, "who launched/cancelled this run" is unanswerable from the table security protects. Task: migration `V5__dsl_runs_triggered_by.sql` (nullable column), identity resolution at run start in `TemporalDslProcessService` (OIDC principal / API-key identity / `system` for internal writers / null-anonymous default unchanged), JDBC + in-memory repo mapping, populate `ExecutionDto.triggeredBy`, render in executions detail, Testcontainers migration test + identity-resolution tests per mode. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T304-run-attribution.md`                 |
| T305 | Done | Retention purger orphans `dsl_run_transactions` — child table grows unbounded | backend — code-confirmed gap: `DslRunRetentionPurger` has ZERO references to transactions — deletes terminal `dsl_runs` rows and nothing else; `dsl_run_transactions` (V3) has no FK and no ON DELETE CASCADE, and `JdbcTransactionExecutionRepository.deleteByRunId` (`:62`) has no purge-path caller. Every purge cycle orphans the purged runs' transaction executions (payload-bearing `input_json` rows) forever — re-introducing in the child table exactly the unbounded DB bloat T276 was built to remove, degrading `idx_dsl_run_transactions_run_id` as dead run_ids pile up. Task: delete child rows with parents in the same batch window preserving T276's batching/lock-avoidance shape, in-memory repo parity, purge metrics for transaction rows, Testcontainers integration test (both tables shrink together, RUNNING/young rows survive), plus one-time cleanup for pre-existing orphans (scheduled sweep or documented manual SQL — pick one). See plan. |  High   | loop  | -      | -          | `./docs/plans/T305-purge-transaction-orphans.md`       |
| T306 | Done | No graceful shutdown — every restart cuts in-flight requests | backend (devops) — config-confirmed gap: `server.shutdown: graceful` unset (application.yml is 4 management lines; Spring default = immediate — SIGTERM closes in-flight responses), and no `stop_grace_period` on any compose service (Docker SIGKILLs at 10s regardless). Worker drain is already handled (`@Bean(destroyMethod="shutdown")`, drain-order comments in DslWorkerConfiguration) — the HTTP half is missing. A restart mid-`POST /run` resets the client while the workflow may have started (row exists, caller saw network error, retry duplicates the run — same failure family idempotency would fix). Task: `server.shutdown: graceful` + `spring.lifecycle.timeout-per-shutdown-phase` (~30s) in application.yml, `stop_grace_period` (~45s) on spring-app compose service, verify worker-vs-HTTP drain ordering stays sane, ops note in runtime.md incl. interaction with long runs + T298 timeout, scripted restart-during-slow-request verification. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T306-graceful-shutdown.md`               |
| T307 | Done | Run idempotency — network retry or double-submit duplicates workflows | backend — code-confirmed gap: `TemporalDslProcessLauncher` sets `workflowId = ctx.runId()` (`:76`) with runId freshly generated per request, and no `Idempotency-Key` handling exists anywhere in starter — every retried/double-submitted `POST /run` launches a new Temporal workflow + `dsl_runs` row. Amplifiers queued: T306 restart-cut mid-run makes caller retry (duplicate) the natural outcome; BFF/proxy timeouts and user double-clicks do the same. Cheap correct mechanism already in the box: Temporal dedups by workflowId natively (`WorkflowExecutionAlreadyStarted`). Task: optional `Idempotency-Key` header on run (validated → 400 on garbage), derive runId/workflowId deterministically from key (e.g. sha256(process:key), documented), absent key = current random behavior unchanged, decide+implement duplicate semantics (replay 200 + `Idempotency-Replayed: true` or 409 w/ existing runId), document Temporal-retention dedup window, BFF forwards header (httpClient explicit list), FE runner generates uuid per submission, tests per layer (same key → same run, no key → two runs, different key → different). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T307-run-idempotency-key.md`           |
| T308 | Done | Correlation id — dead DTO field, zero plumbing end to end | backend — code-confirmed at every layer: `ExecutionDto.correlationId` has zero setters (always null — same dead-field pattern as T304's `triggeredBy`, different semantics: who vs which business flow), `dsl_runs` has no `correlation_id` column (V1–V4), BFF `httpClient.ts` forwards auth/request/trace headers but no correlation header, and FE filter UI + runtime.md have zero references. Related runs (retry chains, saga legs, T307 replays, multi-step business flows launched as separate top-level runs) are connectable only by timestamp guesswork; the forwarded X-Request-Id is per-HTTP-call and unpersisted. Task: `X-Correlation-Id` header on run (caller-supplied only, validated), nullable column + `(correlation_id, started_at DESC)` index (coordinate V-number with T304), persist + map both repos, populate existing DTO field, `correlationId` filter in `search(...)` + BFF + `useExecutions` + filter UI field, BFF header forward, runtime.md contract note, tests per layer incl. filter round-trip. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T308-correlation-id.md`                  |
| T309 | Backlog | BFF OIDC login flow — browser users can't reach the secured backend | frontend — grep-confirmed gap: all security groundwork shipped and idle — T275 opt-in OIDC guard, T280 BFF `Authorization` pass-through (forwards only what the browser sends; UI never acquires a token), T288 compose Keycloak realm with confidential client `cbs-nova-bff` + dev user (`cbs-nova-realm.json:80`) — yet zero login machinery exists (no login/oauth/callback code anywhere in server or composables). Browser user on the guarded stack gets raw 401s with no auth path; the whole opt-in security story dead-ends at curl. Task: BFF OIDC authorization-code flow — `/api/v1/auth/{login,callback,logout,session}` routes (PKCE + state, code→token exchange), tokens in httpOnly SameSite=Lax cookies (browser never sees them), BFF refresh-on-401 + attach `Authorization` in `proxyToBackend` (composes with T280; document precedence), minimal FE affordance (sign in/out via session endpoint, 401-triggered login), runtime config (`AUTH_ISSUER`/`AUTH_CLIENT_SECRET`) fully optional — inert when unset, zero default-DX change, mocked-Keycloak tests + documented compose e2e recipe with `cbs.security.oidc.enabled=true`. See plan. |  High   | loop  | -      | -          | `./docs/plans/T309-bff-oidc-login-flow.md`               |
| T310 | Backlog | Page-level specs — dashboard + executions list uncovered | frontend (tests) — inventory-confirmed gap: page specs exist for runner, dsl-workbench, and executions detail, but NOT for `pages/index.vue` (dashboard — T278 stat tiles/recent-runs/loading/error, most-visited page) or `pages/executions/index.vue` (list — most task-churned page: T254 filters, T269 auto-refresh while RUNNING, T281 cancel+confirm, T284 pagination). Recent behavior changes verified only via composable specs — template wiring (filter binding, cancel visibility per status, polling start/stop, empty/error states) unpinned. Task: mount specs for both pages following runner/[id] conventions (mocked APIs, testid selectors) — dashboard: tiles+counts, recent-runs links, loading/error/empty; list: rows, filter→params binding, cancel only for RUNNING, fake-timer polling start/stop, error retry, pagination offset; fixes only for proven bugs. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T310-page-specs-dashboard-list.md`          |
| T311 | Backlog | Helper catalog UI — endpoint shipped, no FE consumer | frontend — grep-confirmed gap: backend `DslIntrospectionHandler.helpers()` (`:62-63`, dedicated service+mapper) and BFF route `dsl/helpers/index.get.ts` are fully shipped, yet `useDslApi.ts` contains zero helper functions and nothing in the UI consumes the catalog. The 12 registered helpers (`HttpCallHelper`, `JsonExtractHelper`, `SortRecordsHelper`, `FilterRecordsHelper`, `ArithmeticHelper`, `FormatMessageHelper`, `CurrentTimestampHelper`, …) are invisible to DSL authors writing `ctx.runHelper("<name>", …)` — discovering names, input/output types, or side-effect flags (matters for preview faking) requires reading Java source. Task: `listHelpers()` in `useDslApi` (+spec), `HelperCatalog.vue` component (name/types/side-effect badge/description columns aligned to actual payload, substring search, empty/error/loading states), workbench sidebar entry or tab, testids per convention, render+search vitest specs, verify BFF passthrough of full payload. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T311-helper-catalog-ui.md`                 |
| T312 | Backlog | `dsl_run_transactions` write-only — per-run transaction executions never surfaced | backend — code-confirmed gap: write path alive (platform `DefaultExecutionListener` holds the `TransactionExecutionRepository` and records every `TransactionExecution` during runs; starter wires JDBC impl `DslRunRepositoryConfiguration:102-106` against V3's `dsl_run_transactions`), but read path dead — `findByRunId` (`JdbcTransactionExecutionRepository:55`) has zero business callers, no endpoint, no BFF route, no FE consumer. The table is write-only persistence holding the *structured* per-transaction record (name, input_json, executed_at) — the relational sibling of T296's flat trace strings, which heuristic parsing cannot substitute for. Executions detail for a multi-transaction process shows nothing about which transactions ran. Task: `GET /api/executions/{id}/transactions` (404 unknown run, documented order, keeps detail DTO lean), router + OpenAPI entry, in-memory parity so non-JDBC modes work, BFF proxy route + routeCoverage/routes spec entries, FE transactions section on detail (name/time/collapsible input, PayloadTab pattern, testids), tests per layer. See plan. |  High   | loop  | -      | -          | `./docs/plans/T312-run-transactions-surfacing.md`      |
| T313 | Backlog | Architecture docs miss the entire ops/security layer | docs (backend) — grep-confirmed gap: `architecture-backend.md` (151 lines) mentions NONE of OIDC guard (T275), rate limiting (T287), run cancellation (T274), input validation (T291), payload caps (T279), retention purger (T276), health indicator + Temporal probe (T295), preview timeout (T298), OTel (T277), Micrometer (T264/T282), BFF pass-through (T280), Keycloak realm (T288) — only an incidental "idempotent" in a table. CLAUDE.md routes every backend task to these docs first; the documented architecture describes a pre-ops-era system, forcing every task to rediscover structure from source. Task: new "Security" section (opt-in model, filter order, Keycloak pointer), new "Observability & operations" section (metrics per path, tracing, health incl. Temporal probe + fail-status, retention, caps, preview timeout), drift-fix existing construct/module tables against current code, architecture-ui.md BFF security/proxy notes, cross-link (not copy) to T301 config reference when it lands, every claim cites the class/property it describes. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T313-architecture-docs-ops-layer.md`     |
| T314 | Backlog | Bad query params 500 — `?limit=abc` throws NumberFormatException | backend — code-confirmed gap: `DslExecutionsHandler` parses `limit` (`:61`), `offset` (`:62`), `topProcesses` (`:90`) via raw `Integer::parseInt` — non-numeric values throw `NumberFormatException`, which `DslExceptionHandler` does not map, so `GET /api/executions?limit=abc` returns HTTP 500 with a server-error body. Typo'd or hostile query strings masquerade as server faults: noise in error metrics/logs, wrong signal to consumers. Same family as T291's input validation, one layer up (query string vs body). Task: tolerant parse helper or `@ExceptionHandler(NumberFormatException)` mapping → 400 `ErrorResponse` naming the param + bad value (existing body shape), clamp semantics unchanged, sweep other handlers for the pattern, tests per param (non-numeric → 400 w/ code + param name; numeric → current clamped behavior). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T314-param-hardening.md`                   |
| T315 | Backlog | `finished_at` index for retention purge — cleanup seq-scans the table it shrinks | backend (perf) — code-confirmed gap: V4 indexes cover `(status, started_at)`, `(process_name, started_at)`, `(execution_mode)` — nothing on `finished_at`; the purger's predicate is terminal-status + `finished_at < cutoff` (T276; T305's rework added no index — V5 is `triggered_by`). Every purge cycle seq-scans `dsl_runs`, so cleanup gets more expensive exactly as the table grows — worst when retention is most needed. RUNNING rows have NULL `finished_at`, so a partial index (`WHERE finished_at IS NOT NULL`) serves the predicate with minimal write overhead. Task: migration `Vn__dsl_runs_finished_at_index.sql` (next free number, coordinate with T308's column if it lands first) with partial `CREATE INDEX IF NOT EXISTS`, Testcontainers migration test proving the purge DELETE's plan uses the index on a seeded table (or documented actual-plan justification), no regression on list/stats hot paths, record plan before/after in plan notes. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T315-finished-at-purge-index.md`            |
| T316 | Backlog | Stuck RUNNING runs never resolve — crash-orphaned rows immortal in `dsl_runs` | backend — code-confirmed gap: run status is written only by in-process lifecycle callbacks in `TemporalDslProcessService`; zero `describeWorkflow`/`getHistory`/reconcile paths exist in starter (codegraph-confirmed no matches). T306 graceful shutdown covers clean stops only — crash, `kill -9`, OOM, or worker-host loss orphans `RUNNING` rows forever: `JdbcDslRunRepository` CAS (`:164`,`:182`) never sees the terminal write, stats (`ExecutionStatsResponse`) count ghosts as active, executions list shows RUNNING eternally, and the retention purger's predicate (terminal + `finished_at < cutoff`) can NEVER remove them — NULL `finished_at` rows are immortal, unbounded growth exactly when things went wrong (compounds T305/T315). Task: scheduled reconciliation executor (sibling of `cbsNovaDslRunRetentionExecutor`) scanning RUNNING rows past a configurable grace period, Temporal `WorkflowExecution.describe`/history lookup mapping completed/failed/timeout/cancelled/not-found → terminal status + best-effort `finished_at`, batch idempotent CAS update via existing repo pattern, config knobs for interval/grace, per-close-state unit tests, runtime.md lifecycle note (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T316-stuck-running-run-reconciliation.md` |
| T317 | Backlog | Definition publish history + restore — publish overwrites the only copy | backend — code-confirmed gap: `DslDraftHandler.publish` (`:73`) does `writePayload(published/<safeName>.json)` — flat in-place overwrite of `.workbench/published/<name>.json`, no prior copy anywhere (codegraph-confirmed: zero version-history/rollback logic; only JDBC `Connection.rollback` exists). Registry reload is atomic (T255) but the file itself is destroyed — last-good definition recoverable only out-of-band (git / editor undo). Edit a draft after publishing, publish a broken variant → last-good gone with no trace. Task: snapshot previous published file on every publish to per-definition history (`.workbench/history/<name>/<ts>.json`), bounded retention (keep-N prune, configurable), `GET` history list + `POST` restore-by-timestamp reusing publish semantics (failed compile must NOT clobber current published, mirror T297 failed-reload rule), `safeFileName`-consistent traversal safety, BFF proxy routes, FE workbench history panel optional stretch, unit tests (snapshot-on-publish, prune, restore round-trip, broken-restore leaves current intact), runtime.md publish-section note (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T317-definition-publish-history.md` |
| T318 | Backlog | Temporal Schedules — no way to run a definition on a schedule | backend — code-confirmed gap: zero Temporal `Schedule`/`ScheduleClient` usage in starter (codegraph query returns only the two unrelated `@Scheduled` executors `cbsNovaDslProcessHealthcheckExecutor`/`cbsNovaDslRunRetentionExecutor`); `TemporalDslProcessLauncher` is one-shot `startWorkflow` only, so every run requires a manual trigger (API call / workbench button) — an orchestration engine with no recurring-run story. Temporal's native Schedule API gives dedup/overlap/catchup machinery for free. Task: `POST/GET/DELETE /api/v1/dsl/schedules` backed by `ScheduleClient` (definition name + cron/interval + optional static input), decide+document overlap policy (default Skip), catchup policy, timezone (default UTC), schedule-ID naming; runs launched by schedule attributed `triggeredBy=schedule` via T304's `RunIdentityResolver`; validation (published definition, cron spec → 400, conflict → 409); BFF proxy routes + minimal FE workbench schedules panel (list/create/delete); tests service (mocked ScheduleClient) + handler + BFF; runtime.md schedules section (APPEND, never rewrite). See plan. |  High  | loop  | -      | -          | `./docs/plans/T318-temporal-schedules.md` |
| T319 | Backlog | Definition export/import bundle — no env-to-env migration path | backend — code-confirmed gap: zero export/import bundle symbols anywhere (codegraph `export` query returns only OTel span-exporter imports); `DslDraftHandler` publishes one definition at a time via `writePayload(published/<name>.json)` with no bulk read-out or write-in, so published definitions are stranded on the local `.workbench/` dir — fresh compose environment comes up empty and must be rebuilt definition-by-definition by hand, dev→staging→prod migration impossible. Task: `GET /api/v1/dsl/definitions/export` JSON bundle (engine/format version, exported-at, per-definition entries; drafts optional via `?include=drafts`), `POST .../import` with per-entry compile validation + per-entry result (published / failed+diagnostic, no silent partials) and `?dryRun=true` report-only mode, clear 400s for bad format/unsupported version, flow through the existing publish path so T317 history snapshots apply (no direct file writes), BFF proxy routes, handler/service/BFF tests, runtime.md section (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T319-definition-export-import-bundle.md` |
| T320 | Backlog | Run time-series stats — dashboard blind to run-rate/failure trend | backend — code-confirmed gap: `DslRunStatsRepository` exposes exactly one method `stats(windowStart, topProcessesLimit)` (trailing-window counters + top-process counts); no bucketed/time-series aggregation anywhere (no `date_trunc`/bucket/timeseries code — grep-confirmed), `ExecutionStatsResponse` (`:18`) carries nothing chartable over time, and the FE dashboard renders counters only — run-rate spikes and failure bursts are invisible between window snapshots. Task: `GET /api/v1/dsl/executions/stats/timeseries?windowHours&bucketMinutes` (validated + clamped params, T314 lesson) → per-bucket per-status counts via `date_trunc` on `started_at` (V4 `(status, started_at)` index serves it), zero-filled empty buckets for a uniform FE axis, in-memory repo parity, BFF proxy route + composable, FE dashboard trend chart (stacked/multi-series by status, `dataviz` skill when charting), tests repo bucketing (boundaries, empty buckets, clamp) + handler + BFF + FE render smoke. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T320-run-timeseries-stats.md` |
| T321 | Backlog | Executions CSV export — no bulk extraction path for run history | backend — code-confirmed gap: zero csv/CSV symbols anywhere in backend or frontend (codegraph + grep confirmed); executions list endpoint returns JSON page DTOs only and the FE list has no export affordance — run data leaves the system only row-by-row via the detail endpoint, so reporting/auditing outside the UI means hand-rolled DB queries. Task: `GET /api/v1/dsl/executions/export.csv` accepting the list filters (status, processName, correlationId post-T308, time bounds; validated/clamped T314-style), RFC 4180 quoting/escaping (input/output JSON contains commas/quotes/newlines), configurable row cap with `X-Export-Truncated: true` when hit, optional char-budget truncation of input/output JSON (decide + document default), proper `text/csv` + `Content-Disposition` headers, columns runId/processName/status/mode/triggeredBy/correlationId/startedAt/finishedAt/duration/error, BFF passthrough route (content-type preserved, not JSON-wrapped), FE export button honoring active filters, tests: quoting/escaping (comma/quote/newline/unicode), filters, cap+header, BFF, FE render. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T321-executions-csv-export.md` |
| T322 | Backlog | Run-completion webhooks — consumers can only poll for outcomes | backend — code-confirmed gap: zero webhook/notification/callback-delivery code anywhere (codegraph `Webhook` query: no results; no webhook task in kanban history); `TemporalDslProcessService`'s terminal-state write is the natural hook point but nothing observes it externally, so downstream systems and alerting must poll the executions list — latency + load for everyone. Task: opt-in config-driven subscriptions (definition name/pattern, URL, optional HMAC secret, optional status filter; empty default = zero behavior change), async delivery off the run path (enqueue at terminal write, executor delivers), HMAC-SHA256 `X-Cbs-Signature` + timestamp anti-replay headers, bounded retries with backoff, HTTPS-only by default (dev plain-HTTP flag), fixed payload schema `{event, runId, definition, status, startedAt, finishedAt, error?}` with NO input/output by default (secrets), delivery log for debugging, tests (signature, retry on 5xx/timeout, filter exclusion, slow-callback doesn't block run path, config-off = zero side effects, WireMock/MockWebServer), runtime.md webhooks section (APPEND, never rewrite). See plan. |  High  | loop  | -      | -          | `./docs/plans/T322-run-completion-webhooks.md` |
| T323 | Backlog | Workbench new-definition + template gallery — authoring has no bootstrap | frontend — code-confirmed gap: `dsl-workbench.vue` drafts panel lists existing drafts only (`dsl-draft-picker`), no create/template/new affordance on the page, while the backend already supports saving a new-named draft (`drafts/[name]/save.post.ts`) and `docs/dsl/examples.md` documents Java examples not workbench JSON starters — first-run operator authors from blank editor or hand-copied existing definition. Task: verify-first (confirm no new-draft path hides in composables; else templates-only scope), "New definition" action with validated name input (safe filename chars, draft collision → inline error, opens editor pre-filled + dirty so T292 guard applies), 3–4 curated JSON skeletons (plain process, transaction+saga, http pipeline, retry policy) as FE-static assets preferred (zero BE surface), template bodies spec-validated to compile cleanly, vitest (gallery render, selection populates editor, name validation, collision), examples.md pointer (APPEND, never rewrite). See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T323-workbench-template-gallery.md` |
