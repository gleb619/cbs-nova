# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                       |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:----------------------------------------------------------------|
| T52  | Done        | Full DSL Examples integration test with Temporal & Keycloak  | Add `backend/integration-tests` module that compiles DSL from `dsl-examples`, starts Temporal and Keycloak in Testcontainers, and exercises generated DSL workflows via secured REST endpoints.                                                                                                     |   High   | loop  | -                       | -             | `./docs/plans/T52-dsl-examples-full-integration-test.md`        |
| T69  | Done        | BPMN/Mermaid diagram generator tests                         | Add tests for `BpmnDiagramGenerator` and `MermaidDiagramGenerator` covering compensation, external calls, and call counts consumed by explain/runner.                                                                                                                                               |   High   | loop  | -                       | -             | `./docs/plans/T69-bpmn-diagram-tests.md`                        |
| T73  | Done        | DSL input JSON Schema generation                             | Generate JSON Schema (Draft 2020-12) from a DSL definition's `ParameterDescriptor`/`MapInput` input shape and expose it via the introspection REST endpoint so the Runner UI (T58) can render a typed, auto-generated input form instead of guessing the shape.                                     |   High   | loop  | -                       | -             | `./docs/plans/T73-dsl-input-json-schema.md`                     |
| T84  | Backlog     | DSL input JSON Schema generation                             | Implement JSON Schema generation from typed records or ParameterDescriptor list and expose it via DslIntrospectionResource detail endpoints.                                                                                                                                                        |   High   | loop  | -                       | -             | `./docs/plans/T84-json-schema-from-parameter-descriptors.md`    |
| T86  | Backlog     | BPMN diagram generator tests                                 | Add unit tests for BpmnDiagramGenerator covering compensation, call counts, and process/transaction/helper entry points.                                                                                                                                                                            |   High   | loop  | -                       | -             | `./docs/plans/T86-bpmn-diagram-generator-tests.md`              |
| T90  | Backlog     | CI frontend build                                            | Extend GitHub Actions CI to install pnpm and build the Nuxt admin UI alongside backend steps.                                                                                                                                                                                                       |   High   | loop  | -                       | -             | `./docs/plans/T90-ci-frontend-build.md`                         |
| T75  | Backlog     | Process manager and runner tests                             | Add unit tests for `ProcessManager` and `DefaultProcessRunner` covering dispatch, EXPLAIN metadata, and compensation error wrapping.                                                                                                                                                                |   High   | loop  | -                       | -             | `./docs/plans/T75-process-manager-tests.md`                     |
| T76  | Backlog     | Fix mobile drawer DSL Workbench nav link                     | Correct mobile nav `/workbench` → `/dsl-workbench` and share nav items between sidebar and drawer.                                                                                                                                                                                                  |   High   | loop  | -                       | -             | `./docs/plans/T76-mobile-nav-link-fix.md`                       |
| T78  | Backlog     | HelperManager unit tests                                     | Add focused unit tests for HelperManager and DefaultHelperRunner covering registration, lookup, preview/execute dispatch, and missing-name paths.                                                                                                                                                   |   High   | loop  | -                       | -             | `./docs/plans/T78-helper-manager-unit-tests.md`                 |
| T82  | Backlog     | GlobalManager runtime gap tests                              | Extend GlobalManagerTest to cover transaction/function round-trips, unknown-name failures, sorted name lists, and describeHelper.                                                                                                                                                                   |   High   | loop  | -                       | -             | `./docs/plans/T82-global-manager-runtime-gaps.md`               |
| T91  | Backlog     | Actuator runtime enablement                                  | Promote `spring-boot-starter-actuator` from `compileOnly` to `implementation` in `starter` and expose `/actuator/health` (deep, surfaces the existing `DslHealthIndicator`), `/actuator/info` (DSL + build version), and `/actuator/metrics` so the half-wired observability surface actually runs. |   High   | loop  | -                       | -             | `./docs/plans/T91-actuator-runtime-enablement.md`               |
| T93  | Backlog     | Schema field on definition details                           | Wire `inputSchema` into `DslIntrospectionResource` detail records and the BFF `/api/v1/dsl/definitions` route so the Runner UI can render typed `InputForm` from schema instead of guessing shape.                                                                                                  |   High   | loop  | -                       | -             | `./docs/plans/T93-schema-field-on-definition-details.md`        |
| T94  | Backlog     | Actuator runtime enablement                                  | Promote `spring-boot-starter-actuator` from `compileOnly` to `implementation` in `starter`, add `application.yml` exposure config, wire `DslInfoContributor`, and add boot smoke tests for `/actuator/health`, `/actuator/info`, and `/actuator/metrics`.                                           |   High   | loop  | -                       | -             | `./docs/plans/T94-actuator-runtime-enablement.md`               |
| T102 | Backlog | CI backend test-completeness | Extend `.github/workflows/ci.yml` to run the full backend test matrix: add `:dsl-api:test` and `:misc-codegen:build`/`:misc-codegen:test` (currently `misc-codegen` and `:dsl-api:test` are never exercised in CI, so `HelperSpiProcessorTest`, `DslExceptionTest`, `MapInputTest`, `ResultTest` run only locally), and fix the deprecated `cache: gradle` to `gradle: true`. | High | loop | - | - | `./docs/plans/T102-ci-backend-test-completeness.md` |
| T103 | Backlog | Temporal service in docker-compose | Add `temporal` (auto-setup image) and `temporal-ui` services to the root `docker-compose.yml` and a shared network so `docker compose up` yields a full local runtime (Temporal is the workflow engine every worker/example/DSL run depends on, yet it is currently absent from the compose stack). | High | loop | - | - | `./docs/plans/T103-temporal-docker-compose.md` |
| T104 | Backlog     | BFF DSL reload proxy                                       | frontend — Add Nitro BFF route `POST /api/v1/dsl/reload` that proxies to Spring Boot `POST /api/dsl/reload`, wire `useDslApi.reload()` and a Refresh action in `useDslWorkbench`, and add a Refresh button to the DSL Workbench toolbar. |   High   | loop  | -                       | -             | `./docs/plans/T104-bff-dsl-reload-proxy.md`                     |
| T105 | Backlog | Dev orchestration (make dev) | Add a Makefile/Taskfile + `scripts/dev.sh` that brings up the full local dev stack in order — `docker compose up` (Keycloak/Bugsink/Temporal once T103 lands), backend `./gradlew :starter:bootRun`, frontend `pnpm dev` — with healthcheck waits so new contributors get a one-command `make dev` instead of juggling three terminals. No production code touched. | High | loop | - | T103 | `./docs/plans/T105-dev-orchestration.md` |
| T106 | Backlog | Starter Dockerfile | Add a multi-stage Dockerfile that builds `backend/starter` and packages it as the `cbs-nova:latest` image the compose `spring-app` service already references (currently nonexistent), so `docker compose up` + the `make dev` orchestration (T105) yield a fully running local stack including the Spring Boot app. | High | loop | - | T103, T105 | `./docs/plans/T106-starter-dockerfile.md` |
| T107 | Backlog | Root README quickstart | Expand the current 2-line `README.md` stub into a real project front door: what cbs-nova is, prerequisites (Java 25, pnpm, Docker), one-command local stack (`make dev` once T105 lands / `docker compose up`), key ports, architecture-doc pointers (`docs/architecture-*.md`), where DSL examples live, and how to run backend/frontend tests. | High | loop | - | T105, T106 | `./docs/plans/T107-root-readme-quickstart.md` |
| T108 | Backlog | ExecutionTraceCollector runId-scoped trace | Replace `ThreadLocal<List<String>>` in `ExecutionTraceCollector` (shared bean) with a runId-keyed store so traces survive Temporal worker thread-pool reuse, activity/workflow boundary crossings, and replay without bleeding between runs or leaking across reused threads. Fixes a latent correctness bug in the data behind explain/runner and the T69/T86 diagram tests. | High | loop | - | - | `./docs/plans/T108-execution-trace-runid-scoped.md` |
| T109 | Backlog | admin-ui composable tests | Add first Vitest suite for `frontend/admin-ui/app/composables/` (`useDslApi`, `useRunner`, `useExecutions`, `useDslWorkbench`) — the UI's core data-fetch and state layer, currently at 0 tests despite a working `vitest.config.ts`. Mock `$fetch`, assert endpoint URLs/methods/bodies, `useRunner` status transitions and `NO_DEFINITION` guard, and isolate shared module-level `ref` state per test. | High | loop | - | - | `./docs/plans/T109-admin-ui-composable-tests.md` |
| T110 | Backlog | BFF proxy hardening (timeout + request-id) | Harden `frontend/admin-ui/server/utils/httpClient.ts` (`proxyToBackend`, the only BFF→Spring egress for the whole UI): add outbound `timeout` (default `backendTimeoutMs`) yielding 504 `BACKEND_TIMEOUT`, forward/generate `x-request-id` + `traceparent` from the incoming `H3Event` (currently discarded as `_event`), preserve `X-Api-Key` behavior, and add the first `httpClient` unit tests covering success, error mapping, timeout, and header propagation. | High | loop | - | - | `./docs/plans/T110-bff-proxy-hardening.md` |
| T111 | Backlog | Substitutor unit tests | Add first tests for `dsl-api/utils/Substitutor` — the `${var}` template engine on the critical path of all Java codegen (Process/Transaction/DefinitionProvider/HelperSpi), all 3 diagram generators, and runtime `PropertyResolver` (~15 call sites, 5 modules), currently at 0 tests. Pin value escaping (`$`/`\` via `quoteReplacement`), null→"" coercion, missing-placeholder default-throw + custom handler, empty/adjacent/repeated keys, and builder fluency. | High | loop | - | - | `./docs/plans/T111-substitutor-unit-tests.md` |
| T112 | Backlog | DslWorkerConfiguration lifecycle + graceful shutdown | Fix thread-leak bug in `starter/config/DslWorkerConfiguration`: `WorkerFactory` is a discarded local var, `factory.start()` is a side effect in the bean method, and there is no `@PreDestroy`/`SmartLifecycle`/`DisposableBean` anywhere in the backend (grep-confirmed) — so Temporal poller threads leak on every app stop. Expose `WorkerFactory` as a managed bean, move `start()` to a `SmartLifecycle` hook, and wire `shutdown()` on context close + test it. | High | loop | - | - | `./docs/plans/T112-worker-factory-lifecycle.md` |
| T113 | Backlog | Temporal DSL service layer tests | Add first unit tests for the three untested classes in `starter/services/` — `TemporalDslService` (execute/resolveProcess/prepareInput/findRunMethod + RuntimeException error-cause wrapping), `TemporalDslProcessService` (runProcess delegating to GlobalManager + runId generation via contextFactory), and `TemporalDslProcessLauncher` (canRun workflow-thread detection, launch with WorkflowOptions/EXECUTION_TIMEOUT+TASK_TIMEOUT, Result.failure/DslExecutionException wrapping). Core DSL↔Temporal seam, just refactored (ed55763, e3d1dff), currently at 0 tests across ~250 lines. Mock WorkflowClient + GlobalManager, assert timeout values, error-cause unwrapping, and no-generated-process rejection. | High | loop | - | - | `./docs/plans/T113-temporal-service-layer-tests.md` |