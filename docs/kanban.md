# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                               |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:------------------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`                     |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture             | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                                                                                                                                  |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md`            |
| T222 | Done    | DSL Workbench save/publish backend design                                | frontend/backend — `useDslWorkbench.ts` `saveConstruct()`/`publishConstruct()` are stubs (no backend endpoint); the DSL is compile-time-authored from Java source today, so "saving" an edit needs a product/architecture decision on persistence model and publish semantics before any plan or code is written. See plan file.                                                                                                                             |  Medium  | loop  | -      | -          | `./docs/plans/T222-workbench-save-publish-backend-design.md`            |
| T245 | Done    | Fix stale `/api/dsl/helpers/search` doc references                       | frontend (docs) — `docs/architecture-ui.md` still describes the helper-search surface as `GET /api/dsl/helpers/search`; the route was renamed to `/api/dsl/objects/search` (backend fixed in T231, BFF proxy + frontend consumer already use the new path). Only the doc text lags. Docs-only change. See plan.                                                                                                                                              |   Low    | loop  | -      | -          | `./docs/plans/T245-fix-stale-helpers-search-doc-path.md`                |
| T246 | Done    | Component tests for remaining DSL workbench panels                       | frontend — T235 covered 4 of 10 `frontend/components/src/components/dsl/` components and explicitly flagged the other six (`BodyEditor`, `CodeTab`, `ConstructExplorer`, `HelperSearchPanel`, `InputMappingGrid`, `MetadataPanel`) as a follow-up; all six still have zero specs. Test-only change. See plan.                                                                                                                                                |  Medium  | loop  | -      | -          | `./docs/plans/T246-remaining-dsl-panel-component-tests.md`              |
| T247 | Done    | Resolve `SimpleExpressionEvaluator`'s deprecated-but-live-default status | backend — `SimpleExpressionEvaluator` carries `@Deprecated(forRemoval = true)` yet remains the wired default evaluator for platform-standalone runtime, and T241 just hardened 38 tests pinning its behavior. Needs a human decision: un-deprecate, replace with a real successor, or commit to a removal timeline. See plan.                                                                                                                                |  Medium  | loop  | -      | -          | `./docs/plans/T247-simple-expression-evaluator-deprecation-decision.md` |
| T248 | Done | Unit test for `DslSyncedDirs`                                            | backend — `backend/dsl-plugins/dsl-idea-plugin/.../DslSyncedDirs.java` (31 lines, tracks synced DSL/model dirs for the IntelliJ plugin) has no dedicated test while its sibling classes in the same package all do. Test-only change. See plan.                                                                                                                                                                                                              |   Low    | loop  | -      | -          | `./docs/plans/T248-dsl-synced-dirs-test.md`                             |
| T249 | Done | Unit tests for untested `DslExecutionPipeline` stages                    | backend — 8 of 11 classes in `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/stage/` have zero test references: `PreviewCacheStage`, `PreviewCacheKeyBuilder` (78 lines of cache-key canonicalization), `MetricsStage`, `ExecutionTraceStage`, `ExecutionTreeStage`, `ExplainReportStage`, `ExternalCallRecordingStage`, `PreviewReportStage`. Only `DispatchStage`/`FakingStage`/`DryRunLogStage` are covered. Test-only change. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T249-pipeline-stage-tests.md`                             |
| T250 | Done | Honor per-call `followRedirects` in `HttpCallHelper`                     | backend — `HttpCallIn.followRedirects` is documented (`HttpCallIn.java:33/82`, `effectiveRedirects()`) and `HttpCallHelper.toJdkRedirects()` (`HttpCallHelper.java:185`) maps it, but the helper uses a single shared `HttpClient.newHttpClient()` (JDK default NEVER, injected at `DslAutoConfiguration.java:111-113`) — the user-supplied redirect policy is silently ignored. Fix with per-policy client selection/cache + WireMock redirect tests. Bug fix + tests. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T250-http-follow-redirects.md`                              |
| T251 | Done | Enforce `X-Api-Key` on backend API (opt-in)                             | backend (security) — BFF sends `X-Api-Key` on every proxied call (`frontend/admin-ui-plugin/server/utils/httpClient.ts:45`, `BACKEND_API_KEY` in env examples) but backend has zero consumers of the header — only servlet filter is `RequestIdFilter`, no security dependency — while port 8090 is published to the host (`app/compose/app.yml:9`) with unauthenticated state-changing endpoints incl. DSL reload/compile (`DslReloadHandler`). Task: opt-in `dsl.auth.api-key` filter validating the header across all RouterFunctions routes + MockMvc tests; default off so host apps don't break. See plan. |   High   | loop  | -      | -          | `./docs/plans/T251-backend-api-key-enforcement.md`                      |
| T252 | Done | Log correlation: render MDC `rid` + stop dropping stack traces         | backend (observability) — `RequestIdFilter` puts `rid` into MDC and the BFF forwards `X-Request-Id` (`httpClient.ts:49-51`), but `starter-launcher/logback-spring.xml` never prints `%X{rid}` — correlation id captured but invisible. Worse, `DslExceptionHandler.java:27/34/40/46` logs `ex.getMessage()` without passing the throwable, so 500 stack traces are permanently lost. Add `rid` to log pattern + pass exceptions as last log arg. Small diff, high debug value. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T252-log-rid-and-stacktraces.md`                          |
| T253 | Done | User-facing error states + global `error.vue`                          | frontend — `useExecutions.ts:191-194`/`:210-213` swallow all load failures (log + blank the list, no `error` ref), executions pages (`app/pages/executions/index.vue`, `[id].vue`) have zero error handling — a backend outage renders as a silent empty table. No `error.vue` exists anywhere under `frontend/admin-ui-plugin/`, so unhandled render errors show Nuxt's raw default page. Task: `error` refs + retry in load composables, error banners in list/detail pages, branded `app/error.vue`. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T253-frontend-error-states.md`                             |
| T254 | Done | Executions list filters are a silent end-to-end no-op                  | fullstack — filter UI (`ExecutionFilters.vue:19-21`) emits status/mode/entityName and `useExecutions.applyFilters` sends them (`useExecutions.ts:177`), but the BFF proxy forwards only offset/limit (`server/api/v1/executions/index.get.ts:5-12`) — every filter dropped. Even if forwarded, backend `DslExecutionsHandler.list` (`DslExecutionsHandler.java:42-49`) compares raw enum names (`RUNNING`) while UI sends `Completed`, and mode filtering is unsupported anywhere. Task: forward filters in BFF, normalize status casing in handler, decide mode filter support or remove from UI. See plan. |   High   | loop  | -      | -          | `./docs/plans/T254-executions-filters.md`                               |
| T255 | Backlog | Atomic DSL reload — don't brick registry on compile failure            | backend — `DslReloadHandler.java:74` calls `GlobalManager.resetForTests()` *before* `doReload()` compiles; on compile failure (throw at :148) the registry stays empty and every DSL endpoint 404s until a successful reload — and the workbench publish path (`DslDraftHandler.java:76`) triggers it directly, so publishing one broken construct takes down the runtime. Same file: `Files.createTempDirectory` (:126) never deleted, no concurrent-reload guard. Task: compile into fresh state, swap atomically, delete temp dir in finally, add lock. See plan. |   High   | loop  | -      | -          | `./docs/plans/T255-atomic-dsl-reload.md`                                |
| T256 | Backlog | STALE can overwrite COMPLETED — status write race in `dsl_runs`       | backend — `TemporalDslProcessService.healthcheckSweep` (`:166-208`) reads RUNNING rows then async-calls `updateFinished(..., STALE, ...)` racing the completion write from `doExecuteAndRecord` (`:255`), and `JdbcDslRunRepository.getUpdateStatement` (`:142-151`) has no `WHERE status='RUNNING'` guard — last writer wins, so a finished COMPLETED run can be permanently flipped to STALE. Task: conditional UPDATE for stale-marking + affected-row-count check; test for the race. See plan. |   High   | loop  | -      | -          | `./docs/plans/T256-stale-overwrite-race.md`                              |
| T257 | Ready | `data-testid` pass: shell + shared components                     | frontend — 14 root/dashboard/DropdownMenu components under `frontend/components/src/components/` lack `data-testid` debug attributes. Convention: `data-testid` (existing codebase standard, NOT `data-test-id`), root = kebab-case component name, children = `<root>-<role>`, dynamic via template literal; never rename existing ids. Sibling tasks T258–T261. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T257-testid-shell-components.md`                           |
| T258 | Ready | `data-testid` pass: `dsl/` components                            | frontend — 12 `frontend/components/src/components/dsl/` components need `data-testid` audit/fill (`ConstructExplorer`, `PlainConstructListSkeleton` partial; skeleton verify-only). Naming convention in plan; never rename existing ids; smoke assertion per touched spec. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T258-testid-dsl-components.md`                             |
| T259 | Ready | `data-testid` pass: `executions/` components                     | frontend — 12 `frontend/components/src/components/executions/` components need `data-testid` root + interactive-element ids (`StatusBadge` has `stale-poll-indicator` already — keep). Tab components get directory-qualified ids (`executions-logs-tab` etc.) to avoid runner collisions. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T259-testid-executions-components.md`                      |
| T260 | Ready | `data-testid` pass: `runner/` input & control components         | frontend — 9 `frontend/components/src/components/runner/` input/control components (`DefinitionSelector`, `InputField`, `InputForm`, `ModeSwitcher`, `RunConfirmationModal`, `StatusIndicator`, `ResultTab`, `OutputPanel`, `ExplainOutput`) need `data-testid` root + control ids; `OutputPanel` partial already — audit, never rename. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T260-testid-runner-input-components.md`                     |
| T261 | Ready | `data-testid` pass: `runner/` diff/tree/logs components         | frontend — 12 `frontend/components/src/components/runner/` diff/tree/logs components; heaviest existing coverage (`PreviewDiffView` 11 ids, `ASTDiffNode`, `DiffLine`, `MetricsDiffTable`, `ExplainDiffView`, `ExternalCallsTab` partial) — audit + fill gaps only; runner tabs get `runner-*-tab` ids to avoid executions collisions. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T261-testid-runner-diff-components.md`                      |
| T262 | Backlog | `dsl_runs` query indexes — executions list + sweep table-scans      | backend (perf) — `db/migration/V1__create_dsl_runs.sql` defines only PK + unique(`run_id`); zero secondary indexes. Every executions-list query (status/entityName/mode filters from T254 + offset/limit pagination), `findByProcessName`, and the healthcheck RUNNING sweep (`TemporalDslProcessService.healthcheckSweep`) table-scan `dsl_runs`. Task: `V4__dsl_runs_indexes.sql` adding `(status, started_at DESC)`, `(process_name, started_at DESC)`, `(execution_mode)` `CREATE INDEX IF NOT EXISTS`; Testcontainers integration test proving migration applies + filters return. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T262-dsl-runs-query-indexes.md`                            |
