# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture             | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file.                                                                                                                                  |   High   | loop  | T173   | -          | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T254 | Done | Executions list filters are a silent end-to-end no-op                  | fullstack — filter UI (`ExecutionFilters.vue:19-21`) emits status/mode/entityName and `useExecutions.applyFilters` sends them (`useExecutions.ts:177`), but the BFF proxy forwards only offset/limit (`server/api/v1/executions/index.get.ts:5-12`) — every filter dropped. Even if forwarded, backend `DslExecutionsHandler.list` (`DslExecutionsHandler.java:42-49`) compares raw enum names (`RUNNING`) while UI sends `Completed`, and mode filtering is unsupported anywhere. Task: forward filters in BFF, normalize status casing in handler, decide mode filter support or remove from UI. See plan. |   High   | loop  | -      | -          | `./docs/plans/T254-executions-filters.md`                    |
| T255 | Done | Atomic DSL reload — don't brick registry on compile failure            | backend — `DslReloadHandler.java:74` calls `GlobalManager.resetForTests()` *before* `doReload()` compiles; on compile failure (throw at :148) the registry stays empty and every DSL endpoint 404s until a successful reload — and the workbench publish path (`DslDraftHandler.java:76`) triggers it directly, so publishing one broken construct takes down the runtime. Same file: `Files.createTempDirectory` (:126) never deleted, no concurrent-reload guard. Task: compile into fresh state, swap atomically, delete temp dir in finally, add lock. See plan. |   High   | loop  | -      | -          | `./docs/plans/T255-atomic-dsl-reload.md`                     |
| T256 | Done | STALE can overwrite COMPLETED — status write race in `dsl_runs`       | backend — `TemporalDslProcessService.healthcheckSweep` (`:166-208`) reads RUNNING rows then async-calls `updateFinished(..., STALE, ...)` racing the completion write from `doExecuteAndRecord` (`:255`), and `JdbcDslRunRepository.getUpdateStatement` (`:142-151`) has no `WHERE status='RUNNING'` guard — last writer wins, so a finished COMPLETED run can be permanently flipped to STALE. Task: conditional UPDATE for stale-marking + affected-row-count check; test for the race. See plan. |   High   | loop  | -      | -          | `./docs/plans/T256-stale-overwrite-race.md`                  |
| T257 | Done | `data-testid` pass: shell + shared components                     | frontend — 14 root/dashboard/DropdownMenu components under `frontend/components/src/components/` lack `data-testid` debug attributes. Convention: `data-testid` (existing codebase standard, NOT `data-test-id`), root = kebab-case component name, children = `<root>-<role>`, dynamic via template literal; never rename existing ids. Sibling tasks T258–T261. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T257-testid-shell-components.md`               |
| T258 | Done | `data-testid` pass: `dsl/` components                            | frontend — 12 `frontend/components/src/components/dsl/` components need `data-testid` audit/fill (`ConstructExplorer`, `PlainConstructListSkeleton` partial; skeleton verify-only). Naming convention in plan; never rename existing ids; smoke assertion per touched spec. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T258-testid-dsl-components.md`                 |
| T259 | Done | `data-testid` pass: `executions/` components                     | frontend — 12 `frontend/components/src/components/executions/` components need `data-testid` root + interactive-element ids (`StatusBadge` has `stale-poll-indicator` already — keep). Tab components get directory-qualified ids (`executions-logs-tab` etc.) to avoid runner collisions. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T259-testid-executions-components.md`          |
| T260 | Done | `data-testid` pass: `runner/` input & control components         | frontend — 9 `frontend/components/src/components/runner/` input/control components (`DefinitionSelector`, `InputField`, `InputForm`, `ModeSwitcher`, `RunConfirmationModal`, `StatusIndicator`, `ResultTab`, `OutputPanel`, `ExplainOutput`) need `data-testid` root + control ids; `OutputPanel` partial already — audit, never rename. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T260-testid-runner-input-components.md`        |
| T261 | Done | `data-testid` pass: `runner/` diff/tree/logs components         | frontend — 12 `frontend/components/src/components/runner/` diff/tree/logs components; heaviest existing coverage (`PreviewDiffView` 11 ids, `ASTDiffNode`, `DiffLine`, `MetricsDiffTable`, `ExplainDiffView`, `ExternalCallsTab` partial) — audit + fill gaps only; runner tabs get `runner-*-tab` ids to avoid executions collisions. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T261-testid-runner-diff-components.md`         |
| T262 | Done | `dsl_runs` query indexes — executions list + sweep table-scans      | backend (perf) — `db/migration/V1__create_dsl_runs.sql` defines only PK + unique(`run_id`); zero secondary indexes. Every executions-list query (status/entityName/mode filters from T254 + offset/limit pagination), `findByProcessName`, and the healthcheck RUNNING sweep (`TemporalDslProcessService.healthcheckSweep`) table-scan `dsl_runs`. Task: `V4__dsl_runs_indexes.sql` adding `(status, started_at DESC)`, `(process_name, started_at DESC)`, `(execution_mode)` `CREATE INDEX IF NOT EXISTS`; Testcontainers integration test proving migration applies + filters return. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T262-dsl-runs-query-indexes.md`                |
| T263 | Done | DSL MetadataPanel + CodeTab UX improvements | frontend — consolidate TODOs in MetadataPanel.vue and CodeTab.vue: persist MetadataPanel collapse via useLocalStorageState, surface missing DslConstruct metadata fields, replace CodeTab textarea with a syntax-highlighting Vue 3 code editor/display while keeping v-model:code and readOnly behavior. See plan. | Medium | loop | - | - | `./docs/plans/T263-dsl-metadata-code-editor.md`              |
| T264 | Done | Preview metrics → Micrometer; retire deprecated `PreviewMetricsCollector`    | backend (observability) — `PreviewMetricsCollector` is `@Deprecated(forRemoval = true)` yet still the live engine wired via `MetricsStage` + `PreviewMetricsConfiguration` (ThreadLocal instance + static `latestSnapshot`), while Micrometer is already half-adopted (`Gauge`/`MeterBinder` in `PreviewMetricsConfiguration`/`PreviewCacheConfiguration`, actuator dep in launcher) and the compose stack ships Prometheus+OTel — per-run call counts/external calls/duration are invisible to it. Task: record per-run metrics through `MeterRegistry` (counters/timers/tags: kind, mode, process), keep `PreviewMetricsSnapshot` for UI responses, delete or gut the deprecated class + its statics. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T264-preview-metrics-micrometer.md`            |
| T265 | Done | Centralize starter magic strings into `StarterConstant` | backend — Ten classes across `dsl-starter/starter` carry `//TODO` comments to move string literals (context attribute keys and external-call/dry-run-log payload map keys) into `core/StarterConstant.java`. Extract constants, replace literals, reuse existing `ExternalCallRecorder` type constants, resolve the unused `inner` variable TODO in `PreviewReportStage`. No behavior change. See plan. | Medium | loop | - | - | `./docs/plans/T265-centralize-starter-constants.md`              |
| T266 | Done | Mermaid diagram end-to-end — Diagram tab dead, renderer never wired       | fullstack — platform has tested `MermaidDiagramGenerator`, `ExplainDiagramRenderer` wraps it (`@Deprecated`, referenced only by `DevDslRuntimeTest`, TODO at `:21` asks for service + introspection endpoint), yet NO backend endpoint or response ever emits `mermaidDiagram` — while frontend consumes it (`useRunner.ts:104` explain mapping, executions detail `[id].vue:90` `:diagram="selectedExecution.mermaidDiagram"`), so the Diagram tab always renders empty. Task: make renderer a service, expose diagram via explain/run responses or `GET /api/v1/dsl/processes/{name}/diagram?format=mermaid`, add BFF proxy route, verify Diagram tab populates. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T266-mermaid-diagram-end-to-end.md`              |
| T267 | Done | Unbounded `InMemoryTransactionExecutionRepository` — default-wired memory leak | backend — `InMemoryTransactionExecutionRepository` (platform `repository/`) is a `ConcurrentHashMap<runId, List>` with NO cap and NO eviction, and it is the DEFAULT bean (`DslConfiguration.java:77-79` `@ConditionalOnMissingBean`) whenever host app lacks JDBC persistence — while sibling `InMemoryDslRunRepository` caps at 100 and evicts, leaving transaction lists orphaned after their parent run is evicted. Every transaction execution in a long-lived dev/standalone runtime leaks. Task: bound the repo (per-run cap + max tracked runs, mirror sibling's CAPACITY pattern — Caffeine only if dep justified), evict with parent run, tests proving bound + orphan eviction. Class TODO at `:18` requests exactly this. See plan. |  High  | loop  | -      | -          | `./docs/plans/T267-bound-inmemory-transaction-repo.md`         |
| T268 | Done | Replace ad-hoc runtime caches with Caffeine | backend — Eight classes in `dsl-starter/starter` use `ConcurrentHashMap` as a cache plus custom `ScheduledExecutorService` cleanup or manual TTL. Task: add Caffeine to the catalog, introduce `CbsNovaCacheProperties`, replace the maps with bounded/TTL Caffeine caches, remove custom schedulers, preserve public APIs and stats. See plan. | Medium | loop | - | - | `./docs/plans/T268-caffeine-runtime-caches.md`              |
| T269 | Done | Executions list never auto-refreshes — RUNNING rows frozen       | frontend — `app/pages/executions/index.vue:22` calls `loadExecutions()` once on mount (plus error-retry via `ErrorBanner`); no polling anywhere on the list, though the codebase already has the pattern (`useStalePolling` in `useExecutions.ts:50-90`, stale-poll interval config `stalePollMs`). Runs launched from the workbench never appear, RUNNING rows never transition to Completed/Failed without a manual reload. Task: interval refresh of the filtered list while any visible row is RUNNING/Pending (stop when idle), reuse `stalePollMs`-style config, keep filters/pagination, pause when tab hidden. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T269-executions-list-auto-refresh.md`           |
| T270 | Done | Atomic / volatile concurrency cleanup | backend — Five `volatile` fields across `TemporalDslProcessService`, `SingletonSupport.ReplaceableImpl`, `DslWorkerConfiguration.WorkerFactoryLifecycle`, and `DslProjectStateService` have TODOs to redo as atomic. Also resolve the unused `healthcheckHandle` in `TemporalDslProcessService`. Replace with `AtomicReference`/`AtomicBoolean`, remove custom cleanup leaks, preserve public APIs, add focused contention tests. See plan. | Medium | loop | - | - | `./docs/plans/T270-atomic-volatile-concurrency-cleanup.md`              |
| T271 | Done | Compose dependency hygiene — healthchecks + conditional `depends_on` | backend (devops) — `app/compose/app.yml` `depends_on: [bugsink, keycloak, temporal]` uses list form (no conditions) and `spring-app` itself has NO healthcheck despite exposing actuator health (`MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,...`); `orchestration.yml` temporal service also lacks a healthcheck (only postgres + observability containers have them), so `condition: service_healthy` is impossible and the app races Temporal/Keycloak readiness on every `compose up` — worker registrations and early requests fail. Task: add healthchecks (temporal: tctl/TCP 7233; keycloak + bugsink if cheap; spring-app: actuator health w/ start_period), switch app `depends_on` to conditional form, verify clean cold `docker compose up`. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T271-compose-dependency-healthchecks.md`     |
| T272 | Done | `DslDefinitionLoader` drilldown result + reload response detail | backend — `DslDefinitionLoader` returns bare `int` (count) with TODO at `DslDefinitionLoader.java:7` asking for a record with per-type drilldown, and `DslReloadHandler.java:102` discards even that — reload/publish responses say nothing about WHAT loaded (5 processes, 3 transactions, which names), so workbench publish feedback and ops logs are blind. Task: introduce `LoadResult` record (total + per-type counts + names) returned by all three `load` overloads, adapt `ServiceLoaderDslDefinitionLoader` + `CompilingDslDefinitionLoader`, surface it in `DslReloadHandler`/`DslDraftHandler` responses + log lines. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T272-loader-drilldown-result.md`               |
| T273 | Done | Playwright e2e harness — frontend has zero end-to-end tests       | frontend (dx) — no Playwright anywhere in `frontend/`: no dep in any package.json, no config, no e2e dir — all FE tests are vitest unit/component. The T257–T261 `data-testid` passes just landed with no e2e consumer, and BFF↔backend↔UI wiring (T253/T254 fixes) has no regression net. Task: add Playwright (workspace dep + config, webServer against Nuxt dev), 3–4 smoke specs (executions list renders + filters, workbench loads constructs, runner preview happy path, error state on backend down), CI-friendly gating (skip when backend unavailable), use T257–T261 testids as selectors. See plan. |  High  | loop  | -      | -          | `./docs/plans/T273-playwright-e2e-harness.md`                  |
| T274 | Done | Cancel running DSL execution — no user-initiated stop | backend — codegraph-confirmed gap: no `cancel` on any DSL run path; only mechanism is the passive STALE sweep in `TemporalDslProcessService.healthcheckSweep` (`TemporalDslProcessService.java:143-161`), which waits out `staleThreshold` before marking a run STALE. A stuck/looping run can't be stopped from the API or UI; operator must wait for staleness or kill the worker. `WorkflowClient` is already injected in `TemporalDslService`/`TemporalDslProcessLauncher`, and `runRepository.updateFinished` provides the status-write path. Task: `POST /api/v1/dsl/executions/{runId}/cancel` — resolve the run (404 if unknown), call `WorkflowClient.getWorkflowHandle(runId).cancel()`, record CANCELLED (or reuse CANCELLED/STALE per enum) via `updateFinished` with cancel reason, guard against cancelling non-RUNNING rows, add BFF proxy route `executions/[runId]/cancel.post.ts`. UI button is a natural follow-up, not in scope. See plan. |  High  | loop  | -      | -          | `./docs/plans/T274-cancel-running-execution.md`              |
| T275 | Done | Opt-in OIDC guard for DSL REST API — keycloak deployed, API wide open | backend (security) — codegraph-confirmed gap: zero Spring Security wiring in `starter/src/main` (no `SecurityFilterChain`, no spring-security/keycloak dependency anywhere), yet `app/compose/app.yml` runs Keycloak and lists it as a dependency of the Spring app. Every `/api/v1/dsl/*` endpoint — execute run, reload/publish DSL, cancel — accepts anonymous requests from anything that can reach port 8090; the BFF forwards blindly too. Task: add `spring-boot-starter-oauth2-resource-server` + a `SecurityFilterChain` behind an opt-in profile/property (e.g. `cbs.security.oidc.enabled=false` default), JWT validation against the compose Keycloak issuer, permit actuator health + swagger paths unauthenticated, 401 semantics verified; BFF token pass-through documented as follow-up. Default-off keeps local DX and all existing tests unchanged. See plan. |  High  | loop  | -      | -          | `./docs/plans/T275-oidc-guard-dsl-api.md`                    |
| T276 | Backlog | `dsl_runs` retention purge — table grows unbounded, no delete path exists | backend — codegraph-confirmed gap: `JdbcDslRunRepository.java` (166 lines, `starter/src/main/java/cbs/nova/starter/persistence/`) has zero DELETE/purge/retention — grep for delete/purge/retention comes back empty. Every run (with full `input_json`/`output_json`/`context_json` payloads) is kept forever; long-lived deployments bloat Postgres and degrade the very indexes T262 added. Task: configurable retention (`cbs.runs.retention` duration, default e.g. 30d, `0`/disabled escape hatch), scheduled batched DELETE of finished runs (`finished_at < cutoff`, status in COMPLETED/FAILED/STALE — never RUNNING) with LIMIT-style batching to avoid long row locks, purge metrics/counters, integration test proving old finished rows die and RUNNING/young rows survive. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T276-dsl-runs-retention-purge.md`              |
| T277 | Backlog | OTel tracing for DSL runs — Jaeger stack deployed, app emits zero traces | backend (observability) — codegraph-confirmed gap: `starter/build.gradle:55` has ONLY `opentelemetry.api`; no OTel SDK, no OTLP exporter anywhere in starter or platform (grep for otlp/exporter across build files + main sources comes back empty). Meanwhile `app/compose/observability.yml` runs the full stack — otel-collector (`:40`) + Jaeger (`:29`) — all of it decorative: the Spring app ships no spans. A failing run can't be traced end-to-end. Task: add `opentelemetry-sdk` + OTLP exporter (or `micrometer-tracing-bridge-otel`), activate via standard `OTEL_EXPORTER_OTLP_ENDPOINT` env (absent → no-op, default DX unchanged), root span per DSL run with runId/processName/status attributes wired at `TemporalDslProcessService.doExecuteAndRecord`, propagate context into Temporal via existing `WorkflowClient` propagators, compose wiring to otel-collector, verify a run shows as trace in Jaeger. See plan. |  High  | loop  | -      | -          | `./docs/plans/T277-otel-tracing-dsl-runs.md`                  |
| T278 | Backlog | Dashboard lost in admin-ui-plugin migration — landing page is an empty stub | fullstack — regression, NOT a T70/T98 duplicate: both prior dashboard tasks are Done, but against the old `frontend/admin-ui/` tree — the plugin migration dropped the feature. Current `frontend/admin-ui-plugin/app/pages/index.vue` is 9 lines: `<h1>Dashboard</h1>` + welcome text, no data. Backend has no stats endpoint either (`DslExecutionsHandler` = list + detail only), and the old page's client-side counts would miscount past a single page anyway. Task: backend `GET /api/v1/dsl/executions/stats` (total + per-status counts, 24h window failure rate, counts by processName, computed via SQL aggregates not client-side), BFF proxy route, dashboard page with stat tiles (dataviz stat-tile conventions) + recent-runs table linking to `/executions/[id]`, loading/error states per existing component patterns. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T278-restore-dashboard-stats.md`                |
| T279 | Backlog | Run input/output payload size caps — unbounded JSON straight into Postgres and Temporal | backend — codegraph-confirmed gap: `DslRuntimeHandler.run`/`preview` (`DslRuntimeHandler.java:32-45`) deserialize `request.body(DslRequest.class)` with zero size validation, and grep across starter+platform finds no 413/tooLarge/maxBytes guard anywhere. Spring Boot does not cap JSON body size by default — a multi-MB (or hostile) payload is accepted, persisted whole into `dsl_runs.input_json`/`context_json`, and shipped through Temporal workflow execution. Combined with no retention (T276), one bad client bloats the table. Task: configurable `cbs.runs.max-input-bytes` (default ~1MB), reject oversize input with 413 + clear error body before any persistence; configurable `max-output-bytes` with truncate-on-persist (marker + original-size fields, run still completes) so worker-side output can't blow the row; unit tests for both edges, integration test proving oversized input never reaches `dsl_runs`. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T279-run-payload-size-caps.md`                  |
