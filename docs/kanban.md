# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                       |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:----------------------------------------------------------------|
| T164 | Done | Preview result caching with DSL hash invalidation | backend — Implement a caching layer for preview results keyed by DSL source hash and input parameters. Cache invalidation occurs automatically when DSL sources change. Add cache hit/miss metrics via Micrometer. | Medium | loop | - | - | `./docs/plans/T164-preview-result-caching.md` |
| T165 | Blocked | Preview execution sandboxing | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options. | High | loop | - | - | `./docs/plans/T165-preview-execution-sandboxing.md` |
| T166 | Done | Frontend preview diff visualization | frontend — Add a preview diff component that compares two preview runs (e.g., before/after DSL changes). Shows side-by-side output, AST diff, external call diff, and performance metrics comparison to help developers understand DSL changes impact. | High | loop | - | - | `./docs/plans/T166-frontend-preview-diff-visualization.md` |
| T167 | Done | Codegen & DSL internals cleanup | backend — Remove reflective helpers across codegen/SPI/helper resolution/transaction invoker/test fixtures (typed info instead) and clean small DSL-core internals (generator `var` hack, experimental `DefinitionLoader` branch, missing `SingletonSupport` util). Foundation for the JSON/Avaje converter (T171). | High | loop | T171 | - | `./docs/plans/T167-codegen-dsl-internals-cleanup.md` |
| T168 | Blocked | Preview/Explain runtime event-listener & report architecture | backend — Plan is stale: T162/T163/T164 already layered metrics/error-handling/caching directly onto `DevDslRuntime.preview()`'s current structure, and T173 (listed as blocked by this) shipped without it. Needs a replanning pass reconciling the SPI design with the new wrapping order before dispatch. See plan file. | High | loop | T173 | - | `./docs/plans/T168-preview-explain-listener-architecture.md` |
| T169 | Done | Temporal DSL service lifecycle & transaction invoker cleanup | backend — Convert `TemporalDslService`/`TemporalDslProcessLauncher` to `@Bean` lifecycle, parameter-object records, single `WorkerFactory`, typed `DslException`; harden `TemporalTransactionInvoker` (memory-leak fix, logging, Temporal/non-Temporal split, DB-queue caller, DSL-config field reuse). | High | loop | T170 | - | `./docs/plans/T169-temporal-service-lifecycle-invoker-cleanup.md` |
| T170 | Done | Async process-service redesign | backend — Make `TemporalDslProcessService` non-blocking (pending/running results), add configurable clock service, healthcheck thread with `STALE` status, typed empty results, async DB ops on a custom Spring thread pool with MDC/Sentry/OTel propagation, and align `DevDslRuntime` finish signal with async execution. | High | loop | - | T169 | `./docs/plans/T170-async-process-service-redesign.md` |
| T171 | Done | JSON / Avaje serialization unification (reduced scope — see plan) | backend — Scope reduced: T167/T169/T170 already removed every ad-hoc conversion TODO this depended on, leaving only one Jackson call site — building a new codegen converter for it was dropped as disproportionate. Delivered: `JsonExtractHelper` now takes an injected `ObjectMapper` constructor param (matches `HttpCallHelper`'s pattern). | High | loop | - | T167 | `./docs/plans/T171-json-avaje-serialization-unification.md` |
| T172 | Done | Helper library hardening | backend — Relocate `SpringBeanHelperInstanceResolver`, bound/auto-clean state in compensation/unreliable-API helpers, Temporal-aware time/latch, resolver+MVEL formatting, richer `HttpCallHelper`, configurable sort, `BigDecimal` universal math, scenario-driven `UnreliableApiHelper`. | High | loop | - | - | `./docs/plans/T172-helper-library-hardening.md` |
| T173 | Done | Dry-run logging productionization | backend — Configure `DryRunLogbackAppender` via Spring constructor params, emit a typed `DryRunLogEvent`, remove empty ctors/setters, use `GlobalManager` context, tag `runId`; turn `DryRunLoggingContext` into a `ScopedValue` interface with Temporal multi-node support; replace naive `DevDslRuntime` tracing. | High | loop | - | T168 | `./docs/plans/T173-dry-run-logging-productionization.md` |
| T174 | Done | Spring Boot autoconfiguration & starter packaging cleanup | backend — Make `starter` a proper library: single root autoconfig via `@Import`, move `application.yml`/`logback-spring.xml`/`StarterApplication` to integrationTest, SPI worker wiring, build-info OpenAPI version, `PropertyResolver` test, `RouterFunction` reload resource. | High | loop | T175, T176 | - | `./docs/plans/T174-spring-boot-autoconfig-packaging-cleanup.md` |
| T175 | Done | REST controllers & observability improvements | backend — Sentry integration in `DslExceptionHandler` with runId tags, a `/helpers/search` endpoint, classloader+SPI DSL reload, and `runId` propagation into MDC/Sentry/OpenTelemetry tags and headers across async boundaries. | High | loop | - | T174 | `./docs/plans/T175-rest-controllers-observability.md` |
| T176 | Done | Run persistence hardening | backend — Relocate/configure `DslRunEntity` schema, add app-level field encryption, JSON trace/AST/call-tree column, `@Bean`-created `JdbcDslRunRepository`, MapStruct entity/DTO conversion, and targeted update method instead of full re-save. | High | loop | - | T174 | `./docs/plans/T176-run-persistence-hardening.md` |
| T177 | Done | Frontend helper-search wiring | frontend — Surface the orphaned backend `/api/dsl/helpers/search` endpoint (T175) in the admin UI: BFF proxy route, `useHelperSearch` composable, and a search panel listing registered DSL helpers/processes/transactions/functions by name/type/description. | Medium | loop | - | - | `./docs/plans/T177-frontend-helper-search.md` |
| T178 | Done | JDBC capture invocation-handler tests | backend — Add pure-unit tests for the DataSource call-capture proxies (`ConnectionInvocationHandler` 196 lines + `PreparedStatementInvocationHandler` 105 lines, both 0 tests): record-vs-forward behavior, SQL source + op classification, statement re-proxying on `prepareStatement`/`prepareCall`, non-recorded method passthrough, exception propagation. Mockito proxies, no DB. Characterization foundation for the T168 capture refactor. | Medium | loop | - | - | `./docs/plans/T178-jdbc-capture-handler-tests.md` |
| T179 | Done | Executions panel component tests | frontend — The entire `executions/` panel (13 components, 0 specs) — core post-run user surface — has no Vitest coverage. Pin the leaf/presentational layer first: `StatusBadge`, `ExecutionFilters`, `ErrorsTab`, `MetadataTab`, `PayloadTab` (pure props/emits, no async). Status→color map, filter emit shape, error/code/stack-trace toggle, metadata row flattening, payload JSON formatting + undefined handling. Container components (ExecutionList/Trace/Summary) deferred to a follow-up. | Medium | loop | - | - | `./docs/plans/T179-executions-panel-component-tests.md` |
| T180 | Done | Runner panel leaf component tests | frontend — The Runner panel (primary interactive surface) leaf components have no specs (only OutputPanel tested; executions panel is T179). Pin the pure presentational leaves: `ResultTab`, `ModeSwitcher` (RunnerMode v-model + active styling), `DiffLine` (DiffLineKind → color/prefix + line numbers), `StatusIndicator` (RunnerStatus), `ExternalCallsBadge` (count), `ASTDiffNode` (recursive depth + expand/collapse). Pure props/emits, no store. Container/async components deferred. | Medium | loop | - | - | `./docs/plans/T180-runner-leaf-component-tests.md` |
| T181 | Backlog | TemporalTransactionInvoker tests | backend — `TemporalTransactionInvoker` (69 lines, 0 unit tests) is the DSL-tx→Temporal-Activity bridge, just hardened by T167 (reflection removal) + T169 (logging). 12 callers cover it only indirectly via integration tests. Pin the unit-testable branches: fallback path (tx/generated missing → warn + delegate to runTransaction) and retry-option building (tx retryPolicy vs DslConfig default → ActivityOptions; startToCloseTimeout/taskQueue). Cause-unwrap + bad-stub branches via integration harness — `Workflow.newActivityStub` is not unit-mockable. Behavior-pinning characterization for the fresh hardening. | Medium | loop | - | - | `./docs/plans/T181-temporal-transaction-invoker-tests.md` |
| T182 | Backlog | Fix definitions introspection wiring | backend — Broken: `useDslApi.fetchDefinitions()` → BFF `/api/dsl/definitions` → non-existent backend endpoint (DslIntrospectionResource exposes /processes, /transactions, /helpers — no /definitions). `DefinitionSelector` (primary runner definition picker) gets nothing. Add `GET /api/dsl/definitions` aggregator (reuse GlobalManager iteration from searchHelpers, map to {name,type} DefinitionMeta) + OpenAPI + endpoint test + BFF route spec. Complements T177 (search) — fixes the list/picker path. | High | loop | - | - | `./docs/plans/T182-fix-definitions-introspection-wiring.md` |
| T183 | Backlog | Architecture docs refresh | docs — `architecture-backend.md` (Jul 19) + `architecture-ui.md` (Jul 12) badly drifted: backend doc mentions 0 of async/STALE/Sentry/OTel/encryption/capture/reload; UI doc 0 of diff/external-call/call-tree/dry-run/metrics/what-if. Document shipped T146-T182: call-capture, dry-run logging, call-tree+depth guard, preview metrics, result caching, error handling, what-if, run persistence+encryption, observability, async process service+STALE, serialization unification, autoconfig cleanup, FE panels (call-tree/external-calls/dry-run/diff/what-if). Refresh roadmap section. Ground each claim in current source. | High | loop | - | - | `./docs/plans/T183-architecture-docs-refresh.md` |
| T184 | Backlog | AesFieldEncryptor tests | backend — `AesFieldEncryptor` (85 lines, 0 tests) is the AES-256/GCM field encryptor backing run persistence (T176) — protects sensitive persisted fields at rest. Pin security-critical behavior: ctor null/blank-key IAE, null passthrough, round-trip (normal/empty/unicode/long), IV nonce freshness (same plaintext → different ciphertext), GCM tamper detection (flip byte → IllegalStateException), truncation, wrong-key auth failure. Pure unit, no Spring. Silent break here = security regression, must be caught. | High | loop | - | - | `./docs/plans/T184-aes-field-encryptor-tests.md` |
