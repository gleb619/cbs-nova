# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status  | Title                                                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority | Owner | Blocks | Blocked By | Plan File                                                    |
|:-----|:--------|:-------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:-------|:-----------|:-------------------------------------------------------------|
| T165 | Blocked | Preview execution sandboxing                                             | backend — SecurityManager-based approach is infeasible: JDK 25 in use, and JEP 486 (JDK 24) permanently disabled SecurityManager enforcement — checkWrite/checkConnect/etc. are no longer invoked by JDK I/O paths at all. Needs human decision on an alternative isolation approach before any code is written. See plan file for options.                                                                                                                  |   High   | loop  | -      | -          | `./docs/plans/T165-preview-execution-sandboxing.md`          |
| T401 | Blocked | Server-authoritative draft autosave — workbench autosave hits `saveDraft` API (debounced), localStorage demoted to offline fallback; open-time freshness precedence | frontend (Epic 5 — server-side draft persistence, phase 1) — roadmap-endorsed gap (`docs/roadmap.md` Epic 5 "Server-side draft persistence": "Move drafts off browser `localStorage` into the backend, per-user, with multi-tab sync and no TTL data loss"). Code-confirmed HALF-SHIPPED state: the backend draft API is COMPLETE (`DslDraftHandler`: save `:54`, read `:180`, list `:141`, delete `:122`) and the FE client is COMPLETE (`useDslApi.ts:76 saveDraft` + read/list/delete) and MANUAL save already persists server-side (`useDslWorkbench.ts:175-187` `api.saveDraft`); but AUTOSAVE — the thing that actually protects unsaved edits — still writes ONLY to localStorage: `useWorkbenchDraft.ts` (`:16-64`) does TTL'd `localStorage.getItem/setItem` (single-tab, `DRAFT_TTL_MS` expiry — after TTL the draft is GONE, the "no TTL data loss" item is literal), with a recovery banner for local restores. Crash/close before manual save with a dead TTL = silent loss. Phase 1 scope (this task): server-authoritative autosave + freshness precedence; multi-tab sync and per-user scoping are EXPLICIT phase 2 (needs principal identity from Epic 2 work + a sync signal — note as follow-up row material, do NOT build polling). Task (tier `frontend`, BE untouched): (1) autosave path: extend the existing debounce in `useWorkbenchDraft` (or the workbench wiring that drives it — read who calls the composable's save timer first) to ALSO/INSTEAD call debounced `api.saveDraft(name, ...)` — SAME payload shape as the manual save at `:175` (reuse the exact builder; do not fork the serialization); debounce interval for network writes longer than the localStorage one (e.g. local 1s, server 3-5s — pick, document); in-flight guard (skip new save while one is pending; queue trailing edge); failure handling: server save error → keep localStorage write as the ALWAYS-ON fallback (never remove it), surface a subtle "autosave offline — local only" state on the existing recovery-banner pattern (no toast spam); (2) open-time precedence: on entering a construct, fetch server draft (read API) + localStorage draft, take FRESHER by savedAt (server response carries its own timestamp — verify what the save/read API returns and thread it; if the server does not echo savedAt, compare against last-known manual-save time and document the limitation honestly rather than guessing); equal/absent → local wins (offline continuity); (3) TTL: local draft still expires locally (that is fine now — server copy is durable), but the recovery banner logic must not treat local-TTL-expiry as data loss when a server draft exists; (4) `dirty` state semantics unchanged (manual save/publish flows untouched); (5) tests (vitest): debounce fires server save with manual-save payload shape; in-flight guard; server-failure keeps local write + sets offline flag; precedence cases (server newer, local newer, server absent); TTL-expiry-with-server-draft shows no loss banner. Acceptance: editing then hard-closing the tab, waiting past local TTL, reopening → draft recovers from SERVER; autosave failure never loses local copy. Tier `frontend`. Low-medium risk (composable rewiring, API untouched); rollback = revert one commit. Phase-2 note: multi-tab sync + per-user drafts need Epic 2 principal + sync channel — separate rows. See plan. |  High  | loop  | -      | -          | `./docs/plans/T401-server-autosave.md` |
| T402 | Done | Prometheus pipeline repair — `/actuator/prometheus` un-404s (registry dep + exposure), then first alert rules (run error rate, preview latency, app down) wired into compose | backend (Epic 4 — shipped dashboards) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 workstream "Shipped dashboards": "Grafana dashboards + Prometheus scrape config in `app/compose/` for the existing `dsl.run.*` / `dsl.preview.*` meters. Alert rules for error rate, preview timeout rate, Temporal unreachable."). Code-confirmed BROKEN prerequisite: `app/compose/prometheus.yml` scrapes `spring-app:8090/metrics_path=/actuator/prometheus` but the app CANNOT serve that endpoint — (a) no `micrometer-registry-prometheus` dependency anywhere (grep across starter/starter-launcher build.gradle: zero hits), (b) `management.endpoints.web.exposure.include` is `health,info,metrics` in all four application.yml variants (starter-launcher `:122-130`, starter main/test/integrationTest) — `prometheus` not exposed. The scrape has been 404ing since it was written; every future dashboard/alert in this workstream is built on a dead pipeline. Task: (1) add `micrometer-registry-prometheus` (version via the platform BOM if micrometer is managed there — check how other micrometer deps declare; place in `starter` as optional runtime or `starter-launcher` where actuator config lives, executor judgment + document; must not break `:starter:test` contexts that lack it — conditional like T348's `@ConditionalOnBean(MeterRegistry)` pattern if needed); (2) add `prometheus` to exposure include in ALL FOUR ymls (keep list sorted, keep `show-details` settings untouched); (3) VERIFY against a booted app (standard `SERVER_PORT=8090` bootRun): `curl :8090/actuator/prometheus` returns 200 with `dsl_run_count_total` (or dsl.run.count in prom naming — assert the ACTUAL series name after boot and use it verbatim in rules; do not assume naming), `dsl_preview_duration_*`, and the base JVM/http series — record the real names in the runbook section; (4) alert rules `app/compose/alerts.yml` (new file): the roadmap names three alerts — (a) run error rate: expression over the status-tagged run counter (rate of non-success statuses / total rate over 5m, threshold e.g. >10% for 10m — pick + document rationale), (b) preview timeout/latency: CORRECTION (planning follow-up): a timeout counter DOES exist — `DispatchStage.java:127` registers `cbs.nova.preview.timeout.count` (orphaned under the `cbs.nova.` prefix, outside the `dsl.preview.*` family — discovered after this row was written); use it for the timeout-rate alert (and see T403's note on renaming it to `dsl.preview.timeout` — prefer landing the rename first if free, then alert on the final name), falling back to p95 duration threshold on the duration histogram (`histogram_quantile` over `dsl_preview_duration_seconds_bucket`, alert when p95 > Ns for 10m; N picked from current p95 observed at boot, document) and note the honest limitation: a true timeout-rate alert needs a timeout/outcome-tagged preview counter — leave as follow-up note, do NOT invent a metric in this task; (c) "Temporal unreachable": no temporal metric exists — nearest honest proxy NOW is `up{job="cbs-nova"} == 0` (app down) — ship that as the third rule with name `CbsNovaAppDown` and note the Temporal-specific version lands with T390's readiness work (readiness group exposes temporal state — a future row can add a `dsl_temporal_reachable` gauge); (5) wire `rule_files: [alerts.yml]` into prometheus.yml; (6) validation: `promtool check rules app/compose/alerts.yml` if promtool available (note if not), `docker compose -f app/docker-compose.yml config -q` for syntax, plus the boot-verification curl from step 3 as the acceptance evidence; runbook "Monitoring" section: what is scraped, the three alerts, what each means, operator action. Acceptance: live app serves /actuator/prometheus 200 with dsl.* series; alerts file loads in compose; three rules documented. Tier `backend`. Low risk (additive dep + config + new file; no behavior change); rollback = revert one commit. See plan. |  High  | loop  | -      | -          | `./docs/plans/T402-prometheus-pipeline.md` |
| T403 | Done | Helper preview-descriptor nulls — resolve four `todo: add default ...` fields (taskQueue/version/timeouts) in `PreviewCacheKeyBuilder`: real defaults or documented nulls, plus meter-name consistency note | backend (hygiene) — code-confirmed TODO group (lowercase `todo:`): `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/stage/PreviewCacheKeyBuilder.java:36-39` — the helper-describe fallback builds a synthetic `DslDescriptor` (post-T375 builder) with `.taskQueue(null) // todo: add default taskqueue`, `.version(null) // todo: add default version`, `.startToCloseTimeout(null) // todo: add default startToCloseTimeout`, `.heartbeatTimeout(null) // todo: add default heartbeatTimeout`. Usage analysis: the descriptor's ONLY consumer here is `dslDescriptorHash` (`:46`) — sha256 over Jackson-serialized bytes; with the app's `default-property-inclusion: non_null` (starter-launcher application.yml) null components are DROPPED from serialization → hash is stable and collision-free today; helpers are not Temporal-scheduled so taskQueue/timeouts have no true value, and `version` has no helper-side meaning. So the honest resolution may be DOCUMENTED NULLS, not defaults — but that is a DECISION to make explicitly, not four dangling todos that invite a future drive-by "fix". Task: (1) check sibling descriptor producers — what do `describeProcess`/`describeTransaction` return for these four fields (from `DslDescriptor` construction sites T375 swept: `ProcessDslObject.java:46`, `FunctionDslObject.java:35`, `TransactionDslObject.java:45`) and does ANY other consumer read the synthetic helper descriptor's four fields (grep `describeHelper` callers — if the synthetic descriptor escapes `PreviewCacheKeyBuilder` into explain/introspection surfaces, nulls may render as missing fields in API responses and REAL defaults or explicit omission become correctness, not style); (2) decide per field: null-with-comment (semantically honest for helpers — write WHY into the code replacing each todo) OR default value (only if a consumer needs it: e.g. version `""` sentinel or taskQueue from `DslProperties.taskQueue` default — pick from evidence, not aesthetics); (3) hash-stability note: changing any field from null→value changes `dslDescriptorHash` → one-time preview-cache invalidation — harmless but DOCUMENT in the PR so nobody hunts the cold cache; (4) drop the four todos; (5) while in the file's area, NOTE (do not fix unless trivially free): meter naming inconsistency discovered in this cycle's survey — `DispatchStage.java:127` registers `cbs.nova.preview.timeout.count` while the whole preview family is `dsl.preview.*` (roadmap Epic 4 references `dsl.run.*`/`dsl.preview.*`); renaming is a metric-contract change — if trivially free (no dashboards exist yet per T402 pipeline state), rename to `dsl.preview.timeout` and add to T402's alert-rule material; if risky, leave + note for T402 executor. Tests: existing PreviewCacheKeyBuilder/preview-cache tests stay green (hash determinism pinned if a test exists; add one if not — same descriptor in, same hash out, helper path included). Acceptance: zero dangling todos in the file, decision documented per field, hash stability pinned by test. Tier `backend`. Zero-behavior-change hygiene; rollback = revert one commit. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T403-helper-descriptor-nulls.md` |
| T404 | Done | `DslPreprocessor.wrapInClass` StringBuilder → text block — readability refactor of generated-wrapper assembly, output byte-identical | backend (refactoring) — code-confirmed TODO: `backend/dsl-platform/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/preprocessor/DslPreprocessor.java:76` reads `//TODO: redo from StringBuilder to textblock`. `wrapInClass` assembles the wrapper Java source (optional package decl, deduped default imports, user imports, `public class <N> implements DslCompactSource {`, body, `filename()` method) via a ~15-call StringBuilder append-chain. Output is pinned by `DslPreprocessorTest` (5 assertions on the wrapper/class shape) — that test is the equivalence oracle. Task: (1) rewrite `wrapInClass` around a text-block template (`.formatted(...)` or equivalent) for the static skeleton; conditional parts (package decl, import dedup/assembly) may remain small local joins feeding template placeholders; (2) generated source must stay byte-identical — `DslPreprocessorTest` green UNCHANGED; if a test needs changing, stop, output drifted; (3) drop the TODO; (4) `splitImports`/`ensurePublicDefine`/validation out of scope. Acceptance: TODO gone, `:dsl-codegen:test` green without test edits. Tier `backend`. Internal-only, no ABI change → platform republish not required. Rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T404-dsl-preprocessor-textblock.md` |
| T405 | Done | `DslPipeContext` → record with lombok `@Builder` — pipe-context immutability refactor, sweep 14 usage files | backend (refactoring) — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/core/pipe/DslPipeContext.java:13` reads `//TODO: redo to a \`record\` with lombok's builder`. Class holds 4 `@NonNull` final fields (name, dslContext, mode, runId) via `@RequiredArgsConstructor` + manual `@Getter`s, plus a MUTABLE `attributes` ConcurrentHashMap kept OUT of the ctor — record conversion: components = the 4 fields with `@Builder`; attributes map stays in record body (final reference, mutable contents) preserving get/setAttribute + typed-get + null-removes semantics; `withDslContext(...)` copy MUST keep sharing the same map instance (add test pin if none). 14 files reference the type (`DslExecutionPipeline`, `DslPipeStage`, 12 stage classes under `core/stage`): sweep `getX()` calls to record accessors, construction sites to builder — prefer full sweep for idiom consistency; same family as T385/T393 record conversions. Acceptance: TODO gone, `:starter:test` green, attributes semantics + shared-map copy behavior pinned by test. Tier `backend`. Internal to starter module, no published ABI. Rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T405-dsl-pipe-context-record.md` |
| T406 | Done | `MdcDryRunLoggingContext` — MDC-backed default `DryRunLoggingContext`, fix cross-instance dry-run log capture | backend (bug-shaped refactor) — code-confirmed TODO: `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/logging/ThreadLocalDryRunLoggingContext.java:7` (`TODO: Thread local due work with temporal is bad ideas... we need to use db, or redis`). Real gap found while scoping: `DryRunLoggingContextPropagator` (Temporal ContextPropagator) populates **MDC** (`MDC.put(RUN_ID_HEADER, runId)`) on the worker instance, but the DEFAULT bean (`DryRunLoggingConfiguration:25`, `@ConditionalOnMissingBean`) is the deprecated ThreadLocal impl the propagator NEVER writes → `DryRunLogbackAppender.append` gets `currentRunId()==null` and silently DROPS dry-run log events on worker instances. db/redis (TODO suggestion) unnecessary: MDC is the store both paths already touch. Task: (1) new `MdcDryRunLoggingContext implements DryRunLoggingContext` — set→`MDC.put`, null/clear→`MDC.remove`, get→`MDC.get`, KEY=`DryRunLoggingContext.RUN_ID_HEADER` (same constant propagator uses); (2) swap default bean in `DryRunLoggingConfiguration`; keep `@ConditionalOnMissingBean` override; (3) `ThreadLocalDryRunLoggingContext` STAYS (≈20 test files use it as test double) — resolve its TODO with a javadoc note, update `DryRunLoggingAutoConfigurationTest:43` default-bean assertion; (4) new tests: MDC impl round-trip + regression pinning MDC-populated→appender-captures. Acceptance: TODO resolved, `:starter:test` green, regression test proves worker-side capture. Tier `backend`. Starter-internal, no published ABI. Rollback = revert one commit. See plan. |   High   | loop  | -      | -          | `./docs/plans/T406-mdc-dry-run-logging-context.md` |
| T407 | Done | `purgeFinishedBefore` loop refactor — `while(true)` → bounded `for` + extracted batch fetch | backend (refactoring) — code-confirmed TODO (last unclaimed in prod sweep): `backend/dsl-starter/starter/src/main/java/cbs/nova/starter/persistence/JdbcDslRunRepository.java:147` reads `// TODO: refactor 'while' loop, to a 'for' loop`. `purgeFinishedBefore` uses `while (true)` with TWO break exits (empty batch; partial batch `size < batchSize`) and inlines the whole select-build per iteration. Task: (1) extract batch fetch to private method (select + query + PurgeBatchRow mapping); (2) restructure to a `for`-based loop where both exit conditions are visible in the loop structure — no `while (true)` (suggested: `for (var batch = fetch(...); !batch.isEmpty(); batch = fetch(...))` + explicit partial-batch exit, or equivalent; executor may refine); (3) behavior identical — batch order, callback-before-delete, total accounting, `batchSize <= 0` guard untouched; (4) drop TODO. Acceptance: TODO gone; `JdbcDslRunRepositoryPurgeTest` + `DslRunRetentionPurgerTest` green UNCHANGED (they are the equivalence oracle). Tier `backend`. Low value/low risk hygiene; starter-internal. Rollback = revert one commit. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T407-purge-loop-refactor.md` |
| T408 | Done | RBAC phase 1 — roles (`viewer/runner/author/operator/admin`), principal→role resolution, opt-in per-route enforcement filter | backend (security, Epic 2) — roadmap-endorsed gap (`docs/roadmap.md` Epic 2 Authorization model; NOTHING in kanban covers it; sequencing note: Epic 2 roles are prerequisite for Epic 5 approval gate + Epic 3 notification rules). Today any authenticated principal can run/cancel/publish/delete — authorization ends at "authenticated". Task (opt-in, default OFF, mirrors `ApiKeyAuthFilterConfiguration` idiom): (1) `Role` enum with hierarchy (admin ⊇ all); (2) `RoleResolver` — API-key principal → `ADMIN` (service-to-service), OIDC JWT → map configurable claim (`dsl.auth.rbac.claim`, default `scope`/`roles`) to roles, missing → `VIEWER`; (3) `RbacAuthorizationFilter` registered AFTER auth filter, route-class→role table: run/cancel=`RUNNER`, drafts write + publish + reload=`AUTHOR`, schedules write + retention/purge=`OPERATOR`, GET=`VIEWER` — enumerate REAL routes from the 8 handlers in `controller/` first; (4) deny → 403 via existing error envelope (`DslExceptionHandler`) naming required role + principal; (5) `dsl.auth.rbac.enabled` default `false` → zero behavior change when off. Acceptance: route×role matrix + disabled-by-default + claim-mapping tests; `:starter:test` green. Tier `backend`. Unblocks future Epic-5 approval gate + Epic-3 notification rules. Rollback = revert one commit (flag off anyway). See plan. |   High   | loop  | -      | -          | `./docs/plans/T408-rbac-phase1.md` |
| T409 | Done | DSL testing surface phase 1 — definition-attached test cases (`dsl_definition_tests` sidecar table) + `POST .../tests/run` via preview pipeline | backend (+thin BFF, Epic 5) — roadmap-endorsed gap (`docs/roadmap.md` Epic 5 "DSL testing surface": "author-defined example inputs + expected outputs stored with the definition; run the definition's own test cases in preview mode"); kanban grep 0 hits. Phase 1 backend slice: (1) sidecar table `dsl_definition_tests` (definition_name, case_name, input jsonb, expected_output jsonb, timestamps) — V-migration BOTH dialects following T383 dsl_audit pattern; sidecar avoids grammar/compiler changes + re-publish to tweak cases; (2) endpoints `GET/PUT /api/v1/dsl/definitions/{name}/tests` + `POST .../tests/run` (optional `?case=` subset); (3) runner reuses preview pipeline VERBATIM (`DslRuntimeHandler.preview` → `service.preview`) — inherits preview safety, no Temporal dispatch, assert no run rows created; (4) report: per-case `{name, PASS|FAIL|ERROR, actual, expected, durationMs}` + summary counts, preview diagnostics captured on ERROR; (5) deep JSON compare with defined normalization (key-order-insensitive, explicit null-vs-absent policy) unit-tested; (6) explicit BFF Nitro proxy routes (no catch-all rule) + tests; (7) `TESTS_RUN` audit entry IF T383 merged by then, else skip with note. Acceptance: CRUD + run live w/ tests both tiers, report shape pinned, comparison normalization tested. Tier `backend`. FE workbench button = later phase. Rollback = revert one commit + drop migration. See plan. |   High   | loop  | -      | -          | `./docs/plans/T409-dsl-test-surface-phase1.md` |
| T415 | Backlog | Wire `DslScheduleRouterConfiguration` — schedule routes unreachable at runtime (no router registration, no handler bean) | backend — discovered during T381 (`docs/openapi.json` generation): `/api/dsl/schedules` (GET/POST/DELETE) is defined in `DslScheduleRouterConfiguration` but the class is NOT wired into `DslRouterConfiguration` and there is no `DslScheduleHandler` bean definition anywhere, so the entire schedule feature is dead at runtime — `/v3/api-docs` omits it and any BFF/FE call 404s. Task: (1) register the schedule router the same way sibling routers are (check how `DslRouterConfiguration` / `DslExecutionsRouterConfiguration` compose — add the schedule router to that composition); (2) add the `DslScheduleHandler` bean definition (conditional on `ScheduleClient` per the class javadoc); (3) verify against a booted app: `make openapi` shows `/api/dsl/schedules` and `MIN_OPENAPI_PATHS` in the Makefile bumps to the new count; (4) handler tests + BFF contract test (schedule proxy routes already exist in `routeCoverage.spec.ts` manifest — they currently point at a dead backend); (5) if `ScheduleClient` requires Temporal and cannot bootstrap in the `make openapi` Postgres-only path, gate the doc-count assertion accordingly and note it. Acceptance: schedule routes reachable, `make openapi` regenerates with them present, tests green. Tier `backend`. Rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T415-wire-schedule-router.md` |
| T410 | Backlog | Rotatable API keys — multiple active, labelled, revocable; hashed storage; property key stays as bootstrap | backend (security, Epic 2) — roadmap-endorsed gap (`docs/roadmap.md` Epic 2 Secrets & key management: "rotate-able API keys (multiple active keys, labelled, revocable) instead of a single `dsl.auth.api-key` string"); kanban grep 0 hits. Today rotation = edit property + restart, old key dies instantly (no overlap). Task: (1) table `dsl_api_keys` (label, key_hash SHA-256, prefix for display, created/revoked/last_used) — migration both dialects (T383 pattern); (2) `ApiKeyStore`: matches(plaintext) over active keys, create(label) returns plaintext ONCE, revoke, list (never hashes); (3) `ApiKeyAuthFilter` accepts property key (kept, deprecation hint) OR stored keys; best-effort `last_used_at` update off the hot path; (4) admin endpoints `GET/POST /api/v1/dsl/auth/keys`, `DELETE .../keys/{id}` (OPERATOR if T408 merged); (5) constant-time compare, hash before lookup, no plaintext at rest/logs. Acceptance: create→auth→revoke→401 lifecycle test; two keys concurrently valid; list exposes no secrets; back-compat property key works. Tier `backend`. Rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T410-rotatable-api-keys.md` |
| T411 | Backlog | Domain event model + transactional outbox phase 1 — typed events on `dsl_events` (same-TX writes) + replay/list API | backend (+thin BFF, Epic 3) — roadmap-endorsed gap (`docs/roadmap.md` Epic 3: typed events `RunStarted/Completed/Failed/Cancelled/Stale`, `DraftSaved/Published`, `ReloadFailed` + "transactional outbox on `dsl_runs` writes → durable event log"; sequencing note: event store shared by Epic 3 + Epic 4). Distinct from T397 (webhook DELIVERY persistence — downstream). Phase 1 = store + writes ONLY (SSE/webhooks/MQ/notification rules later): (1) table `dsl_events` (event_type, aggregate_type/id, correlation_id, payload jsonb, schema_version, created_at) both dialects; (2) typed records w/ stable frozen JSON field names (public contract); (3) writes on run lifecycle transitions + draft save/publish + reload failure, SAME TX as the state change — enumerate real transition sites from run service/pipeline first; swallow-on-failure NOT acceptable here; (4) `GET /api/v1/dsl/events?type=&aggregate=&correlationId=&since=&limit=` paginated (T382 envelope) + explicit BFF route; (5) delivery-consumer seam left for T397/later phases. Acceptance: integration test asserts event row per transition; same-TX proof (forced failure rolls both back or documented closest boundary); payload shape stable. Tier `backend`. Rollback = revert one commit + drop migration. See plan. |   High   | loop  | -      | -          | `./docs/plans/T411-domain-events-outbox-phase1.md` |
| T412 | Backlog | Unified retention/GC — one scheduled `DslMaintenanceService` folding retention purger + orphan purge (+audit retention seam), shared config + metrics | backend (observability, Epic 4) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 Unified retention/GC: "fold `DslRunRetentionPurger`, orphan purge, and audit-log retention into one scheduled maintenance service with shared config and metrics"); kanban grep 0 hits. Task: (1) FIRST inventory every `@Scheduled` purge job in starter (retention, orphans, preview-cache GC, any T397 delivery retention) with config keys + cadence; (2) `DslMaintenanceService` single schedule (`dsl.maintenance.schedule`) iterating `MaintenanceTask` beans — current purge logic wrapped UNTOUCHED (their tests stay green); (3) per-task enabled flags + old config keys honored (deprecation hint); (4) shared Micrometer meters `dsl.maintenance.task.duration`/`.purged` tagged by task (T348 idiom); (5) forward-compat `...audit-retention.*` keys (no-op until T383-derived retention ships); (6) `dsl.maintenance.unified.enabled` rollout flag — document cadence-preservation choice. Acceptance: unified tick invokes all enabled tasks, ordering + skip tested; existing purge/orphan tests green unchanged. Tier `backend`. Rollback = flag off / revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T412-unified-retention-gc.md` |
| T413 | Backlog | `production` secure-default Spring profile — OIDC + API key + rate limit default-on (escape hatches win), startup posture log | backend (security, Epic 2) — roadmap-endorsed gap (`docs/roadmap.md` Epic 2 Secure default profile: "a bare deploy is not anonymous"); kanban grep 0 hits. Today all three guards opt-in OFF. Task: (1) `application-production.yml` — `dsl.auth.enabled=true`, rate limit on, OIDC resource-server required, fail-fast w/ clear message if issuer unset; (2) profile changes DEFAULTS ONLY — explicit per-guard `false` in prod wins (`@ConditionalOnProperty` semantics: profile sets property only when unset — verify mechanism, document); (3) one startup posture log block: auth/rate-limit/RBAC/OIDC states; (4) tests: prod boot w/ guards active + anonymous 401; explicit-off override; dev/default byte-identical (existing context tests green); (5) doc section in `docs/architecture-backend.md`. Works standalone; complements T408 (RBAC only matters once auth on). Acceptance: prod anonymous 401, escape-hatch override test, posture log once at boot, dev untouched. Tier `backend`. Rollback = don't set profile. See plan. |   High   | loop  | -      | -          | `./docs/plans/T413-production-profile.md` |
| T414 | Backlog | SBOM generation + dependency CVE gate — CycloneDX for Gradle + pnpm trees, `make sbom` / `make cve-scan`, baseline suppressions | backend+frontend tooling (supply chain, Epic 2) — roadmap-endorsed gap (`docs/roadmap.md` Epic 2 Supply chain: "SBOM generation + dependency CVE gate in CI for both Gradle and pnpm trees"); kanban grep 0 hits. Task: (1) CycloneDX Gradle plugin (pinned) on platform build → backend bom; (2) pnpm-LOCKFILE-AWARE frontend bom (tool must read `pnpm-lock.yaml`, not synthetic npm tree — verify output matches lock contents); (3) `make sbom` writes both to `build/sbom/*.cdx.json`; (4) `make cve-scan` = OWASP dependency-check (backend) + `pnpm audit --prod --audit-level high`, non-zero exit on HIGH+ non-baselined, NON-default (never in `make test`); (5) baseline/suppression file (`docs/security/`) so gate usable day one + how-to-add doc; (6) Makefile help + short security doc. Acceptance: both boms produced (frontend spot-checked vs lock); gate exits non-zero on non-baselined HIGH; baseline suppression proven; `make test` unaffected. Tier `backend` (touches FE tooling). Rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T414-sbom-cve-gate.md` |
| T415 | Backlog | Default-on tracing in compose — backend OTLP export to existing collector, end-to-end span coverage, `make trace-smoke` | backend (+compose tooling, Epic 4) — roadmap-endorsed gap (`docs/roadmap.md` Epic 4 End-to-end tracing: "default-on tracing in the compose stack (bundled collector)... Spans across BFF → backend → DSL dispatch → Temporal activity"); kanban grep 0 hits. KEY FIND: OTel collector ALREADY exists (`app/compose/observability.yml` → otel-collector contrib 0.121.0, OTLP 4317/4318 + Jaeger 14250) — gap is backend side: ZERO OTLP config in `application.yml`, tracing no-op unless endpoint manually set. Task: (1) compose backend service env: `MANAGEMENT_OTLP_TRACING_ENDPOINT=http://otel-collector:4318/v1/traces` (HTTP) + sampling 1.0 in compose — match existing Postgres/Temporal env idiom; default OFF outside compose unchanged; (2) propagation W3C tracecontext+baggage, `correlation_id` flowing across BFF→backend→dispatch→Temporal activity (check `TracingConfiguration` + `TemporalConfiguration` context propagators + `OpenTelemetryContextPropagator` first); (3) span-coverage verification: preview + real run → Jaeger shows HTTP→dispatch→activity chain sharing trace id; (4) `make trace-smoke`: curl one run, query Jaeger `/api/traces`, exit 0 iff backend+activity spans present; (5) docs recipe (`docs/architecture-backend.md`): how to view (localhost:16686), expected spans, no-op-tracing troubleshooting. Acceptance: spans appear with zero manual env setup in compose; trace-smoke green; non-compose default unchanged. Tier `backend`. Rollback = revert one commit (env-only + make target). See plan. |   High   | loop  | -      | -          | `./docs/plans/T415-compose-default-tracing.md` |
| T417 | Backlog | `HelperManager` interceptor: `ThreadLocal` → `Context`-threaded | backend (bug-shaped refactoring) — code-confirmed TODO `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/HelperManager.java:21-27` (`// TODO: it can cause to a memory leak ... method executeHelper must accept some ExecuteContext ... context must have an Interceptor`); the field is already `@Deprecated(forRemoval = true)`. `HelperManager` is a process-wide singleton (`GlobalManager.helperManager`); interception state lives in `private final ThreadLocal<HelperInterceptor> threadLocalInterceptor` (`:29`), set via `GlobalManager.registerHelperInterceptor` (`:78`) → `setInterceptor` and cleared in a `finally` in `DispatchStage.execute` (`DispatchStage.java:65,71`). On pooled / Temporal worker threads a missed `remove()` (exception before `finally`, nested pipe, later refactor) strands the interceptor and the object graph it captured. `Context` is already the correct carrier — created per execution, and it already threads `executionListener()` / `saga()` / `executionTraceCollector()` via nullable-default accessors + no-op-default `with*` methods (`Context.java:67-100`). Task: (1) `Context` gains `@Nullable default HelperInterceptor helperInterceptor()` (→ `null`) + `@NonNull default Context<T> withHelperInterceptor(@Nullable HelperInterceptor)` (no-op default), mirroring the executionListener members; `dsl-api` gets no new required deps; (2) `HelperManager.executeHelper` / `executeFunction` read `ctx.helperInterceptor()` — delete the `ThreadLocal` field + `setInterceptor`; (3) remove `GlobalManager.registerHelperInterceptor` (grep says 0 external callers — prefer delete over `@Deprecated` shim); (4) `DispatchStage` sets the interceptor on the `Context` built via `contextFactory` instead of mutating `GlobalManager`; drop the `finally` un-set; (5) the concrete `Context` impl behind `contextFactory` (under `starter/core`) carries the field (+ copy semantics if it is an immutable record — prefer inject-at-construction); (6) tests: `HelperManagerTest`, `DispatchStageTest`, `FakeHelperInterceptorTest` — interceptor still fires for preview/explain/dry-run pipes (`PreviewDslPipe`/`ExplainDslPipe`/`RunDslPipe` via `FakeHelperInterceptor`), no behavior change for real runs. Sibling of T406 (same `ThreadLocal`→context move for `DryRunLoggingContext`) — keep accessor naming consistent. Acceptance: no `ThreadLocal` in `HelperManager`, `setInterceptor`/`registerHelperInterceptor` gone, `:dsl:test` + `:starter:test` green. Tier `backend`. Rollback = revert one commit; generated output unaffected. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T417-helper-interceptor-context-threaded.md` |
| T418 | Backlog | DSL `@Helper` — `interpolate` (safe `${key}` string templating, literal substitution, no expression eval) | backend (@Helper addition) — roadmap-adjacent (`docs/roadmap.md` Epic 3 "Notification rules": operator-configured `"on RunFailed for process X notify channel Y"` must render a message body from run context; that template is config data, not code, and must not be able to call methods). Gap confirmed against the full helper list (`starter/src/main/java/cbs/nova/starter/helper/*.java`, 43 helpers): `FormatMessageHelper` (`@Helper("formatMessage")`) renders via `ctx.eval(template, params)` — ONE expression, full expression power, unsafe for untrusted templates; nothing does flat literal `${...}` multi-placeholder substitution. Task: (1) `InterpolateHelper implements Executable<InterpolateIn, InterpolateOut>`, `@Helper(name = "interpolate")`, standard helper shape (no ctor, `execute` → `Result.success/failure`, `RuntimeException` → failure); (2) `InterpolateIn(String template, Map<String,Object> params, String onMissing)` — `template` required (null → failure), `params` null → `Map.of()`, `onMissing` case-insensitive `error`(default: unknown key → failure) | `empty`(→ `""`) | `keep`(→ leave `${key}` verbatim), unknown value → failure; (3) `InterpolateOut(String result, List<String> resolvedKeys)` — distinct keys substituted, first-seen order; (4) scanner: `${`…`}` delimits a `trim()`-ed key, `${}` → failure, unclosed `${` → failure, no nesting, `$${` escapes to literal `${`; (5) value rendering: present-but-null → `""` (missing ≠ null), `BigDecimal` → `stripTrailingZeros().toPlainString()` (reuse `FormatMessageHelper.formatValue` idiom, private copy), else `String.valueOf`; (6) `InterpolateHelperTest` — substitution/repeat/multi, each `onMissing` mode, `$${` escape, unclosed + `${}` failure, null template/params, present-null → empty, numeric/boolean, `resolvedKeys` order+distinct, bad `onMissing`; (7) catalog doc entry next to `formatMessage` with "literal substitution, no expression eval" note; (8) check no registered-helper-count test needs bumping (discovery is via `@Helper` processor — likely nothing else). Phase 1 flat keys only — dotted paths (`${order.id}`) and default syntax (`${name:-anon}`) are noted follow-ups, not built. Companion to T391 (`MaskHelper` — bodies often interpolate then mask). Acceptance: `:starter:test` green, helper resolvable as `interpolate`, preview-safe (no side effects). Tier `backend`. Pure function, no I/O, no deps; rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T418-interpolate-helper.md` |
| T419 | Backlog | `make openapi-check` — fail on `docs/openapi.json` drift + breaking-vs-additive schema diff | backend tooling (DX + Epic 1 contract enforcement) — roadmap-endorsed gap (`docs/roadmap.md` Epic 1: "contract drift fails the build", "schema-diff check: a breaking change to a backend DTO fails CI unless the OpenAPI version bumps"); kanban grep 0 hits. `make openapi` (T381) regenerates and OVERWRITES `docs/openapi.json` — useless as a guard; a stale committed spec ships silently and the FE has no reliable schema source. T335 contract tests check runtime shape against the BFF, not the committed artifact — orthogonal. Task: (1) extract the boot/fetch/normalize/teardown block from the `openapi` target into `scripts/openapi-fetch.sh <outpath>` (headless `:starter-launcher:bootRun` on `SERVER_PORT` default 8090, wait `/actuator/health` UP, curl `/v3/api-docs`, `json.dumps(indent=2, sort_keys=True)`, assert `MIN_OPENAPI_PATHS`, same cleanup/`trap` discipline — no orphan JVM); `make openapi` becomes fetch→`mv`; (2) `make openapi-check`: fetch→temp→compare to `docs/openapi.json`, byte-identical → exit 0, differs → print `diff -u` + run classifier + exit per class, NEVER writes the committed file; (3) `scripts/openapi-diff.py <old> <new>` (stdlib only) — BREAKING = path/operation removed, `required` prop removed, enum value removed, prop `type` changed; ADDITIVE = new path/operation/prop/enum; exit 0 if only additive, exit 2 if any breaking AND `info.version` unchanged, exit 0 + `[warn]` if breaking but version bumped; (4) NOT in `make test` (needs Postgres boot) — CI-opt-in, mirror `make cve-scan` (T414) convention; (5) `make help` entry + paragraph in `backend/AGENTS.md`; (6) regenerate `docs/openapi.json` in the same PR if stale so the check starts green. Classifier intentionally coarse phase 1 (no `$ref` graph walk, no `oneOf`/`allOf` deep semantics) — follow-ups noted in the script header, no gold-plating. bats e2e deferred (existing convention). Acceptance: clean tree → green; add a route → red listing it ADDITIVE; breaking change without version bump → exit 2. Tier `backend`. Tooling-only, no runtime code; rollback = revert one commit. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T419-openapi-drift-check.md` |
| T420 | Backlog | `DslGitStatusResolver` unit tests — cache TTL, config resolution, dirty-path aggregation, failure fallback | backend (missing tests) — `DslGitStatusResolver` (96 lines, JGit-backed working-tree inspector feeding definition-status introspection) has NO dedicated test; grep `-lw DslGitStatusResolverTest` = 0 (only transitive coverage via `DslIntrospectionResourceTest` etc.). Untested branching: TTL cache hit/miss/expiry (`ConcurrentHashMap<Path,Snapshot>` + `Instant` expiry), `git().enabled()` gate (+ null `git()`), configured `repositoryDir` vs `candidateDir` arg resolution, dirty-path aggregation across 6 JGit status buckets (added/changed/modified/untracked/removed/missing), `loadStatus` exception → `Optional.empty()` + warn. Feeds T394 (history diff) + Workbench status — a silent regression (cache never expiring, wrong root) degrades the UI without failing any test. Pure to test: `DslProperties.Git` is a `@Builder` record, JGit runs against `@TempDir`, no Spring context / Mockito. Task: new `DslGitStatusResolverTest` (`src/test/.../service/`), JUnit 5 + `@TempDir`; cases — empty on null `git()` / `enabled()==false` / no `.git` found (warn, no throw); clean `git init`+commit repo → empty `dirtyPaths`, `workTree` absolute+normalized; each dirty bucket (untracked/modified/staged-add/deleted) surfaces its path; `repositoryDir` set overrides `candidateDir`, null/blank uses `candidateDir`; cache — non-zero TTL returns STALE result after repo mutation (hit), `statusCacheTtlSeconds=0` → immediately expired → fresh scan reflects change; corrupt `.git` → empty, no propagation. JGit test setup: `Git.init().setDirectory(tempDir).call()` + fixed `PersonIdent` (no global git config dep) — reuse an existing `src/test` JGit helper if one exists (grep `Git.init`). Fresh resolver per test (cache keyed by repo root). No production-code change. Acceptance: `:starter:test` green, all branches hit. Tier `backend`. Rollback = delete the test file. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T420-git-status-resolver-tests.md` |
| T421 | Backlog | ADR 0004 — Preview / Dry-run / Explain execution modes | backend (documentation) — the three-mode model (Run→Temporal, Preview→direct in-process execution bypassing Temporal, Explain→Preview + description + Mermaid diagrams) is the engine's differentiator and the spine of `DslRuntime`, the pipe classes (`RunDslPipe`/`PreviewDslPipe`/`ExplainDslPipe`), `DispatchStage`, `FakeHelperInterceptor`, and the external-call capture `BeanPostProcessor`s (`DataSourceProxyBeanPostProcessor`, `MessagingCallCaptureProducerFactoryBeanPostProcessor`, `ExternalCallFeignInterceptor`) — but there is NO ADR for it; only `docs/ideas/dry-run-preview-explain.ignore.md` (a brainstorm, not authoritative) + scattered `architecture-backend.md` mentions. Only 3 ADRs exist (`0001` Temporal, `0002` Helper SPI, `0003` BFF Nitro). Three backlog items lean on this design (T398 bundle import dry-run, T409 DSL testing surface, T418 `interpolate` preview-safety) and would each be cheaper to reason about with an ADR to cite; new contributors currently reverse-engineer it from `DevDslRuntime`/`PreviewDslPipe`. Task: new `docs/adr/0004-preview-dry-run-explain-modes.md` following the `0003` template (`Status: Accepted`, dated today, "retroactive — records a foundational decision"); `## Context` (analysts + CI must validate a definition in seconds with no Temporal cluster); options considered w/ honest trade-offs (`TestWorkflowEnvironment` / mock cluster / direct in-process execution); `## Decision` = the three modes + entry points; **Mechanism** subsection naming the real seams — VERIFY each against code first, one `codegraph_explore` pass — (`DslRuntime` impls + mode selection, the pipe classes, `DispatchStage` + `HelperInterceptor`/`FakeHelperInterceptor` for side-effect faking, the capture `BeanPostProcessor`s recording outbound calls instead of performing them, the `jdbc`/`http`/`mq` external-call taxonomy); `## Consequences` explicit on the fidelity gap (preview does NOT reproduce durable timers, signals/queries, continue-as-new, activity retries; side-effect suppression is best-effort, bounded by which call types the capture layer intercepts; preview = validation, not a proof of the deployed workflow). Cross-link `architecture-backend.md`, `docs/dsl/preview-mode.md` (+ add a one-line "see ADR 0004" pointer there — no content dup), `0001`; note the ideas doc is superseded as reference. Update `docs/adr/README.md` index. No code change. Acceptance: ADR reads accurately against the tree, index updated. Tier `backend`. Rollback = delete file + revert two one-line edits. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T421-adr-preview-explain-modes.md` |
| T422 | Backlog | `make lint` / `make fmt` + root `.editorconfig` | backend tooling (DX) — formatting IS enforced per-tier (backend Spotless via `backend/gradle/code-style.gradle` + `eclipse-formatter.xml`, `spotlessCheck`/`spotlessApply`; frontend Biome `pnpm lint` / `pnpm format`) but there is NO aggregate entry point and `make test` runs NEITHER — a style regression only fails if the contributor recalls the exact per-tier invocation (incl. the `dsl-platform` vs `dsl-starter` split + Java-25 wrapper caveat). No repo-root `.editorconfig` — every IDE/agent re-derives "2-space, LF, trailing newline" from `AGENTS.md` prose. Task: (1) `make lint` = backend `spotlessCheck` on every project applying the shared code-style (confirm set via `grep -rl code-style.gradle backend/**/build.gradle` — at least `dsl-platform`+`dsl-starter`, use `backend/dsl-platform/gradlew`) + `cd frontend && pnpm lint`, both always run, summary + non-zero exit if either fails, mirror the `make test` target structure exactly; (2) `make fmt` = `spotlessApply` + `cd frontend && pnpm format`, non-fatal on reformat; (3) `make lint-backend`/`make lint-frontend` split matching `test-backend`/`test-frontend`; (4) NOT in `make test`; optional thin `make check` = lint then test (pre-push target); (5) new repo-root `.editorconfig` — `root=true`, `[*]` utf-8/lf/insert_final_newline/trim_trailing_whitespace/space, `[*.{java,ts,vue,js,mjs,cjs,json,yml,yaml,gradle}]` indent_size=2, `[*.md]` trim_trailing_whitespace=false, `[Makefile]` tab — every value cross-checked against `eclipse-formatter.xml` + Biome config so it never contradicts the real formatters; (6) one caveat line in root `CLAUDE.md`; (7) `make help` picks up new targets via `##` comments. Manual verify in PR: `make lint` green clean tree, red on mis-indent, `make fmt` fixes. Tier `backend`. Tooling-only, no runtime code; rollback = revert one commit. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T422-make-lint-fmt.md` |
| T423 | Backlog | Memoize `JacksonJsonSchemaGenerator.generateSchema(Class<?>)` | backend (performance) — `generateSchema(Class<?>)` builds a fresh `SchemaBuildingVisitor`, runs `objectMapper.acceptJsonFormatVisitor(inputType, visitor)` and allocates nested `LinkedHashMap`/`ArrayList` structures on EVERY call; the result is a pure deterministic function of `inputType` (records only). `DslIntrospectionService.inputSchema()` calls it for every process + transaction on every introspection list/detail request (`/api/v1/dsl/processes`, `/constructs/{name}`, helper catalog) — the path backing Workbench catalog load + autocomplete (T362/T400) — with NO caching. (The validation path `InputValidator` already has its own Caffeine `schemaCache` keyed `constructName|descriptorHash`, so this is specifically the introspection gap.) Task: (1) bounded `Cache<Class<?>,Map<String,Object>>` in `JacksonJsonSchemaGenerator` (Caffeine `maximumSize` 1_024, mirror the `MapInputConverter` adapter-cache idiom + its "interned by Class identity, eviction pressure negligible" rationale comment); (2) `generateSchema(Class<?>)` returns cached on record types, computes+stores on miss; `null`/non-record still returns a fresh `emptyObjectSchema()` (uncached — trivial, avoids null key); (3) cached value DEEPLY unmodifiable via a private recursive `deepUnmodifiable(Object)` applied once at store time (schemas nest Map+List) — a caller mutating the returned map must not corrupt the shared entry; test asserts `UnsupportedOperationException` + subsequent call still correct; (4) `generateSchema(List<ParameterDescriptor>)` left uncached — one-line `// TODO` noting it could key on `List.copyOf(parameters)` (`ParameterDescriptor` is a record — value equality holds), do NOT build; (5) byte-identical behaviour for all existing cases — extend `JsonSchemaGeneratorTest`: same-class second call returns `.equals` schema, distinct classes cached independently, deep-immutability, all current shape assertions unchanged; (6) no API change (`JsonSchemaGenerator` interface untouched), bean stays `new JacksonJsonSchemaGenerator()`. Reload note: recompiled input classes are new `Class` identities → new keys, stale entries harmless + bounded by `maximumSize` (state in comment). Caffeine already a dep. Acceptance: `:starter:test` green, `acceptJsonFormatVisitor` invoked once per distinct class across repeated calls (spy/counter test). Tier `backend`. Low risk (pure-function memo); rollback = revert one commit, no wire change. See plan. |   Medium  | loop  | -      | -          | `./docs/plans/T423-memoize-json-schema-generator.md` |
| T425 | Backlog | Repo-wide `spotlessApply` + fix the accumulated formatting debt | backend (hygiene) — discovered during T395: `./gradlew :starter:spotlessCheck` (and `:dsl-platform` modules) fail on ~28–33 files, mostly javadoc line-wrapping drift in recently-added code (MaskHelper T391, PageResponse T382, LineDiff T394, DslAudit* T383, health indicators T390, package-info files, plus many test files). Because `make test` / `:starter:test` do NOT gate on spotless, the debt has been growing unnoticed as loop tasks land. Task: (1) run `backend/dsl-platform/gradlew -p backend/dsl-platform spotlessApply` and `-p backend/dsl-starter spotlessApply` across all modules; (2) commit the pure-formatting result in ONE isolated commit `style: spotlessApply across backend` — no functional changes, easy to review as "format only"; (3) verify `:dsl-codegen:test` + `:starter:test` still green (formatting must not change behavior); (4) OPTIONAL follow-up note: wire `spotlessCheck` into `make test-backend` or a pre-commit/CI gate so this cannot silently recur (flag for human — adds friction to the loop, needs buy-in). Tier `backend`. Zero behavior change; rollback = revert one commit. Do this as a STANDALONE task, never bundled into a feature commit. See plan. |   Low   | loop  | -      | -          | `./docs/plans/T425-spotless-debt.md` |
| T426 | Backlog | `GradleService` JDK-discovery hardcode → `DslBuilderProperties` / app.yml | backend (refactoring, module `dsl-builder`) — code-confirmed TODO group (3×, one concern): `backend/dsl-plugins/dsl-builder/src/main/java/cbs/nova/dsl/builder/service/GradleService.java:26,61,81` each read `//TODO: redo, remove hardcode, use app.yml for settings`. `resolveBuildJavaHome()`/`installedJdks()`/`logLines()` compile in: `MAX_GRADLE_JAVA_MAJOR=25` + `MIN_GRADLE_JAVA_MAJOR=8` (Gradle Tooling API JVM compat window), `MAX_LOG_LINES=200` (build-log tail cap), env var name `DSL_BUILDER_JAVA_HOME`, and JDK search roots `~/.sdkman/candidates/java` + `/usr/lib/jvm` — Linux/SDKMAN only, breaks on macOS/Windows/slim containers. Module is NEW (commit `5f97b852`), port 8091. `DslBuilderProperties` (`@ConfigurationProperties("cbs.dsl.builder")` record, already wired, carries `gradleJavaHome`/`buildTasks`/`sourceFolders`/...) — EXTEND it, no new props class. Task: (1) add record components `gradleJavaMin`/`gradleJavaMax`/`buildLogMaxLines`/`jdkSearchPaths` (`List<Path>`)/`jdkHomeEnvVar`; (2) `application.yml` `cbs.dsl.builder.*` keys with values EQUAL to current constants; (3) `GradleService` reads all five from `properties`, no `private static final` for these remain, explicit `${java.home}` fallback unchanged; (4) null/absent element → coded default = today (partial yml must not NPE the builder); (5) drop the 3 TODOs; (6) do NOT hand-edit `build/resources/main/application.yml` (build artifact). Tests: new `GradleServiceTest` — compat window + log-tail cap from config, `@TempDir` fake JDK dirs found via configured search paths, empty/partial props → default behavior; existing `CompileServiceTest`/`CompileControllerTest`/`GitServiceTest` green; `@ConfigurationProperties` binding covers new fields. Out of scope: `GitService.java:37` `pull()` wiring (separate row), redesign of the JDK-selection heuristic. Acceptance: zero dangling TODOs in the file, defaults reproduce today's behavior, `dsl-builder` tests green. Tier `backend`. Low risk (additive config, behavior-preserving defaults, module not yet on any critical path); rollback = revert one commit. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T426-gradle-service-jdk-discovery-config.md` |
| T427 | Backlog | `GitService` dead `pull()` + worktree methods — wire repo-reuse (base clone cache + `git worktree` per session) or delete | backend (module `dsl-builder`) — code-confirmed TODO: `backend/dsl-plugins/dsl-builder/src/main/java/cbs/nova/dsl/builder/service/GitService.java:37` reads `//TODO: wire, add endpoint and use method` above `@Deprecated pull(...)`. FOUR `GitService` methods are dead — only `cloneRepository()` is wired (`CompileService.compile():69`): `pull`, `createWorktree`, `listWorktrees`, `removeWorktree` (grep: zero non-test callers). Module NEW (commit `5f97b852`, port 8091). Today every `compile()` with a `repoUrl` does a FRESH FULL CLONE into `session.getSessionDir()/repo` — no reuse across requests for the same repo; the worktree helpers imply an intended design (one long-lived base clone per repo + cheap `git worktree` per build session) that is half-built and unreferenced. Task — DECIDE then act: (A) drop the worktree model → delete the 4 methods + their `GitServiceTest` cases + the TODO, keep simple fresh-clone-per-session; OR (B) wire it: base-clone cache `Map<repoUrl,Path>` under `properties.workspaceDir()` (concurrent-safe — two compiles of one repo must not corrupt the base), `compile()` clones base on miss / `pull` on hit / `createWorktree` into the session dir instead of full clone, session cleanup calls `removeWorktree`, `pull` loses `@Deprecated`, optional `POST` refresh endpoint on `CompileController` (only if it earns its place), the `@Scheduled` cleanup in `CompileService` also prunes orphan worktrees + stale base clones past `sessionTtl`/new `repoCacheTtl`. Tests: `GitServiceTest` — pull-updates-existing + worktree add/list/remove vs `@TempDir` repos; `CompileServiceTest` — second compile of same `repoUrl` reuses base (spy clone count) + produces a worktree; `CompileControllerTest` green. TODO removed either way. Out of scope: `GradleService` JDK hardcode (T426), private-repo credential handling (still `null` today). Acceptance: zero dead git methods + zero dangling TODO in the file; if wired, repeat-compile of one repo does not re-clone. Tier `backend`. Risk: medium if wiring (shared mutable repo cache + concurrency + cleanup on a new module), low if deleting; rollback = revert one commit. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T427-gitservice-pull-worktree-wiring.md` |
| T428 | Backlog | `dsl-builder` `repoUrl` validation — scheme allowlist + local-address SSRF guard + optional host allowlist before clone | backend (security, module `dsl-builder`) — code-confirmed gap: `CompileService.compile()` (`backend/dsl-plugins/dsl-builder/.../service/CompileService.java:69`) clones ANY `request.repoUrl()` via `GitService.cloneRepository` → JGit `Git.cloneRepository().setURI(repoUrl)` with ZERO URL validation (only package NAMES checked via `PACKAGE_PATTERN`), then runs Gradle against the clone. JGit honours `file://`, `http://`, `git://`, `ssh://` → attack surface: `file:///etc/...` local disclosure into the build, `http://169.254.169.254/...` cloud-metadata SSRF, `http://internal-host/...` network probing. Module is a standalone Boot app on 8091 (commit `5f97b852`). Precedent to mirror: `WebhookDispatcher.isUrlAllowed()` + `WebhookProperties.allowPlainHttp` (scheme allowlist, https-only unless opted in). Task: (1) `DslBuilderProperties` gains `allowedRepoSchemes` (default `["https"]`), `allowPlainHttpRepo` (default false), `allowedRepoHosts` (default empty = any); (2) new `RepoUrlValidator` (or private method — cohesion call) invoked at top of `compile()` BEFORE `createSession()` when `repoUrl` non-blank: parse via `java.net.URI` (unparseable → `IllegalArgumentException` → existing 400 `INVALID_REQUEST`), scheme not in allowlist → reject (`http` only if `allowPlainHttpRepo`), reject `file`/`ssh`/opaque/schemeless, host missing → reject, host resolving to loopback/link-local/site-local/any-local (`InetAddress`) → ALWAYS reject, `allowedRepoHosts` non-empty → host must match (exact or `*.suffix`); (3) rejection message names reason, strips URL userinfo from logs/response; (4) `application.yml` documents the 3 keys. Tests — new `RepoUrlValidatorTest`: https public → pass; `file:///` → reject; `http://` reject-by-default / pass with flag; `https://127.0.0.1`, `https://169.254.169.254`, `https://[::1]` → reject; `git://`/`ssh://` → reject; host allowlist match/no-match; userinfo absent from exception message; blank `repoUrl`+`sources` still works; existing `CompileServiceTest`/`CompileControllerTest`/`GitServiceTest` green. PR note: resolve-check is best-effort (DNS-rebind TOCTOU with JGit's own resolve) — full fix needs custom resolver/proxy, follow-up only. Out of scope: sandboxing the Gradle build itself (arbitrary `build.gradle` exec — relates to T165), private-repo credentials, T426/T427. Acceptance: dangerous repoUrls rejected pre-clone with documented escape hatches; tests pin each case. Tier `backend`. Low risk (additive rejection, `allowPlainHttpRepo`/`allowedRepoSchemes` escape hatches); rollback = revert one commit. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T428-dsl-builder-repo-url-validation.md` |
| T429 | Backlog | `DslDefinitionHistoryService` unit tests — snapshot/list/readEntry/readPublished/prune + path-traversal guard | backend (missing tests) — `DslDefinitionHistoryService` (154 lines, `starter/src/main/java/cbs/nova/starter/service/`) backs draft publish-history snapshotting, listing, entry read, published read, retention prune — the read side of workbench history/restore. NO dedicated test; only transitive via `DslDraftResourceTest`/`DslDefinitionBundleResourceTest`. Pins current behavior AND de-risks T394 (history diff builds on `readEntry`/`readPublished`). Pure filesystem + Jackson-3 (`tools.jackson.databind.ObjectMapper`) + `DslProperties` (`drafts().historyLimit()` only) — no Spring context. Task: new `DslDefinitionHistoryServiceTest` (`starter/src/test/.../service/`), JUnit 5 + `@TempDir`, fresh service+dir per test. Cases: `snapshotBeforePublish` — no published file → no-op no-throw; published present → exactly one `history/<name>/<digits>.json`, content byte-equal; unreadable source → warn-swallowed. `list` — missing dir → `[]`; 3 entries → newest-first by `timestampMillis` with correct `timestamp`/`size`/`modified`; non-numeric-named `.json` → skipped. `readEntry` — `null`/`"abc"`/`"12.3"` → empty; numeric but no file → empty; valid → `DraftRequest` parsed; malformed JSON → empty. `readPublished` — missing/valid/malformed. `prune` via snapshot with `historyLimit=2` → 4 snapshots → 2 newest remain; `historyLimit<=0` → all kept. Path safety — `name="../../etc/passwd"` → `safeFileName` collapses, paths stay inside `dir/.workbench/...`, `safeHistoryDir` escape check throws for crafted escaping name if reachable. Existing draft/bundle resource tests green. No production-code change. Acceptance: `:starter:test` green, all branches hit. Tier `backend`. Zero risk (test-only); rollback = delete the file. Sibling of T420 (`DslGitStatusResolver` tests). Out of scope: history diff endpoint/FE (T394), git-status-resolver tests (T420). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T429-definition-history-service-tests.md` |
| T464 | Backlog | Frontend type errors phase 2 — SFC prop defaults, vitest mock typing, `mount()` prop types, then flip CI typecheck to blocking | frontend — the bulk of the `vue-tsc` backlog after T456 phase 1: Vue SFC `() => null`/literal default mismatches across ~8 components, widespread `mockReturnValue on never` in specs + `vitest.setup.ts`, `mount(C, {props:{}})` → `Record<string,unknown>` rejections, `ExecutionStatus` string-vs-enum, `JsonSchema` construct-schema typing, API `unknown`/`any` casts. Multi-pass. Acceptance: `pnpm typecheck` exits 0, remove `continue-on-error: true` from the CI `Typecheck frontend` step. Tier `frontend`. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T456-fix-frontend-type-errors.md` |
| T463 | Backlog | Semantic color tokens phase 2b — `executions/index.vue` (app-chrome) + `executions/[id].vue` (region split) | frontend — follow-up to T453/T462. `executions/index.vue` → app-chrome vocab. `executions/[id].vue` straddles per `docs/colors.md` §12: header/filters/status-pills/pagination → app-chrome; diagram/logs/trace panels → workbench vocab. Split by region, not file. Mapping tables in `docs/color-token-mapping.md`. Tier `frontend`. |   Low  | loop  | -      | T462       | `./docs/color-token-mapping.md` |
| T458 | Backlog | Semantic color tokens phase 3 + raw-palette guard | frontend — follow-up to T453/T457/T463/T462. Swap `app/components/RunTrendChart.vue` (non-series colors only — series scale may stay categorical), `DslHistoryPanel.vue`, `DslTemplateGallery.vue` — these are workbench-area components → **workbench** vocab per `docs/color-token-mapping.md`. Then add a guard test forbidding raw `(gray\|blue\|red\|yellow)-` palette in `admin-ui-plugin/**/*.vue` (only after T453+T457+T463 land, else it fails immediately). Tier `frontend`. |   Low  | loop  | -      | T462       | `./docs/plans/T453-semantic-color-tokens-pages.md` |
| T459 | Backlog | `CbsDrawer` unit spec — base drawer, 5 consumers, no test | frontend (missing tests) — `components/src/components/CbsDrawer.vue` used by `SavedDraftsWidget.vue`, `HelperSearchPanel.vue`, `dsl-workbench.vue` (×2) — 5 call sites, real behaviour (`open` v-model, overlay-click close, close button, `Teleport`, transitions, dialog a11y), NO spec. Pin: `open = defineModel<boolean>('open',{default:false})`; `open=false` → neither overlay nor `<aside>`; `open=true` → overlay `div[aria-hidden]` + `aside[role="dialog"][aria-modal="true"]`; aside `aria-label` = `ariaLabel || title`; aside `[data-testid]` = `testId` (default `'drawer'`); `widthClass` (default `w-80`) on aside; overlay `@click` → `open=false` (emits `update:open` false); close btn `[data-testid="drawer-close-button"]` `:aria-label="closeLabel"` @click → same; `title` in header `<h3>`; default slot in aside. Task: new `components/src/components/__tests__/CbsDrawer.spec.ts`, `@vue/test-utils`+vitest, handle `Teleport` (`global.stubs:{Teleport:true}` or `attachTo` — match existing Teleport specs e.g. `SavedDraftsWidget.spec.ts`). Cases: not rendered `open=false` / rendered `open=true`; overlay click → `update:open` false; close-btn click → `update:open` false; `aria-label`=`title` when `ariaLabel` omitted, =`ariaLabel` when given; `data-testid`=custom `testId`, `widthClass` on aside; `role`/`aria-modal` present, close btn `aria-label`=`closeLabel`; `title` text + slot content rendered; runs in `@cbs/components` vitest project; `pnpm test`+`pnpm lint` green. Test-only, no production-code change. Tier `frontend` (`frontend/components`). Zero risk; rollback = delete spec. Out of scope (→ candidate follow-ups): `CbsButton.vue` is DEAD CODE (0 usages grep-confirmed — delete + `index.ts` export + spec, or justify public), `CbsDrawer` colour tokens (`bg-gray-900`/`text-gray-100`/`border-gray-800` — dark surface, needs dark-token decision, part of T453 colour work), focus-trap/Esc-to-close (CbsDrawer has neither — feature not test). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T459-cbsdrawer-spec.md` |
| T460 | Backlog | Delete dead `CbsButton.vue` — 0 usages, exported but unused | frontend (refactoring) — `frontend/AGENTS.md` Code Style: "Delete dead code, unused imports". `components/src/components/CbsButton.vue` (12L, `variant:'primary'|'secondary'` → class map + slot) exported from `components/src/index.ts:9` but ZERO usages anywhere (`admin-ui-plugin`/`components/src`/`e2e`, grep-confirmed excl. own file + `.claude/worktrees`). No spec, no story. `@cbs/components` is workspace-internal (unpublished) → public export has no external consumer. Early stub superseded by inline `<button>` + DS classes. Task: (1) delete `CbsButton.vue`; (2) drop `index.ts:9` `export { default as CbsButton } …`; (3) re-grep `CbsButton` across `frontend/` → must be zero. Acceptance: `grep -rn CbsButton frontend` (excl node_modules) → nothing; `pnpm build:components` + `pnpm test` + `pnpm typecheck` (T452) + `pnpm lint` green; no other export/auto-import references it. Tier `frontend` (`frontend/components`). Very low risk (deleting 0-ref code in unpublished pkg); rollback = revert one commit; if reviewer wants it kept as deliberate public primitive → close won't-fix + add one-line SFC comment. Out of scope: `CbsDrawer` spec (T459), other untested `Cbs*` primitives, full `index.ts` dead-export sweep (own row). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T460-delete-dead-cbsbutton.md` |
| T461 | Backlog | Executions detail page — polling toggle + interval selector (AGENTS rule, mirror T441) | frontend (DX / rule compliance) — `frontend/AGENTS.md` Reactivity: "Polling requires a user-controllable UI toggle (pause/play + interval selector). Never start polling silently. Persist via `useLocalStorageState`." T441 fixed the executions LIST page; DETAIL page still violates: `admin-ui-plugin/app/pages/executions/[id].vue:63` — on mount, `status==='Running'` → `startPolling(id.value)` SILENTLY (no toggle/interval/persist); `useExecutions.ts:356` `startPolling(id)` uses hardcoded `3000` ms, ignoring `runtimeConfig.public.stalePollMs` + shared `resolveStalePollMs` (T446); no visibility-pause. Task (mirror T441): (1) `useExecutions.ts` — `startPolling(id, intervalMs = resolveStalePollMs())` param, default via shared `useStalePollInterval` (T446), drop literal `3000`; cleanup unchanged; (2) `[id].vue` — `useLocalStorageState` toggle key `executions.detail.livePolling.enabled` (default ON = keep today's on-for-Running, now pausable+remembered) + interval key `executions.detail.livePolling.intervalMs` (default = resolved `stalePollMs`); `<select>` 2000/5000/10000/30000 `data-testid="execution-detail-live-polling-interval"` + toggle `data-testid="execution-detail-live-polling-toggle"` near status header; mount `if (status==='Running' && toggleEnabled) startPolling(id, intervalMs)` (restoring choice, not silent); interval-change while ON → restart; toggle OFF → `stopPolling`; stale-polling (T199) path untouched (exempt, self-terminating). Tests: `[id].spec.ts` — toggle/interval persist, restart-on-change (fake timers), fresh-storage default (on + resolved), Running-gate; `useExecutions.spec.ts` — `startPolling` honours interval, default ≠ 3000; existing cases green; `pnpm test`+`pnpm lint` green. Tier `frontend`. Low-medium risk (real feature; mitig: mirrors shipped T441, `useLocalStorageState` established, default preserves behaviour); rollback = revert one commit. Deps: best after T446 (import shared resolver; else inline + `// TODO T446`). Sibling of T441. Out of scope: visibility-pause for `startPolling` (unify w/ `useStalePolling` — own row), shared polling-controls component (own row). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T461-executions-detail-polling-toggle.md` |
| T465 | Backlog | Delete dead `useCodeHighlight` + drop `prismjs` dep | frontend (refactoring) — `frontend/AGENTS.md` Code Style: "Delete dead code, unused imports". `components/src/composables/useCodeHighlight.ts` (Prism Java highlighter) is dead: only ref anywhere = barrel `composables/index.ts:1` `export * from './useCodeHighlight'`; zero real consumers (`CodeTab.vue` moved to Monaco per T343); grep `useCodeHighlight`/`highlightedHtml`/`Prism.` across `frontend/` (excl node_modules, worktrees) → only the file itself; no spec. `prismjs` (`^1.30.0`) + `@types/prismjs` (`^1.26.6`) in `components/package.json` exist ONLY for it (`import Prism from 'prismjs'` + `import 'prismjs/components/prism-java'` eager at module top). Task: (1) delete `useCodeHighlight.ts`; (2) drop `export * from './useCodeHighlight'` from `composables/index.ts`; (3) remove `prismjs` + `@types/prismjs` from `components/package.json`, `pnpm install` to update lock; (4) re-grep → `prismjs`/`useCodeHighlight`/`Prism` gone from `frontend/src` + `**/package.json`. Acceptance: `grep -rn 'prismjs\|useCodeHighlight\|Prism\.' frontend` (excl node_modules) → nothing; `pnpm-lock.yaml` no longer resolves `prismjs`/`@types/prismjs` (unless pulled transitively — check + note); `pnpm build:components`+`pnpm test`+`pnpm typecheck` (T452)+`pnpm lint` green; `@cbs/components` public API no longer advertises `useCodeHighlight`/`highlightedHtml`. Tier `frontend` (`frontend/components`). Very low risk (0 refs, no spec, dep used only here; bundle shrinks by prism core + java grammar); rollback = revert one commit + `pnpm install`. Sibling of T460. Out of scope: `CbsButton` (T460), broader `index.ts` dead-export audit (own row), Monaco lazy-load (already `await import`). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T465-delete-dead-usecodehighlight.md` |
| T466 | Backlog | Route `executions/export.get.ts` through `proxyToBackend` — CSV export bypasses token refresh + session security | frontend / BFF (refactoring / reliability) — `admin-ui-plugin/server/api/v1/executions/export.get.ts` hand-rolls its backend call (`$fetch.raw` + `buildBackendHeaders` + `attachAuth`); 45/52 routes use `proxyToBackend` (`server/utils/httpClient.ts`), non-users = 5 OIDC auth + `health` + this. Bypassing it, CSV export MISSES: (a) 401/403 → transparent token refresh + retry (`refreshTokens`→`writeSession`→`doFetch({Authorization})`) — export just fails; exports fire from long-lived pages where the BFF access token is most likely expired → broken download instead of silent refresh; (b) absolute-timeout session clear + refresh-token-reuse detection; (c) `TimeoutError→504` + BFF error envelope (`{code,message,diagnostics}`) — so `extractApiError` (T439) can't parse a failed export; (d) `[BFF >]`/`[BFF <]`/`[BFF !]` logging w/ requestId+correlationId. `dsl/definitions/export.get.ts` already uses `proxyToBackend` (JSON). Only CSV export (text body + upstream header passthrough) is stuck outside. Task: (1) `httpClient.ts` — extend `proxyToBackend` opts w/ `raw?: boolean` (→ `$fetch.raw` + `responseType:'text'`) + `forwardResponseHeaders?: string[]` (copy upstream headers onto H3 response on success); `doFetch` branches on `raw`, closes over it so refresh-retry path still works; non-raw callers unchanged (default false); (2) rewrite `export.get.ts` → keep query mapping, `return proxyToBackend(event, '/api/executions/export.csv', { query: params, raw: true, forwardResponseHeaders: ['content-type','content-disposition','x-export-truncated'] })`, drop `ofetch`/`attachAuth`/`buildBackendHeaders` imports; (3) note only: query mapping dup'd w/ `executions/index.get.ts` → `executionFilterParams` helper follow-up. Tests: `httpClient.spec.ts` — `raw:true` case: backend 401 → 1 refresh + retry → 200 body returned + headers set; timeout → 504+envelope; header forwarding; `executions.spec.ts` export cases green (+ error case if uncovered); `routeCoverage.spec.ts` green; `pnpm test`+`pnpm typecheck`+`pnpm lint` green. Tier `frontend` (BFF). Low-medium risk (touches shared `proxyToBackend` — well-specced; `raw` branch additive + off by default; export contract preserved); rollback = revert one commit. Out of scope: auth routes + `health` (special), `dsl/definitions/export.get.ts` (already proxied), `executionFilterParams` dedup (follow-up), streaming/chunked export (keep buffered). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T466-export-route-through-proxy.md` |
| T467 | Backlog | `CbsDrawer` + `AppMobileDrawer` adopt `useModalDialog` — false `aria-modal` (no focus trap / Esc) | frontend / a11y — `components/src/composables/useModalDialog.ts` is a complete accessible-dialog behaviour (focus in on open, Tab/Shift+Tab trap, Esc close, bg `inert`+`aria-hidden`, return-focus); 3 confirmation modals use it (`RunConfirmationModal`, `CancelExecutionConfirmationModal`, `DeleteDraftConfirmationModal`). But `components/src/components/CbsDrawer.vue` (used by `SavedDraftsWidget`, `HelperSearchPanel`, `dsl-workbench.vue` History + Helper-catalog) AND `components/src/components/AppMobileDrawer.vue` (mobile nav) render `role="dialog"` + `aria-modal="true"` but DON'T behave modally — only overlay `@click` closes; no trap, no Esc, no inert, no return-focus. `aria-modal` without a trap is WORSE than omitting it (SR announces modal, focus walks behind). Task per file: (1) import `useModalDialog`; (2) `drawerRef` on `<aside>`; (3) `const { open: openDialog, close: closeDialog } = useModalDialog(drawerRef, { onClose: () => close() })`; (4) `watch` `open` model → `true`:`openDialog()` / `false`:`closeDialog()` (single-composable same-scope sync — AGENTS-allowed); (5) keep overlay `@click`, close button, `role`/`aria-modal`/`aria-label`; `CbsDrawer` props (`closeLabel`/`title`/`testId`/`widthClass`) unchanged. Net-new: Esc closes, focus enters+trapped, page `inert`, focus returns. Tests: opening moves focus in + Tab cycles; Esc closes (emits `update:open` false / mobile close); body children `inert`+`aria-hidden` while open, restored on close; return-focus; overlay+button still work; `CbsDrawer` spec (T459 or new) + `SavedDraftsWidget`/`HelperSearchPanel`/`AppMobileDrawer`/`dsl-workbench` specs green; `pnpm test`+`pnpm typecheck`+`pnpm lint` green. Tier `frontend` (`frontend/components` + admin-ui-plugin specs). Medium risk (behavioural change to 2 shared components — Esc closes, focus moves; mitig: `useModalDialog` proven w/ 3 consumers+spec, is the CORRECT a11y behaviour, `inert` tolerates stubbed-Teleport mounts); rollback = revert one commit. Deps: best after T459 (`CbsDrawer` basic spec — extend it; else add here); T459 deferred trap/Esc as "a feature" = this. Out of scope: shared `<AppModal>` consolidation (own row), `useModalDialog` internals, non-dialog popovers (dropdown/tooltip). See plan. |   Low  | loop  | -      | T459       | `./docs/plans/T467-drawers-adopt-usemodaldialog.md` |
| T468 | Backlog | `module.ts` runtimeConfig merge uses `||` — drops valid `0`/`false`/`''` host/env overrides | frontend (correctness) — `admin-ui-plugin/module.ts` `setup()` merges module options + existing `runtimeConfig` + default via ~25 lines of `||` chains. `runtimeConfig.X` is already populated by Nuxt from `NUXT_*` env, so `||` means an explicit falsy-but-valid value can't override the default: `NUXT_BACKEND_TIMEOUT_MS=0`→`10000`; `authSessionIdleTimeoutSeconds:0` (valid "no idle timeout") indistinguishable from unset; `authSessionSecureCookies:false`/`''`→overridden; `public.authEnabled:false` w/ issuer set (staging)→forced `true`. Comment claims "unset values preserve pre-hardening behaviour exactly" but code can't tell unset from falsy. T442 READ side already uses `?? default`; write side contradicts it. NO test (T442+T452 both out-of-scoped for lack of harness). Task: (1) extract pure fn `resolveRuntimeConfig(existing, options)` → `{config, publicConfig}` in new `admin-ui-plugin/server/utils/moduleRuntimeConfig.ts` (or `module.internals.ts`), using `existing.X ?? options.X ?? DEFAULT` for every key; `authEnabled` = `existing.public.authEnabled ?? Boolean(resolvedIssuer)`; keep all keys + defaults verbatim (`http://localhost:8090`, `cbs-nova-bff`, `.../auth/callback`, `/`, `0`, `''`, `10000`, `CBS Nova Admin`, `default`); (2) `module.ts setup()` calls it, assigns onto `nuxt.options.runtimeConfig`/`.public`; (3) new `__tests__/moduleRuntimeConfig.spec.ts` (plain vitest, pure fn, no Nuxt harness): all-empty→defaults; options>default, existing>options; `backendTimeoutMs` existing `0`→`0` not `10000`; idle/absolute `0` preserved; `secureCookies` `false`/`''` preserved; `authEnabled` `false` wins w/ issuer set, else `Boolean(issuer)`; callback/postLogout/appName/namespace defaults. Acceptance: no `||` fallback left in the merge (all `??`); merge is pure + unit-tested; `pnpm build`+`pnpm test`+`pnpm typecheck`+`pnpm lint` green; `nuxt.config.dev.ts` still boots same effective config; PR notes the one intentional change (explicit falsy config now overrides). Tier `frontend`. Medium risk (module config runs every host build/deploy; mitig: mechanical extraction, `??` = semantics comment already claims + read side already uses, pure fn fully tested, defaults unchanged); rollback = revert one commit. Out of scope: `defu` adoption (`??` enough, no dep — note as alt), rest of `module.ts` (pages/layout/imports), config value validation (negative timeouts — T442 note). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T468-module-runtimeconfig-merge.md` |
| T469 | Backlog | `useAuth` triple-fires `/api/v1/auth/session`; SSR state never awaited | frontend (correctness / perf) — `admin-ui-plugin/app/composables/useAuth.ts`: `onMounted(loadSession)` PLUS `if (process.client) { loadSession() } else { loadSession() }` — the if/else branches have IDENTICAL bodies (no-op split, misleading comments). Client hydration → setup-body call + `onMounted` both fire → 2 concurrent `GET /api/v1/auth/session` per page load racing to write `user`/`authenticated`. SSR → setup-body call is `async` + NOT awaited → resolves after render, so "server render has the state" comment is false → SSR always renders logged-out, client flips → sign-in-button flash. One caller (`app/layouts/admin.vue:27`), so ~3 session fetches per full load. Nuxt `^3.11` → `callOnce` available. Task: (1) replace `onMounted`+`if/else` with single `await callOnce('cbs-auth-session', loadSession)` (or `useAsyncData` if pending state needed) — runs once across SSR+hydration, safe on later navs; (2) if auth must refresh on route change, do it once via `useRouter().afterEach → loadSession()` in the composable, not per-setup — decide based on post-login redirect flow, document; (3) delete dead `process.client`/`else` branch + misleading comments; (4) `loadSession`/`login`/`logout` bodies unchanged. Acceptance: exactly 1 `/api/v1/auth/session` per mount (assert w/ `$fetch` spy call-count in `useAuth.spec.ts`); SSR path awaits load → `authenticated`/`user` correct in server HTML; no identical-body `process.*` branching left; `admin.vue` still shows correct affordance, no flash (manual `pnpm dev` w/ live OIDC, PR note); existing `useAuth.spec.ts`+`admin.spec.ts` green; `pnpm test`+`pnpm typecheck`+`pnpm lint` green. Tier `frontend`. Low-medium risk (auth state feeds layout sign-in UI; mitig: same `loadSession` logic + state keys, `callOnce` is the documented Nuxt primitive for this, one caller); rollback = revert one commit. Out of scope: route middleware / server auth guard (useAuth is display-state only), BFF token refresh (`httpClient.ts`), `login`/`logout` behaviour. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T469-useauth-dedupe-session-load.md` |
| T470 | Backlog | Extract `docsBaseUrl` git-remote parsing from `admin.vue` into a tested util | frontend (refactoring / missing tests) — `frontend/AGENTS.md`: "Keep pages thin; logic in composables/stores." `admin-ui-plugin/app/layouts/admin.vue:68` has a ~20-line `docsBaseUrl` computed parsing a git remote URL (SSH `git@github.com:owner/repo(.git)` + HTTPS `https://github.com/owner/repo(.git)`), strips `.git`, branch defaults `main`, builds `https://github.com/<owner>/<repo>/blob/<branch>/docs/` or `undefined` (no origin / non-GitHub). Regex logic in a layout, UNTESTED (`admin.spec.ts` only stubs `docsBaseUrl` as a prop name). Task: (1) new `components/src/utils/docsBaseUrl.ts` (`GitInfo` type in `components/src/types/buildInfo.ts`) exporting `buildDocsBaseUrl(git: GitInfo | undefined): string | undefined` — move body verbatim; (2) `admin.vue` → `computed(() => buildDocsBaseUrl(info.value?.git))`; (3) no behaviour change. Tests: new `__tests__/docsBaseUrl.spec.ts` — SSH `.git` + branch `feat/x` → `.../blob/feat/x/docs/`; SSH no `.git` → same; HTTPS no branch → `.../blob/main/docs/`; HTTPS `.git` stripped; no `git`/no `remote.origin.url` → `undefined`; non-GitHub (`git@gitlab.com`, `https://bitbucket.org`) → `undefined`; repo name w/ `/` (subgroup) → pin current `(.+?)` behaviour. `admin.vue` computed now one-liner; `admin.spec.ts` green (+ optional case: `useAdminInfo` mock → asserts `docs-base-url` prop to footer); `pnpm test`+`pnpm typecheck`+`pnpm lint` green. Tier `frontend` (`frontend/components`). Very low risk (pure extraction, verbatim logic, one call site); rollback = revert one commit. Out of scope: non-GitHub docs hosts (stays "GitHub or nothing"), `displayName` computed same file (trivial `??` — leave), `useAdminInfo` composable. See plan. |   Low  | loop  | -      | -          | `./docs/plans/T470-extract-docs-base-url.md` |
| T476 | Backlog | DSL `@Helper` — `parseDuration` (ISO-8601 + `"90m"`/`"1h30m"`/`"250ms"` shorthand → millis/seconds/normalized ISO) | backend (@Helper addition) — 45 helpers exist; time helpers are `dateMath` (DST-safe **date** arithmetic vs an anchor), `formatDate`/`parseDate`/`currentTimestamp` (timestamps). NONE parse a bare duration string → number. A DSL wiring `delayMillis`/`timeoutSeconds`/retention windows (T412) from a config string has no helper. Sibling of T418 (`interpolate`). Helper idiom (`SemverHelper`): `@Helper(name=...)`, `implements Executable<In,Out>`, In/Out records under `helper/model/`, `execute(Context<In>)`→`Result.success/failure`, wrap `RuntimeException`. Task: (1) `ParseDurationHelper` `@Helper(name="parseDuration")`; (2) `ParseDurationIn(String value)` required, null/blank→failure; accepts (case-insensitive, trimmed): ISO-8601 via `Duration.parse` (incl. fractional secs, `P2DT3H`; bare `P2D` normalize), AND shorthand `<number><unit>` segments units `d`/`h`/`m`(minutes)/`s`/`ms` — `"90m"`,`"1h30m"`,`"2d12h"`,`"250ms"`; whitespace between segments OK; bare number no unit → failure (ambiguous, documented; NOT assumed millis); `m`=minutes always; (3) negative → failure phase 1 (note signed follow-up); overflow past `Long.MAX_VALUE` millis → failure not wrap; (4) `ParseDurationOut(long millis, long seconds, String iso)` — `iso`=`Duration.toString()` normalized; (5) `ParseDurationHelperTest` — ISO forms, each shorthand unit incl `ms`, compound, fractional, whitespace, case, null/blank/bare-number/garbage/unknown-unit→failure, negative→failure, `iso` round-trips, large-valid, overflow→failure; (6) `docs/dsl/helpers.md` entry next to `dateMath` w/ accepted-forms table; (7) grep `hasSize`/`containsExactly` in `starter/src/test/.../helper` for a fixed helper-count/name test, adjust if present (discovery is `@Helper`-driven — likely none). Preview-safe: pure fn, no I/O, no dep. Acceptance: `:starter:test` green, resolvable as `parseDuration`. Tier `backend`. Low risk (additive helper); rollback = revert one commit. Out of scope: signed durations, `formatDuration` inverse, month/year units. See plan. |  Medium  | loop  | -      | -          | `./docs/plans/T476-parse-duration-helper.md` |
| T475 | Backlog | Fix `docs/dsl/examples.md` module-path & command drift — stale `backend/dsl-examples` / `starter-example` paths, wrong gradle wrapper, 10 missing example files | backend (documentation) — code-confirmed drift: `docs/dsl/examples.md` says examples live in `backend/dsl-examples/src/` but the module moved to `backend/dsl-starter/dsl-examples/src/dsl/` (+ `src/models/`) per `dsl-starter/settings.gradle` `include 'dsl-examples'`. Concrete stale facts: (1) every path prefix wrong (`backend/dsl-examples` → `backend/dsl-starter/dsl-examples`); (2) file list shows ~11, tree has 21 (`ls backend/dsl-starter/dsl-examples/src/dsl/` — adds CsvYamlExportDsl, HelperPipelineDsl, HttpResilienceDsl, JsonPatchRoundTripDsl, JwtHttpCallDsl, MaskSensitiveDataDsl, PaginatedUrlDsl, RecordAggregationDsl, ReleaseWindowDsl, UnreliableApiDsl, VersionProbeDsl); (3) `cd backend && ./gradlew :dsl-examples:compileDsl` — root `./gradlew` is Gradle 8.13 and FAILS under Java 25 (CLAUDE.md caveat); correct wrapper `backend/dsl-platform/gradlew -p backend/dsl-starter :dsl-examples:compileDsl` (executor verify exact); (4) `backend/starter-example` module + `:starter-example:test` don't exist — integration tests are `backend/dsl-starter/starter/src/integrationTest/java/cbs/nova/dsl/example/integration/*` (BatchProcessingDslIntegrationTest, HttpResilienceDslIntegrationTest, UnreliableApiDslIntegrationTest, DslVersioningIntegrationTest, PreviewDryRunIntegrationTest + `cbs/nova/starter/DslExamplesEndToEndTest`), run via the `starter` module `integrationTest` source set (verify task name). Task: rewrite paths + file list (exact 21, or a `ls`-note + 4-5 canonical) + every gradle block to be copy-paste runnable (verify by running `compileDsl` at least); spot-check the generated-package-path section against a real `build/generated` dir; grep `docs/` + `backend/AGENTS.md` for sibling docs repeating the stale paths, fix or note. No code change, `docs/` only. Tier `backend`. Zero risk; rollback = revert one commit. Out of scope: adding NEW example files (own row), a doc-drift guard covering `examples.md` (T380 territory — note as follow-up). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T475-fix-dsl-examples-doc-drift.md` |
| T474 | Backlog | `DropdownMenu.vue` unit spec — 150-line reusable primitive, keyboard-nav + focus mgmt, no spec | frontend (missing tests) — `components/src/components/dropdownMenu/DropdownMenu.vue` (trigger `button[aria-haspopup][aria-expanded][aria-controls]` + `role="menu"` panel of `role="menuitem"` buttons) has real logic and NO spec: `toggleDropdown`/`openAndFocusFirst` (ArrowDown on trigger), `navigate(±1)` with disabled-item skip + modulo wraparound, `selectItem` (emits `select`, closes; no-op on `item.disabled`), `closeDropdown(returnFocus)` (Escape → focus back to trigger), `handleFocusOut` (closes unless focus went to trigger / stays in menu), `defineExpose({ close })`. Props `label`/`items`/`menuId?`/`align` (`'right'` default → `right-0` vs `left-0`); emits `select(item)`. Only consumer `dsl-workbench.vue`. Sibling gaps: `CbsDrawer` (T459), `HotkeyTooltip` (T472). Task: new `components/src/components/__tests__/DropdownMenu.spec.ts` (`@vue/test-utils`+vitest) — closed by default (no `[role=menu]`, `aria-expanded=false`); click toggles; `aria-haspopup` always, `aria-controls`===`menuId`; ArrowDown on trigger opens+focuses first; Arrow up/down move focus, wrap both ends; disabled item skipped by nav; click/Enter/Space on item → one `select` emit + close; disabled item → no emit, stays open; Escape (trigger + menu) closes + returns focus; `align="left"`→`left-0`; exposed `close()` works; `focusout` outside closes, to trigger/menuitem does not. `pnpm test`+`pnpm lint` green. Test-only, no production change. Tier `frontend` (`frontend/components`). Zero risk; rollback = delete the spec. Out of scope: raw-palette tokens in the SFC (T453), focus-trap/`inert` (component deliberately uses focusout-to-close), layout-chrome component specs (own row). See plan. |  Low  | loop  | -      | -          | `./docs/plans/T474-dropdownmenu-spec.md` |
| T473 | Backlog | Codegen provenance — `dslPlatformVersion` on `@DslGenerated` + `@Generated.comments`, so a generated class records which cbs-nova platform version built it | backend (DX, `dsl-platform`) — code-confirmed TODO pair, one concern: `dsl-codegen/.../generator/GeneratorMetadata.java:22` (`// TODO: we also need to add version of dsl platform itself, for later debug`) emits `@DslGenerated(generator,timestamp,javaVersion,user)` + `@Generated(...)` onto every generated type via text templates; `dsl-api/.../annotation/DslGenerated.java:20` (`// TODO: add here two new fields: dslBuildInfo, dslGitInfo ...`). Version lives in `backend/gradle.properties` `version=` applied via `dsl-platform/build.gradle:13`; nothing carries it into `dsl-codegen` runtime today (grep `getImplementationVersion`/`build-info` = 0). `@DslGenerated` is `@Retention(SOURCE)` (marker only); generator tests use substring asserts (`source().contains("@DslGenerated")`) not golden → adding an attribute is safe. Phase 1 = platform version ONLY (`dslBuildInfo`/`dslGitInfo` need a Gradle git plugin → follow-up row). Task: (1) `DslGenerated` gains `String dslPlatformVersion();`; (2) filtered resource `dsl-codegen/src/main/resources/cbs-nova-dsl-codegen.properties` (`dslPlatformVersion=@dslPlatformVersion@`) + `processResources` expand from project `version` in `dsl-codegen/build.gradle`; (3) `GeneratorMetadata` loads it once (static, classloader), `"unknown"` fallback when resource absent / token unexpanded (keeps raw-classes-dir test runs green); (4) template emits `dslPlatformVersion = "<v>"` (via `escapeJavaString`) + append `dsl.platform.version=<v>` to `@Generated.comments`; (5) remove both TODOs, replace `DslGenerated.java` comment with a one-line "build/git info = tracked follow-up" note; (6) tests: new/extended generator test asserts emitted source contains `dslPlatformVersion = "` + real version when a processed resource is visible + `"unknown"` fallback path; `:dsl-codegen:test` + `:dsl-api:test` green with NO golden edits (if a snapshot does full-string compare, update it same commit + call out in PR). ABI: `dsl-api` public annotation gains a method → `make publish` before downstream regen — note in PR. Acceptance: generated classes carry the platform version; both TODOs gone; tests green. Tier `backend`. Low risk (additive attribute + 1 resource + 1 gradle block; `@Retention(SOURCE)` = no runtime change); rollback = revert one commit + `make publish`. See plan. |  Low  | loop  | -      | -          | `./docs/plans/T473-codegen-provenance-platform-version.md` |
| T472 | Backlog | `useHotkeyOverlay` + `HotkeyTooltip.vue` unit specs — last untested composable in `components/src` | frontend (missing tests) — `components/src/composables/useHotkeyOverlay.ts` (module-singleton `visible` ref; global `keydown`/`keyup` on `key === 'Alt'` + `blur` reset; returns `{ visible: readonly(visible) }`) and its only consumer `components/src/components/HotkeyTooltip.vue` (`props: { keys }`, slot passthrough, renders `span[data-testid="hotkey-tooltip"]` with `{{ keys }}` only while `visible`) have NO spec. Every other composable in `components/src/composables/` has a `__tests__` spec except this one and the dead `useCodeHighlight` (T465). Real consumer: `dsl/CodeTab.vue:179` (`keys="Ctrl+S"`). Task: (1) new `useHotkeyOverlay.spec.ts` — `visible` starts false; window `keydown {key:'Alt'}` → true, `keyup {key:'Alt'}` → false; `{key:'Alt',repeat:true}` no-op from false; non-Alt keys unchanged; window `blur` → false while Alt held; returned `visible` read-only; reset via `blur` in `afterEach` (module singleton persists across file). (2) new `HotkeyTooltip.spec.ts` — slot always rendered; tooltip absent when not visible; after `keydown {key:'Alt'}` + `nextTick` tooltip visible with text === `keys`; `keys` reflected verbatim. Runs in `@cbs/components` vitest project; `pnpm test` + `pnpm lint` green. Test-only, no production change — if genuinely untestable without a seam, stop and reassess rather than add a test-only export. Sibling of T459. Tier `frontend` (`frontend/components`). Zero risk; rollback = delete the two spec files. Out of scope: listener-cleanup/HMR refactor of the module singleton (note only), `useCodeHighlight` removal (T465). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T472-hotkey-overlay-tooltip-specs.md` |
| T471 | Backlog | `/executions/[id]` e2e smoke spec — last route with no e2e | frontend (missing tests) — `frontend/e2e/` covers `/` (T437), `/dsl-workbench`, `/executions` list + error-banner, `/runner`, `/schedules` (T454). Execution detail `/executions/[id]` has NO e2e — last route without one. Template = 3 states (`ErrorBanner` on fetch fail / `Loading…` / detail w/ summary + tabs), no top-level heading or testid → a white-screen regression wouldn't be caught. Task: new `frontend/e2e/execution-detail.spec.ts`, backend-optional (mirror `executions-list.spec.ts`): (1) unknown id → `page.goto('/executions/nonexistent-e2e-xyz')` → `error-banner` OR `error-page` visible ≤15s (backend 404→ErrorBanner; backend down→same/Nuxt error page), assert page not blank; (2) click-through IF a row exists → `goto('/executions')`, if `execution-list` has ≥1 row click first, `waitForURL(/\/executions\/.+/)`, assert detail renders (summary component / `run-again-button` / tab container visible); no rows → `test.skip()` + comment. Add `data-testid="execution-detail"` to the outer `<div>` in `executions/[id].vue` (1 line, in scope) if no stable anchor fits. Header comment matching siblings. Acceptance: unknown-id assertion passes (backend up or down); click-through runs w/ executions present, `test.skip`s cleanly when not; `pnpm test:e2e` green; no other e2e touched; if testid anchor added, `executions/__tests__/[id].spec.ts` green. Tier `frontend` (`frontend/e2e` + maybe 1 line `[id].vue`). Very low risk (new e2e + at most 1-line testid); rollback = delete spec. Sibling of T437/T454. Out of scope: tab/diagram/logs/trace interaction coverage (smoke only), detail polling toggle (T461), `[id].vue` unit coverage (already has spec). See plan. |   Low  | loop  | -      | -          | `./docs/plans/T471-execution-detail-e2e-smoke.md` |
