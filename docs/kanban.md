# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                       |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:----------------------------------------------------------------|
| T140 | Done | SpringBeanHelperInstanceResolver characterization tests | backend — Add unit tests for `backend/starter/src/main/java/cbs/nova/starter/helper/SpringBeanHelperInstanceResolver.java` (38 lines, 0 tests). It implements `HelperInstanceResolver.resolve(Class)`: asks the Spring `ApplicationContext` for the helper bean, and on `NoSuchBeanDefinitionException` falls back to reflective `helperClass.getDeclaredConstructor().newInstance()` (the `newInstance` path is `@Deprecated(forRemoval = true)` with a `TODO: remove reflection, use typed info instead`). This is the *other* reflection site beyond `TemporalDslProcessLauncher` (refactored already) and the next target of the reflection-removal effort — a characterization test now lets that removal land safely. Pin (mock `ApplicationContext`): (1) bean present → returns the bean, no reflection; (2) bean absent (`NoSuchBeanDefinitionException`) → reflective no-arg construction returns a fresh `Executable` instance; (3) class with no public no-arg ctor → `IllegalStateException` with class name + cause; (4) ctor that throws → `IllegalStateException("Failed to create helper instance: …")`. Assert no interaction with the context beyond the single `getBean` call. Pure unit (`Mockito.mock(ApplicationContext.class)`), no Spring context bootstrap. | High | loop | - | - | `./docs/plans/T140-spring-bean-helper-resolver-tests.md` |
| T145 | Backlog | OutputPanel component tests | frontend — Add Vitest suite for `frontend/components/src/components/runner/OutputPanel.vue` (57 lines, 0 tests) — the tab orchestrator at the center of the Runner UI's result panel. Owns `activeTab` state (`result`/`metadata`/`errors`), the `showExplain` computed (renders `ExplainOutput` when `mode==='explain'`), the `isEmpty` computed (placeholder + hides tabs when `output` null), and delegates to `ResultTab`/`MetadataTab`/`ErrorsTab`. Every runner run's output flows through it; untested — a regression in tab switching, explain routing, or empty state slips into the primary user surface. Closes runner-output gap left by T123 (InputField)/T132 (InputForm). Pin: default `activeTab=result` → `ResultTab` renders; click `Metadata`/`Errors` toggles child + active class (`border-blue-600`); 3 tab buttons w/ labels `Result`/`Metadata`/`Errors`; `mode='explain'` → `ExplainOutput` renders w/ `:description`/`:mermaid-diagram` pass-through from `output`; `output=null` → placeholder "Output will appear here after running.", no tabs, no child; non-null output w/ undefined nested fields doesn't throw. `mount`/`shallowMount` + happy-dom (vitest config present). Spec at `src/components/__tests__/OutputPanel.spec.ts` (matches `AppSidebar.spec.ts`/`InputField.spec.ts` convention). | High | loop | - | - | `./docs/plans/T145-outputpanel-tests.md` |
| T144 | Backlog | EscapeUtil unit tests | Add first tests for `backend/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/util/EscapeUtil.java` (34 lines, 0 tests) — the single static `escapeJavaString(String)` method on the codegen critical path. Every generated Java source string literal flows through it: `GeneratedClassProviderGenerator` and `GeneratorMetadata` both call it to embed DSL metadata (names, descriptors, versions) into emitted Java source. A wrong escape produces uncompilable generated code or silently corrupted embedded literals — a build break surfacing far downstream from the cause, not at codegen time. Pin: `"`→`\"`, `\`→`\\` (per-char so backslash isn't double-counted), `\b`/`\f`/`\n`/`\r`/`\t` exact emitted forms; control chars `<0x20` emit as lowercase 4-digit unicode via `\u%04x` (pin **lowercase** hex so a future `%04X` flip is caught); chars `>=0x20` pass through verbatim incl. high-codepoint `'€'`/emoji (no high-char `\u`-escaping); empty string→empty; mixed-content single-pass escaping in source order; already-escaped-looking input treated literally (`\"` input→`\\\"`). Pure `String→String`, private ctor (static-only). Mirrors `JsonTest`/`CodegenNamingTest` style (JUnit 5 + AssertJ, parameterized). | High | loop | - | - | `./docs/plans/T144-escape-util-tests.md` |
| T143 | Backlog | CompensationRegistry unit tests | Add first tests for `backend/dsl/src/main/java/cbs/nova/dsl/CompensationRegistry.java` (97 lines, 0 tests) — the `ConcurrentHashMap<String,List<CompensationEntry>>`-backed registry that tracks per-`runId` compensatable transactions and drives saga rollback. Sits on the compensation hot path: `GlobalManager` calls `register(...)` after each transaction step, `compensate(name, runId, ...)` on single-txn failure, `compensateAll(...)` on process-level failure. T136 just fixed fragile-txn failure *propagation* at the Temporal-invoker layer; this registry is the layer *below* that decides whether/which compensation fires, and has zero direct tests (only indirect via `GlobalManagerTest`/`MultiTransactionCompensationTest`). Pin: `register` returns false + stores nothing when `compensationLogic==null`, true otherwise; `compensate` invokes the matching entry once with `CompensationRichContext.error()` = passed `Throwable`, then removes that single entry, searches LIFO within runId, no-op on unknown runId / unknown name (other entries untouched); `compensateAll` invokes every entry in reverse-registration order and clears the runId's entries; multi-runId isolation; `clear()` wipes all; exact-cause propagation. All collaborators concrete + instantiable (`ContextFactory`, `ExecutionTraceCollector`, `TransactionDslObject` record w/ lambda `compensationLogic`) — no mocks. Mirrors `GlobalManagerTest` style (JUnit 5 + AssertJ). | High | loop | - | - | `./docs/plans/T143-compensation-registry-tests.md` |
| T142 | Backlog | InMemoryDslRunRepository unit tests | Add first tests for `backend/dsl/src/main/java/cbs/nova/dsl/repository/InMemoryDslRunRepository.java` (37 lines, 0 tests) — the `ConcurrentHashMap`-backed `DslRunRepository` implementation that persists DSL process runs and is the data layer behind the executions REST surface (every `/api/v1/executions` list/get by `runId`/`processName` goes through it). A wrong branch is a silent data-integrity bug in the executions UI (wrong run returned, runs bleeding between processes, status updates lost), not a build break. Pin: `save(run)` round-trips through `findByRunId`; `findByRunId` miss → empty `Optional`; `findByProcessName` returns only matching runs (empty when none, multiple when shared process name, distinct processes don't bleed); re-`save` with same `runId` overwrites last-write-wins (pins `RUNNING`→`COMPLETED` status-update pattern); returned `List` isolated from internal state. `DslRun` is a Lombok `@Builder` record (`status` is a raw `String`, not enum-bound despite `DslRunStatus` existing in `dsl-api`). Pure unit test, mirrors `GeneratedClassRegistryTest`/`GlobalManagerTest` style (JUnit 5 + AssertJ). | High | loop | - | - | `./docs/plans/T142-in-memory-dsl-run-repository-tests.md` |
| T141 | Done | MapInputConverter unit tests | backend — Add unit tests for `backend/dsl/src/main/java/cbs/nova/dsl/converter/MapInputConverter.java` (199 lines, 0 tests), the static reflection converter that turns a flat `Map<String,Object>` (or already-typed value) into the target input type a generated Temporal workflow/activity expects. It is on the hot path of every run — `TemporalDslProcessService` (and the broader process-launch refactor) calls `convert(value, targetType)` to materialise record inputs from REST JSON, so a wrong branch is a silent runtime type error, not a build break. Pin across every branch: (1) `null` value → `null`; (2) `targetType == MapInput` → existing MapInput passthrough, `Map` → `MapInput.fromMap`, other → returned as-is; (3) `Class` target: already-instance passthrough, `String` via `String.valueOf`, each primitive + wrapper (boolean/byte/short/int/long/float/double/char — incl. `Number`-narrowing and `Boolean.parseBoolean`), `enum` via `Enum.valueOf`, array from `Collection`, record from `Map` (component-by-component recursive convert + canonical-ctor invoke), `Map`/`Collection` passthrough, unsupported pair → `IllegalArgumentException`; (4) `ParameterizedType` target: `List<E>` element-wise convert, `Map` passthrough, else delegate to `convertToClass`; (5) record with non-Map input → IAE; array from non-Collection → IAE; unsupported primitive type → IAE (defensive). Pure static (`MapInputConverter.convert(...)`), no Spring/Temporal. Use small fixture records declared in the test. | High | loop | - | - | `./docs/plans/T141-mapinputconverter-unit-tests.md` |
| T146 | Done | Preview DataSource call-capture autoconfiguration | Add a Spring Boot autoconfiguration in `:starter` that proxies every `DataSource` bean and records JDBC operations as external `database` calls for preview/explain reports. | High | loop | T151 | - | `./docs/plans/T146-preview-datasource-call-capture-autoconfig.md` |
| T147 | Done | Preview Feign HTTP call-capture autoconfiguration | Add a conditional Spring Boot autoconfiguration that records Feign HTTP requests as external `http` calls for preview/explain reports. | High | loop | T151 | - | `./docs/plans/T147-preview-feign-call-capture-autoconfig.md` |
| T148 | Done | Runtime call-tree collector for preview/explain | Build a runId-scoped nested call tree of process/transaction/helper/function invocations and include it in preview/explain reports. | High | loop | T150, T151 | - | `./docs/plans/T148-runtime-call-tree-collector.md` |
| T149 | Backlog | Conditional dry-run Logback consumer | Capture log events emitted during preview/explain via a Logback appender active only in dry-run context; expose them in reports. | High | loop | T150, T151 | - | `./docs/plans/T149-conditional-dry-run-logback-consumer.md` |
| T150 | Backlog | Enrich preview/explain reports with AST and logs | Wire the call-tree collector and dry-run log consumer into `DevDslRuntime` so `PreviewReport`/`ExplainReport` include `astTree` and `dryRunLogs`. | High | loop | T151 | T148, T149 | `./docs/plans/T150-enrich-preview-explain-reports.md` |
| T151 | Backlog | Preview dry-run integration test | End-to-end integration test executing a DSL flow in RUN and PREVIEW modes, asserting different content, captured DB/Feign calls, call-tree AST, and dry-run logs. | High | loop | - | T146, T147, T150 | `./docs/plans/T151-preview-dry-run-integration-test.md` |
