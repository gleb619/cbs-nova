# Kanban Board

This file is a lightweight planner for tracking implementation tasks. It is intended to be used by the coding agent.

- **Use this file only as a high-level planner.** Detailed task descriptions, acceptance criteria, technical notes, and diagrams must be stored under [`./docs/plans`](./plans).
- The coding agent **must** update the `Status` column when taking a task and again when the work is finished.
- Keep the table sorted by `ID` for quick lookup.

## Status Legend

| Status        | Meaning                                                       | Who updates it  |
|---------------|---------------------------------------------------------------|-----------------|
| `Backlog`     | Task is defined but not yet ready to start.                   | Planner / Agent |
| `Ready`       | Task is ready to be picked up by the coding agent.            | Planner / Agent |
| `In Progress` | Coding agent is actively working on the task.                 | Coding agent    |
| `Review`      | Implementation done; pending review / validation.             | Coding agent    |
| `Blocked`     | Work cannot continue until another task or issue is resolved. | Coding agent    |
| `Done`        | Task completed and accepted.                                  | Coding agent    |

## Task Board

| ID   | Status      | Title                                                        | Description                                                                                                                                                                                                                                                                                         | Priority | Owner | Blocks                  | Blocked By    | Plan File                                                       |
|:-----|:------------|:-------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------:|:------|:------------------------|:--------------|:----------------------------------------------------------------|
| T52  | Done        | Full DSL Examples integration test with Temporal & Keycloak  | Add `backend/integration-tests` module that compiles DSL from `dsl-examples`, starts Temporal and Keycloak in Testcontainers, and exercises generated DSL workflows via secured REST endpoints.                                                                                                     |   High   | loop  | -                       | -             | `./docs/plans/T52-dsl-examples-full-integration-test.md`        |
| T69  | Done        | BPMN/Mermaid diagram generator tests                         | Add tests for `BpmnDiagramGenerator` and `MermaidDiagramGenerator` covering compensation, external calls, and call counts consumed by explain/runner.                                                                                                                                               |   High   | loop  | -                       | -             | `./docs/plans/T69-bpmn-diagram-tests.md`                        |
| T73  | Done        | DSL input JSON Schema generation                             | Generate JSON Schema (Draft 2020-12) from a DSL definition's `ParameterDescriptor`/`MapInput` input shape and expose it via the introspection REST endpoint so the Runner UI (T58) can render a typed, auto-generated input form instead of guessing the shape.                                     |   High   | loop  | -                       | -             | `./docs/plans/T73-dsl-input-json-schema.md`                     |
| T84  | Done        | DSL input JSON Schema generation                             | Superseded by T73 — `inputSchema` already wired on ProcessDetail/TransactionDetail in DslIntrospectionResource.                                                                                                                                                                                      |   High   | loop  | -                       | -             | `./docs/plans/T84-json-schema-from-parameter-descriptors.md`    |
| T86  | Done        | BPMN diagram generator tests                                 | Superseded by T69 — BpmnDiagramGeneratorTest already covers compensation, call counts, and process/transaction/helper entry points.                                                                                                                                                                 |   High   | loop  | -                       | -             | `./docs/plans/T86-bpmn-diagram-generator-tests.md`              |
| T90  | Done        | CI frontend build                                            | Extend GitHub Actions CI to install pnpm and build the Nuxt admin UI alongside backend steps.                                                                                                                                                                                                       |   High   | loop  | -                       | -             | `./docs/plans/T90-ci-frontend-build.md`                         |
| T75  | Done        | Process manager and runner tests                             | Add unit tests for `ProcessManager` and `DefaultProcessRunner` covering dispatch, EXPLAIN metadata, and compensation error wrapping.                                                                                                                                                                |   High   | loop  | -                       | -             | `./docs/plans/T75-process-manager-tests.md`                     |
| T76  | Done        | Fix mobile drawer DSL Workbench nav link                     | Already resolved — `frontend/admin-ui-plugin/app/layouts/default.vue` builds a single shared `navItems` array (`/dsl-workbench`) passed as `items` prop to both `AppSidebar` and `AppMobileDrawer`; no per-component nav arrays exist anymore.                                                     |   High   | loop  | -                       | -             | `./docs/plans/T76-mobile-nav-link-fix.md`                       |
| T78  | Done        | HelperManager unit tests                                     | Add focused unit tests for HelperManager and DefaultHelperRunner covering registration, lookup, preview/execute dispatch, and missing-name paths.                                                                                                                                                   |   High   | loop  | -                       | -             | `./docs/plans/T78-helper-manager-unit-tests.md`                 |
| T82  | Done        | GlobalManager runtime gap tests                              | Already covered — `GlobalManagerTest` has `transactionRoundTrip`/`unknownTransactionReturnsFailure`, `functionRoundTrip`/`unknownFunctionReturnsFailure`, `*NamesSorted`, and `describeHelperReturnsDescriptorForRegistered`.                                                                       |   High   | loop  | -                       | -             | `./docs/plans/T82-global-manager-runtime-gaps.md`               |
| T91  | Done        | Actuator runtime enablement                                  | Promote `spring-boot-starter-actuator` from `compileOnly` to `implementation` in `starter` and expose `/actuator/health` (deep, surfaces the existing `DslHealthIndicator`), `/actuator/info` (DSL + build version), and `/actuator/metrics` so the half-wired observability surface actually runs. |   High   | loop  | -                       | -             | `./docs/plans/T91-actuator-runtime-enablement.md`               |
| T93  | Done        | Schema field on definition details                           | Wire `inputSchema` into `DslIntrospectionResource` detail records and the BFF `/api/v1/dsl/definitions` route so the Runner UI can render typed `InputForm` from schema instead of guessing shape.                                                                                                  |   High   | loop  | -                       | -             | `./docs/plans/T93-schema-field-on-definition-details.md`        |
| T94  | Done        | Actuator runtime enablement                                  | Duplicate of T91 (identical plan) — see T91 for implementation.                                                                                                                                                                                                                                       |   High   | loop  | -                       | -             | `./docs/plans/T94-actuator-runtime-enablement.md`               |
| T102 | Done | CI backend test-completeness | Extend `.github/workflows/ci.yml` to run the full backend test matrix: add `:dsl-api:test` and `:misc-codegen:build`/`:misc-codegen:test` (currently `misc-codegen` and `:dsl-api:test` are never exercised in CI, so `HelperSpiProcessorTest`, `DslExceptionTest`, `MapInputTest`, `ResultTest` run only locally), and fix the deprecated `cache: gradle` to `gradle: true`. | High | loop | - | - | `./docs/plans/T102-ci-backend-test-completeness.md` |
| T103 | Done | Temporal service in docker-compose | Add `temporal` (auto-setup image) and `temporal-ui` services to the root `docker-compose.yml` and a shared network so `docker compose up` yields a full local runtime (Temporal is the workflow engine every worker/example/DSL run depends on, yet it is currently absent from the compose stack). | High | loop | - | - | `./docs/plans/T103-temporal-docker-compose.md` |
| T104 | Done | BFF DSL reload proxy                                       | frontend — Add Nitro BFF route `POST /api/v1/dsl/reload` that proxies to Spring Boot `POST /api/dsl/reload`, wire `useDslApi.reload()` and a Refresh action in `useDslWorkbench`, and add a Refresh button to the DSL Workbench toolbar. |   High   | loop  | -                       | -             | `./docs/plans/T104-bff-dsl-reload-proxy.md`                     |
| T105 | Done | Dev orchestration (make dev) | Add a Makefile/Taskfile + `scripts/dev.sh` that brings up the full local dev stack in order — `docker compose up` (Keycloak/Bugsink/Temporal once T103 lands), backend `./gradlew :starter:bootRun`, frontend `pnpm dev` — with healthcheck waits so new contributors get a one-command `make dev` instead of juggling three terminals. No production code touched. | High | loop | - | T103 | `./docs/plans/T105-dev-orchestration.md` |
| T106 | Done | Starter Dockerfile | Add a multi-stage Dockerfile that builds `backend/starter` and packages it as the `cbs-nova:latest` image the compose `spring-app` service already references (currently nonexistent), so `docker compose up` + the `make dev` orchestration (T105) yield a fully running local stack including the Spring Boot app. Unblocked — T120 added a runnable main class and enabled `bootJar`. | High | loop | - | - | `./docs/plans/T106-starter-dockerfile.md` |
| T120 | Done | Runnable Spring Boot main application in `:starter` | `:starter` has no `@SpringBootApplication` under `src/main/java` (only in test/integrationTest) and `bootJar` is explicitly disabled in `starter/build.gradle`. `./gradlew :starter:bootRun` fails immediately with "Main class name has not been configured". Silently breaks T105's `make backend`/`scripts/dev.sh` and blocks T106's Docker image. Add a minimal main app class + re-enable `bootJar`. | High | loop | T106 | - | `./docs/plans/T120-starter-runnable-main-application.md` |
| T107 | Done | Root README quickstart | Expand the current 2-line `README.md` stub into a real project front door: what cbs-nova is, prerequisites (Java 25, pnpm, Docker), one-command local stack (`make dev` once T105 lands / `docker compose up`), key ports, architecture-doc pointers (`docs/architecture-*.md`), where DSL examples live, and how to run backend/frontend tests. | High | loop | - | T105, T106 | `./docs/plans/T107-root-readme-quickstart.md` |
| T108 | Done | ExecutionTraceCollector runId-scoped trace | Replace `ThreadLocal<List<String>>` in `ExecutionTraceCollector` (shared bean) with a runId-keyed store so traces survive Temporal worker thread-pool reuse, activity/workflow boundary crossings, and replay without bleeding between runs or leaking across reused threads. Fixes a latent correctness bug in the data behind explain/runner and the T69/T86 diagram tests. | High | loop | - | - | `./docs/plans/T108-execution-trace-runid-scoped.md` |
| T109 | Done | admin-ui composable tests | Add first Vitest suite for `frontend/admin-ui/app/composables/` (`useDslApi`, `useRunner`, `useExecutions`, `useDslWorkbench`) — the UI's core data-fetch and state layer, currently at 0 tests despite a working `vitest.config.ts`. Mock `$fetch`, assert endpoint URLs/methods/bodies, `useRunner` status transitions and `NO_DEFINITION` guard, and isolate shared module-level `ref` state per test. | High | loop | - | - | `./docs/plans/T109-admin-ui-composable-tests.md` |
| T110 | Done | BFF proxy hardening (timeout + request-id) | Harden `frontend/admin-ui/server/utils/httpClient.ts` (`proxyToBackend`, the only BFF→Spring egress for the whole UI): add outbound `timeout` (default `backendTimeoutMs`) yielding 504 `BACKEND_TIMEOUT`, forward/generate `x-request-id` + `traceparent` from the incoming `H3Event` (currently discarded as `_event`), preserve `X-Api-Key` behavior, and add the first `httpClient` unit tests covering success, error mapping, timeout, and header propagation. | High | loop | - | - | `./docs/plans/T110-bff-proxy-hardening.md` |
| T111 | Done | Substitutor unit tests | Add first tests for `dsl-api/utils/Substitutor` — the `${var}` template engine on the critical path of all Java codegen (Process/Transaction/DefinitionProvider/HelperSpi), all 3 diagram generators, and runtime `PropertyResolver` (~15 call sites, 5 modules), currently at 0 tests. Pin value escaping (`$`/`\` via `quoteReplacement`), null→"" coercion, missing-placeholder default-throw + custom handler, empty/adjacent/repeated keys, and builder fluency. | High | loop | - | - | `./docs/plans/T111-substitutor-unit-tests.md` |
| T112 | Done | DslWorkerConfiguration lifecycle + graceful shutdown | Fix thread-leak bug in `starter/config/DslWorkerConfiguration`: `WorkerFactory` is a discarded local var, `factory.start()` is a side effect in the bean method, and there is no `@PreDestroy`/`SmartLifecycle`/`DisposableBean` anywhere in the backend (grep-confirmed) — so Temporal poller threads leak on every app stop. Expose `WorkerFactory` as a managed bean, move `start()` to a `SmartLifecycle` hook, and wire `shutdown()` on context close + test it. | High | loop | - | - | `./docs/plans/T112-worker-factory-lifecycle.md` |
| T113 | Done | Temporal DSL service layer tests | Add first unit tests for the three untested classes in `starter/services/` — `TemporalDslService` (execute/resolveProcess/prepareInput/findRunMethod + RuntimeException error-cause wrapping), `TemporalDslProcessService` (runProcess delegating to GlobalManager + runId generation via contextFactory), and `TemporalDslProcessLauncher` (canRun workflow-thread detection, launch with WorkflowOptions/EXECUTION_TIMEOUT+TASK_TIMEOUT, Result.failure/DslExecutionException wrapping). Core DSL↔Temporal seam, just refactored (ed55763, e3d1dff), currently at 0 tests across ~250 lines. Mock WorkflowClient + GlobalManager, assert timeout values, error-cause unwrapping, and no-generated-process rejection. | High | loop | - | - | `./docs/plans/T113-temporal-service-layer-tests.md` |
| T115 | Done | EXPLAIN metadata dead-store bug | `DefaultProcessRunner.run` and `DefaultTransactionRunner.run` compute `ctx = ctx.withMetadata("explain.description", ...)` in EXPLAIN mode but the reassigned local `ctx` is never read again — `Result` has no metadata field, so the enrichment is a no-op dead store today. Fix so EXPLAIN mode actually surfaces the description (e.g. thread it through `ExplainReport`/`DslRuntime` explain path) and add a regression test. Found while implementing T75. | High | loop | - | - | `./docs/plans/T115-explain-metadata-dead-store.md` |
| T114 | Done | CompactSourcePreprocessor unit tests | Add first tests for `dsl/CompactSourcePreprocessor` — the regex engine that turns a compact DSL source file (no package/class decl, single `define()` method implementing `DslCompactSource`) into a compilable Java class. On the compile critical path for all compact DSL examples, currently 0 tests despite heavy regex surface (PACKAGE_PATTERN, IMPORT_PATTERN, DEFINE_PATTERN, TOP_LEVEL_TYPE_PATTERN). Pin: className derivation from fileName, package injection (default vs explicit), import hoisting/collection + dedup, define() detection, top-level-type rejection, and `Result(className, preprocessedSource)` shape. Pure string I/O — trivially unit-testable, high edge-case bug surface. | High | loop | - | - | `./docs/plans/T114-compact-source-preprocessor-tests.md` |
| T116 | Done | GeneratedClassRegistry unit tests | Add first direct tests for `dsl/registry/GeneratedClassRegistry` — the `ConcurrentHashMap`-backed lookup backbone behind `GlobalManager` (every process/transaction `find`/`list` goes through it), currently 0 dedicated tests (only exercised indirectly via GlobalManagerTest). Pin: `register(descriptor)` routes PROCESS vs TRANSACTION (FUNCTION silently ignored per `default` branch), name-collision override (last `put` wins), `findProcess`/`findTransaction` miss → `Optional.empty()`, `processes()`/`transactions()` return immutable `List.copyOf` snapshots, `register(GeneratedClassProvider)` delegates via `descriptor()`, and the no-arg constructor invokes `ServiceLoader.load` without throwing. Pure unit test, no infra. | High | loop | - | - | `./docs/plans/T116-generated-class-registry-tests.md` |
| T117 | Done | JsonExtractHelper built-in @Helper | Add a new built-in `@Helper(name = "jsonExtract")` under `starter/helpers/` following the existing `FilterRecordsHelper`/`SumValuesHelper` template (`@Helper` + `Executable<In,Out>` + In/Out model records). Extracts a value (or nested value via dotted path) from a JSON string using Jackson (`ObjectMapper`, already transitively on the Spring classpath — no new dep). Fills a real data-shaping gap: current helpers cover filter/sum/format/timestamp but none parse JSON, yet DSL flows routinely handle JSON message bodies. Include `JsonExtractIn{json, path}`, `JsonExtractOut{value, present}`, missing-path handling (empty Optional → `present=false`), malformed-JSON → `Result.failure`, and a focused unit test alongside the other helper tests. | High | loop | - | - | `./docs/plans/T117-json-extract-helper.md` |
| T118 | Done | ContextFactory unit tests | Add first tests for `dsl/config/ContextFactory` — on the runtime hot path (builds the `SimpleContext` + generates the `runId` for every process/transaction run via `TemporalDslProcessService`/runner), currently 0 tests. Foundational to T108 (runId-scoped traces): runId collisions here would corrupt trace correlation. Pin: `generateRunId()` returns `"run-"+UUID` (non-blank, `"run-"` prefix, valid trailing UUID, unique across N calls); `of(body, mode)` attaches empty metadata + a fresh runId; `of(body, mode, runId)` uses the supplied runId verbatim + empty metadata; `of(body, metadata, mode, runId)` preserves the supplied metadata map and runId; `ExecutionMode` propagates to the context in all overloads. Pure unit test, no infra. | High | loop | - | - | `./docs/plans/T118-context-factory-tests.md` |
| T119 | Done | AppSidebar / AppMobileDrawer nav contract tests | frontend — Add Vitest suite under `frontend/components/src/components/__tests__/` (next to the existing `AppNavItem.spec.ts`) covering the two nav containers that T76 unified onto a single shared `navItems` array: `AppSidebar` and `AppMobileDrawer`. Both take an `items: NavItem[]` prop and `v-for` over it; today neither is tested, so a future per-component nav array (the exact T76 regression) would slip in silently. Pin: each renders one nav entry per item (count = `items.length`), passes `to`/`label` through to `AppNavItem`, renders the same link set from the same `items` input (contract equivalence), and tolerates an empty `items` array without error. Test infra (vitest + `@vue/test-utils` + happy-dom) already configured in `frontend/components/vitest.config.ts`. | High | loop | - | - | `./docs/plans/T119-sidebar-drawer-nav-contract-tests.md` |
| T121 | Done | DslConfig unit tests | Add first tests for `dsl/config/DslConfig` — the central config holder just refactored in `ed55763` ("replace TemporalProcessLauncherHolder with DslConfig Replaceable"), 90 lines, currently 0 tests. On a recently-churned, low-coverage seam backing Temporal launcher wiring. Pin: default/empty construction, accessor/mutator round-trips for each held dependency (WorkflowClient, registry, context factory — confirm exact fields), `replace(...)` semantics (returns new immutable instance vs mutates in place), `@NonNull` rejection, and that the launcher reads the latest replaced instance (the regression `ed55763` fixed). Pure unit test, no Spring/Temporal infra. | High | loop | - | - | `./docs/plans/T121-dsl-config-tests.md` |
| T122 | Done | DefaultTransactionRegistry unit tests | Add first tests for `dsl/registry/DefaultTransactionRegistry` — runtime registry for transaction definitions (distinct from `GeneratedClassRegistry`/T116 which holds generated Temporal classes), 31 lines, 0 tests. Pin: `register`/`find` by name, miss → empty `Optional`, name-collision override (last wins), sorted `names()` list, immutability of returned lists. Mirrors T116's shape but for the transaction-definition lookup path used by the runner. Pure unit test, no infra. | High | loop | - | - | `./docs/plans/T122-default-transaction-registry-tests.md` |
| T123 | In Progress | InputField component tests | frontend — Add Vitest suite for `frontend/components/src/components/InputField.vue` (115 lines, the form primitive reused across Runner UI / InputForm, 0 tests). Pin: renders label + input, `v-model` round-trip (emits `update:modelValue` on input), placeholder passthrough, disabled/readonly attrs, error message render (slot/prop), type attr (text/number). Test infra (vitest + `@vue/test-utils` + happy-dom) already configured in `frontend/components/vitest.config.ts`. | High | loop | - | - | `./docs/plans/T123-inputfield-component-tests.md` |
| T124 | Backlog | Descriptor & report record tests | Add first tests for the three small `dsl-api` records backing preview/explain metadata — `ExecutableDescriptor` (17 lines), `PreviewReport` (18), `ExplainReport` (21) — 0 tests despite being the public API returned by the introspection/preview/explain endpoints consumed by the Runner UI. Pin record component shape, static factories/builders if any, `@NonNull` rejection, equals/hashCode/toString, and that EXPLAIN-vs-PREVIEW reports carry expected fields. Pure unit test, no infra. | High | loop | - | - | `./docs/plans/T124-descriptor-report-tests.md` |
| T125 | Backlog | DSL exception hierarchy + DslErrorCode tests | Extend `dsl-api` coverage beyond base `DslExceptionTest` to concrete subclasses + error-code enum: `DslValidationException`, `DslCompensationException`, `DslExecutionException`, `DslEntityNotFoundException` (~10 lines each), `DslErrorCode` (5-line enum). Pin: each subclass carries the right `DslErrorCode`, message + cause propagation, every `DslErrorCode` maps to a stable identity (name/ordinal) so REST error mapping via `DslExceptionHandler`/`ErrorResponse` stays stable. Guards the public error contract. | High | loop | - | - | `./docs/plans/T125-dsl-exception-hierarchy-tests.md` |
| T126 | Backlog | DSL example: helper-composition data pipeline | Add a new example under `backend/dsl-examples/` (registered in the existing examples aggregator) demonstrating built-in helper composition — a data-pipeline process that ingests records, `filterRecords` by field, `sumValues` to aggregate, `formatMessage` to render, and (once T117 lands) `jsonExtract` to pull a nested field. Fills an examples gap: existing examples cover saga/compensation/retry/batch but none show helper chaining, the core DSL value-prop for data shaping. Compile-validated by the existing `dsl-examples` build + T52 integration test; no new infra. | High | loop | - | - | `./docs/plans/T126-dsl-example-helper-pipeline.md` |
| T127 | Backlog | GeneratedClassProviderGenerator unit tests | Add first tests for `dsl-codegen/.../GeneratedClassProviderGenerator` (109 lines, 0 tests) — the codegen step that emits the `GeneratedClassProvider` implementation source consumed by `GeneratedClassRegistry` (T116) via `ServiceLoader`. Every sibling codegen class (`ProcessCodeGenerator`, `TransactionCodeGenerator`, `DefinitionProviderGenerator`) has tests except this one, so a regression in provider-class naming/structure would only surface at registry-load time. Pin: `forProcess`/`forTransaction` emit a `public final class ${providerClass} implements GeneratedClassProvider`, the descriptor accessor returns the right name/type/taskQueue, `CodegenNaming`-derived class names are stable, generated source compiles (smoke via `GeneratedSource` content assertions), and import collection handles the descriptor's types. Pure unit test — mirror `ProcessCodeGeneratorTest`/`TransactionCodeGeneratorTest` style. | High | loop | - | - | `./docs/plans/T127-generated-class-provider-generator-tests.md` |
| T128 | Backlog | DslCompileTask / DslCompilerPlugin Gradle tests | Add first tests for the gradle-plugin task + plugin classes — `DslCompileTask` (107 lines, the `@TaskAction` entry point users invoke to compile DSL) and `DslCompilerPlugin` (77 lines, registers the task + extension on the project). Only `DslCompileExtension` is tested today; the task dispatch and plugin wiring are 0-test, so a regression in DSL compile invocation surfaces only at user-build time. Use Gradle TestKit (`gradleTestKit()` already in `dsl-gradle-plugin/build.gradle`) with a temp project fixture: assert the plugin registers the `dslCompile` task + extension, the task is wired to the right source dir / outputs, and invoking it delegates to `DslSourceCompiler` (or asserts the expected task outcome). Distinct from T102 (which adds the module to CI). | High | loop | - | - | `./docs/plans/T128-dsl-gradle-task-plugin-tests.md` |
| T129 | Backlog | BFF route handler tests (Nitro v1 API) | frontend — Add the first tests for the `admin-ui-plugin` Nitro BFF route handlers under `server/api/v1/`: the entire UI→Spring proxy layer (10 handlers) is 0-test today. T110 covers the `httpClient` util itself; this task covers the **route layer on top** — `definitions.get`, `run/[name].post`, `preview/[name].post`, `explain/[name].post`, `executions/index.get`, `executions/[id].get`, `reload.post`, `health.get`. Each is a thin `defineEventHandler` that extracts router params + body and calls `proxyToBackend(event, path, opts)`. Mock `proxyToBackend` (`vi.mock('~/server/utils/httpClient')`), invoke each handler with a stub H3 event, and assert it forwards the correct backend path, HTTP method, request body, and router-param (`:name`/`:id`) interpolation. Vitest already configured in `admin-ui-plugin`. | High | loop | - | - | `./docs/plans/T129-bff-route-handler-tests.md` |
| T130 | Backlog | SortRecordsHelper built-in @Helper | Add a new built-in `@Helper(name = "sortRecords")` under `starter/helpers/` following the `FilterRecordsHelper`/`SumValuesHelper` template (`@Helper` + `Executable<In,Out>` + In/Out model records). Completes the data-ops trio alongside `filterRecords` and `sumValues` (the T126 helper-pipeline example chains filter→sum; sort is the natural missing op). Sort a `List<Map<String,Object>>` by a given field, ascending/descending, null-safe. Include `SortRecordsIn{records, field, ascending}`, `SortRecordsOut{records}`, null/empty-input handling (return empty), non-comparable/mixed-type values (documented fallback — e.g. string coercion), and a focused unit test next to the other helper tests. Pure JDK (`Comparator`), no new dep. | High | loop | - | - | `./docs/plans/T130-sort-records-helper.md` |
| T131 | Backlog | OpenAPI / Swagger runtime enablement | Promote `springdoc-openapi-starter-webmvc-ui` from `compileOnly` to `implementation` in `starter/build.gradle` (line 27 — exactly the half-wired pattern T91 fixed for actuator). The REST controllers already carry `@Operation`/`@Tag` swagger annotations (`DslIntrospectionResource`, `DslRuntimeResource`, `DslReloadResource`), but the metadata is never served: `/v3/api-docs` and `/swagger-ui.html` are dead at runtime because springdoc is compileOnly. After promotion, add `springdoc.*` config to `application.yml` (api-docs + swagger-ui paths, packages-to-scan), an optional `OpenAPI` `@Bean` (title/version/description), and a boot smoke test asserting `/v3/api-docs` returns 200 and lists the DSL endpoints. Makes the full DSL REST contract (definitions/run/preview/explain/reload/executions) discoverable for the BFF (T129) and external consumers. | High | loop | - | - | `./docs/plans/T131-openapi-runtime-enablement.md` |
| T132 | Backlog | InputForm component tests (schema-driven + freeform) | frontend — Add Vitest suite for `frontend/components/src/components/runner/InputForm.vue` (101 lines, 0 tests) — the T73 JSON-schema consumer that renders the Runner UI's typed input form and closes the schema→UI→InputField (T123) loop. Two modes: schema-driven (renders typed fields from `schema` prop) and freeform (textarea with `JSON.parse` when no schema). Pin: renders a field per schema property; `v-model` round-trip emits `update:modelValue` with the aggregated object; schema-less → freeform textarea, valid JSON emits parsed object, invalid JSON sets `freeformError` and does NOT emit a bad value; `required` field flag honored. Test infra (vitest + `@vue/test-utils` + happy-dom) already configured in `frontend/components/vitest.config.ts`. | High | loop | - | - | `./docs/plans/T132-inputform-component-tests.md` |
| T133 | Backlog | CONTRIBUTING.md developer guide | Add a top-level `CONTRIBUTING.md` — the repo has `backend/AGENTS.md` + `frontend/AGENTS.md` (agent-facing) and a README quickstart (T107), but no human contributor guide. Cover: project layout (backend modules `dsl`/`dsl-api`/`dsl-codegen`/`dsl-examples`/`starter`/`dsl-gradle-plugin`; frontend `admin-ui-plugin` + `components`), prerequisites, build & test commands per module (Java 25 `./gradlew`, pnpm for FE), how to add a built-in `@Helper` (mirror `FilterRecordsHelper`/T117/T130 template: `@Helper` + `Executable<In,Out>` + In/Out model records + unit test), how to add a DSL process/transaction example (T126), the kanban + loop workflow (`docs/kanban.md` → `docs/plans/<ID>-*.md`, the idea-factory + execution loops), and the commit convention (`feat(kanban): add <ID> — <title>`, conventional commits). Distinct from T107 (README = front door) — this is the "how to contribute" deep-dive. Docs-only, no code. | High | loop | - | - | `./docs/plans/T133-contributing-guide.md` |
| T136 | Done | Fix UnreliableApi fragile-transaction failure propagation | Two methods in `backend/starter/src/integrationTest/.../UnreliableApiDslIntegrationTest.java` fail — `fragileTransactionFailsAndProcessCompensates` and `fragileTransactionFailsWithoutCompensationWhenNoneConfigured`. The fragile activity (`unreliableApiTxFragile`, `RetryPolicy(1,…)`, helper `failCount=5`) fails correctly at the Temporal layer (`unreliableApi attempt 1/5 failed`) but the process returns `Result.success` — the transaction failure is swallowed across the activity boundary. Sibling tests (`BatchProcessing`, `DslVersioning`, `DslExamplesEndToEnd`, and the `resilient…` method in the same class) pass through the identical `TemporalDslProcessService.runProcess` path, so the bug is localised to transaction failure propagation, not the service refactor. Likely culprits: `TemporalTransactionInvoker.invoke` (`return Result.success(value)` on the non-throwing path) and/or `GlobalManager.runTransactionWithCompensation`. Diagnostic-first plan: reproduce → root-cause via systematic-debugging → regression test → minimal fix → verify all 3 class methods green + siblings unaffected. | High | loop | - | - | `./docs/plans/T136-unreliable-api-fragile-failure-propagation.md` |
| T137 | Backlog | WireMock external-HTTP resilience integration test | Wire up the half-wired WireMock dependency — `wiremock = "3.13.2"` + `wiremock = { org.wiremock:wiremock-standalone }` already declared in `backend/gradle/libs.versions.toml` (lines 9, 53) but never added to any `build.gradle` `testImplementation` (exact half-wired pattern T91 fixed for actuator, T131 flagged for springdoc). Today the DSL's retry/compensation resilience is exercised only via `UnreliableApiHelper` (`backend/starter/.../helpers/UnreliableApiHelper.java`), which simulates failure with an in-memory `ConcurrentHashMap` attempt counter — no real network I/O, so HTTP failure modes (5xx, latency timeout, connection reset, malformed body) are never validated end-to-end. Add a new built-in `@Helper(name = "httpCall")` (`HttpCallHelper` + `HttpCallIn{url, method, timeoutMillis, ...}` / `HttpCallOut{status, body}` records, JDK `HttpClient`, mirrors `UnreliableApiHelper`/T117/T130 template), then a `HttpResilienceDslIntegrationTest` under `backend/starter/src/integrationTest/` that boots WireMock per-test (`@DynamicPropertySource` → base-url into the helper) and asserts: (1) transient 500 → `RetryPolicy` retries → eventual success; (2) persistent 500 → failure propagates as `Result.failure` (overlaps T136's propagation concern but at the HTTP/transport layer, distinct); (3) compensation fires when configured; (4) timeout path (delayed stub > `timeoutMillis`) yields failure, not hang. Optional companion DSL example `backend/dsl-examples/src/dsl/HttpResilienceDsl.java`. Complements T136 (in-process activity-layer fix) by pinning the real-HTTP transport layer this loop neglected. | High | loop | - | - | `./docs/plans/T137-wiremock-http-resilience-test.md` |
| T138 | Backlog | useExecutionsApi composable tests | frontend — Add Vitest suite for `frontend/admin-ui-plugin/app/composables/useExecutionsApi.ts` (17 lines, 0 tests). It is the raw `$fetch` wrapper behind the stateful `useExecutions` composable (which IS tested) — exposes `list(filters?)` → `$fetch('/api/v1/executions', { query })` and `get(id)` → `$fetch('/api/v1/executions/${id}')`. Every sibling composable has a spec (`useExecutions.spec`, `useDslApi.spec`, `useRunner.spec`, `useDslWorkbench.spec`, `useSidebar.spec`) but this one slipped T109. Pin: `list()` passes `filters` object as `query` (incl. empty/undefined → no query), `get()` interpolates `id` into path, both return typed shapes (`Execution[] | {items,total}` / `ExecutionDetail`), and `$fetch` is called exactly once per invocation with the right URL. Mock `$fetch` via `vi.stubGlobal`/`vi.mock('#imports')` mirroring `useDslApi.spec` pattern. Closes the admin-ui composable test matrix. | High | loop | - | - | `./docs/plans/T138-useexecutionsapi-composable-tests.md` |
| T139 | Backlog | AstExtractor unit tests | backend — Add focused unit tests for `backend/dsl-codegen/src/main/java/cbs/nova/dsl/codegen/util/AstExtractor.java` (115 lines, 0 tests), shipped in commit `6a9ebba` ("extract DSL execute AST into JSON during codegen"). It parses preprocessed DSL source with JavaParser, locates the `process(...)`/`transaction(...)` builder call matching a given name, walks up to its `.execute(...)` lambda argument, and serializes that AST node to JSON via the injected `Json` — the payload that backs EXPLAIN/preview metadata. Bugs here surface as silently wrong explain output, not build failures. Pin: (1) happy path — a minimal `process("p").execute(ctx -> ...)` source extracts a non-empty JSON tree whose root `type` is the lambda's AST class; (2) `DslType.TRANSACTION` switches builder name to `transaction`; (3) name mismatch → returns `"{}"`; (4) builder call with no enclosing `.execute(...)` → `"{}"`; (5) unparseable source skipped (loop continues) → `"{}"` when all sources fail; (6) `toTree` mapping for each literal kind (String/Int/Long/Double/Boolean/Null/Char), SimpleName, NameExpr, MethodCallExpr, FieldAccessExpr, plus `children` recursion and `range` field. Inject a real `Json` (same instance codegen uses) — assert deterministic, sorted-key output. Pure unit, no Temporal. | High | loop | - | - | `./docs/plans/T139-astextractor-unit-tests.md` |
| T140 | Backlog | SpringBeanHelperInstanceResolver characterization tests | backend — Add unit tests for `backend/starter/src/main/java/cbs/nova/starter/helper/SpringBeanHelperInstanceResolver.java` (38 lines, 0 tests). It implements `HelperInstanceResolver.resolve(Class)`: asks the Spring `ApplicationContext` for the helper bean, and on `NoSuchBeanDefinitionException` falls back to reflective `helperClass.getDeclaredConstructor().newInstance()` (the `newInstance` path is `@Deprecated(forRemoval = true)` with a `TODO: remove reflection, use typed info instead`). This is the *other* reflection site beyond `TemporalDslProcessLauncher` (refactored already) and the next target of the reflection-removal effort — a characterization test now lets that removal land safely. Pin (mock `ApplicationContext`): (1) bean present → returns the bean, no reflection; (2) bean absent (`NoSuchBeanDefinitionException`) → reflective no-arg construction returns a fresh `Executable` instance; (3) class with no public no-arg ctor → `IllegalStateException` with class name + cause; (4) ctor that throws → `IllegalStateException("Failed to create helper instance: …")`. Assert no interaction with the context beyond the single `getBean` call. Pure unit (`Mockito.mock(ApplicationContext.class)`), no Spring context bootstrap. | High | loop | - | - | `./docs/plans/T140-spring-bean-helper-resolver-tests.md` |
| T141 | Backlog | MapInputConverter unit tests | backend — Add unit tests for `backend/dsl/src/main/java/cbs/nova/dsl/converter/MapInputConverter.java` (199 lines, 0 tests), the static reflection converter that turns a flat `Map<String,Object>` (or already-typed value) into the target input type a generated Temporal workflow/activity expects. It is on the hot path of every run — `TemporalDslProcessService` (and the broader process-launch refactor) calls `convert(value, targetType)` to materialise record inputs from REST JSON, so a wrong branch is a silent runtime type error, not a build break. Pin across every branch: (1) `null` value → `null`; (2) `targetType == MapInput` → existing MapInput passthrough, `Map` → `MapInput.fromMap`, other → returned as-is; (3) `Class` target: already-instance passthrough, `String` via `String.valueOf`, each primitive + wrapper (boolean/byte/short/int/long/float/double/char — incl. `Number`-narrowing and `Boolean.parseBoolean`), `enum` via `Enum.valueOf`, array from `Collection`, record from `Map` (component-by-component recursive convert + canonical-ctor invoke), `Map`/`Collection` passthrough, unsupported pair → `IllegalArgumentException`; (4) `ParameterizedType` target: `List<E>` element-wise convert, `Map` passthrough, else delegate to `convertToClass`; (5) record with non-Map input → IAE; array from non-Collection → IAE; unsupported primitive type → IAE (defensive). Pure static (`MapInputConverter.convert(...)`), no Spring/Temporal. Use small fixture records declared in the test. | High | loop | - | - | `./docs/plans/T141-mapinputconverter-unit-tests.md` |