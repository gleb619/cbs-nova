# T645 — dsl-examples README: example catalog

## Goal

Write `backend/dsl-starter/dsl-examples/README.md`: a catalog table of every shipped example —
name, helpers/features demonstrated, one-line scenario — plus how the examples relate to the
integration tests that execute them and where their DSL YAML lives.

## Why

`dsl-examples` accumulates examples (20+ model files, more landing via the example task queue
T576–T583, T603) with no README or index. Discovering what the platform can demonstrate requires
reading source. Distinct from T615 (generated API helper reference): this is a curated,
scenario-oriented catalog.

## Acceptance criteria

- [ ] Table row per example: name, demonstrated helpers/features, scenario one-liner.
- [ ] Rows generated by reading the actual example sources — no invented capabilities.
- [ ] Section pointing at the integration tests that execute each example (find the test mapping)
      and where DSL definitions/YAML for each live.
- [ ] Short "how to add an example" note linking the kanban example-task pattern.
- [ ] Cross-link from root README or DEVELOPING.md so the catalog is findable.
- [ ] `make lint` passes.

## Tier

`backend`

## Files to create/modify

- Create: `backend/dsl-starter/dsl-examples/README.md`
- Modify: `README.md` or `DEVELOPING.md` (one cross-link line)
- Read first: `backend/dsl-starter/dsl-examples/src/**` (all examples),
      `starter/src/integrationTest/java/cbs/nova/dsl/example/integration/`.

## Build/test commands

```bash
make lint
```

## Constraints

- On work start, load and use skills `caveman` and `codegraph`.
- Doc-only; verified content only — every table row must cite a real example file.

---

You are not alone, focus on your task, ignore other errors. Keep caveman/ultra-brief responses to user when reporting progress. Minimal texting, save tokens.
