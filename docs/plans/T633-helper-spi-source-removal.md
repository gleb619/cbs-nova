# T633 — Helper SPI cleanup: remove GeneratedHelperSource + helperFilenames

## Goal

Remove the `GeneratedHelperSource` SPI (generated by `HelperSpiProcessor`) and the
`helperFilenames` map in `GeneratedClassRegistry`; fold filename resolving into the existing
`GeneratedHelperResolver` / `GeneratedHelperInstanceResolver` SPIs.

## Why

Two paired TODOs flag the same debt:
- `HelperSpiProcessor.java:40` — "remove a GeneratedHelperSource, reuse a GeneratedHelperResolver
  and GeneratedHelperInstanceResolver, for filename resolving"
- `GeneratedClassRegistry.java:19` — "modify GeneratedClassProvider, remove a helperFilenames,
  make a refactoring"

Three generated SPI classes where two suffice — every helper pays an extra service-file load and
the registry keeps a redundant filename map.

## Acceptance criteria

- [ ] `HelperSpiProcessor` no longer generates `GeneratedHelperSource` or its service file;
      filename resolution moved onto `GeneratedHelperResolver`/`GeneratedHelperInstanceResolver`
      (e.g. a `filename()` method or descriptor carrying it).
- [ ] `GeneratedClassRegistry.helperFilenames` map and its accessor(s) removed; callers
      (`codegraph_callers` on the accessor) migrated.
- [ ] `GeneratedClassProvider` touched only as needed for the refactor; no unrelated API change.
- [ ] Regenerated sources verified: full `publishToMavenLocal` + a compile of `app/dsl` or an
      integration test that loads generated SPIs.
- [ ] dsl + misc-codegen + starter test suites green; `make lint` passes.

## Tier

`backend`

## Files to create/modify

- Modify: `backend/dsl-platform/misc-codegen/src/main/java/cbs/nova/misc/codegen/HelperSpiProcessor.java`
- Modify: `backend/dsl-platform/dsl/src/main/java/cbs/nova/dsl/registry/GeneratedClassRegistry.java`
- Modify: SPI interfaces in `dsl-api` (`GeneratedHelperResolver`, `GeneratedHelperInstanceResolver`,
  `HelperSource`) and their consumers — discover via `codegraph_callers`.

## Build/test commands

```bash
backend/dsl-platform/gradlew -p backend/dsl-platform publishToMavenLocal -x test
backend/dsl-platform/gradlew -p backend/dsl-platform test
backend/dsl-platform/gradlew -p backend/dsl-starter :starter:test
make lint
```

## Constraints

- On work start, load and use skills `caveman` and `codegraph`.
- Cross-module SPI change — run full platform + starter suites, not just unit scopes.

---

You are not alone, focus on your task, ignore other errors. Keep caveman/ultra-brief responses to user when reporting progress. Minimal texting, save tokens.
