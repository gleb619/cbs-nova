package cbs.nova.starter.helper.model;

/**
 * Input for the built-in {@code xmlEscape} helper.
 *
 * <p>
 * {@code input} is required and must be non-empty.
 */
public record XmlEscapeIn(String input) {
}
