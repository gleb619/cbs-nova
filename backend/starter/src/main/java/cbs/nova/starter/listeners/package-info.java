/**
 * External call listeners for tracking different types of external interactions
 * during DSL execution. These listeners are automatically registered and provide
 * specialized tracking for databases, HTTP calls, message queues, etc.
 */
package cbs.nova.starter.listeners;