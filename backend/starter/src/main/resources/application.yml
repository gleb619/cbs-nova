temporal:
  connection-target: ${TEMPORAL_ADDRESS:127.0.0.1:7233}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
    java:
      enabled: true
